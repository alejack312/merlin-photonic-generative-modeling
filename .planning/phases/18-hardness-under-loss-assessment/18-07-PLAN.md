---
phase: 18-hardness-under-loss-assessment
plan: 07
type: execute
wave: 4
depends_on: ["18-06", "18-01"]
files_modified:
  - hardness_analysis.py
  - docs/hardness-under-loss-study.md
  - results/phase18_weight1_tvd_plot.png
  - results/phase18_mixed_tvd_plot.png
  - results/phase18_anticoncentration_plot.png
autonomous: true

must_haves:
  truths:
    - "docs/hardness-under-loss-study.md exists as this phase's canonical results document (mirroring docs/trainability-study.md's Phase-17 precedent), with a methodology-stated-before-results structure -- eta grid, n range actually reached, n_draws, seed, and generator scopes all stated explicitly before any results are shown."
    - "TVD-vs-eta is reported for BOTH generator scopes (weight1, mixed), against BOTH classically-easy baselines (uniform, product-of-marginals) AND the lossless reference, as measured curves/tables -- never collapsed to a single number or a single baseline (HARD-05)."
    - "The anticoncentration parameter alpha(eta) (BMS Theorem 4's own quantity) is reported for both generator scopes as an explicit function of eta, not just described qualitatively."
    - "Weight-2's herald-success-rate is reported as an explicit function of eta, and the weight-2 TVD is explicitly stated as conditioned on herald success (postselected), matching this project's existing heralded_cz convention (HARD-07)."
  artifacts:
    - path: "docs/hardness-under-loss-study.md"
      provides: "Phase 18's canonical results document: methodology, TVD-vs-eta results (both scopes, both baselines), anticoncentration-vs-eta, weight-2 herald-compounding results"
      min_lines: 60
    - path: "hardness_analysis.py"
      provides: "Root-level analysis script (mirrors trainability_analysis.py) loading both CSVs, producing the plots referenced in the doc"
      min_lines: 30
  key_links:
    - from: "hardness_analysis.py"
      to: "results/phase18_weight1_loss_sweep.csv / results/phase18_mixed_loss_sweep.csv"
      via: "csv.DictReader load, matplotlib plotting"
      pattern: "phase18_(weight1|mixed)_loss_sweep\\.csv"
---

<objective>
Write up Plan 18-06's real measured loss-sweep dataset into `docs/hardness-under-loss-study.md` -- this phase's canonical results document, mirroring `docs/trainability-study.md`'s established Phase-17 precedent (methodology-stated-before-results, per this project's `WRITE-01` structure and `docs/iqp-photonic-encoding.md`'s ENC-02 rigor bar). Covers HARD-01 (loss sweep exists and is reported), HARD-05 (TVD-vs-eta against both the lossless reference and both classically-easy baselines, plus anticoncentration alpha(eta)), and HARD-07 (weight-2 herald-compounding results, herald-success-rate-vs-eta, TVD conditioned on herald success).

This plan deliberately does NOT include HARD-04's depolarizing-translation positioning or HARD-06's final scope statement -- those require the owner's attempt-first checkpoint (Plan 18-08, per this project's CLAUDE.md) and are sequenced as their own separate plan so a mandatory human checkpoint never sits in the same plan as ordinary automated write-up work (this project's own plan-authoring convention -- see Phase 15's 15-03).

Purpose: turns Plan 18-06's raw CSVs into this project's next citable literature-grounded document, and gives Plan 18-08 real measured numbers (weight-2's herald-failure-vs-eta curve in particular) to ground its own derivation against, per `18-RESEARCH.md`'s HARD-04 guidance ("Compounded gate-failure rate" candidate direction).
Output: `docs/hardness-under-loss-study.md` (new canonical doc), `hardness_analysis.py` (new root-level script), 3 new plots in `results/`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-hardness-under-loss-assessment/18-CONTEXT.md
@.planning/phases/18-hardness-under-loss-assessment/18-RESEARCH.md
@results/phase18_weight1_loss_sweep.csv
@results/phase18_mixed_loss_sweep.csv
@docs/trainability-study.md
@docs/iqp-baseline.md
@trainability_analysis.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build hardness_analysis.py -- load CSVs, produce plots</name>
  <files>hardness_analysis.py, results/phase18_weight1_tvd_plot.png, results/phase18_mixed_tvd_plot.png, results/phase18_anticoncentration_plot.png</files>
  <action>
Mirror `trainability_analysis.py`'s established shape (module docstring stating data provenance,
`matplotlib.use("Agg")` before importing `pyplot`, CSV paths as module constants, one function per
plot).

Produce 3 plots:
1. `results/phase18_weight1_tvd_plot.png`: for weight-1, one subplot (or combined figure) per
   tracked quantity vs eta -- `tvd_to_lossless_mean` (with error bars from `_std`),
   `tvd_to_uniform_mean`, `tvd_to_product_marginals_mean` -- one line per n value, x-axis eta
   (reversed or labeled so eta=1 low-loss reads left-to-right naturally), y-axis TVD.
2. `results/phase18_mixed_tvd_plot.png`: same shape, for mixed scope's CSV.
3. `results/phase18_anticoncentration_plot.png`: `alpha_mean` vs eta, both scopes on the same
   figure (different line styles/colors), one line per n value per scope, with a horizontal
   reference line at `alpha=1` (uniform/maximally-anticoncentrated) for visual context.

Also produce a small printed/logged summary (stdout) of the headline numbers Task 2 will need to
transcribe into the doc: at the lowest measured eta, the largest measured eta, and eta=1.0 (the
lossless anchor), for each scope: TVD-to-lossless, TVD-to-each-baseline, alpha, and (mixed only)
herald_success_rate.
  </action>
  <verify>`./venv/Scripts/python.exe hardness_analysis.py` runs cleanly and produces all 3 PNG files in `results/`.</verify>
  <done>hardness_analysis.py exists and produces the 3 required plots from the real Plan 18-06 datasets.</done>
</task>

<task type="auto">
  <name>Task 2: Write docs/hardness-under-loss-study.md</name>
  <files>docs/hardness-under-loss-study.md</files>
  <action>
Create the phase's canonical results document, following `docs/trainability-study.md`'s
established structure (methodology section first, then results, matching `WRITE-01`'s
"methodology-stated-before-results" requirement):

1. **Methodology** (stated before any results, per WRITE-01): the loss mechanism used (`pcvl.LC`
   component insertion + explicit `min_detected_photons_filter(0)`, NOT `NoiseModel` -- state
   Pitfall 1/Pitfall 2 plainly, since a reader unfamiliar with Perceval would otherwise reasonably
   expect the more "obvious" `NoiseModel(transmittance=eta)` API and needs to understand why this
   project doesn't use it for its own circuits); the eta grid (`hardness.sweep.ETA_GRID`'s actual 7
   values, stated explicitly); the n range actually reached for each scope (read from Plan
   18-06's SUMMARY -- state honestly if mixed scope's largest size is partial/still-running,
   matching this project's TRAIN-05/TRAIN-08 honesty convention, never implying a size was reached
   if it wasn't); `n_draws=5` and the deterministic seed (`seed_base=180814`) used, per WRITE-06's
   traceability requirement; the "uniform" theta-init convention used for this sweep, stated as a
   deliberate scope decision (not silently inherited from Phase 17); the two classically-easy
   baselines (uniform, product-of-marginals-computed-once-from-the-lossless-reference), explicitly
   citing `18-CONTEXT.md`'s locked decision that no single numeric crossover threshold is defined
   in this phase (both curves reported as measured, interpretation deferred).
2. **HARD-01/HARD-02 results**: state the loss sweep ran successfully via `Processor.probs()` +
   `LC`, and reference Plan 18-02's HARD-02 cross-check result (NoiseModel vs LC agreement on the
   simplified toy circuit) with its measured tolerance.
3. **HARD-05 results (weight-1 and mixed)**: embed `results/phase18_weight1_tvd_plot.png` and
   `results/phase18_mixed_tvd_plot.png`, with a results table (or the CSV's headline rows
   transcribed) showing TVD-to-lossless, TVD-to-uniform, TVD-to-product-marginals at a few
   representative eta values (highest, lowest, and eta=1.0) for each n. State plainly (not
   interpreted) how the two TVD-to-baseline curves compare to the TVD-to-lossless curve as eta
   decreases -- report the numbers, leave the "is this classically easy" judgment call unmade
   here (matching `18-CONTEXT.md`'s explicit "no hard numeric crossover threshold... interpretation
   is Phase 20's/the owner's job" lock).
4. **Anticoncentration results**: embed `results/phase18_anticoncentration_plot.png`, state
   alpha(eta) values at the same representative eta points for both scopes, and note this is the
   exact quantity `docs/iqp-baseline.md`'s BMS/arXiv:1610.01808 bullet's Theorem 4 keys its
   hardness-vs-simulability threshold on (a forward pointer to Plan 18-08's positioning work, not
   itself a positioning claim).
5. **HARD-07 results (weight-2 herald compounding)**: state the herald-success-rate-vs-eta curve
   explicitly (numbers, not just "it decreases"), and restate that the reported weight-2 TVD is
   CONDITIONED on herald success (postselected) per `18-CONTEXT.md`'s locked convention -- and that
   `herald_failure_prob` itself (the un-renormalized number) is tracked and reported separately,
   never folded into `residual`.
6. Leave a placeholder heading `## HARD-04/HARD-06: Positioning and Scope Statement (Plan 18-08)`
   with a one-line note that this section is completed by Plan 18-08 after the owner's
   attempt-first checkpoint -- so a reader of this doc mid-phase understands the doc is
   intentionally incomplete at this point, not abandoned.
  </action>
  <verify>Read back `docs/hardness-under-loss-study.md`: confirm the methodology section appears before any results section, both baselines are reported separately (never collapsed into one "classically easy?" verdict), alpha(eta) is reported for both scopes, herald-success-rate-vs-eta is reported explicitly, and the placeholder heading for Plan 18-08's work is present.</verify>
  <done>docs/hardness-under-loss-study.md exists with methodology-first structure and complete HARD-01/HARD-02/HARD-05/HARD-07 results sections, ready for Plan 18-08 to append the HARD-04/HARD-06 sections.</done>
</task>

</tasks>

<verification>
1. `hardness_analysis.py` runs cleanly and produces 3 plots from the real Plan 18-06 datasets.
2. `docs/hardness-under-loss-study.md` has a methodology-before-results structure with both baselines, alpha(eta) for both scopes, and herald-success-rate-vs-eta all reported explicitly with real numbers (not placeholders).
</verification>

<success_criteria>
- HARD-01, HARD-02, HARD-05, HARD-07 are all satisfied in this doc with real measured numbers and plots.
- The doc is structured so Plan 18-08 can append its HARD-04/HARD-06 sections without restructuring anything already written.
</success_criteria>

<output>
After completion, create `.planning/phases/18-hardness-under-loss-assessment/18-07-SUMMARY.md`
</output>
