---
phase: 18-hardness-under-loss-assessment
plan: 06
type: execute
wave: 3
depends_on: ["18-05"]
files_modified:
  - results/phase18_weight1_loss_sweep.csv
  - results/phase18_mixed_loss_sweep.csv
autonomous: true

must_haves:
  truths:
    - "A real TVD-vs-eta dataset exists for the weight-1-only generator across the full ETA_GRID and >=3 system sizes (matching Phase 17's established weight-1 n=2..6 CORE range, or wider per Plan 18-05's timing probe), tracked against both the lossless reference AND both classically-easy baselines (HARD-01, HARD-05)."
    - "A real TVD-vs-eta dataset exists for the mixed weight-1+weight-2 generator across the full ETA_GRID and >=2 system sizes, with herald_failure_prob/herald_success_rate tracked explicitly as a function of eta (HARD-07), sized per Plan 18-05's timing-probe-derived 'Final n-range decision' rather than an unverified assumption."
    - "The anticoncentration parameter alpha is a finite, non-NaN number for every (n, eta, scope) row in both CSVs -- confirming Pitfall 2 (the silent loss-invariant-filter bug) is not present in the executed run: alpha must visibly change with eta at fixed n, not stay constant across the whole eta grid."
    - "Whatever max-n is honestly reached for mixed scope by the time this plan wraps up (full CORE range, or a partial/still-running best-effort stretch) is recorded plainly, per this project's established honest-reporting convention (Phase 17/17.1 precedent) -- no time-box, no silently-abandoned attempt."
  artifacts:
    - path: "results/phase18_weight1_loss_sweep.csv"
      provides: "Full ETA_GRID x n-range dataset for weight-1, all quantities from hardness.sweep.pooled_cell_for_neta"
    - path: "results/phase18_mixed_loss_sweep.csv"
      provides: "Full ETA_GRID x n-range dataset for mixed (weight-1+weight-2), including herald_failure_prob/herald_success_rate columns"
  key_links: []
---

<objective>
Run the actual loss sweep and produce the real datasets this phase's central measured claims (HARD-01, HARD-05, HARD-07) depend on -- the compute-heavy step Plan 18-05's timing probe exists specifically to plan around. Mirrors Phase 17-06's/17.1-04's established pattern: run the comfortably-affordable weight-1 range synchronously and in full, and treat mixed scope's largest size as an explicitly best-effort, chunked, no-time-boxed stretch if Plan 18-05's timing probe found it too expensive for a single synchronous run -- per `18-CONTEXT.md`'s locked decision ("best-effort, chunked/resumable execution... no fixed time-box for this phase... report whatever max-n is honestly reached").

Purpose: produces the raw numbers Plans 18-07/18-08 will analyze, plot, and write up -- and the dataset Phase 19's VERIFY-04 needs to cross-check against.
Output: `results/phase18_weight1_loss_sweep.csv` and `results/phase18_mixed_loss_sweep.csv`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-hardness-under-loss-assessment/18-CONTEXT.md
@.planning/phases/18-hardness-under-loss-assessment/18-RESEARCH.md
@results/phase18_timing_probe.md
@loss_sweep.py
@hardness/sweep.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the weight-1 CORE loss sweep across the full eta grid</name>
  <files>results/phase18_weight1_loss_sweep.csv</files>
  <action>
Read `results/phase18_timing_probe.md`'s "Final n-range decision" section (produced by Plan
18-05) and use its stated weight-1 n-range -- expected to be at least n=2..6 (Phase 17's
established weight-1 ceiling; per `18-RESEARCH.md`'s cost analysis this scope is "much cheaper
than the naive worst-case combinatorial estimate," so a wider range than Phase 17's own
parameter-shift ceiling is plausible and should be used if the timing probe confirmed it's cheap).

Run synchronously (weight-1 is expected to complete in well under an hour total, per the timing
probe):
```
./venv/Scripts/python.exe loss_sweep.py --scope weight1 --n-values <probe-confirmed range> \
    --n-draws 5 --out results/phase18_weight1_loss_sweep.csv
```
If any individual n value's timing (per Plan 18-05's probe) suggests chunking is warranted even
for weight-1, use the `--draw-start`/`--draw-count`/`--combine-chunks` flags exactly as
demonstrated in `loss_sweep.py`'s own docstring -- otherwise a single synchronous invocation is
expected to suffice.

Print per-cell progress as the sweep runs (already built into `loss_sweep.py`'s `run()` function
from Plan 18-05) so progress is visible, not a black box.
  </action>
  <verify>`results/phase18_weight1_loss_sweep.csv` exists with `len(n_range) * len(ETA_GRID)` rows (i.e. n-values x 7 eta points) plus header; every row's `alpha_mean`, `tvd_to_lossless_mean`, `tvd_to_uniform_mean`, `tvd_to_product_marginals_mean` are finite (no NaN/inf). At fixed n, `alpha_mean` visibly varies across the eta grid (not constant) -- the Pitfall-2 sanity check this plan's must_haves require.</verify>
  <done>The weight-1 CORE loss sweep is complete across the full eta grid and the timing-probe-confirmed n-range, with finite, eta-varying values throughout.</done>
</task>

<task type="auto">
  <name>Task 2: Run the mixed CORE loss sweep, then attempt the largest size best-effort with no time-box</name>
  <files>results/phase18_mixed_loss_sweep.csv</files>
  <action>
Read `results/phase18_timing_probe.md`'s "Final n-range decision" section for the mixed-scope CORE
range (expected n=2..4, per `18-RESEARCH.md`'s conservative sizing recommendation) and any
best-effort stretch size it identified (expected n=5).

Run the CORE mixed sweep synchronously:
```
./venv/Scripts/python.exe loss_sweep.py --scope mixed --n-values <probe-confirmed CORE range> \
    --n-draws 5 --out results/phase18_mixed_loss_sweep.csv
```

If the timing probe found the CORE range itself already expensive enough to warrant chunking
(likely at n=4, per `18-RESEARCH.md`'s measured ~16.8s/cell-at-loss=0.3 extrapolated across 5
draws x 7 eta points ~= 10+ minutes for n=4 alone), use `--draw-start`/`--draw-count`/
`--combine-chunks` per `loss_sweep.py`'s own documented pattern rather than one long-running
process, matching this machine's documented memory constraints (STATE.md).

After the CORE mixed sweep completes and is saved, ATTEMPT the timing-probe-identified stretch
size (expected n=5) as an explicitly best-effort step -- not a blocking requirement for this
plan's own done-criteria, but also not subject to any short-window cutoff, per `18-CONTEXT.md`'s
locked "no fixed time-box for this phase" decision:
```
./venv/Scripts/python.exe loss_sweep.py --scope mixed --n-values <stretch n> --n-draws 5 \
    --out results/phase18_mixed_loss_sweep_stretch.csv \
    --draw-start 0 --draw-count 1
... (chunk across draws as needed, per the timing probe's own per-cell estimate) ...
```
Run any long-running stretch chunk with `run_in_background` and check back at natural session
boundaries rather than blocking the rest of this phase on it -- mirroring Phase 17-06's/17.1-04's
established pattern exactly, including this project's standing safety rule (STATE.md): never
kill/inspect-by-pattern-match a process not started and tracked by the current tool session; if a
background job's state is uncertain, report it and let the user investigate.

Whatever the state of the stretch attempt when this plan needs to wrap up (complete, still running
in the background, or genuinely stopped with a stated reason), record the exact status plainly in
this plan's SUMMARY.md -- this is the honest-reporting outcome this project's established
convention calls for, not a failure of this plan. Do NOT let an in-progress stretch run block
Plans 18-07/18-08's completion -- they can proceed with whatever CORE-range data exists and note
the stretch data as pending/partial if it isn't ready.
  </action>
  <verify>`results/phase18_mixed_loss_sweep.csv` exists with `len(CORE n-range) * len(ETA_GRID)` rows plus header, all `alpha_mean`/`herald_failure_prob_mean`/TVD columns finite. `herald_failure_prob_mean` visibly varies with eta at fixed n (compounding sanity check). Stretch CSV may or may not exist/be complete at the time this task wraps up -- its state (done / still running in background / genuinely stopped-with-reason) is recorded in this plan's SUMMARY.md, not silently unmentioned either way.</verify>
  <done>The mixed CORE sweep is complete and saved with finite, eta-varying herald-failure and TVD values. The largest (stretch) size was attempted in good faith with no artificial time-box, and its outcome is explicitly documented.</done>
</task>

</tasks>

<verification>
1. `results/phase18_weight1_loss_sweep.csv` has the expected row count, all quantities finite, alpha/TVD visibly varying with eta.
2. `results/phase18_mixed_loss_sweep.csv` has the expected CORE row count, all quantities finite, herald_failure_prob visibly varying with eta (not stuck at the lossless 2/27 baseline).
3. Mixed-scope stretch attempt outcome is explicitly documented in the plan's SUMMARY, whatever it turned out to be.
</verification>

<success_criteria>
- Real TVD-vs-eta and alpha-vs-eta data exists for weight-1 across the timing-probe-confirmed n-range and the full eta grid, satisfying HARD-01/HARD-05's floor requirements.
- Real herald-failure-vs-eta and TVD-vs-eta data exists for mixed scope, satisfying HARD-07.
- The compute-bound largest mixed size was genuinely attempted with no time-box and its outcome honestly recorded.
</success_criteria>

<output>
After completion, create `.planning/phases/18-hardness-under-loss-assessment/18-06-SUMMARY.md`
</output>
