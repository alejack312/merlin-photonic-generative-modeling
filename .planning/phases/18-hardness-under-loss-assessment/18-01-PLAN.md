---
phase: 18-hardness-under-loss-assessment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/papers/1510.05245.pdf
  - docs/iqp-baseline.md
autonomous: true

must_haves:
  truths:
    - "docs/iqp-baseline.md cites the genuine Aaronson-Brod paper (arXiv:1510.05245, 'BosonSampling with Lost Photons,' Phys. Rev. A 93, 012335 (2016)), read in full (not abstract-only), with its fixed-loss-count hardness regime/threshold stated explicitly -- satisfying HARD-03's 'not abstract-only' bar for this second paper and grounding HARD-04's fixed-loss-count comparison."
    - "docs/iqp-baseline.md cites arXiv:2510.24137 (Park & Oh)'s Theorem 1 (lossy-boson-sampling/passive-linear-optics threshold) with its formula extracted -- explicitly distinguishing it from the same paper's differently-badged Section V 'Noisy IQP Sampling' result (a qubit-level Pauli-noise channel, not photon transmittance), so neither citation is silently substituted for the other."
    - "arXiv:1510.05245 and arXiv:2510.24137 are stated as two distinct papers in the doc, never conflated under a single 'Aaronson-Brod' or 'the noisy-IQP paper' label -- correcting 18-CONTEXT.md's original misattribution before it propagates further into this project's citable literature record."
  artifacts:
    - path: "docs/papers/1510.05245.pdf"
      provides: "Locally-saved copy of the genuine Aaronson-Brod paper, matching this project's existing docs/papers/ self-containment convention"
    - path: "docs/iqp-baseline.md"
      provides: "Two new bullets in the 'Fresh Primary-Source Verification' section: Park & Oh Theorem 1 (lossy boson sampling), and Aaronson-Brod's fixed-loss-count regime -- both with verbatim-quoted formulas/thresholds, both explicitly distinguished from each other and from Park & Oh's Section V result"
  key_links: []
---

<objective>
Read the genuine Aaronson-Brod paper (arXiv:1510.05245, "BosonSampling with Lost Photons") in full for the first time in this project, and formally document both it and Park & Oh's arXiv:2510.24137 Theorem 1 (already fully read during `/gsd:plan-phase` research -- see `18-RESEARCH.md` Findings 1-2) into `docs/iqp-baseline.md`'s established "Fresh Primary-Source Verification" citation convention. This closes HARD-03's literal "not abstract-only, formula extracted and cited" bar for both papers this phase's hardness-vs-loss claim depends on, and lays the literature groundwork HARD-04's later positioning work (Plan 18-08) needs -- without which Plan 18-08 would either have to re-do this reading itself (breaking that plan's own scope) or cite secondhand.

This corrects a real mistake: `18-CONTEXT.md`'s original text called arXiv:2510.24137 "Aaronson-Brod" -- it is actually Park & Oh (KAIST, 2026), a different paper. The real Aaronson-Brod paper is arXiv:1510.05245, older (2016) and never read in this project until this plan. Both papers must be cited explicitly by name/arXiv ID from here forward so neither is silently substituted for the other -- see `18-CONTEXT.md`'s "Literature correction" subsection for the full owner decision.

Purpose: satisfies HARD-03 in full and de-risks HARD-04 by front-loading both required literature reads before any loss-sweep code or positioning-derivation work depends on them.
Output: `docs/papers/1510.05245.pdf` (downloaded), and two new verbatim-quoted, explicitly-distinguished citation bullets added to `docs/iqp-baseline.md`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-hardness-under-loss-assessment/18-CONTEXT.md
@.planning/phases/18-hardness-under-loss-assessment/18-RESEARCH.md
@docs/iqp-baseline.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Download and fully read arXiv:1510.05245 (the genuine Aaronson-Brod paper)</name>
  <files>docs/papers/1510.05245.pdf</files>
  <action>
Download the paper to match this project's existing `docs/papers/` self-containment convention (every other cited paper in this project is a locally-saved PDF there, not a live URL reference):

```
curl -L -o docs/papers/1510.05245.pdf https://arxiv.org/pdf/1510.05245
```

Read the full PDF (not abstract-only -- use the Read tool's `pages` parameter if the paper exceeds ~15-20 pages, in 2 calls if needed). Extract and record (for Task 2's write-up):
1. The precise statement of the paper's central result: hardness is preserved when a *fixed, constant number* `k` of photons are lost from an `N`-photon boson-sampling experiment (as opposed to a fractional loss *rate*) -- the exact theorem number, the exact bound/threshold on `k` (as a function of `N` and/or circuit size, if stated), and any stated caveats/conditions.
2. How the paper's own noise model is defined (which photons are lost, whether loss is adversarial/worst-case or random, whether it's photon-count-preserving on the "surviving" subspace) -- precise enough to state plainly, in Plan 18-08, whether/how this project's fractional-rate `η` loss model relates to it (a constant `k` is not the same shape of quantity as a rate `η ∈ [0,1]`; do not blur this distinction).
3. Any passages the paper itself uses to contrast its fixed-count regime against a fractional/rate-based loss model (if present) -- useful for Plan 18-08's positioning work.

Quote key passages verbatim (matching `docs/iqp-baseline.md`'s and `18-RESEARCH.md`'s existing citation style of exact quotes with page numbers, not paraphrase-only).
  </action>
  <verify>`docs/papers/1510.05245.pdf` exists and is a valid, non-empty PDF (`file docs/papers/1510.05245.pdf` reports PDF document, or the Read tool successfully renders its contents).</verify>
  <done>The paper has been read in full (not abstract-only) and its central fixed-loss-count theorem, exact threshold, and noise-model definition are extracted with verbatim quotes, ready for Task 2's write-up.</done>
</task>

<task type="auto">
  <name>Task 2: Document both papers into docs/iqp-baseline.md's Fresh Primary-Source Verification section</name>
  <files>docs/iqp-baseline.md</files>
  <action>
Add two new bullets to `docs/iqp-baseline.md`'s existing "## Fresh Primary-Source Verification (2026-08-12)" section (append after the existing BMS/arXiv:1610.01808 bullet -- that section is a running, dated log of primary-source reads; keep the existing 2026-08-12 heading and its existing bullets completely intact, this is a pure addition), matching the section's established style (bold one-line summary + verbatim-quoted formula/theorem + explicit relevance statement):

**Bullet 1 -- Park & Oh (arXiv:2510.24137), Theorem 1.** State plainly this paper is NOT Aaronson-Brod (correcting the misattribution that appeared in `18-CONTEXT.md`'s first draft). Cite its Theorem 1 result (from `18-RESEARCH.md` Finding 2, part 1): the setup (`N` single photons through an `M`-mode passive linear-optical interferometer under uniform transmittance `η`), the Rényi-entropy bound (`S_α(ρ_A) = O(Nη^{2α})`), and the resulting `η = Θ((log N / N)^{1/2α})` (up to log factors, ≈ `η = Θ(1/√N)`) MPS-simulability threshold -- explicitly note this is an *upper bound on where one specific classical simulation method (MPS) is efficient*, not a complexity-theoretic hardness lower bound above that threshold (the paper itself draws this distinction). Also explicitly note the paper's *other* result (Section V, "Noisy IQP Sampling," a qubit-level Pauli-noise depth-vs-noise-rate threshold) is a *different, non-matching* noise channel and is NOT the one being cited here for this project's photon-transmittance-loss claim -- name this distinction so a future reader (or Plan 18-08/Phase 20) cannot accidentally cite the wrong half of this paper.

**Bullet 2 -- The genuine Aaronson-Brod paper (arXiv:1510.05245), read for the first time in this project via Task 1 above.** Cite its central fixed-loss-count result using Task 1's extracted verbatim quotes and exact theorem/threshold. State explicitly that its noise model (a *fixed count* `k` of lost photons) is a structurally different kind of quantity than this project's own fractional-rate `η` loss model -- not directly substitutable without an explicit translation, which is exactly why HARD-04 (Plan 18-08) treats "which regime does this project's tested loss actually sit in" as an open positioning question rather than an assumed answer.

Close both bullets with one shared sentence stating plainly: arXiv:2510.24137 (Park & Oh) and arXiv:1510.05245 (Aaronson-Brod) are two distinct papers with two distinct noise models, cited separately in this document and in every downstream write-up (Plan 18-08, Phase 20) -- never merged under one name or one citation.
  </action>
  <verify>Read back `docs/iqp-baseline.md`: confirm the two new bullets exist under the existing "Fresh Primary-Source Verification (2026-08-12)" heading, each contains a verbatim-quoted formula/theorem (not paraphrase-only), the Park & Oh bullet explicitly distinguishes Theorem 1 from Section V, and the closing sentence explicitly separates the two papers by arXiv ID. Confirm every pre-existing bullet in that section (Rudolph et al., Mhiri et al., Recio-Armengol et al., Herbst et al., BMS/arXiv:1610.01808) is unmodified.</verify>
  <done>docs/iqp-baseline.md cites both arXiv:2510.24137's Theorem 1 and arXiv:1510.05245's fixed-loss-count result, each with an extracted formula/threshold and verbatim quote, explicitly distinguished from each other and from Park & Oh's own Section V result -- HARD-03 fully satisfied, HARD-04's literature groundwork in place for Plan 18-08.</done>
</task>

</tasks>

<verification>
1. `docs/papers/1510.05245.pdf` exists locally.
2. `docs/iqp-baseline.md`'s Fresh Primary-Source Verification section contains two new, clearly-separated citation bullets (Park & Oh Theorem 1; Aaronson-Brod fixed-loss-count) with verbatim-quoted formulas, and every pre-existing bullet is untouched.
3. No other doc in the repo still conflates the two papers (spot-check: `.planning/phases/18-hardness-under-loss-assessment/18-CONTEXT.md`'s own "Literature correction" subsection already self-corrects; no other file in the repo was found to misattribute this, per this plan's pre-planning grep).
</verification>

<success_criteria>
- HARD-03 satisfied: arXiv:2510.24137 read in full (done during research, now formally cited with its actual threshold formula, not the misattributed one).
- Aaronson-Brod's genuine paper (arXiv:1510.05245) read in full for the first time and cited with its own fixed-loss-count threshold -- required literature groundwork for HARD-04, delivered before Plan 18-08 needs it.
- Both papers stated as distinct, never conflated, in this project's canonical literature-citation doc.
</success_criteria>

<output>
After completion, create `.planning/phases/18-hardness-under-loss-assessment/18-01-SUMMARY.md`
</output>
