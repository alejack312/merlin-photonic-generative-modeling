---
phase: 15-arb-01-core-gate-de-risking-validation
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - iqp_photonic_encoding.py
  - tests/test_iqp_photonic_encoding.py
autonomous: true

must_haves:
  truths:
    - "_build_cp_insertion_core(alpha) reproduces diag(1,1,1,e^{i*alpha}) EXACTLY on this module's own MODULE_DUAL_RAIL convention (the same convention _build_cz_insertion_core already uses), verified via Simulator.prob_amplitude at the 3 non-trivial alpha values from Plan 15-01 plus the alpha=pi boundary."
    - "At alpha=pi, _build_cp_insertion_core's truth table matches the ALREADY-CONFIRMED _build_cz_insertion_core's diag(1,1,1,-1) truth table exactly, sign-for-sign -- the bare-core-level instance of the boundary-agreement claim."
    - "build_cp_insertion(n, i, j, alpha) returns a local Circuit + ancilla-vacuum spec following build_cz_insertion's exact external-contract pattern (module's normal port order in, correctly-signed CP(alpha) out, convention adapter fully internal)."
    - "A first attempt at the mode-convention fix not immediately matching the target truth table is treated as expected debugging work (per 15-RESEARCH.md's explicit flag that this is a real, unresolved risk), not a blocker requiring re-planning -- the plan is not considered failed until the bounded debugging steps below are exhausted."
  artifacts:
    - path: "iqp_photonic_encoding.py"
      provides: "_build_cp_insertion_core(alpha) and build_cp_insertion(n, i, j, alpha), analogous to _build_cz_insertion_core/build_cz_insertion but for the CP(alpha) gate family"
      min_lines: 40
    - path: "tests/test_iqp_photonic_encoding.py"
      provides: "Parametrized truth-table tests for _build_cp_insertion_core/build_cp_insertion at the 3 non-trivial alpha plus the alpha=pi boundary"
      min_lines: 30
  key_links:
    - from: "_build_cp_insertion_core"
      to: "perceval.components.core_catalog.controlled_rotation_gates.PostProcessedControlledRotationsItem"
      via: "item.build_circuit(n=2, alpha=float(alpha)), wrapped with whatever PERM adapter combination is found to reproduce MODULE_DUAL_RAIL's truth table"
      pattern: "PostProcessedControlledRotationsItem\\(\\)\\.build_circuit"
    - from: "build_cp_insertion"
      to: "_build_cp_insertion_core"
      via: "PBS-wrap -> _build_cp_insertion_core(alpha) -> PBS-unwrap, mirroring build_cz_insertion's exact structure"
      pattern: "_build_cp_insertion_core"
---

<objective>
De-risk the `CP(α)` gate's mode-convention wiring standalone, before any full-pipeline (PBS + state-prep + conjugation + readout) integration is attempted: find and verify the PERM adapter(s) needed so `PostProcessedControlledRotationsItem`'s dual-rail truth table matches this module's own `MODULE_DUAL_RAIL` convention (the same convention `_build_cz_insertion_core` already reproduces for `heralded_cz`), mirroring exactly how Phase 11 validated `heralded_cz`'s convention adapter in isolation before Phase 11-02 wired PBS/state-prep around it.

**This is real, budgeted debugging work, not a trivial copy-paste of `_build_cz_insertion_core`.** `15-RESEARCH.md` already tried the direct analog fix (and 3 other PERM combinations) in a full end-to-end pipeline context (PBS + CP + PBS, composed with state-prep/diagonal/conjugation/readout) and did NOT reach the target — best TVD ~0.30 against a ≤1e-6 target. This plan isolates the bare gate + PERM adapters ALONE (no PBS, no state-prep, no pipeline) to remove that confound and narrow the search, the same isolation strategy that worked for `heralded_cz` in Phase 11-01. A first attempt not immediately matching the target truth table is expected debugging work — budget real iteration here per the bounded search strategy below, do not treat a failed first attempt as a reason to stop or re-plan.

Purpose: closes "does the isolated CP gate + convention adapter reproduce the correct dual-rail truth table" before Plan 15-04 attempts full-pipeline wiring and TVD validation — the same de-risk-before-integrate sequencing Phase 10→11 and Phase 11-01→11-02 already established for `heralded_cz`.
Output: `_build_cp_insertion_core(alpha)`, `build_cp_insertion(n, i, j, alpha)` in `iqp_photonic_encoding.py`, plus truth-table tests in `tests/test_iqp_photonic_encoding.py`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-arb-01-core-gate-de-risking-validation/15-RESEARCH.md
@.planning/phases/15-arb-01-core-gate-de-risking-validation/15-CONTEXT.md
@cp_gate_derisking.py
@iqp_photonic_encoding.py
@tests/test_iqp_photonic_encoding.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Determine and verify CP's dual-rail convention adapter — _build_cp_insertion_core(alpha)</name>
  <files>iqp_photonic_encoding.py</files>
  <action>
Goal: a local `Circuit(4n)` function (n=2, matching `PostProcessedControlledRotationsItem`'s own `n` parameter for a 2-qubit gate — 4 data modes + 4 ancilla modes = 8 total) that reproduces `diag(1,1,1,e^{iα})` EXACTLY on this module's own `MODULE_DUAL_RAIL` convention (`{"0": (0,1), "1": (1,0)}`, matching `tests/test_iqp_photonic_encoding.py`'s existing constant and `_build_cz_insertion_core`'s precedent), for arbitrary `alpha`.

**Step 1 — direct analog of heralded_cz's fix.** `_build_cz_insertion_core` needed `PERM([1,0])` on each qubit's own dual-rail pair (local modes 0-1 for the "ctrl" port, local modes 2-3 for the "data" port), immediately before and after `heralded_cz`'s bare circuit, because `heralded_cz` uses Perceval's own `Encoding.DUAL_RAIL` standard (mirror image of this module's PBS-derived convention). `PostProcessedControlledRotationsItem` also registers ports via `Encoding.DUAL_RAIL` (15-RESEARCH.md, confirmed from source) — try the exact same fix first: `PERM([1,0])` on local (0,1) and local (2,3), CP's bare `build_circuit(n=2, alpha=float(alpha))` at local offset 0, `PERM([1,0])` swap-back on local (0,1) and (2,3), ancilla modes (local 4-7) passed through untouched.

Verify this against the target truth table at TWO points: `alpha=π` (compare directly against `_build_cz_insertion_core`'s ALREADY-CONFIRMED `diag(1,1,1,-1)` truth table as ground truth — if this matches sign-for-sign, the direct analog is confirmed) and one non-special value, e.g. `alpha=π/3` (compare against `cp_gate_derisking.py`'s Plan 15-01 measurements, adjusted for the `MODULE_DUAL_RAIL` vs `Encoding.DUAL_RAIL` relabeling).

**Step 2 — if Step 1 does not reproduce the correct truth table.** `15-RESEARCH.md` already found that this same 4-combination search (no swap / ctrl-only / data-only / both) failed in a full end-to-end pipeline context — but that context has confounds (PBS, state-prep, conjugation, readout, and a separate mode-mapping-dict arithmetic question that is explicitly out of scope for THIS plan, deferred to Plan 15-04). Re-run all 4 combinations here, in this isolated bare-core context, since removing those confounds may surface a different result than the full-pipeline attempt did.

**Step 3 — if Step 2 still does not find a match.** Directly inspect `PostProcessedControlledRotationsItem().build_circuit(n=2, alpha=alpha).compute_unitary()`'s actual 8×8 matrix, restricted to the single-photon-per-qubit-pair, vacuum-ancilla subspace (the 4 computational basis states), and determine empirically which physical mode index within each qubit's local pair corresponds to logical bit 0 vs. bit 1 — do not assume Perceval's `Encoding.DUAL_RAIL` labeling applies uniformly across both the "ctrl" and "data" ports. This is a bounded, small search (which 2 of modes 0-3 group as qubit i's pair vs. qubit j's pair is already known from `n`'s port-order documentation; only the bit-0/bit-1 assignment within each pair, and whether it's independently determined per-port, is in question) — verify each candidate relabeling against the target truth table until one matches exactly.

Once found: implement `_build_cp_insertion_core(alpha)` in `iqp_photonic_encoding.py`, with a docstring following `_build_cz_insertion_core`'s "convention adapter, not a bug fix" framing — explicitly state which specific adapter combination was found to work and why, citing this plan. Document the α-vs-θ distinction inline (`alpha` is CP's own raw dial; `theta=alpha/4` is the `Z_iZ_j` generator angle in this codebase's existing `pair_thetas` convention) since this remains the single highest-priority disambiguation `15-CONTEXT.md` flagged.
  </action>
  <verify>A standalone `Simulator(SLOSBackend())` run against `_build_cp_insertion_core(alpha)` at `alpha=π` reproduces `diag(1,1,1,-1)` exactly (matching `_build_cz_insertion_core`'s already-confirmed truth table sign-for-sign), and at `alpha=π/3` reproduces `diag(1,1,1,e^{iπ/3})`'s magnitude/phase pattern on `MODULE_DUAL_RAIL`.</verify>
  <done>_build_cp_insertion_core(alpha) exists and its dual-rail truth table (on this module's own MODULE_DUAL_RAIL convention) matches CP(alpha)'s target diag(1,1,1,e^{i*alpha}) pattern exactly, confirmed at both the alpha=pi boundary and a non-special alpha.</done>
</task>

<task type="auto">
  <name>Task 2: build_cp_insertion(n, i, j, alpha) wrapper + truth-table tests</name>
  <files>iqp_photonic_encoding.py, tests/test_iqp_photonic_encoding.py</files>
  <action>
Implement `build_cp_insertion(n, i, j, alpha)` mirroring `build_cz_insertion(n, i, j)`'s exact structure and docstring conventions: `PBS`-wrap qubit i's and qubit j's polarization ports into dual rail, `_build_cp_insertion_core(alpha)` at local offset 0, `PBS`-unwrap back to polarization. LOCAL, self-contained `Circuit(2n+4)` for n=2 (4 data modes: local 0-3, matching `build_cz_insertion`'s local 0-3 layout; 4 ancilla modes: local 4-7, NOT 2 like `heralded_cz` — this is a structural difference from `build_cz_insertion`, state it explicitly in the docstring).

`assert 0 <= i < n and 0 <= j < n and i != j` (interface symmetry with the other `build_*` functions, matching `build_cz_insertion`).

Returns `(circuit, ancilla_spec)` where `ancilla_spec` covers all 4 ancilla modes, each expected photon count 0 (vacuum both ends — a fundamentally different meaning than `heralded_cz`'s `herald_spec`, whose values are 1-photon herald counts). Read this from `PostProcessedControlledRotationsItem().build_experiment().in_heralds` (not hardcoded, matching `build_cz_insertion`'s pattern of reading `herald_spec` from the item's own `build_experiment()` rather than assuming). Name the returned spec distinctly (e.g. `ancilla_spec`, not `herald_spec`) so a future reader isn't misled into thinking this is a photon-click herald — document this distinction inline, since `15-CONTEXT.md`/`15-RESEARCH.md` both flag "post-selection + ancilla vacuum" (CP) vs. "ancilla heralding" (heralded_cz) as the core mechanism difference ARB-05 requires stating plainly.

Add parametrized tests to `tests/test_iqp_photonic_encoding.py`, matching `test_cz_insertion_phase_sign_computational_basis`'s exact style: at the 3 non-trivial α values (`π/6`, `π/3`, `2π/5`) plus the `α=π` boundary, assert `|amplitude|²` and sign on `MODULE_DUAL_RAIL` match `diag(1,1,1,e^{iα})` for `_build_cp_insertion_core`. At `α=π` specifically, add a dedicated boundary-agreement test asserting the sign pattern is IDENTICAL to `_build_cz_insertion_core`'s (both give `diag(1,1,1,-1)`) — this is the bare-core-level instance of ARB-06's "direct boundary-agreement test" requirement; the full-pipeline-level instance is Plan 15-04's job. Also add a basic contract test (`test_cp_insertion_returns_circuit_and_ancilla_spec`) mirroring `test_cz_insertion_returns_circuit_and_herald_spec`.
  </action>
  <verify>`pytest tests/test_iqp_photonic_encoding.py -v` — all new tests pass. Then run the full repo suite (`pytest -v`) to confirm zero regressions from this plan's additions.</verify>
  <done>build_cp_insertion(n, i, j, alpha) exists with a confirmed truth table matching diag(1,1,1,e^{i*alpha}) at all 4 tested alpha values, including an explicit boundary-agreement assertion against _build_cz_insertion_core's diag(1,1,1,-1) at alpha=pi. Full test suite green.</done>
</task>

</tasks>

<verification>
1. `_build_cp_insertion_core(alpha)`'s bare-circuit truth table matches `diag(1,1,1,e^{iα})` on `MODULE_DUAL_RAIL`, confirmed at 3 non-trivial α plus the `α=π` boundary.
2. At `α=π`, the sign pattern matches `_build_cz_insertion_core`'s already-confirmed `diag(1,1,1,-1)` exactly.
3. `build_cp_insertion(n, i, j, alpha)` returns `(Circuit, ancilla_spec)` following `build_cz_insertion`'s external-contract pattern.
4. `pytest tests/test_iqp_photonic_encoding.py -v` and the full repo suite pass with zero regressions.
</verification>

<success_criteria>
- CP's mode-convention wiring is de-risked standalone (bare circuit + PERM adapter only, no PBS/state-prep/pipeline), separate from and prior to full-pipeline integration (Plan 15-04) — matching 15-RESEARCH.md's explicit recommendation.
- The convention adapter (whatever combination was found to work) is documented as a convention adapter, not a bug fix, matching `_build_cz_insertion_core`'s established framing.
- Boundary-agreement confirmed at the bare-core level: CP(α=π) matches heralded_cz's diag(1,1,1,-1) exactly.
</success_criteria>

<output>
After completion, create `.planning/phases/15-arb-01-core-gate-de-risking-validation/15-02-SUMMARY.md`
</output>
