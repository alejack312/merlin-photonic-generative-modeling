---
phase: 15-arb-01-core-gate-de-risking-validation
plan: 04
type: execute
wave: 3
depends_on: ["15-02", "15-03"]
files_modified:
  - iqp_photonic_encoding.py
  - tests/test_iqp_photonic_encoding.py
  - docs/iqp-photonic-encoding.md
autonomous: true

must_haves:
  truths:
    - "photonic_cp_iqp_distribution(n, i, j, thetas, alpha) reproduces the extended exact reference (exact_qubit_iqp_distribution with pair_thetas={(i,j): alpha/4}) to TVD < 1e-6 at n=2 and n=3, across the 3 non-trivial alpha values (pi/6, pi/3, 2*pi/5)."
    - "CP at alpha=pi reproduces heralded_cz's existing photonic_weight2_iqp_distribution output end-to-end (the full-pipeline-level boundary-agreement test) -- not just at the bare-gate (Plan 15-01) or bare-core (Plan 15-02) level."
    - "Success probability is reported as an explicit table across alpha at the full-pipeline level (1 - postselect_failure_prob), never a single collapsed number."
    - "docs/iqp-photonic-encoding.md contains a descriptive, no-recommendation side-by-side comparison table between CP(alpha) and heralded_cz (success probability, ancilla/resource cost, circuit depth, post-selection vs ancilla-heralding mechanism)."
  artifacts:
    - path: "iqp_photonic_encoding.py"
      provides: "_build_weight2_cp_processor_no_postselect, _weight2_cp_input_state (or equivalent), and photonic_cp_iqp_distribution(n, i, j, thetas, alpha) -- the CP-specific analogue of the existing weight-2 measurement path"
      min_lines: 60
    - path: "tests/test_iqp_photonic_encoding.py"
      provides: "TVD validation tests (n=2,3, 3 alpha values), the full-pipeline boundary-agreement test against heralded_cz, and a success-probability-vs-alpha table test"
      min_lines: 40
    - path: "docs/iqp-photonic-encoding.md"
      provides: "heralded_cz-vs-CP comparison table and updated Open Questions/Conclusion sections reflecting arbitrary-alpha weight-2 validation"
  key_links:
    - from: "photonic_cp_iqp_distribution"
      to: "build_cp_insertion (Plan 15-02)"
      via: "Processor.add(mode_mapping_dict, cp_circuit) wiring qubit i/j's ports and ALL 4 tail ancilla modes (2n, 2n+1, 2n+2, 2n+3) -- CP has 4 ancilla modes, not heralded_cz's 2, so total_modes must be 2n+4, not 2n+2"
      pattern: "proc\\.add\\("
    - from: "photonic_cp_iqp_distribution"
      to: "exact_qubit_iqp_distribution"
      via: "pair_thetas={(i,j): alpha/4}, reusing exact_qubit_iqp_distribution unmodified per 15-RESEARCH.md's confirmation it is already generalized"
      pattern: "pair_thetas"
---

<objective>
Wire `build_cp_insertion` (Plan 15-02) into the full weight-2 pipeline (state prep → theta-folded diagonal layer → CP insertion via mode-mapping dict → conjugation → readout), build a postselect-free measurement path (`photonic_cp_iqp_distribution`) mirroring `photonic_weight2_iqp_distribution`'s manual-filtering pattern (Pitfall 3: `Processor.set_postselection()`/`add_herald()` cannot compose with later pipeline components — filter by hand instead), validate against the extended exact reference at TVD≤1e-6 for n=2,3 across all 3 non-trivial α, add the direct full-pipeline boundary-agreement test (CP at α=π vs. heralded_cz's already-validated output), and write the descriptive heralded_cz-vs-CP comparison table into `docs/iqp-photonic-encoding.md`.

**Structural warning before starting** (the single most likely, cheapest-to-check culprit behind `15-RESEARCH.md`'s prior end-to-end attempt failing at TVD~0.3): `CP`'s bare circuit has 4 ancilla modes (not `heralded_cz`'s 2). If the prior attempt mirrored `build_weight2_processor`'s exact mode arithmetic (`total_modes = 2n+2`, a 2-entry ancilla mapping), that alone would break everything regardless of PERM/convention correctness. Verify the mode-count arithmetic FIRST, explicitly, before assuming a deeper convention bug — see Task 1.

Purpose: satisfies ARB-03 (TVD validation), ARB-04 (success-probability-vs-α, full-pipeline level), ARB-05 (heralded_cz comparison + boundary-agreement), and ARB-06 (test coverage matching existing conventions).
Output: `photonic_cp_iqp_distribution` and its supporting builders in `iqp_photonic_encoding.py`, TVD + boundary-agreement tests in `tests/test_iqp_photonic_encoding.py`, and the comparison table in `docs/iqp-photonic-encoding.md`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-arb-01-core-gate-de-risking-validation/15-RESEARCH.md
@.planning/phases/15-arb-01-core-gate-de-risking-validation/15-CONTEXT.md
@iqp_photonic_encoding.py
@tests/test_iqp_photonic_encoding.py
@docs/iqp-photonic-encoding.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full pipeline wiring + manual-filtering measurement function</name>
  <files>iqp_photonic_encoding.py</files>
  <action>
**First, verify the mode-count arithmetic explicitly** (see objective's structural warning): `build_cp_insertion(n=2, i, j, alpha)` returns a local `Circuit(2n+4)` with 4 ancilla modes at local 4-7 (Plan 15-02). The outer `Processor` this gets wired into must therefore be sized `total_modes = 2n+4` (matching the OUTER register's `n`, not CP's fixed internal `n=2` — for this phase's scope, weight-2 pairs are always exactly 2 qubits, so this only matters for the outer circuit's total qubit count `n`, which can be 2 or 3), and the mode-mapping dict must map ALL 4 tail ancilla modes: `{2*n: 4, 2*n+1: 5, 2*n+2: 6, 2*n+3: 7}` (4 entries, not `build_weight2_processor`'s 2-entry `{2*n: 4, 2*n+1: 5}`). Get this right before debugging anything else.

Implement `_build_weight2_cp_processor_no_postselect(n, i, j, thetas, alpha)`, mirroring `_build_weight2_processor_no_herald`'s structure (state prep → theta-folded diagonal layer → CP insertion via the corrected mode-mapping dict → conjugation → readout), but with NO `Processor.add_herald()` and NO `Processor.set_postselection()` calls anywhere (Pitfall 3: these cannot compose with the later conjugation/readout components on the same modes — confirmed to crash with `AssertionError`). Theta folding uses the α↔θ relationship Plan 15-03 derived: `thetas_folded[i] += alpha/4`, `thetas_folded[j] += alpha/4` (never mutate the caller's list, matching `build_weight2_processor`'s convention exactly).

Implement `_weight2_cp_input_state(n, ancilla_spec)`, building the input `BasicState` for this bare processor: `all_h_input(n)`'s `{P:H},0` pattern for the n qubit ports, PLUS the 4 ancilla ports. Since `ancilla_spec`'s expected photon count is 0 (vacuum) at every ancilla mode — unlike `heralded_cz`'s herald ancilla, which needs a REAL input photon (Phase 10 Pitfall 1) — determine empirically whether any special annotation is needed here at all (12-RESEARCH.md's `{P:V}` fix was specifically for a REAL photon on a polarization-tracking circuit; a genuinely empty/vacuum mode may need no annotation, or may need an explicit `0` rather than an annotated state). Do not assume the `{P:V}` fix transfers unchanged — verify against a known-good reference (e.g. compare a definite computational-basis input's output against `exact_qubit_iqp_distribution` at a trivial configuration first, before running the full TVD sweep) and state explicitly in a code comment which choice was correct and why.

Implement `photonic_cp_iqp_distribution(n, i, j, thetas, alpha)`: build the no-postselect processor, run `Analyzer`, and for each output state:
- If ANY of the 4 ancilla output modes is non-zero, count that probability into `postselect_failure_prob` (name this distinctly from `heralded_cz_failure_prob`/`herald_failure_prob` — different gate mechanism, per ARB-05's requirement to state the distinction plainly: CP succeeds on ancilla VACUUM, not a photon click).
- Otherwise, decode the remaining `2n` qubit modes via `fock_to_bitstring` exactly as `photonic_weight2_iqp_distribution` does (`None` → `residual`, otherwise → `dist`).
- Renormalize `dist`/`residual` by `(1 - postselect_failure_prob)`, matching the existing 3-tuple convention (`dist`, `residual`, `postselect_failure_prob` — three separate, never-merged numbers).

Cast `alpha` to `float()` at every call site (Pitfall 1 — especially inside any sweep using `np.linspace`-style generation).
  </action>
  <verify>A smoke-test call at n=2, i=0, j=1, thetas=[0.0, 0.0], alpha=pi produces a dist/residual/postselect_failure_prob triple where dist matches heralded_cz's photonic_weight2_iqp_distribution output at the same configuration (both realize the same CZ gate at this boundary) -- run this manually before writing the full test suite in Task 2, since it is the cheapest available correctness signal.</verify>
  <done>photonic_cp_iqp_distribution(n, i, j, thetas, alpha) exists, uses the corrected 4-ancilla-mode mapping, and produces a smoke-test result matching heralded_cz's output at the alpha=pi boundary.</done>
</task>

<task type="auto">
  <name>Task 2: TVD validation, boundary-agreement test, success-probability-vs-α table (full-pipeline level)</name>
  <files>tests/test_iqp_photonic_encoding.py</files>
  <action>
Add a parametrized TVD test at n=2, pair (0,1), across the 3 non-trivial α values from `15-CONTEXT.md` (`π/6`, `π/3`, `2π/5`): compare `exact_qubit_iqp_distribution(n, thetas, pair_thetas={(0,1): alpha/4})` against `photonic_cp_iqp_distribution(n, 0, 1, thetas, alpha)`, asserting `TVD < 1e-6` — the SAME bar `WT2-05` cleared for `heralded_cz` (do not relax it for this different gate mechanism, per `15-CONTEXT.md`'s locked decision).

Add at least one n=3 test with a bystander qubit at nonzero weight-1 theta, mirroring `test_wt2_tvd_gate_n3_bystander_qubit`'s style, for at least one of the 3 α values.

Add a direct full-pipeline boundary-agreement test: `photonic_cp_iqp_distribution` at `alpha=π` (the corrected literal value per `15-CONTEXT.md` — NOT `π/4`) vs. the ALREADY-VALIDATED `photonic_weight2_iqp_distribution` (heralded_cz-based), at the same `(n, i, j, thetas)` configuration. Assert the two `dist` dictionaries agree (TVD between them near 0, or bitstring-by-bitstring match within tolerance) — this confirms `CP(α=π)` genuinely reproduces `heralded_cz`'s construction end-to-end, the full-pipeline-level confirmation ARB-05/ARB-06 require (Plans 15-01/15-02 already confirmed the bare-gate and bare-core levels; this is the missing third level).

Add a test that prints/asserts a success-probability-vs-α table at the full-pipeline level (`1 - postselect_failure_prob` across all 3 tested α, plus `α=π`) — satisfies ARB-04 at the pipeline level (Plan 15-01 already satisfied it at the bare-gate level; this closes the loop at the level that actually matters for the shipped pipeline).
  </action>
  <verify>`pytest tests/test_iqp_photonic_encoding.py -v` — all new tests pass (TVD < 1e-6 at every tested configuration, boundary-agreement test passes). Then run the full repo test suite to confirm zero regressions.</verify>
  <done>TVD validation passes at n=2,3 across all 3 non-trivial alpha values; the alpha=pi boundary-agreement test confirms CP matches heralded_cz's output end-to-end; success-probability-vs-alpha is reported as an explicit table at the pipeline level.</done>
</task>

<task type="auto">
  <name>Task 3: heralded_cz-vs-CP comparison table + doc finalization</name>
  <files>docs/iqp-photonic-encoding.md</files>
  <action>
Add the side-by-side comparison table in the same section as Plan 15-03's general-α derivation (per `15-CONTEXT.md`'s locked decision: "Lives in docs/iqp-photonic-encoding.md, alongside the new general-α derivation"). Required dimensions (per `15-CONTEXT.md`'s locked scope): success probability (CP's varies with α, e.g. 1/9 at α=π; heralded_cz's is fixed at 2/27), ancilla/resource cost (CP: 4 ancilla modes, post-selected on vacuum; heralded_cz: 2 ancilla modes, heralded on a photon click), circuit depth (state whatever was actually measured/observed), and mechanism (post-selection + ancilla vacuum vs. ancilla heralding — stated plainly, not conflated, per ARB-05). **Purely descriptive — do not recommend when to use which gate family** (`15-CONTEXT.md`'s locked constraint: "does not recommend when to use which gate family. That judgment call is left for later").

State the measured full-pipeline TVD results (n=2,3, across the tested α) from Task 2, and the boundary-agreement confirmation, in prose near the comparison table.

Update the "Open questions and limitations, collected" and "Conclusion and Open Questions" sections' generator-weight-scope bullets (currently stating weight-2 is fixed-π/4-only): weight-2 is no longer fixed-angle-only — `CP(α)` is validated at the tested arbitrary α values, satisfying this phase's ROADMAP success criteria. State the remaining scope explicitly: Phase 16 extends to denser α sweeps (8-16 points), mixed weight-1+arbitrary-θ weight-2 composability, and Forge-based postselection verification — not yet done by this phase.
  </action>
  <verify>Read back the updated doc sections: confirm the comparison table is present with all 4 required dimensions, no "which to prefer" recommendation language, the measured TVD results and boundary-agreement confirmation are stated, and the Open Questions/Conclusion sections no longer claim weight-2 is fixed-π/4-only without qualification.</verify>
  <done>docs/iqp-photonic-encoding.md's comparison table, TVD results, and updated scope statements are all present and consistent with this plan's measured results.</done>
</task>

</tasks>

<verification>
1. `photonic_cp_iqp_distribution` matches the extended exact reference to TVD < 1e-6 at n=2,3 across the 3 non-trivial α values.
2. The α=π boundary-agreement test confirms CP's full pipeline output matches heralded_cz's already-validated output.
3. Success-probability-vs-α is reported as an explicit table at the full-pipeline level.
4. `pytest tests/test_iqp_photonic_encoding.py -v` and the full repo suite pass with zero regressions.
5. `docs/iqp-photonic-encoding.md` contains the descriptive comparison table and updated scope statements.
</verification>

<success_criteria>
- ARB-03: TVD validation against the extended exact reference passes at ≥1 (here, all 3) representative non-special α, n=2,3, ≤1e-6.
- ARB-04: success probability reported as an explicit function of α at the full-pipeline level, never a single number.
- ARB-05: written comparison against heralded_cz, stated plainly (mechanism difference not conflated), no recommendation.
- ARB-06: new test coverage added matching existing tolerance/parametrization conventions, including the direct boundary-agreement test.
</success_criteria>

<output>
After completion, create `.planning/phases/15-arb-01-core-gate-de-risking-validation/15-04-SUMMARY.md`
</output>
