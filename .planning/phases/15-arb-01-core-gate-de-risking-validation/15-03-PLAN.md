---
phase: 15-arb-01-core-gate-de-risking-validation
plan: 03
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - docs/iqp-photonic-encoding.md
autonomous: false

must_haves:
  truths:
    - "docs/iqp-photonic-encoding.md contains a new general-alpha operator identity section, added ALONGSIDE (not replacing) the existing fixed-pi/4 derivation, connecting CP(alpha) to exp(i*theta*Z_i*Z_j), with full step-by-step algebra matching this project's existing rigor bar."
    - "The alpha (CP's own raw dial) vs theta (Z_iZ_j generator angle, this codebase's pair_thetas convention) relationship (alpha=4*theta) is stated explicitly and unambiguously, correcting 15-CONTEXT.md's originally-stated alpha=pi/4 boundary to the verified alpha=pi."
    - "A closed-form success-probability-vs-alpha relationship is derived analytically, then compared against Plan 15-01's measured sweep (cp_gate_derisking.py's table) and shown to match."
    - "The owner attempted the derivation FIRST (per this repo's CLAUDE.md attempt-first gating), before Claude wrote it into the doc -- the actual Q&A (including wrong turns) is recorded in the doc, not just the polished final derivation, matching this document's existing ENC-01/ENC-05 style."
  artifacts:
    - path: "docs/iqp-photonic-encoding.md"
      provides: "New general-alpha operator identity section (algebra, alpha-vs-theta distinction, closed-form success-probability derivation, owner's attempt-first Q&A transcript) added alongside the existing Ingredient 2 fixed-pi/4 derivation"
  key_links: []
---

<objective>
Derive and write down the general-α operator identity connecting `CP(α)` to `exp(iθZᵢZⱼ)`, extending `docs/iqp-photonic-encoding.md`'s existing fixed-π/4 derivation — following this repo's CLAUDE.md attempt-first gating: Claude explains the ingredients/approach, the owner attempts the derivation themselves first, Claude then writes up/checks the confirmed derivation into the doc. Full step-by-step algebra required (matches this project's existing rigor bar — defensible unaided to Vincent Espitalier), plus a closed-form success-probability-vs-α relationship derived analytically, then confirmed against Plan 15-01's measured sweep.

**This plan has a mandatory human checkpoint (Task 1) — do not skip to Task 2 without it.** Per `CLAUDE.md`'s "Attempt-first gating" and `15-CONTEXT.md`'s explicit lock: "Attempt-first gating applies... Do not skip straight to writing the derivation."

Purpose: satisfies ARB-02 (general-α operator identity written down) without shortcutting the owner's own understanding — the risk this project's CLAUDE.md names explicitly ("finishing with code the owner can't explain unaided... is worse than not finishing").
Output: a new section in `docs/iqp-photonic-encoding.md`, alongside the existing Ingredient 2 π/4-only derivation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-arb-01-core-gate-de-risking-validation/15-RESEARCH.md
@.planning/phases/15-arb-01-core-gate-de-risking-validation/15-CONTEXT.md
@docs/iqp-photonic-encoding.md
@cp_gate_derisking.py
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: ARB-02 attempt-first derivation — owner attempts before Claude writes anything</name>
  <what-to-present>
Before asking the owner to attempt anything, present these confirmed ingredients (already established, do not re-derive them — cite where each comes from):
1. `CP(α) = diag(1,1,1,e^{iα})` on the two-qubit computational basis `{00,01,10,11}` — confirmed by Plan 15-01's `cp_gate_derisking.py` at 4 tested α values.
2. `WP(φ,0) = exp(iφZ)` on a single qubit's polarization mode — already established in `docs/iqp-photonic-encoding.md`'s Ingredient 2.
3. The EXISTING fixed-case identity already in the doc: `exp(iπ/4·Z_iZ_j) = CZ · exp(iπ/4·Z_i) · exp(iπ/4·Z_j)` (up to global phase), where `CZ = diag(1,1,1,-1)`.
4. The confirmed boundary fact from Plan 15-01/15-02: `CP(α=π)` reproduces `CZ` exactly (`15-CONTEXT.md`'s corrected note — NOT `α=π/4`).
5. The goal: find the GENERAL relationship connecting `CP(α)` (for arbitrary α) to `exp(iθZᵢZⱼ)` (for arbitrary θ) — i.e., what single-qubit corrections (if any) combine with `CP(α)` to realize `exp(iθZᵢZⱼ)`, and what the α↔θ relationship is. A second sub-goal: derive the success probability (`|amplitude|²`, per Plan 15-01's measurements) as a closed-form function of α.
  </what-to-present>
  <owner-task>
Ask the owner to sketch or attempt, on paper or in their own words, BEFORE any answer is written into the doc:
(a) What single-qubit `WP`/phase corrections (if any) need to accompany `CP(α)` to realize `exp(iθZᵢZⱼ)` for general θ, and what is `α` as a function of `θ` (or vice versa)? (Hint budget, if the owner is stuck after a genuine attempt: point them at ingredient 3 above and ask them to generalize the same commuting-diagonal-operator factoring argument that produced it, this time leaving `φ` general instead of fixing it at `π/4`.)
(b) How would you derive the success probability as a function of α in closed form, using the ingredients already known about how `PostProcessedControlledRotationsItem` is built (post-selection on ancilla vacuum + per-qubit data validity)?
Record the actual attempt — including any wrong turns or incomplete answers — matching the style already used throughout this document (see ENC-01's "Owner's Attempt" and ENC-05's multi-round Q&A transcripts). Do not silently correct and move on; show the correction the way the rest of the document does.
  </owner-task>
  <resume-signal>Owner has shared their attempt (or explicitly says "just implement this, I've got the concept already" per CLAUDE.md's stated exception) — proceed to Task 2 only after this.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write the confirmed general-α derivation into docs/iqp-photonic-encoding.md</name>
  <files>docs/iqp-photonic-encoding.md</files>
  <action>
Using Task 1's recorded Q&A as the basis (check the owner's attempt against the algebra; correct if needed, exactly the way ENC-01/ENC-05 record corrections rather than hiding them), add a new subsection immediately following the existing fixed-π/4 derivation inside Ingredient 2 (per `15-CONTEXT.md`'s locked decision: "added alongside the existing fixed-π/4 derivation... not replacing it — the π/4 section stays intact, the new section extends it and shows π/4 as the special case").

Content required:
1. **Full step-by-step algebra for the general identity** — the candidate result (pending confirmation against Task 1's attempt): `CP(4φ) · WP(φ,0)_i · WP(φ,0)_j = e^{iφ} · exp(iφ·Z_iZ_j)`, derived the same way the existing π/4 case was (write `I − Z_i − Z_j + Z_iZ_j` generalized appropriately, or the direct 4×4 diagonal-matrix-multiplication argument — match whichever form the existing π/4 derivation uses, generalized to arbitrary φ). Explicitly verify the reduction: substituting `φ=π/4` must recover `CP(π)=CZ` exactly, matching the existing special case already in the doc — show this substitution as an explicit check, not an assertion.
2. **The α (CP's raw dial) vs θ (Z_iZ_j generator angle) distinction, stated unambiguously**: `α=4θ` (equivalently `θ=α/4`). State this is the corrected relationship (`15-CONTEXT.md`'s note: the document's earlier draft conflated these). Use this exact relationship anywhere existing text (or the new section) refers to "θ=π/4 boundary" — it corresponds to `α=π`, not `α=π/4`.
3. **Closed-form success-probability-vs-α relationship**, derived analytically (using the post-selection-on-ancilla-vacuum + data-validity mechanism, per `15-RESEARCH.md`'s Architecture Patterns section on how `PostProcessedControlledRotationsItem` is built), then a table or plot comparing the derived closed form against Plan 15-01's `cp_gate_derisking.py` measured sweep (should match to floating-point precision, since 15-RESEARCH.md already independently confirmed the algebra as an abstract 4×4 matrix identity — if the derived closed form does NOT match the measured sweep, that is a real finding to report honestly, not paper over).
4. **The attempt-first Q&A transcript** from Task 1, recorded in the same style as ENC-01/ENC-05 (their actual words, corrections shown, not smoothed into only the final polished answer).

Follow the document's existing "Owner's Attempt" subsection convention (see ENC-01, ENC-03, ENC-04 for the established pattern).
  </action>
  <verify>Read back the new section: confirm it contains (a) full step-by-step algebra with the π/4→CP(π)=CZ reduction shown explicitly, (b) the α=4θ relationship stated unambiguously, (c) a closed-form success-probability-vs-α expression compared against Plan 15-01's measured table, (d) the recorded attempt-first Q&A transcript. Confirm the existing fixed-π/4 section is unmodified/intact.</verify>
  <done>docs/iqp-photonic-encoding.md contains the new general-alpha derivation section, added alongside (not replacing) the existing fixed-pi/4 section, with all four required elements present.</done>
</task>

</tasks>

<verification>
1. Task 1's checkpoint actually occurred — the owner's attempt (or explicit skip signal) is on record before Task 2 started.
2. `docs/iqp-photonic-encoding.md`'s new section contains full step-by-step algebra, the α=4θ distinction, a closed-form success-probability-vs-α expression validated against Plan 15-01's data, and the recorded Q&A transcript.
3. The existing fixed-π/4 derivation section remains intact and unmodified.
</verification>

<success_criteria>
- ARB-02 satisfied: general-α operator identity derived and written down, extending (not replacing) the existing fixed-π/4 derivation.
- Attempt-first gating honored: the owner's own attempt is on record before the written derivation, per this repo's CLAUDE.md.
- Success-probability-vs-α derived analytically AND confirmed empirically (CONTEXT.md's explicit requirement — not left purely empirical).
</success_criteria>

<output>
After completion, create `.planning/phases/15-arb-01-core-gate-de-risking-validation/15-03-SUMMARY.md`
</output>
