---
phase: 15-arb-01-core-gate-de-risking-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cp_gate_derisking.py
  - tests/test_cp_gate_derisking.py
autonomous: true

must_haves:
  truths:
    - "Running `python cp_gate_derisking.py` measures PostProcessedControlledRotationsItem's bare 8-mode circuit (n=2) via Simulator.prob_amplitude at 3 non-trivial alpha values (pi/6, pi/3, 2*pi/5) plus the alpha=pi boundary check, and prints/asserts phase(amp on |1,1>)/phase(amp on |0,0>) == e^{i*alpha} at every value tested."
    - "At alpha=pi specifically, the script asserts |amplitude|^2 == 1/9 (the post-selected-construction literature figure already cited in docs/iqp-photonic-encoding.md, now independently confirmed for this exact gate) and confirms the sign pattern exactly matches heralded_cz's diag(1,1,1,-1) -- CP(alpha=pi) is CP's own dial value that reproduces this boundary, NOT alpha=pi/4 (15-CONTEXT.md's corrected note); theta=pi/4 is the separate Z_iZ_j generator-angle convention this codebase's pair_thetas already uses, related by alpha=4*theta."
    - "Success probability (|amplitude|^2) is printed as an explicit table across all 4 tested alpha values, never collapsed to a single number."
  artifacts:
    - path: "cp_gate_derisking.py"
      provides: "measure_cp_amplitudes(alpha, n=2) and a main() that prints a phase/magnitude table and asserts phase(amp)=e^{i*alpha} plus the alpha=pi boundary check against heralded_cz's diag(1,1,1,-1)"
      min_lines: 60
    - path: "tests/test_cp_gate_derisking.py"
      provides: "Pytest regression coverage for the phase/magnitude checks, parametrized over the 3 non-trivial alpha values plus the alpha=pi boundary"
      min_lines: 30
  key_links:
    - from: "cp_gate_derisking.py"
      to: "perceval.components.core_catalog.controlled_rotation_gates.PostProcessedControlledRotationsItem"
      via: "item.build_circuit(n=2, alpha=float(alpha)) -- bare circuit, no build_experiment() wrapper, matching heralded_cz_derisking.py's measure_cz_phase pattern"
      pattern: "PostProcessedControlledRotationsItem\\(\\)\\.build_circuit"
    - from: "tests/test_cp_gate_derisking.py"
      to: "cp_gate_derisking.py"
      via: "direct function imports, matching tests/test_heralded_cz_derisking.py's import pattern"
      pattern: "from cp_gate_derisking import"
---

<objective>
Build a standalone script and committed pytest test that measure `PostProcessedControlledRotationsItem`'s (the `CP(α)` gate) actual behavior in this repo's Perceval installation — confirms `CP(α) = diag(1,1,1,e^{iα})` via `Simulator.prob_amplitude` on the bare 8-mode circuit (n=2) at ≥3 non-trivial α values plus the `α=π` boundary check (which reproduces `heralded_cz`'s `CZ=diag(1,1,1,-1)` exactly — NOT `α=π/4`, per `15-CONTEXT.md`'s corrected note), reporting success probability as an explicit table, never a single number.

Purpose: de-risk `PostProcessedControlledRotationsItem` standalone, before any circuit-wiring or full-pipeline integration is attempted (Plans 15-02/15-04). Satisfies ARB-01 (gate phase/structure confirmed at ≥3 non-trivial α) and lays the empirical groundwork ARB-04 (success-probability-vs-α table) needs.
Output: `cp_gate_derisking.py`, `tests/test_cp_gate_derisking.py`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-arb-01-core-gate-de-risking-validation/15-RESEARCH.md
@.planning/phases/15-arb-01-core-gate-de-risking-validation/15-CONTEXT.md
@heralded_cz_derisking.py
@tests/test_heralded_cz_derisking.py
@docs/iqp-photonic-encoding.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build cp_gate_derisking.py — CP(α) phase/structure confirmation</name>
  <files>cp_gate_derisking.py</files>
  <action>
Create the standalone module following `heralded_cz_derisking.py`'s established pattern: module docstring stating this reproduces `15-RESEARCH.md`'s live-measured numbers as executable, asserted checks, plain module-level functions returning data, a `main()` that prints results and asserts, `if __name__ == "__main__": main()`.

**Gate identity confirmation (ARB-01 criterion 1, reuse 15-RESEARCH.md's "Code Examples" section as the base):**
- `measure_cp_amplitudes(alpha, n=2)`: build `PostProcessedControlledRotationsItem().build_circuit(n=n, alpha=float(alpha))` (bare `Circuit(4n)` — NOT `build_experiment()`, which drops phase-sensitive information and adds herald/postselect metadata the phase-only Simulator path doesn't need; matches `heralded_cz_derisking.py`'s `measure_cz_phase` pattern exactly). Cast `alpha` to plain `float` explicitly at the call site (Pitfall 1 — `TypeError` otherwise; this matters even more here than for `heralded_cz` since this function will be called in a loop over multiple α values, where a `numpy.float64` from any generator would silently break it).
- Use `Simulator(SLOSBackend())` on the bare circuit. Ancilla modes (local 4..2n+3, i.e. modes 4-7 for n=2) held at vacuum (`[0]*(2*n)`) on both input and output. For each of the 4 computational-basis combos, build `BasicState` using Perceval's own `Encoding.DUAL_RAIL` convention (`DUAL_RAIL = {'0': (1, 0), '1': (0, 1)}`, per 15-RESEARCH.md's verified convention — note this is DIFFERENT from this module's own `MODULE_DUAL_RAIL` convention used elsewhere in this repo; that distinction is Plan 15-02's job, not this one). Return a dict `{(ctrl_bit, data_bit): amplitude}`.

**α values to test — state the α (CP's own raw dial) vs θ (Z_iZ_j generator angle, this codebase's `pair_thetas` convention) distinction explicitly in a module-level comment, since 15-CONTEXT.md flags this as the single highest-priority disambiguation:**
- 3 non-trivial values: `π/6`, `π/3`, `2π/5` (per 15-CONTEXT.md's locked choice — round fractions of π with varied denominators).
- 1 boundary value: `α=π` (NOT `α=π/4`) — this is CP's own dial value that reproduces `θ=π/4` (this codebase's existing `Z_iZ_j` generator-angle convention, matching `pair_thetas={(0,1): np.pi/4}` in existing tests) via the relationship `α=4θ`. Document this relationship inline with a comment citing 15-CONTEXT.md's correction.

**Assertions, for every α tested:**
- `|amplitude|²` is uniform across all 4 basis-state combos (genuine phase-only gate, no population distortion) — assert with `np.isclose`.
- `amp(1,1) / amp(0,0)` matches `e^{iα}` to floating-point precision (`~1e-6` tolerance, matching PHASE_TOLERANCE conventions elsewhere in this repo).

**At α=π specifically (the boundary check):**
- Assert `|amplitude|² ≈ 1/9` (the "post-selected construction" literature figure already cited, unverified-for-this-exact-gate, in `docs/iqp-photonic-encoding.md`'s ENC-01 section — this independently confirms it for `PostProcessedControlledRotationsItem` specifically). Do NOT conflate this with `heralded_cz`'s 2/27 figure — these are different numbers for different gate mechanisms (post-selection vs. heralding); state this explicitly in a comment.
- Assert the sign pattern exactly matches `heralded_cz`'s `diag(1,1,1,-1)`: negative real amplitude on `(1,1)`, positive on the other three.

**main():**
- Print a success-probability-vs-α table: for each of the 4 tested α values, print α, `|amplitude|²`, and the measured phase — this is the explicit table ARB-04 requires, never a single collapsed number.
- Print PASS/FAIL per check category (phase-matches-e^{iα}, uniformity, α=π boundary magnitude, α=π boundary sign).
- `assert` all checks, matching `heralded_cz_derisking.py`'s convention of assertions after printed PASS/FAIL lines.
  </action>
  <verify>`python cp_gate_derisking.py` runs to completion with exit code 0, prints "PASS" for every check, and the printed table shows phase(amp) matching e^{iα} at all 4 tested α, with the α=π row showing |amplitude|²≈0.1111 (1/9) and a sign flip matching heralded_cz's diag(1,1,1,-1).</verify>
  <done>Script runs standalone, all assertions pass, CP(α)'s phase/structure is confirmed programmatically at 3 non-trivial α plus the α=π boundary (not just eyeballed), with the success-probability-vs-α table printed explicitly.</done>
</task>

<task type="auto">
  <name>Task 2: Write tests/test_cp_gate_derisking.py — pytest regression coverage</name>
  <files>tests/test_cp_gate_derisking.py</files>
  <action>
Follow `tests/test_heralded_cz_derisking.py`'s exact pattern: `sys.path.insert(0, ...)` to import the root-level module, import specific functions from `cp_gate_derisking` (not `import *`), one test function per distinct claim, `pytest.approx`/`np.isclose` for numeric checks.

Tests to include:
- `test_cp_phase_matches_e_i_alpha` — parametrized over the 3 non-trivial α values (`π/6`, `π/3`, `2π/5`), asserts `amp(1,1)/amp(0,0)` matches `e^{iα}` to floating-point precision.
- `test_cp_amplitude_uniform_magnitude_per_alpha` — parametrized over the same 3 α values plus `α=π`, asserts `|amplitude|²` is identical across all 4 computational-basis combos for a given α.
- `test_cp_boundary_alpha_pi_matches_1_9_and_negative_sign` — asserts `|amplitude|² ≈ 1/9` and the sign pattern (`(1,1)` negative, others positive) at `α=π` specifically — the literal boundary-agreement value per 15-CONTEXT.md's correction (not `α=π/4`).

Do not duplicate script logic — import and re-assert on `measure_cp_amplitudes`, matching `test_heralded_cz_derisking.py`'s division of labor (script = build + print + assert once in `main()`; test file = import + re-assert as pytest cases).
  </action>
  <verify>`pytest tests/test_cp_gate_derisking.py -v` — all tests pass. Then `pytest tests/test_iqp_photonic_encoding.py tests/test_perceval_fluency_demo.py tests/test_heralded_cz_derisking.py -v` — full existing suite remains green (no regression from anything touched in Task 1/2, which only add new files).</verify>
  <done>New test file passes standalone; existing full test suite passes unmodified.</done>
</task>

</tasks>

<verification>
1. `python cp_gate_derisking.py` exits 0, all PASS, prints the success-probability-vs-α table.
2. `pytest tests/test_cp_gate_derisking.py -v` — all new tests pass.
3. `pytest tests/test_iqp_photonic_encoding.py tests/test_perceval_fluency_demo.py tests/test_heralded_cz_derisking.py -v` — existing suite stays green, no regression.
</verification>

<success_criteria>
- `CP(α)`'s phase/structure confirmed at 3 non-trivial α values (`π/6`, `π/3`, `2π/5`) plus the `α=π` boundary, via `Simulator.prob_amplitude` on the bare circuit (ARB-01).
- The `α=π` boundary check independently confirms `CP` reproduces `heralded_cz`'s `CZ=diag(1,1,1,-1)` exactly, with the α-vs-θ relationship (`α=4θ`) stated unambiguously (not conflated with the incorrect `α=π/4` value 15-CONTEXT.md flagged and corrected).
- Success probability reported as an explicit table across α, never collapsed to one number (ARB-04, bare-gate level).
- Existing test suite stays green.
</success_criteria>

<output>
After completion, create `.planning/phases/15-arb-01-core-gate-de-risking-validation/15-01-SUMMARY.md`
</output>
