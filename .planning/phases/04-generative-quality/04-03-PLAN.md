---
phase: 04-generative-quality
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - results/phase4_summary.md
autonomous: false

must_haves:
  truths:
    - "A written summary documents which artifacts/path were used to reach the final judgment (sigma=0.1 cheap-check only, or full sweep), and any hyperparameter tuning performed -- satisfying Phase 4's success criterion 2 ('Any hyperparameter or architecture tuning needed to reach recognizable output is documented')."
    - "The owner has reviewed the final relevant plot(s) and the ring/gap-band metric, and explained in their own words whether the generated distribution recognizably forms two rings -- not an automated metric-threshold pass."
    - "GEN-07 is marked complete only after this explicit human confirmation, per this project's self-explanation-checkpoint pattern (CLAUDE.md, same pattern as Phase 3's GEN-06 checkpoint)."
    - "If the final judgment is that no sigma value produced genuinely ring-like output, that outcome is reported plainly (not glossed over), per PROJECT.md's 'don't gloss over it' rule -- the exact phrasing is worked out with the owner once real results are in hand, per 04-CONTEXT.md's explicit deferral."
  artifacts:
    - path: "results/phase4_summary.md"
      provides: "which sigma(s) were tried, the cheap-check and (if run) sweep outcome, the ring/gap-band metric values for the relevant sigma(s), and the final visual judgment"
      min_lines: 15
  key_links:
    - from: "results/phase4_summary.md"
      to: "results/phase4_scatter_comparison.png, results/phase4_heatmap_comparison.png, results/phase4_sweep_comparison.png (if it exists)"
      via: "explicit reference to which PNG(s) and metric values the judgment was based on"
      pattern: "phase4_.*\\.png"
---

<objective>
Aggregate Phase 4's evidence (from Plan 04-01's cheap check, and Plan 04-02's sweep if it ran) into a short written summary, then run the phase's mandatory human-verification checkpoint: the owner reviews the actual final plot(s) and the ring/gap-band metric, and confirms — in their own words — whether GEN-07 is genuinely met. This must NOT be an automated metric-threshold pass (04-CONTEXT.md, "Success judgment method").

Purpose: This is Phase 4's closing checkpoint, structurally identical in spirit to Phase 3's GEN-06 self-explanation checkpoint (CLAUDE.md's self-explanation-checkpoints rule) — GEN-07 is a claim about what a human recognizes in the generated samples, so a human, not a script, must be the one who confirms it.
Output: `results/phase4_summary.md` documenting the tuning path and final metric values, and the owner's explicit GEN-07 confirmation (or honest non-confirmation) recorded in the plan's SUMMARY.md.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-generative-quality/04-CONTEXT.md
@.planning/phases/04-generative-quality/04-01-SUMMARY.md
@.planning/phases/04-generative-quality/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write results/phase4_summary.md documenting the path taken and final metrics</name>
  <files>results/phase4_summary.md</files>
  <action>
Read `04-01-SUMMARY.md` (and `04-02-SUMMARY.md`, which may say "skipped" if 04-01's decision was "sufficient") to determine which path was actually taken. Write `results/phase4_summary.md` with:

1. **Path taken**: whether the sigma=0.1 cheap check alone was judged sufficient, or the full 5-value sweep was run (and why, quoting the owner's 04-01 decision notes if any were given).
2. **Artifacts reviewed**: list the specific PNG file(s) that were the basis for judgment — `results/phase4_scatter_comparison.png` + `results/phase4_heatmap_comparison.png` always; plus `results/phase4_sweep_comparison.png` and `results/phase4_sweep_metrics.csv` if the sweep ran.
3. **Ring/gap-band metric values**: the actual numbers (ring_mass, gap_mass) for the sigma value(s) that were considered, pulled from the printed output recorded in 04-01-SUMMARY.md / 04-02-SUMMARY.md — report real numbers, not placeholders.
4. **Any tuning performed**: which sigma value the final judgment is based on, and whether the epoch-increase escape hatch (04-CONTEXT.md) was used (expected: no, for this first pass — note explicitly if it was).

This document is the artifact that satisfies Phase 4's success criterion 2 ("Any hyperparameter or architecture tuning needed to reach recognizable output is documented") — write it against the real values from the actual runs, not hypothetically.
  </action>
  <verify>`results/phase4_summary.md` exists, is non-empty, and contains real (not placeholder) sigma/metric values matching what 04-01-SUMMARY.md and 04-02-SUMMARY.md actually recorded.</verify>
  <done>`results/phase4_summary.md` documents the actual path taken, the specific artifacts reviewed, and real ring/gap-band metric numbers for the sigma(s) considered.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 4's complete evidence set for GEN-07: `results/phase4_scatter_comparison.png` and `results/phase4_heatmap_comparison.png` (real vs. generated, sigma=0.1), and — if Plan 04-02 ran — `results/phase4_sweep_comparison.png` and `results/phase4_sweep_metrics.csv` covering all 5 SIGMA_GRID values. `results/phase4_summary.md` documents which path was taken and the real ring/gap-band metric numbers.
  </what-built>
  <how-to-verify>
1. Open `results/phase4_summary.md` first — it tells you which plot(s) to look at and what path was taken (cheap-check-only, or full sweep).
2. Open the relevant PNG(s): `results/phase4_scatter_comparison.png` / `results/phase4_heatmap_comparison.png`, and `results/phase4_sweep_comparison.png` if the sweep ran. Look at the actual generated distribution(s) — do they visibly form two rings resembling the real circles data, not a blob, not concentrated in the empty middle?
3. Read the ring/gap-band metric values in `results/phase4_summary.md` — high `ring_mass` and low `gap_mass` supports what you see in the plots, but the plot is the primary evidence, not the number (04-CONTEXT.md: "not eyeball-only, and explicitly not a full Phase-5-style benchmark").
4. Per this project's CLAUDE.md self-explanation-checkpoint rule: explain in your own words why the generator's output does or doesn't recognizably approximate the two-ring shape, referencing the specific plot you looked at. If you can't explain it, or the result genuinely doesn't look ring-like even after whatever tuning was attempted, say so plainly — this is not a pass/fail script, and a "no" here is a valid, useful outcome per PROJECT.md's "don't gloss over it" rule, not something to be talked past.
5. If the result is a clear "no" even after the full sweep: this is the point 04-CONTEXT.md explicitly deferred ("how to report a 'no sigma value worked' outcome... Claude proposes a plain, honest write-up... and checks with the owner rather than deciding unilaterally") — flag this back rather than silently marking GEN-07 complete.
  </how-to-verify>
  <resume-signal>Type "approved" once you've reviewed the actual plot(s)/metric and can explain in your own words why GEN-07 is genuinely met, or describe what you're seeing instead (e.g. "still a blob at every sigma" or "ring-like at sigma=0.2 but not the others") so the next step (more tuning, or an honest non-met write-up) can be planned.</resume-signal>
</task>

</tasks>

<verification>
`results/phase4_summary.md` exists with real values. The owner has given an explicit "approved" (or explicit non-approval with reasoning) on the final checkpoint — this response, verbatim, must be captured in the plan's SUMMARY.md, matching Phase 3's precedent of recording the human-verify outcome rather than assuming it.
</verification>

<success_criteria>
1. Generated samples, plotted, were reviewed by the owner and judged against the real circles data's two-ring shape (Phase 4 success criterion 1) — with the owner's own explanation captured, not just a checkbox.
2. Any hyperparameter/architecture tuning performed is documented in results/phase4_summary.md (Phase 4 success criterion 2).
3. GEN-07's status (met / not met, with reasoning) is recorded honestly, whichever way it turned out.
</success_criteria>

<output>
After completion, create `.planning/phases/04-generative-quality/04-03-SUMMARY.md`, recording the owner's checkpoint response verbatim and the final GEN-07 status.
</output>
