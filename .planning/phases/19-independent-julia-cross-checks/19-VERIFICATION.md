---
phase: 19-independent-julia-cross-checks
verified: 2026-08-17T15:48:06Z
status: passed
score: 5/5 must-haves verified
---

# Phase 19: Independent Julia Cross-Checks Verification Report

**Phase Goal:** Independently cross-check the Python/Perceval pipeline's exact and lossy distributions using the Julia toolchain confirmed working in Phase 14 -- the full cross-check scripts, sequenced after real Python-side numbers exist to diff against.
**Verified:** 2026-08-17T15:48:06Z
**Status:** passed
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Yao.jl reproduces the exact qubit-side IQP distribution (n=2, at least) within a stated tolerance of Python/NumPy | VERIFIED | Ran verify_qubit_iqp.jl live: n=2 TVD=2.26e-17, n=3 TVD=1.12e-16, both against locked tolerance 1e-6. Script exits 0, PASS printed at every checkpoint. |
| 2 | BosonSampling.jl reproduces the photonic-level exact distribution (weight-1 and/or weight-2) within tolerance of Perceval's results | VERIFIED | Ran verify_photonic_iqp_weight1.jl live: n=2 TVD=2.36e-16, n=3 TVD=3.04e-16 against 1e-6. Ran verify_photonic_iqp_weight2.jl live: n=2 locked-gate (Knill-CZ) TVD=3.50e-15, herald_failure_prob diff=5.55e-16, both against 1e-6. Both scripts exit 0 with explicit PASS/GO output. |
| 3 | BosonSampling.jl cross-checks Phase 18's loss-model numbers at >=1 shared eta, agreement (or documented disagreement) stated against the Python TVD-vs-eta computation | VERIFIED | Ran verify_loss_model.jl live: weight-1 leg TVD approx 8e-18 to 2e-16 at eta in 0.99/0.80/0.05; mixed (weight-1+weight-2) leg TVD approx 1.75e-14 at same eta values, herald_failure_prob differences <= 2.55e-15. All within the 1e-6 tolerance. Uses BosonSampling.jl's own native UniformLossInterferometer API (not a hand-rolled attenuation), documented with two real findings (unit convention, a package dispatch bug) fixed and verified via an n=1 closed-form sanity check first. |
| 4 | Cross-checks are independent builds, not mechanical ports of Perceval's circuit structure | VERIFIED | Each script derives its own phase/bit-ordering/matrix-orientation conventions algebraically and confirms them empirically against closed-form cases before trusting the full comparison. docs/julia-cross-check-study.md states this explicitly as the phase's load-bearing design decision. |
| 5 | Results are honestly documented, including scope limits and any bugs found along the way | VERIFIED | docs/julia-cross-check-study.md's scope section states limits explicitly (small-n, fixed test cases, not a formal proof, weight-2 limited to i=0,j=1 locked angle). Real bugs found (transpose bug, package dispatch gap) are reported as found-and-fixed, not smoothed over. |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| julia/generate_reference.py | Python reference-distribution generator | VERIFIED | Exists, produces all 11 CSVs under results/julia_reference/ consumed by the 4 Julia scripts. |
| julia/verify_qubit_iqp.jl | VERIFY-02 Yao.jl cross-check | VERIFIED | Exists, runs cleanly, substantive (222 lines, independent circuit build + empirical convention checks + TVD assertions), executed live with PASS result. |
| julia/verify_photonic_iqp_weight1.jl | VERIFY-03 weight-1 leg | VERIFIED | Exists, runs cleanly, executed live with PASS result, regenerates its own results doc on each run. |
| julia/verify_photonic_iqp_weight2.jl | VERIFY-03 weight-2 leg | VERIFIED | Exists, runs cleanly, executed live with GO result, includes a real bug-found-and-fixed (matrix transpose) with a runnable zero-leak regression assertion. |
| julia/verify_loss_model.jl | VERIFY-04 loss-model cross-check | VERIFIED | Exists, runs cleanly, executed live with PASS result for both weight-1 and mixed legs at 3 eta values each, uses BosonSampling.jl's native loss API. |
| results/phase19_verify02_results.md | VERIFY-02 results doc | VERIFIED | Exists, matches live-run output numbers exactly, states methodology and verdict. |
| results/phase19_verify03_weight1_results.md | VERIFY-03 weight-1 results doc | VERIFIED | Exists, regenerated by the script itself on the verification run, matches live output. |
| results/phase19_verify03_weight2_results.md | VERIFY-03 weight-2 results doc | VERIFIED | Exists (10.7KB), substantive methodology write-up including the transpose-bug diagnosis. |
| results/phase19_verify04_results.md | VERIFY-04 results doc | VERIFIED | Exists, regenerated by the script itself on the verification run, matches live output for both legs at all 3 eta values. |
| docs/julia-cross-check-study.md | Canonical phase results document | VERIFIED | Exists (13KB), covers all 4 legs, methodology, scope limits, and an overall status table with all four requirements marked GO. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| verify_qubit_iqp.jl | results/julia_reference/qubit_n2.csv, qubit_n3.csv | direct CSV read (DelimitedFiles, String-typed) | WIRED | File paths resolved relative to @__DIR__; live run read both files and computed TVD against them successfully. |
| verify_photonic_iqp_weight1.jl / weight2.jl / verify_loss_model.jl | corresponding results/julia_reference/*.csv | direct CSV read | WIRED | All three scripts ran live and produced non-trivial TVD numbers computed against the reference files (not hardcoded pass values). |
| julia/generate_reference.py | iqp_photonic_encoding.py / hardness/loss_model*.py | direct function calls (Task 1/2 of 19-01-PLAN.md) | WIRED (indirect confirmation) | Downstream Julia scripts' agreement with these CSVs at floating-point-noise-level TVD is itself strong indirect evidence the CSVs were generated correctly from the real Python functions, not fabricated; CSVs were not regenerated fresh in this verification pass since existing files already exist and were consumed successfully by all 4 downstream scripts. |
| .planning/REQUIREMENTS.md VERIFY-02/03/04 rows | phase 19 plans/results | status marked Complete | WIRED | Confirmed rows read Complete for all three requirements, consistent with the phase's all-GO outcome. |

### Requirements Coverage

| Requirement | Status | Blocking Issue |
|-------------|--------|-----------------|
| VERIFY-02 | SATISFIED | None |
| VERIFY-03 | SATISFIED | None (both weight-1 and weight-2 legs reached GO) |
| VERIFY-04 | SATISFIED | None |

### Anti-Patterns Found

None. No TODO/FIXME/placeholder patterns found in the four Julia scripts or the four results docs; all scripts contain real numerical computation, hard assert statements tied to computed values (not hardcoded), and were confirmed to execute successfully with non-trivial, non-hardcoded output during this verification pass.

### Human Verification Required

None. All three success criteria are objectively checkable via script execution and TVD comparison against a stated numeric tolerance, and were directly re-run and confirmed during this verification pass (not taken on SUMMARY.md's word alone).

### Gaps Summary

No gaps found. All four Julia cross-check scripts (verify_qubit_iqp.jl, verify_photonic_iqp_weight1.jl, verify_photonic_iqp_weight2.jl, verify_loss_model.jl) were executed live during this verification (not just read) and independently reproduced the exact numeric results claimed in their respective SUMMARY.md/results docs -- TVDs at floating-point-noise level (1e-14 to 1e-18) against the locked 1e-6 tolerance, for all three ROADMAP success criteria (VERIFY-02, VERIFY-03 weight-1/weight-2, VERIFY-04 weight-1/mixed at 3 eta values). The phase's canonical write-up docs/julia-cross-check-study.md states scope limits honestly and documents two real bugs found and fixed during the work (a Knill-CZ matrix transpose-convention error, a BosonSampling.jl package dispatch gap) rather than smoothing them over.

One methodological note (not a gap): VERIFY-04's Python reference was generated fresh in Plan 19-01 with a new fixed single-theta-draw (seed_base 190819, distinct from Phase 18's 180814 pooled-sweep seed), reusing Phase 18's own loss-model functions and eta grid, rather than diffing against Phase 18's literal pooled-mean CSV. This is an explicit, documented design choice (per 19-RESEARCH.md Pitfall 4) to get an exact, non-statistical single-case agreement rather than comparing against a multi-draw mean -- a defensible interpretation of "cross-checks Phase 18's loss-model numbers," but worth knowing this is same-methodology-same-grid agreement, not a byte-for-byte diff against Phase 18's already-published sweep CSV.

---

Verified: 2026-08-17T15:48:06Z
Verifier: Claude (gsd-verifier)
