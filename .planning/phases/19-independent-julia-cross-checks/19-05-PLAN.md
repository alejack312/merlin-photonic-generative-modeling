---
phase: 19-independent-julia-cross-checks
plan: 05
type: execute
wave: 3
depends_on: ["19-01", "19-04"]
files_modified:
  - julia/verify_loss_model.jl
  - results/phase19_verify04_results.md
autonomous: true

must_haves:
  truths:
    - "BosonSampling.jl's own native loss/noise API is used (not a hand-attenuation fallback) unless investigation confirms no usable native API exists in the installed v1.0.2 -- checked against the local depot source, not assumed from GitHub main"
    - "The doubled-mode output of the native loss API (if used) is correctly marginalized back to a physical-mode distribution before any TVD comparison, not silently mismatched in shape"
    - "BosonSampling.jl independently reproduces Phase 18's weight-1 and mixed loss-model numbers within TVD <= 1e-6 at 2-3 shared eta values (n=2 only), using the SAME fixed theta draw as the Python reference, OR the disagreement is documented honestly after a time-boxed debug attempt"
  artifacts:
    - path: "julia/verify_loss_model.jl"
      provides: "Independent BosonSampling.jl native-loss cross-check against results/julia_reference/weight1_loss_n2_eta*.csv and mixed_loss_n2_eta*.csv"
    - path: "results/phase19_verify04_results.md"
      provides: "VERIFY-04 methodology (including the native-loss-API investigation outcome), measured TVD per eta, and honest go/partial-go verdict"
  key_links:
    - from: "julia/verify_loss_model.jl"
      to: "results/julia_reference/weight1_loss_n2_eta*.csv, results/julia_reference/mixed_loss_n2_eta*.csv"
      via: "file read + reimplemented TVD formula, at the same 3 eta values and same fixed thetas Plan 19-01 generated"
      pattern: "weight1_loss_n2_eta|mixed_loss_n2_eta"
---

<objective>
Investigate and use BosonSampling.jl's native loss API (`UniformLossInterferometer`/`to_lossy`, confirmed to exist in `19-RESEARCH.md`) to independently cross-check Phase 18's photon-loss model at n=2, satisfying VERIFY-04 -- the strongest independence guarantee available, since it's a structurally different loss mechanism (beamsplitter-to-environment-mode model) than Perceval's `pcvl.LC` component.

Purpose: closes out the phase's third and final requirement. Isolated into its own plan because the native loss API's doubled-mode output semantics are an unverified, nontrivial new API surface (19-RESEARCH.md Pitfall 3/Open Question 2) -- its own investigation risk should not be entangled with the weight-1/weight-2 exact-case plans (19-02/19-03/19-04). This plan now depends on Plan 19-04 directly (not just 19-01) so it deterministically reads 19-04's finished, final outcome before deciding whether the mixed-scope leg is attemptable -- 19-04's stall path is fast/bounded, so this ordering doesn't reintroduce a "stall blocks everything" problem.
Output: `julia/verify_loss_model.jl` and `results/phase19_verify04_results.md`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-independent-julia-cross-checks/19-CONTEXT.md
@.planning/phases/19-independent-julia-cross-checks/19-RESEARCH.md
@julia/hello_bosonsampling.jl
@hardness/loss_model.py
@hardness/loss_model_weight2.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Investigate the installed BosonSampling.jl v1.0.2 loss API and confirm marginalization</name>
  <files>julia/verify_loss_model.jl</files>
  <action>
Before writing any comparison logic, confirm the ACTUAL installed v1.0.2 API (not the GitHub `main` branch snippets in 19-RESEARCH.md, which are explicitly flagged as unverified against the exact installed tag). Run `julia --project=julia -e 'using BosonSampling; println(pathof(BosonSampling))'` to locate the local depot source, then read the actual `src/types/loss.jl` (or wherever loss-related types live in the installed version) to confirm: the exact function signatures for `UniformLossInterferometer`/`to_lossy`, what shape their outputs take (confirm the "doubled mode space" claim from 19-RESEARCH.md against the real source, don't assume it), and whether helper functions like `sort_by_lost_photons`/`lossless_part`/`keep_lossless_part!` exist and what they actually do (read their docstrings/implementations directly).

Per CONTEXT.md's explicit "Claude's Discretion" allowance: if this investigation confirms a usable native loss API exists with a traceable marginalization path, use it (this is the strongly-preferred path per 19-RESEARCH.md's "Don't Hand-Roll" table). If investigation reveals the native API's installed-version behavior is unusable within a reasonable effort (e.g. undocumented, broken, or the marginalization step cannot be determined from the installed source), fall back to hand-attenuating the exact distribution in Julia (per-mode independent photon-loss probability applied directly to the exact circuit's output, an equivalent physical model to Perceval's `pcvl.LC`) -- document which path was taken and why in the script's header comments; this is an explicit, allowed discretionary call, not a silent shortcut.

Verify whichever mechanism is chosen against a minimal, hand-derivable n=1 sanity case (single mode, single photon, known eta -- e.g. transmission probability = eta, loss probability = 1-eta) before trusting the n=2 weight-1/mixed comparison, following the same "verify against a closed form before trusting the full case" pattern used in Plans 19-02/19-03.
  </action>
  <verify>Run the n=1 sanity check portion of julia/verify_loss_model.jl and confirm it prints PASS with the loss mechanism's source (native API vs. hand-attenuation) stated explicitly in output/comments</verify>
  <done>The loss mechanism used (BosonSampling.jl's native API, or a documented discretionary hand-attenuation fallback) is confirmed against the actual installed v1.0.2 source (not assumed from GitHub main) and verified against a known n=1 closed form</done>
</task>

<task type="auto">
  <name>Task 2: Run weight-1 and mixed loss cross-checks at 3 eta values and write results</name>
  <files>julia/verify_loss_model.jl, results/phase19_verify04_results.md</files>
  <action>
Using the SAME fixed theta draw and eta values Plan 19-01 recorded in `results/julia_reference/weight1_loss_n2_eta*.csv` / `mixed_loss_n2_eta*.csv` (read the header comment of any one of those files to get the literal theta list, or read it directly from `julia/generate_reference.py`'s Task 2 output -- do not regenerate/resample), build the n=2 weight-1 lossy circuit (reusing Plan 19-03's dual-rail weight-1 construction pattern, or a fresh minimal n=2 version if that's cleaner) and the n=2 mixed (weight-1 + weight-2) lossy circuit (reusing Plan 19-04's Knill-CZ construction IF Plan 19-04 succeeded -- since this plan now depends on Plan 19-04 directly, Plan 19-04 has already finished by the time this task runs; check `julia/verify_photonic_iqp_weight2.jl`'s existence and Task-1-success status to determine its final outcome before assuming it's available; if Plan 19-04 stalled, the mixed-scope leg of THIS plan is correspondingly not attemptable with an independent CZ construction, and that must be reported honestly rather than silently falling back to something else).

For each of the 3 eta values (0.99, 0.80, 0.05), apply the confirmed loss mechanism (Task 1) to the weight-1 circuit and compute the resulting bitstring distribution; do the same for mixed scope if available. Compute TVD against the corresponding reference CSV using the same reimplemented TVD formula as prior plans. Assert TVD <= 1e-6 for each; apply the time-boxed disagreement-handling rule from CONTEXT.md if any fail (check mode-ordering/loss-parameter-convention mismatches first, per 19-RESEARCH.md's stated culprits), and report an honest documented disagreement rather than forcing a pass if unresolved.

If mixed scope isn't attemptable (Plan 19-04 stalled), report weight-1's result on its own and state explicitly that the mixed-scope loss leg is blocked on the weight-2 gate sourcing outcome, not silently omitted.

Write `results/phase19_verify04_results.md`: methodology (including Task 1's loss-mechanism investigation outcome), measured TVD per eta per scope, and an explicit go/partial-go/no-go verdict for VERIFY-04.
  </action>
  <verify>julia --project=julia julia/verify_loss_model.jl runs to completion; results/phase19_verify04_results.md exists with measured TVD values for each attempted (eta, scope) combination and an honest statement of any scope that wasn't attemptable</verify>
  <done>VERIFY-04 satisfied: BosonSampling.jl independently cross-checks Phase 18's loss model at 2-3 eta values, n=2, for weight-1 (and mixed, if Plan 19-04's gate construction was available), with a stated, honest go/no-go verdict</done>
</task>

</tasks>

<verification>
`julia --project=julia julia/verify_loss_model.jl` runs to completion (pass or honestly-reported disagreement). `results/phase19_verify04_results.md` exists with measured TVD numbers and an explicit statement of the loss mechanism used and any scope limitations.
</verification>

<success_criteria>
VERIFY-04 is satisfiable from this plan's shipped artifacts: an independently-sourced loss mechanism, a real measured TVD against Phase 18's numbers at 2-3 eta values, and an honest written verdict.
</success_criteria>

<output>
After completion, create `.planning/phases/19-independent-julia-cross-checks/19-05-SUMMARY.md`
</output>
