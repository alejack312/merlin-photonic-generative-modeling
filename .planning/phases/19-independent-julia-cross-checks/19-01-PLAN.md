---
phase: 19-independent-julia-cross-checks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - julia/generate_reference.py
  - results/julia_reference/qubit_n2.csv
  - results/julia_reference/qubit_n3.csv
  - results/julia_reference/weight1_n2.csv
  - results/julia_reference/weight1_n3.csv
  - results/julia_reference/weight2_locked_n2.csv
  - results/julia_reference/weight1_loss_n2.csv
  - results/julia_reference/mixed_loss_n2.csv
autonomous: true

must_haves:
  truths:
    - "A single, reusable Python script produces every reference distribution the Julia cross-checks (Plans 19-02..19-05) need to diff against"
    - "The loss-model reference cases use ONE fixed theta draw per scope, not a pooled multi-draw mean (Pitfall 4) -- the exact theta values used are recorded as literals, not just a seed"
    - "Every reference file's probabilities sum to 1.0 minus any explicitly-reported residual (never silently renormalized)"
  artifacts:
    - path: "julia/generate_reference.py"
      provides: "Reference-distribution generator calling this repo's already-tested exact/lossy distribution functions and writing bitstring,probability CSVs"
    - path: "results/julia_reference/qubit_n2.csv"
      provides: "VERIFY-02 exact qubit-side reference (n=2)"
    - path: "results/julia_reference/weight2_locked_n2.csv"
      provides: "VERIFY-03 weight-2 locked-gate reference (n=2, i=0, j=1, pair_theta=pi/4)"
    - path: "results/julia_reference/weight1_loss_n2.csv"
      provides: "VERIFY-04 weight-1 loss reference at fixed theta, multiple eta rows"
  key_links:
    - from: "julia/generate_reference.py"
      to: "iqp_photonic_encoding.exact_qubit_iqp_distribution / photonic_iqp_distribution / photonic_weight2_iqp_distribution"
      via: "direct function call, same shared test-case thetas documented in 19-RESEARCH.md"
      pattern: "exact_qubit_iqp_distribution|photonic_iqp_distribution|photonic_weight2_iqp_distribution"
    - from: "julia/generate_reference.py"
      to: "hardness.loss_model.photonic_iqp_distribution_lossy / hardness.loss_model_weight2.photonic_weight2_iqp_distribution_lossy"
      via: "direct function call with a single fixed theta draw obtained via trainability.rng.get_rng + hardness.sweep.sample_thetas, never the pooled-mean sweep path"
      pattern: "photonic_iqp_distribution_lossy|photonic_weight2_iqp_distribution_lossy|get_rng|sample_thetas"
---

<objective>
Generate every Python-side reference distribution Phase 19's Julia cross-checks need, as plain CSV files, using this repo's already-tested exact and lossy distribution functions directly -- no new physics, no new Python logic beyond wiring and file I/O.

Purpose: VERIFY-02/03/04 (Plans 19-02..19-05) all need a fixed, file-based reference to diff their independently-built Julia distributions against. Producing all of it in one wave-1 plan means every downstream Julia plan can start immediately and in parallel (wave 2), rather than each having to reach back into Python itself.
Output: `julia/generate_reference.py` plus 7 CSV files under `results/julia_reference/`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-independent-julia-cross-checks/19-CONTEXT.md
@.planning/phases/19-independent-julia-cross-checks/19-RESEARCH.md
@iqp_photonic_encoding.py
@hardness/loss_model.py
@hardness/loss_model_weight2.py
@hardness/sweep.py
@trainability/rng.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Exact-distribution reference generator (VERIFY-02/VERIFY-03 targets)</name>
  <files>julia/generate_reference.py, results/julia_reference/qubit_n2.csv, results/julia_reference/qubit_n3.csv, results/julia_reference/weight1_n2.csv, results/julia_reference/weight1_n3.csv, results/julia_reference/weight2_locked_n2.csv</files>
  <action>
Create `julia/generate_reference.py`. Use the exact shared test cases already documented in `19-RESEARCH.md`'s "Code Examples" section (these are the SAME inputs the Julia scripts in Plans 19-02/19-03/19-04 will hardcode as literals -- do not invent new theta values here):

- Qubit-side (VERIFY-02): `exact_qubit_iqp_distribution(n=2, thetas=[0.3, 1.1])` and `exact_qubit_iqp_distribution(n=3, thetas=[0.3, 1.1, 0.75])`.
- Weight-1 photonic (VERIFY-03): `photonic_iqp_distribution(n=2, thetas=[0.3, 1.1])` and `photonic_iqp_distribution(n=3, thetas=[0.3, 1.1, 0.75])` -- these return `(dist, residual)`; write `dist` to CSV and print `residual` to stdout (expected ~0 for the lossless n<=3 case; assert it is below 1e-9 before writing).
- Weight-2 locked gate (VERIFY-03): `photonic_weight2_iqp_distribution(n=2, i=0, j=1, thetas=[0.0, 0.0])` -- NOTE this repo's weight-2 API takes `pair_thetas` via `exact_qubit_iqp_distribution`'s separate keyword, but `photonic_weight2_iqp_distribution`'s own signature encodes the CZ angle differently; read the function's actual signature/docstring in `iqp_photonic_encoding.py` (around line 506) before calling it -- use the exact same locked-gate case already covered by `tests/test_iqp_photonic_encoding.py::test_wt2_tvd_gate_n2_theta_pi_4` (n=2, i=0, j=1, pair angle pi/4) so this reference is provably consistent with an existing, passing test, not a fresh untested case. Record the herald_failure_prob / residual this call returns in a one-line comment at the top of the CSV (`# herald_failure_prob=...`), since it's not part of the bitstring distribution itself.

Write each distribution as a 2-column CSV (`bitstring,probability`), one file per case, to `results/julia_reference/`. Sort rows by bitstring for deterministic diffing. Before writing, assert each file's probability column sums to 1.0 within 1e-9 (or, for the weight-2 case, to `1.0 - herald_failure_prob` within 1e-9) -- fail loudly (raise, don't silently write) if not, since a silently-wrong reference would corrupt every downstream Julia comparison.

Run the script once (`python julia/generate_reference.py`) and confirm all 5 files are created with sane contents (spot-check qubit_n2.csv sums to 1.0, weight2_locked_n2.csv's probabilities are close to the known TVD~2.2e-16-verified Python numbers).
  </action>
  <verify>python julia/generate_reference.py; then inspect the 5 CSV files exist under results/julia_reference/ with correct row counts (4 for n=2, 8 for n=3) and probability columns summing to ~1.0 (or 1.0-residual for weight-2)</verify>
  <done>5 CSV files exist, each internally consistent (probabilities sum correctly), generated directly from this repo's already-tested distribution functions with no new Python physics logic</done>
</task>

<task type="auto">
  <name>Task 2: Loss-model reference generator with fixed single-draw thetas (VERIFY-04 target)</name>
  <files>julia/generate_reference.py, results/julia_reference/weight1_loss_n2.csv, results/julia_reference/mixed_loss_n2.csv</files>
  <action>
Extend `julia/generate_reference.py` (same file, additional section/functions) to generate VERIFY-04's loss-model references. Per `19-RESEARCH.md` Pitfall 4: do NOT use Phase 18's pooled-mean CSVs or `hardness.sweep._raw_values_for_cell`'s multi-draw pooling -- generate exactly ONE fixed theta draw per scope and hardcode/print its literal values so Julia can reproduce the identical circuit instance.

For `scope="weight1"` and `scope="mixed"`, n=2:
1. `draw_rng = trainability.rng.get_rng(seed_base=190819, scope, n=2, draw=0)` (a fresh, phase-19-specific seed_base -- do not reuse Phase 18's `180814` seed_base, to keep this reference generation obviously independent of Phase 18's own pooled sweep).
2. `thetas = hardness.sweep.sample_thetas(draw_rng, n=2)`.
3. Print the resulting `thetas` list to stdout AND write it as a comment line at the top of each output CSV (`# thetas=[...]`) -- this is the literal value Plan 19-05's Julia script must hardcode.
4. Select 3 eta values spread across `hardness.sweep.ETA_GRID` (`[0.99, 0.95, 0.90, 0.80, 0.60, 0.35, 0.05]`) -- use the first, a middle, and the last: `eta in {0.99, 0.80, 0.05}`.
5. For weight1: call `hardness.loss_model.photonic_iqp_distribution_lossy(n=2, thetas=thetas, eta=eta)` for each of the 3 eta values; each call returns `(dist, residual)` -- write one CSV per eta (`weight1_loss_n2_eta099.csv`, `weight1_loss_n2_eta080.csv`, `weight1_loss_n2_eta005.csv`), with `# eta=..., residual=..., thetas=[...]` as the header comment.
6. For mixed: call `hardness.loss_model_weight2.photonic_weight2_iqp_distribution_lossy(n=2, i=0, j=1, thetas=thetas, eta=eta)` for the same 3 eta values -- this returns whatever tuple shape Plan 18-03's function actually returns (read its signature/docstring first; it includes herald_failure_prob per `18-03-SUMMARY.md`). Write one CSV per eta (`mixed_loss_n2_eta099.csv` etc.), with `# eta=..., herald_failure_prob=..., thetas=[...]` as the header comment.

(The task's `files_modified` list above uses `weight1_loss_n2.csv`/`mixed_loss_n2.csv` as shorthand for this per-eta file family -- name the actual files with the eta suffix as described, and note the real filenames in the plan's SUMMARY.)

Do not assert a specific numeric TVD-vs-Phase-18 value here (that's Plan 19-05's job) -- only assert internal consistency (probabilities + residual/herald_failure_prob sum sanely) before writing.
  </action>
  <verify>python julia/generate_reference.py (re-run, idempotent); confirm 6 new per-eta CSV files exist under results/julia_reference/ (3 weight1 + 3 mixed), each with a header comment recording the exact thetas and eta used</verify>
  <done>A single fixed theta draw (not a pooled mean) is used consistently across all 3 eta values per scope; the literal theta values are recorded in every output file's header for Julia to reproduce exactly</done>
</task>

</tasks>

<verification>
Run `python julia/generate_reference.py` end-to-end with no errors. Confirm all 11 CSV files exist under `results/julia_reference/` (5 exact-case files from Task 1, 6 loss-case files from Task 2), each internally consistent (probabilities sum to 1.0 minus any reported residual/herald_failure_prob).
</verification>

<success_criteria>
`julia/generate_reference.py` exists, runs cleanly, and produces a complete, self-documenting (theta values recorded in-file) set of reference CSVs that Plans 19-02 through 19-05 can consume without any further Python-side work.
</success_criteria>

<output>
After completion, create `.planning/phases/19-independent-julia-cross-checks/19-01-SUMMARY.md`
</output>
