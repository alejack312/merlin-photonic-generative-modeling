---
phase: 19-independent-julia-cross-checks
plan: 04
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - julia/verify_photonic_iqp_weight2.jl
  - results/phase19_verify03_weight2_results.md
autonomous: true

must_haves:
  truths:
    - "The Knill CZ gate's unitary/circuit is sourced from the literature (arXiv:quant-ph/0110144 or an independent secondary derivation), never by extracting Perceval's own already-built matrix -- the sourcing method is stated explicitly in the results doc"
    - "If literature sourcing succeeds: BosonSampling.jl independently reproduces the weight-2 locked-gate distribution within TVD <= 1e-6 of the Python reference, OR the disagreement is documented honestly after a time-boxed debug attempt, including checking whether a different-but-valid Knill CZ variant explains it (Pitfall 5)"
    - "If literature sourcing stalls within the time-box: this is reported plainly as a partial-go for VERIFY-03's weight-2 leg, and does NOT block VERIFY-02 (19-02), VERIFY-03's weight-1 leg (19-03), or VERIFY-04 (19-05) from shipping"
  artifacts:
    - path: "julia/verify_photonic_iqp_weight2.jl"
      provides: "Independent BosonSampling.jl Knill-CZ construction + TVD computation against results/julia_reference/weight2_locked_n2.csv (or a documented stall report if sourcing fails)"
    - path: "results/phase19_verify03_weight2_results.md"
      provides: "VERIFY-03 (weight-2 leg) methodology, sourcing outcome, measured TVD (if reached), and honest go/partial-go/no-go verdict"
  key_links:
    - from: "julia/verify_photonic_iqp_weight2.jl"
      to: "results/julia_reference/weight2_locked_n2.csv"
      via: "file read + reimplemented TVD formula (only reached if Task 1's literature sourcing succeeds)"
      pattern: "weight2_locked_n2\\.csv"
---

<objective>
Independently source the Knill CZ gate's unitary construction from the literature (not by extracting Perceval's own matrix) and build it in BosonSampling.jl via `UserDefinedInterferometer` + hand-herald post-selection, then diff against Python's `photonic_weight2_iqp_distribution` locked-gate reference, satisfying VERIFY-03's weight-2 leg.

Purpose: this is the phase's single highest-risk piece, isolated into its own plan per CONTEXT.md's explicit "independently gradeable, a stall on one doesn't block the others" design and this project's Sep-1-deadline stall-avoidance pattern (Phase 14's own precedent). A stall here must be visible and time-boxed, not silently absorbed into a larger plan.
Output: `julia/verify_photonic_iqp_weight2.jl` and `results/phase19_verify03_weight2_results.md`, reporting either a real TVD result or an honest, time-boxed partial-go.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-independent-julia-cross-checks/19-CONTEXT.md
@.planning/phases/19-independent-julia-cross-checks/19-RESEARCH.md
@docs/iqp-photonic-encoding.md
@heralded_cz_derisking.py
@julia/hello_bosonsampling.jl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source the Knill CZ gate's unitary from the literature (time-boxed)</name>
  <files>julia/verify_photonic_iqp_weight2.jl</files>
  <action>
This repo's own `docs/iqp-photonic-encoding.md` identifies Perceval's `heralded_cz` as implementing the Knill CZ gate (Knill, 2002, arXiv:quant-ph/0110144) -- a 6-mode linear-optical circuit (4 data modes + 2 heralded ancilla modes), with `heralded_cz_derisking.py` having already independently confirmed (via Perceval) its herald-success rate (2/27, uniform across computational-basis inputs) and phase-sign behavior (negative only on |1,1>).

Fetch and read arXiv:quant-ph/0110144 (or, if the original paper's circuit diagram/unitary isn't practically extractable within a focused effort, a reliable independent secondary source that reproduces the same construction -- e.g. a review paper or lecture notes citing the Knill 2002 heralded-CZ circuit explicitly) to obtain the gate's beamsplitter-network structure or explicit 6x6 (or equivalent) unitary, independent of Perceval's own implementation. Per `19-RESEARCH.md`'s explicitly-flagged anti-pattern: do NOT extract Perceval's already-built circuit's `.compute_unitary()` matrix and hardcode those numbers -- that produces agreement by construction and defeats the point of an independent check.

This is a genuine open research question, not a known quantity -- time-box this sourcing effort to a few focused hours per `19-CONTEXT.md`'s stop-condition rule. If a usable independent unitary/circuit description is found within that time-box: document the source explicitly (paper, section/figure reference) in a comment block at the top of `julia/verify_photonic_iqp_weight2.jl`, and proceed to Task 2.

If the time-box is exhausted without a usable independent source: STOP here. Do not fall back to extracting Perceval's matrix as a substitute (that would silently defeat CONTEXT.md's "independent build" requirement without saying so). Instead, write `results/phase19_verify03_weight2_results.md` directly, stating plainly: what was attempted, what sources were checked, why a usable independent unitary wasn't obtained within the time-box, and an explicit partial-go verdict for VERIFY-03's weight-2 leg -- consistent with this project's established honesty norm (HARD-04, GEN-07, Phase 7 precedent) and CONTEXT.md's explicit "a stall on one doesn't block the others" design. In that case, this plan's remaining scope (Task 2) is not attempted, and the SUMMARY must state this outcome clearly, not report it as complete.
  </action>
  <verify>Either: a documented, cited, independent Knill-CZ unitary/circuit description exists in julia/verify_photonic_iqp_weight2.jl's header comments (proceed to Task 2), OR results/phase19_verify03_weight2_results.md exists with a plainly-stated partial-go verdict and documented sourcing attempt (Task 2 not attempted)</verify>
  <done>The sourcing outcome (success or time-boxed stall) is documented honestly and explicitly, with no silent fallback to porting Perceval's matrix</done>
</task>

<task type="auto">
  <name>Task 2: Build the gate in BosonSampling.jl, herald by hand, and cross-check (only if Task 1 succeeded)</name>
  <files>julia/verify_photonic_iqp_weight2.jl, results/phase19_verify03_weight2_results.md</files>
  <action>
(Skip entirely if Task 1 ended in a documented stall -- this task does not apply in that case.)

Using the independently-sourced construction from Task 1, build the 6-mode Knill CZ circuit in BosonSampling.jl via `UserDefinedInterferometer(U)` where U is the sourced (not-extracted-from-Perceval) unitary/beamsplitter-network composition, expressed directly in Julia.

Herald by hand (BosonSampling.jl has no `Processor.add_herald`-equivalent, per 19-RESEARCH.md Open Question 3): for each of the 4 computational-basis data-mode input combinations (matching `weight2_locked_n2.csv`'s n=2, i=0, j=1 case), enumerate output patterns via `compute_probability!` calls in a loop, keep only outcomes where both ancilla modes show exactly 1 photon each (the herald condition), and renormalize/aggregate the surviving data-mode outcomes into a bitstring distribution plus a herald_failure_prob (sum of probability where the herald condition fails) -- mirroring Python's own herald-accounting logic in `photonic_weight2_iqp_distribution`/`heralded_cz`, conceptually, via BosonSampling.jl's different mechanism.

Compare against `results/julia_reference/weight2_locked_n2.csv` (thetas=[0.0,0.0], i=0, j=1, pair angle pi/4 -- the locked gate case). Compute TVD via the same reimplemented formula pattern as Plans 19-02/19-03. Also compare the measured herald_failure_prob against Python's reported ~1-2/27.

Per 19-RESEARCH.md Pitfall 5: if TVD or herald_failure_prob disagree, first check whether the independently-sourced construction is a genuinely different-but-valid Knill CZ variant (e.g. a different herald pattern or post-selection-vs-heralding choice) before assuming a bug -- a different-but-internally-consistent result from a verifiably different-but-valid construction is a legitimate, honestly-reportable finding, not necessarily a failure. Apply the same time-boxed debug rule as other plans before concluding either way.

Write/update `results/phase19_verify03_weight2_results.md`: full methodology (including the literature source cited in Task 1), measured TVD and herald_failure_prob comparison, and an explicit go/partial-go/no-go verdict.
  </action>
  <verify>If Task 1 succeeded: julia --project=julia julia/verify_photonic_iqp_weight2.jl runs to completion (pass or honestly-reported disagreement); results/phase19_verify03_weight2_results.md states the measured TVD and herald_failure_prob comparison</verify>
  <done>VERIFY-03's weight-2 leg is satisfied either with a real measured cross-check result, or with an honest, time-boxed, explicitly-stated partial-go -- never silently skipped or falsely reported as complete</done>
</task>

</tasks>

<verification>
`results/phase19_verify03_weight2_results.md` exists in all cases and states, unambiguously, one of: (a) a measured TVD/herald_failure_prob cross-check result with go/no-go verdict, or (b) a documented, time-boxed sourcing stall with an explicit partial-go verdict. The distinction between these two outcomes is never blurred.

</verification>

<success_criteria>
VERIFY-03's weight-2 leg produces an explicit, honest artifact either way: a real independent cross-check result, or a documented, time-boxed partial-go that does not block Plans 19-02, 19-03, or 19-05 from completing.
</success_criteria>

<output>
After completion, create `.planning/phases/19-independent-julia-cross-checks/19-04-SUMMARY.md`
</output>
