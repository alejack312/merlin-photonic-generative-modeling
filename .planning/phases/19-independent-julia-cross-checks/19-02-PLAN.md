---
phase: 19-independent-julia-cross-checks
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - julia/verify_qubit_iqp.jl
  - results/phase19_verify02_results.md
autonomous: true

must_haves:
  truths:
    - "Yao.jl independently reproduces the exact qubit-side IQP distribution at n=2 and n=3 within TVD <= 1e-6 of the Python reference, OR the disagreement is documented honestly with a stated root cause after a time-boxed debug attempt"
    - "The phase-convention translation (this repo's WP(theta,0)=diag(e^{i*theta},e^{-i*theta}) vs Yao's Rz(phi)=diag(e^{-i*phi/2},e^{i*phi/2})) is derived algebraically and confirmed numerically before trusting the multi-qubit comparison"
    - "Yao's bit-ordering convention is verified empirically (not assumed) and the two sides' distributions are diffed by bitstring key, never by raw vector index"
  artifacts:
    - path: "julia/verify_qubit_iqp.jl"
      provides: "Independent Yao.jl circuit construction + TVD computation against results/julia_reference/qubit_n2.csv and qubit_n3.csv"
    - path: "results/phase19_verify02_results.md"
      provides: "VERIFY-02 methodology, measured TVD, and honest go/partial-go verdict"
  key_links:
    - from: "julia/verify_qubit_iqp.jl"
      to: "results/julia_reference/qubit_n2.csv, results/julia_reference/qubit_n3.csv"
      via: "file read + reimplemented TVD formula (0.5 * sum(|a(x)-b(x)|) over the union of bitstring keys)"
      pattern: "qubit_n2\\.csv|qubit_n3\\.csv"
---

<objective>
Independently build the qubit-side IQP circuit in Yao.jl (Hadamard state prep, per-qubit Z-phase layer, Hadamard conjugation) and diff its output distribution against Python's `exact_qubit_iqp_distribution` reference at n=2 and n=3, satisfying VERIFY-02.

Purpose: this is the phase's lowest-risk cross-check (Yao.jl's qubit-gate API is a close conceptual match to the Python reference's own qubit-side math) -- a clean, fast independent confirmation before the two harder photonic-level checks (Plans 19-03/19-04) and the loss check (19-05).
Output: `julia/verify_qubit_iqp.jl` (passing script) and `results/phase19_verify02_results.md` (methodology + measured TVD).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-independent-julia-cross-checks/19-CONTEXT.md
@.planning/phases/19-independent-julia-cross-checks/19-RESEARCH.md
@julia/hello_yao.jl
@julia/Project.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive phase convention and build the Yao.jl circuit</name>
  <files>julia/verify_qubit_iqp.jl</files>
  <action>
Create `julia/verify_qubit_iqp.jl`, following `julia/hello_yao.jl`'s established shape (version/package banner, then circuit, then asserted checks, then PASS message).

Before the multi-qubit case, verify the phase-convention translation algebraically and numerically (19-RESEARCH.md Pitfall 2): this repo's weight-1 gate is `WP(theta,0) = diag(e^{i*theta}, e^{-i*theta})`; Yao's `Rz(phi) = diag(e^{-i*phi/2}, e^{i*phi/2})`. Confirm these are the same matrix at `phi = -2*theta` (write this out as a comment showing the algebra), then verify numerically at n=1: build `chain(1, put(1=>H), put(1=>Rz(-2*theta)), put(1=>H))` applied to `zero_state(1)`, for theta=0.3, and assert the resulting `probs()` match the known closed form `P(H)=cos(theta)^2, P(V)=sin(theta)^2` (this repo's own single-qubit marginal convention, stated in 19-RESEARCH.md) within 1e-10.

Then verify Yao's bit-ordering convention empirically (Pitfall 1) at n=2 with an asymmetric circuit (e.g. differing thetas on qubit 1 vs qubit 2) where the expected marginal per qubit is known independently -- confirm which of Yao's `probs()` vector positions correspond to which qubit's bit=0/1 by inspection, do not assume little-endian without checking.

Write a reusable function `build_qubit_iqp_circuit(n, thetas)` that: prepares `zero_state(n) |> chain(n, [put(k=>H) for k in 1:n]...)`, applies `put(k => Rz(-2*thetas[k]))` for each qubit k, then re-applies the Hadamard layer, and returns the resulting register. Write a second function `probs_to_bitstring_dict(reg, n)` that converts Yao's `probs()` output vector into a `Dict{String,Float64}` keyed by this repo's OWN bitstring convention (qubit 0 = most-significant bit, matching Python's `exact_qubit_iqp_distribution` docstring) -- using the bit-ordering fact just confirmed, not an assumption.
  </action>
  <verify>Run `julia --project=julia julia/verify_qubit_iqp.jl` up through the n=1 phase-convention check and the n=2 bit-ordering check; both must print PASS before proceeding to Task 2's full comparison</verify>
  <done>Phase convention (Rz(-2*theta) == WP(theta,0)) and bit-ordering convention are both independently confirmed with explicit numerical assertions, not assumed from documentation</done>
</task>

<task type="auto">
  <name>Task 2: Run n=2/n=3 cross-check against the Python reference and write results</name>
  <files>julia/verify_qubit_iqp.jl, results/phase19_verify02_results.md</files>
  <action>
Using the same shared theta values as `results/julia_reference/qubit_n2.csv` / `qubit_n3.csv` were generated with (`n=2, thetas=[0.3, 1.1]` and `n=3, thetas=[0.3, 1.1, 0.75]` -- hardcode these literals in the Julia script, matching Plan 19-01's Python side exactly, per CONTEXT.md's "same test inputs" lock), call `build_qubit_iqp_circuit` and `probs_to_bitstring_dict` for both n values.

Read the corresponding reference CSV (`results/julia_reference/qubit_n2.csv`, `qubit_n3.csv`) into a `Dict{String,Float64}` using Julia's stdlib `DelimitedFiles` (skip the header row).

Reimplement Python's exact TVD formula in Julia (`0.5 * sum(|a(x)-b(x)|)` over the union of both dicts' keys, per `iqp_photonic_encoding.py::total_variation_distance`) as a small local function -- do not import or shell out to Python.

Compute TVD for both n=2 and n=3. Assert TVD <= 1e-6 for both. If either assertion fails: apply CONTEXT.md's disagreement-handling rule -- spend a time-boxed debugging pass (check the phase-convention/bit-ordering assumptions from Task 1 first, since those are the documented likely culprits) and if still unresolved after a focused attempt, do NOT force a pass -- report the honest disagreement instead.

Write `results/phase19_verify02_results.md`: state the methodology (Yao.jl circuit construction, phase-convention derivation, bit-ordering confirmation), the measured TVD for n=2 and n=3, and an explicit go/partial-go/no-go verdict for VERIFY-02.
  </action>
  <verify>julia --project=julia julia/verify_qubit_iqp.jl exits 0 and prints PASS with both TVD values; results/phase19_verify02_results.md exists and states the measured numbers</verify>
  <done>VERIFY-02 satisfied: an independently-built Yao.jl circuit's output distribution is diffed against the Python reference at n=2 and n=3, with a stated, honest go/no-go verdict recorded in results/phase19_verify02_results.md</done>
</task>

</tasks>

<verification>
`julia --project=julia julia/verify_qubit_iqp.jl` runs to completion (pass or honestly-reported documented disagreement, never a silent skip or a forced/fudged pass). `results/phase19_verify02_results.md` exists with the measured TVD numbers.
</verification>

<success_criteria>
VERIFY-02 is satisfiable from this plan's shipped artifacts: an independent Yao.jl build, a real measured TVD against the Python reference at n=2 and n=3, and an honest written verdict.
</success_criteria>

<output>
After completion, create `.planning/phases/19-independent-julia-cross-checks/19-02-SUMMARY.md`
</output>
