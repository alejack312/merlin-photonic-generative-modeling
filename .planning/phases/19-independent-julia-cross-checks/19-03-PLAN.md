---
phase: 19-independent-julia-cross-checks
plan: 03
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - julia/verify_photonic_iqp_weight1.jl
  - results/phase19_verify03_weight1_results.md
autonomous: true

must_haves:
  truths:
    - "A dual-rail (2-modes-per-qubit) weight-1 IQP circuit is built independently in BosonSampling.jl's own idioms (beamsplitters, phase shifters, UserDefinedInterferometer), not a mechanical port of Perceval's polarization circuit"
    - "The dual-rail encoding convention (bit->mode mapping, Hadamard=50/50 beamsplitter, Z-phase=phase_shift) is verified against a known closed-form marginal (cos^2(theta)/sin^2(theta)) before trusting the full n=2/n=3 comparison"
    - "BosonSampling.jl independently reproduces the weight-1 photonic exact distribution within TVD <= 1e-6 of the Python reference at n=2 and n=3, OR the disagreement is documented honestly after a time-boxed debug attempt"
  artifacts:
    - path: "julia/verify_photonic_iqp_weight1.jl"
      provides: "Independent BosonSampling.jl dual-rail weight-1 circuit + TVD computation against results/julia_reference/weight1_n2.csv and weight1_n3.csv"
    - path: "results/phase19_verify03_weight1_results.md"
      provides: "VERIFY-03 (weight-1 leg) methodology, measured TVD, and honest go/partial-go verdict"
  key_links:
    - from: "julia/verify_photonic_iqp_weight1.jl"
      to: "results/julia_reference/weight1_n2.csv, results/julia_reference/weight1_n3.csv"
      via: "file read + reimplemented TVD formula"
      pattern: "weight1_n2\\.csv|weight1_n3\\.csv"
---

<objective>
Independently build the weight-1 photonic IQP circuit in BosonSampling.jl using a plain dual-rail (2-mode-per-qubit) encoding -- BosonSampling.jl has no polarization/PBS component, so this is the natural, idiomatic Julia-side equivalent of Perceval's polarization encoding, not a workaround -- and diff its output against Python's `photonic_iqp_distribution` reference at n=2 and n=3, satisfying VERIFY-03's weight-1 leg.

Purpose: isolates the weight-1 (simpler, no herald/ancilla) half of VERIFY-03 into its own plan, separate from the weight-2 Knill-CZ leg (Plan 19-04), which research flagged as the phase's real stall risk -- so weight-1 can ship independently regardless of how the weight-2 literature-sourcing investigation goes.
Output: `julia/verify_photonic_iqp_weight1.jl` (passing script) and `results/phase19_verify03_weight1_results.md`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-independent-julia-cross-checks/19-CONTEXT.md
@.planning/phases/19-independent-julia-cross-checks/19-RESEARCH.md
@julia/hello_bosonsampling.jl
@julia/Project.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and verify the dual-rail weight-1 encoding convention</name>
  <files>julia/verify_photonic_iqp_weight1.jl</files>
  <action>
Create `julia/verify_photonic_iqp_weight1.jl`, following `julia/hello_bosonsampling.jl`'s established shape.

Dual-rail encoding (2 modes per qubit, n qubits -> 2n total modes): bit=0 -> photon in the qubit's "upper" mode (even-indexed), bit=1 -> photon in the "lower" mode (odd-indexed). Input state: `Input{Bosonic}(ModeOccupation([1,0,1,0,...]))` (all qubits start in bit=0, matching the `|+>` prep's computational-basis starting point before the first Hadamard-equivalent step -- confirm this matches Python's own prep convention in `iqp_photonic_encoding.py`, i.e. the H layer that creates `|+>` from each qubit's rail-basis start state).

Per-qubit gates: Hadamard-equivalent conjugation = 50/50 beamsplitter on that qubit's mode pair (`beam_splitter(1/sqrt(2))`, already proven correct in `hello_bosonsampling.jl`'s Task); weight-1 Z-phase layer = `phase_shift(theta)`-equivalent on ONE of the two rail modes (verify from BosonSampling.jl's own source/docs which of `phase_shift`/`PhaseShift`/direct diagonal-unitary construction it exposes, and derive algebraically which single-mode phase reproduces this repo's `WP(theta,0)=diag(e^{i*theta},e^{-i*theta})` phase difference between the two rails -- do not assume, derive it the same way Plan 19-02 derived Yao's Rz convention).

Compose the full n-qubit unitary via direct-sum/block-diagonal construction across qubit pairs plus the beamsplitter mixing (use `UserDefinedInterferometer(U)` with U built as an explicit `2n x 2n` matrix in Julia, composing the per-qubit-pair beamsplitter and phase blocks -- this is the "independent build from primitives" approach CONTEXT.md requires, not extracting Perceval's own unitary).

Verify the convention at n=1 first: apply the constructed circuit to input `[1,0]`, compute `compute_probability!` for both possible single-qubit outputs, and assert the result matches the known closed form `P(bit=0)=cos(theta)^2, P(bit=1)=sin(theta)^2` at theta=0.3, within 1e-10 -- the same closed-form check Plan 19-02 used for the qubit-side circuit, now confirming the photonic-side encoding independently.
  </action>
  <verify>Run `julia --project=julia julia/verify_photonic_iqp_weight1.jl` through the n=1 convention check; must print PASS before proceeding to Task 2</verify>
  <done>Dual-rail bit->mode convention and per-qubit gate construction are independently derived (not ported from Perceval) and confirmed against a known closed-form single-qubit marginal</done>
</task>

<task type="auto">
  <name>Task 2: Run n=2/n=3 cross-check against the Python reference and write results</name>
  <files>julia/verify_photonic_iqp_weight1.jl, results/phase19_verify03_weight1_results.md</files>
  <action>
Using the same shared theta values as `results/julia_reference/weight1_n2.csv` / `weight1_n3.csv` (`n=2, thetas=[0.3, 1.1]` and `n=3, thetas=[0.3, 1.1, 0.75]` -- identical literals to Plan 19-01's Python side and Plan 19-02's qubit-side check), build the full n-qubit circuit and enumerate all valid computational-basis outputs (each qubit pair has exactly 1 photon total, in one of its 2 modes -- loop over all `2^n` combinations, call `compute_probability!` once per combination via a fresh `Event`).

Aggregate the results into a `Dict{String,Float64}` keyed by this repo's bitstring convention (same qubit-0-is-most-significant-bit convention verified in Plan 19-02).

Read the reference CSV (`results/julia_reference/weight1_n2.csv`, `weight1_n3.csv`) via `DelimitedFiles`, reimplement the TVD formula (reuse the same local function shape as Plan 19-02, written fresh here since these are separate scripts), compute TVD for both n values, and assert TVD <= 1e-6.

If either assertion fails: apply CONTEXT.md's disagreement-handling rule (time-boxed debug of mode-ordering/phase-convention/normalization first, per 19-RESEARCH.md Pitfall 6 -- BosonSampling.jl's beamsplitter sign convention is not guaranteed to match Perceval's, though |amplitude|^2-level agreement was already spot-checked for the trivial 50/50 case in Phase 14) -- report an honest documented disagreement if unresolved rather than forcing a pass.

Write `results/phase19_verify03_weight1_results.md`: methodology, measured TVD for n=2/n=3, explicit go/partial-go/no-go verdict for VERIFY-03's weight-1 leg.
  </action>
  <verify>julia --project=julia julia/verify_photonic_iqp_weight1.jl exits 0 and prints PASS with both TVD values; results/phase19_verify03_weight1_results.md exists with the measured numbers</verify>
  <done>VERIFY-03's weight-1 leg satisfied: an independently-built BosonSampling.jl dual-rail circuit's output distribution is diffed against the Python reference at n=2 and n=3, with a stated, honest go/no-go verdict</done>
</task>

</tasks>

<verification>
`julia --project=julia julia/verify_photonic_iqp_weight1.jl` runs to completion (pass or honestly-reported documented disagreement). `results/phase19_verify03_weight1_results.md` exists with measured TVD numbers.
</verification>

<success_criteria>
VERIFY-03's weight-1 leg is satisfiable from this plan's shipped artifacts, independent of how Plan 19-04's weight-2 leg turns out.
</success_criteria>

<output>
After completion, create `.planning/phases/19-independent-julia-cross-checks/19-03-SUMMARY.md`
</output>
