---
phase: 17.1-trainability-follow-up-bandwidth-init-sensitivity
plan: 04
type: execute
wave: 3
depends_on: ["17.1-03"]
files_modified:
  - results/phase171_train09_weight1_gradient_variance.csv
  - results/phase171_train09_mixed_gradient_variance.csv
autonomous: true

must_haves:
  truths:
    - "A real gradient-variance-vs-n dataset exists for BOTH generator scopes, at Phase 17's own CORE n-range (weight1 n=2..6, mixed n=2..5), BOTH init schemes (small_angle, uniform), at EACH of six fixed sigma values {0.03, 0.1, 0.3, 1.0, 3.0, 9.0} -- 6 independent full-CORE-sweep runs, not one sigma-schedule sweep (owner-decided design, 17.1-RESEARCH.md's 'Owner Decisions' section, superseding the research document's own earlier per-n sigma-schedule recommendation)."
    - "The sigma=0.1 subset of this new dataset reproduces Phase 17's original CORE CSVs (results/phase17_weight1_gradient_variance.csv, results/phase17_mixed_gradient_variance.csv) bit-for-bit -- an explicit, checked consistency guarantee, not an assumption (ROADMAP.md Phase 17.1's critical note)."
    - "Every row records its own sigma and bin_spacing explicitly (Plan 17.1-02's CSV columns), so the sigma grid's coverage is verifiable directly from the CSV, not just implied."
  artifacts:
    - path: "results/phase171_train09_weight1_gradient_variance.csv"
      provides: "n, generator_scope, init_scheme, sigma, bin_spacing, n_tracked_params, n_samples, mean, var, std, median, abs_mean, rms -- weight1, n=2..6, both init schemes, 6 sigma values (60 data rows)"
    - path: "results/phase171_train09_mixed_gradient_variance.csv"
      provides: "Same columns, mixed weight-1+weight-2, n=2..5, both init schemes, 6 sigma values (48 data rows)"
---

<objective>
Run TRAIN-09's actual sigma-grid sweep: Phase 17's full CORE gradient-variance measurement (weight1 n=2..6, mixed n=2..5, both init schemes), repeated once per fixed sigma value in `{0.03, 0.1, 0.3, 1.0, 3.0, 9.0}` -- the owner-decided design (17.1-RESEARCH.md's "Owner Decisions" section) that replaces the research document's own earlier "scale sigma to hold a constant ratio" recommendation, directly modeled on the sibling project's own `AC12` bandwidth sweep. This is 6x Phase 17's own CORE-sweep compute cost (same circuit evaluations per cell, sigma only changes the cheap kernel-matrix step) -- expect this plan to span multiple hours of wall-clock compute, run sequentially (never concurrently with itself or any other heavy job in this repo, per `.planning/STATE.md`'s documented ~1GB-free-memory constraint) and across session boundaries if needed, exactly as Phase 17's own long-running sweeps did.

Purpose: produces the raw sigma-sensitivity dataset Plan 17.1-05 will curve-fit and compare against Phase 17's original verdicts.
Output: two new CSVs in `results/`, one per generator scope, each containing all 6 sigma values' worth of rows.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17.1-trainability-follow-up-bandwidth-init-sensitivity/17.1-RESEARCH.md
@gradient_variance_sweep.py
@results/phase17_weight1_gradient_variance.csv
@results/phase17_mixed_gradient_variance.csv
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the weight1 sigma-grid sweep (6 sigma values x n=2..6 x both init schemes)</name>
  <files>results/phase171_train09_weight1_gradient_variance.csv</files>
  <action>
Run the CLI once per sigma value, appending into one CSV (first invocation writes the header, the
rest append -- use `--append` on every invocation after the first, or simplest: write the header
once by omitting `--append` on the FIRST sigma value only):

```
./venv/Scripts/python.exe gradient_variance_sweep.py --scope weight1 --n-values 2 3 4 5 6 \
    --init-schemes small_angle uniform --sigma 0.03 --out results/phase171_train09_weight1_gradient_variance.csv
./venv/Scripts/python.exe gradient_variance_sweep.py --scope weight1 --n-values 2 3 4 5 6 \
    --init-schemes small_angle uniform --sigma 0.1 --out results/phase171_train09_weight1_gradient_variance.csv --append
./venv/Scripts/python.exe gradient_variance_sweep.py --scope weight1 --n-values 2 3 4 5 6 \
    --init-schemes small_angle uniform --sigma 0.3 --out results/phase171_train09_weight1_gradient_variance.csv --append
./venv/Scripts/python.exe gradient_variance_sweep.py --scope weight1 --n-values 2 3 4 5 6 \
    --init-schemes small_angle uniform --sigma 1.0 --out results/phase171_train09_weight1_gradient_variance.csv --append
./venv/Scripts/python.exe gradient_variance_sweep.py --scope weight1 --n-values 2 3 4 5 6 \
    --init-schemes small_angle uniform --sigma 3.0 --out results/phase171_train09_weight1_gradient_variance.csv --append
./venv/Scripts/python.exe gradient_variance_sweep.py --scope weight1 --n-values 2 3 4 5 6 \
    --init-schemes small_angle uniform --sigma 9.0 --out results/phase171_train09_weight1_gradient_variance.csv --append
```

Expected wall-clock: each single-sigma invocation costs roughly what Phase 17's own weight1 CORE
sweep cost (~30-40 minutes, per `17-06-PLAN.md`'s measured estimate) -- circuit evaluation count
is unchanged by sigma, only the kernel matrix (cheap) changes. Total for this task: roughly
3-4 hours. This is expected; do not shorten the draw count or n range. Run via background
execution (this repo's established pattern for long sweeps -- see Phase 17's Plan 17-06) and check
progress periodically rather than blocking synchronously on the full multi-hour total; each
invocation's own stdout prints per-(n, init_scheme)-cell progress as it completes.

**Resumability:** because each sigma value is a separate CLI invocation appending to the same
file, if execution is interrupted partway through this grid, the already-completed sigma values'
rows are safely on disk -- before re-running a sigma value, check whether its rows already exist
in the CSV (`grep ",0.03," results/phase171_train09_weight1_gradient_variance.csv` or equivalent)
to avoid duplicate rows; do not re-run a sigma value that's already fully present.
  </action>
  <verify>`results/phase171_train09_weight1_gradient_variance.csv` has 60 data rows (6 sigma values x 5 n-values x 2 init schemes) plus header; every row has a finite, non-NaN `var`; exactly 6 distinct `sigma` values appear, each with exactly 10 rows.</verify>
  <done>The weight1 sigma-grid sweep is complete across all 6 sigma values, both init schemes, n=2..6.</done>
</task>

<task type="auto">
  <name>Task 2: Run the mixed sigma-grid sweep (6 sigma values x n=2..5 x both init schemes), reusing draw-chunking for n=5</name>
  <files>results/phase171_train09_mixed_gradient_variance.csv</files>
  <action>
Same pattern as Task 1, but for `--scope mixed --n-values 2 3 4 5`. Expected wall-clock per sigma
value: roughly what Phase 17's own mixed CORE sweep cost (~1.5-2 hours, `17-06-PLAN.md`'s
measured estimate, dominated by n=5's per-call cost) -- total for this task across all 6 sigma
values: roughly 9-12 hours. This is the heaviest single task in this phase; treat it as a
background job spanning multiple sessions if needed, exactly as Phase 17's own STRETCH job did,
and never run it concurrently with anything else heavy in this repo (per `.planning/STATE.md`'s
documented memory constraint -- confirmed live in Phase 17 that two heavy jobs at once reproduces
a `MemoryError` neither alone hits).

**Mixed n=5's known memory leak (Phase 17's own finding) recurs at EVERY sigma value**, since it's
driven by circuit-evaluation call count, not sigma. Reuse the existing draw-chunking flags for
just the n=5 cell, at each of the 6 sigma values, exactly as Phase 17's Plan 17-06 needed for this
same cell (5 chunks of 20 draws each, per init_scheme, per sigma):

```
# For each sigma in {0.03, 0.1, 0.3, 1.0, 3.0, 9.0} and each init_scheme in {small_angle, uniform}:
./venv/Scripts/python.exe gradient_variance_sweep.py --scope mixed --n-values 5 \
    --init-schemes small_angle --sigma <SIGMA> --draw-start 0 --draw-count 20 \
    --out results/phase171_train09_mixed_gradient_variance.csv
./venv/Scripts/python.exe gradient_variance_sweep.py --scope mixed --n-values 5 \
    --init-schemes small_angle --sigma <SIGMA> --draw-start 20 --draw-count 20 \
    --out results/phase171_train09_mixed_gradient_variance.csv
... (5 chunks of 20 = 100 draws total) ...
./venv/Scripts/python.exe gradient_variance_sweep.py --scope mixed --n-values 5 \
    --init-schemes small_angle --sigma <SIGMA> --out results/phase171_train09_mixed_gradient_variance.csv \
    --append --combine-chunks
```

Run n=2,3,4 (no chunking needed, cheap) via the same single-invocation-per-sigma pattern as
Task 1. Pass `--sigma` IDENTICALLY across every chunk invocation and its corresponding
`--combine-chunks` call for that sigma (Plan 17.1-02's Pitfall-2 fix makes a mismatch fail loudly
with `FileNotFoundError` rather than silently -- if that happens, it means a `--sigma` value was
typed inconsistently; fix and re-run, do not work around it).

Same resumability note as Task 1: check which sigma values already have complete rows before
re-running.
  </action>
  <verify>`results/phase171_train09_mixed_gradient_variance.csv` has 48 data rows (6 sigma values x 4 n-values x 2 init schemes) plus header; every row has a finite, non-NaN `var`; exactly 6 distinct `sigma` values appear, each with exactly 8 rows.</verify>
  <done>The mixed sigma-grid sweep is complete across all 6 sigma values, both init schemes, n=2..5, with no chunk/combine sigma mismatches.</done>
</task>

<task type="auto">
  <name>Task 3: Consistency check -- sigma=0.1 reproduces Phase 17's original CORE CSVs bit-for-bit</name>
  <files>results/phase171_train09_weight1_gradient_variance.csv, results/phase171_train09_mixed_gradient_variance.csv</files>
  <action>
Write and run a short one-off verification (a scratch script or an inline
`./venv/Scripts/python.exe -c "..."` command is fine -- this is a verification step, not a
permanent artifact) that: for each of weight1 and mixed, loads the `sigma=0.1` subset of the new
CSV and Phase 17's original CSV (`results/phase17_weight1_gradient_variance.csv`,
`results/phase17_mixed_gradient_variance.csv`), and asserts, for every matching
`(n, init_scheme)` row, that `mean`, `var`, `std`, `median`, `abs_mean`, `rms`, and `n_samples`
are IDENTICAL (exact equality is expected here -- same code path, same seed_base, same RNG
substreams, only the CLI invocation differs; report as a real bug, not a rounding footnote, if any
value differs even slightly). This is the explicit, built-in consistency check ROADMAP.md's
critical note calls for: if this fails, something is wrong with the sigma-threading plumbing
(Plan 17.1-02/17.1-03), not with the data.

Record the outcome (pass/fail, with the actual comparison) in this plan's SUMMARY.md.
  </action>
  <verify>The consistency check passes for every (n, init_scheme) row in both scopes -- 10 weight1 comparisons, 8 mixed comparisons, all exact matches.</verify>
  <done>sigma=0.1's new-sweep rows are proven bit-identical to Phase 17's original CORE CSVs, confirming the sigma-threading refactor introduced no behavior change at the old default value.</done>
</task>

</tasks>

<verification>
1. Both new CSVs have the expected row counts (60 weight1, 48 mixed) with 6 distinct sigma values each.
2. The sigma=0.1 consistency check (Task 3) passes exactly for every row.
3. `./venv/Scripts/python.exe -m pytest` (full suite) -- no regressions (this plan only runs existing, already-tested code paths at new argument values; no source files are modified).
</verification>

<success_criteria>
- TRAIN-09's real sigma-sensitivity dataset exists for both generator scopes across the full sigma grid and the full CORE n-range, satisfying ROADMAP.md Phase 17.1 success criterion 1.
- The sigma=0.1 built-in consistency check passes, proving the new pipeline reproduces Phase 17's original result exactly at the original bandwidth.
</success_criteria>

<output>
After completion, create `.planning/phases/17.1-trainability-follow-up-bandwidth-init-sensitivity/17.1-04-SUMMARY.md`
</output>
