---
phase: 17.1-trainability-follow-up-bandwidth-init-sensitivity
plan: 05
type: execute
wave: 4
depends_on: ["17.1-04"]
files_modified:
  - results/phase171_train10_weight1_gradient_variance.csv
  - results/phase171_train10_mixed_gradient_variance.csv
  - trainability_analysis_1701.py
  - results/phase171_train09_curve_fit_summary.csv
  - results/phase171_train10_curve_fit_summary.csv
  - results/phase171_train09_weight1_sigma_sensitivity.png
  - results/phase171_train09_mixed_sigma_sensitivity.png
autonomous: true

must_haves:
  truths:
    - "A real gradient-variance-vs-n dataset exists under TRAIN-10's data-dependent init scheme, for both generator scopes at Phase 17's CORE n-range, at scale_factor=1.0 and n_draws=1 (both owner-decided, 17.1-RESEARCH.md's 'Owner Decisions' section)."
    - "For TRAIN-09: the poly-vs-exponential verdict (trainability.curve_fit.fit_and_compare, reused unmodified) is computed at every one of the 6 sigma values for weight1/uniform and mixed/uniform specifically (the two cells that had a definite 'exp' verdict in Phase 17's original CORE data), with an explicit survives/weakens/disappears statement against the original verdict."
    - "For TRAIN-10: the same verdict machinery is applied to the new data_dependent cells (weight1 and mixed), with an explicit statement of whether the result is a clearer (non-inconclusive: 'exp' or 'poly') verdict than small_angle's original 'inconclusive' result, and if so which direction."
    - "Phase 17's own trainability_analysis.py is NOT modified -- it remains Phase 17's canonical, already-shipped output-generator, referenced by docs/trainability-study.md as-is; this comparison analysis lives in a new, separate script."
  artifacts:
    - path: "trainability_analysis_1701.py"
      provides: "New root-level script (does not import from or modify trainability_analysis.py): loads TRAIN-09's 6-sigma CSVs and TRAIN-10's data_dependent CSVs, reuses trainability.curve_fit.fit_and_compare unchanged, builds sigma-vs-verdict and original-vs-new comparison tables, writes summary CSVs and plots"
      min_lines: 60
    - path: "results/phase171_train09_curve_fit_summary.csv"
      provides: "Per (generator_scope, init_scheme, sigma): exp/poly params, r2, aic, verdict -- 6 sigma rows per cell"
    - path: "results/phase171_train10_curve_fit_summary.csv"
      provides: "Per (generator_scope): exp/poly params, r2, aic, verdict, and explicit comparison against the original small_angle verdict for that scope"
  key_links:
    - from: "trainability_analysis_1701.py"
      to: "trainability.curve_fit.fit_and_compare"
      via: "direct call per (generator_scope, init_scheme, sigma) for TRAIN-09 and per (generator_scope) for TRAIN-10 -- same statistical machinery Phase 17 used, applied to new data"
      pattern: "fit_and_compare\\("
    - from: "trainability_analysis_1701.py"
      to: "results/phase17_curve_fit_summary.csv"
      via: "loaded as the baseline for the survives/weakens/disappears and non-inconclusive comparisons"
      pattern: "phase17_curve_fit_summary"
---

<objective>
Run TRAIN-10's sweep (cheap: deterministic init means `n_draws=1` produces the full dataset, per 17.1-RESEARCH.md's ~100x-cheaper-than-TRAIN-09 finding, owner-confirmed), then close out both follow-up experiments' data analysis: apply Phase 17's own, unmodified `fit_and_compare` machinery to TRAIN-09's 6-sigma-per-cell dataset and TRAIN-10's new init-scheme dataset, and build the explicit comparison tables ROADMAP.md's success criteria 2 and 4 require (survives/weakens/disappears for TRAIN-09; clearer-or-still-inconclusive for TRAIN-10). Per 17.1-RESEARCH.md's explicit instruction, this analysis lives in a NEW script (`trainability_analysis_1701.py`) -- Phase 17's own `trainability_analysis.py` is Phase 17's canonical, already-shipped output-generator and stays untouched.

Purpose: turns Plan 17.1-04's raw sigma-grid data and this plan's TRAIN-10 data into the actual measured verdicts Plan 17.1-06 will write up.
Output: `trainability_analysis_1701.py`, two new gradient-variance CSVs (TRAIN-10), two new curve-fit summary CSVs, and sigma-sensitivity plots.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17.1-trainability-follow-up-bandwidth-init-sensitivity/17.1-RESEARCH.md
@gradient_variance_sweep.py
@trainability_analysis.py
@trainability/curve_fit.py
@results/phase17_curve_fit_summary.csv
@results/phase171_train09_weight1_gradient_variance.csv
@results/phase171_train09_mixed_gradient_variance.csv
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the TRAIN-10 sweep (data_dependent init, n_draws=1, scale_factor=1.0)</name>
  <files>results/phase171_train10_weight1_gradient_variance.csv, results/phase171_train10_mixed_gradient_variance.csv</files>
  <action>
Run, sequentially (this plan depends on Plan 17.1-04's completion specifically so this never runs
concurrently with TRAIN-09's heavy sweep, per the memory-constraint rule -- even though this run
is individually cheap):

```
./venv/Scripts/python.exe gradient_variance_sweep.py --scope weight1 --n-values 2 3 4 5 6 \
    --init-schemes data_dependent --n-draws 1 --scale-factor 1.0 \
    --out results/phase171_train10_weight1_gradient_variance.csv
./venv/Scripts/python.exe gradient_variance_sweep.py --scope mixed --n-values 2 3 4 5 \
    --init-schemes data_dependent --n-draws 1 --scale-factor 1.0 \
    --out results/phase171_train10_mixed_gradient_variance.csv
```

Expected wall-clock: a few minutes total (each cell now needs only
`n_tracked_params x 1 draw x 2 shifts` circuit evaluations, ~100x fewer than TRAIN-09/Phase 17's
100-draw sweeps, per 17.1-RESEARCH.md's cost-asymmetry finding). `--sigma` is deliberately left at
its default (0.1, Phase 17's original bandwidth) -- TRAIN-10 tests init sensitivity, not bandwidth
sensitivity, so holding sigma at Phase 17's original value keeps this an apples-to-apples
comparison against the original `small_angle`/`uniform` CORE results.
  </action>
  <verify>Both CSVs exist with the expected row counts (5 rows weight1, 4 rows mixed) and finite `var` values. Because `n_draws=1`, `n_samples` per row equals `n_tracked_params` (not `n_tracked_params * 100` as in the other schemes) -- confirm this is the case and not a sign draws were silently multiplied.</verify>
  <done>TRAIN-10's real gradient-variance dataset exists for both generator scopes at Phase 17's CORE n-range.</done>
</task>

<task type="auto">
  <name>Task 2: Build trainability_analysis_1701.py -- curve-fit both follow-ups and compare against Phase 17's original verdicts</name>
  <files>trainability_analysis_1701.py, results/phase171_train09_curve_fit_summary.csv, results/phase171_train10_curve_fit_summary.csv, results/phase171_train09_weight1_sigma_sensitivity.png, results/phase171_train09_mixed_sigma_sensitivity.png</files>
  <action>
Create `trainability_analysis_1701.py` at the repo root, following `trainability_analysis.py`'s
existing structure (`load_series` -> `fit_and_compare` -> `save_summary_csv`/`save_plot`) but as a
genuinely separate script with its own CSV paths and constants -- do not import from or modify
`trainability_analysis.py`.

**TRAIN-09 analysis:** for each `generator_scope in {"weight1", "mixed"}`, each
`init_scheme in {"small_angle", "uniform"}`, each `sigma in {0.03, 0.1, 0.3, 1.0, 3.0, 9.0}`: load
the matching `(n, var)` series from `results/phase171_train09_{scope}_gradient_variance.csv`
(filter rows by `sigma` in addition to `generator_scope`/`init_scheme`), call
`fit_and_compare(ns, variances)`, and record `{generator_scope, init_scheme, sigma, bin_spacing
(from that scope/sigma's rows), n_min, n_max, verdict, exp_r2, exp_aic, exp_b (the fitted decay
rate, params[1]), poly_r2, poly_aic}` -- 24 rows total (2 scopes x 2 init x 6 sigma). Write to
`results/phase171_train09_curve_fit_summary.csv`.

Build the explicit survives/weakens/disappears comparison ROADMAP.md success criterion 2 requires,
focused on `weight1/uniform` and `mixed/uniform` (the two cells with a definite "exp" verdict in
Phase 17's original CORE data, loaded from `results/phase17_curve_fit_summary.csv` as the
baseline) -- print and record, per sigma: "survives" (verdict stays "exp" with `b>0`), "weakens"
(stays "exp" but R²/AIC margin or `b` shrinks materially vs. the sigma=0.1 row), or "disappears"
(verdict flips to "poly" or "inconclusive"). Also run the small_angle cells across the sigma grid
for completeness (ROADMAP.md's success criterion 1 says "both init schemes"), but frame the
survives/weakens/disappears narrative around the `uniform` cells specifically, since only those
had an "exp" signature to test for survival (17.1-RESEARCH.md's explicit scope note).

Plot `results/phase171_train09_weight1_sigma_sensitivity.png` and `..._mixed_sigma_sensitivity.png`:
one figure per scope, x-axis = sigma (log scale), y-axis = the fitted exp-model decay rate `b` (or
the AIC-delta between poly and exp, whichever more directly shows verdict stability -- choose one
and state which in the script's docstring), one line per init_scheme, so the sigma-sensitivity of
the exp-decay signature is visible at a glance, not just tabulated.

**TRAIN-10 analysis:** for each `generator_scope in {"weight1", "mixed"}`: load the `(n, var)`
series from `results/phase171_train10_{scope}_gradient_variance.csv` (`init_scheme ==
"data_dependent"`), call `fit_and_compare`, and record the same per-cell fields as above (no sigma
column needed here, since sigma is held at 0.1). Write to
`results/phase171_train10_curve_fit_summary.csv`, with an added `vs_small_angle_verdict` column
stating explicitly whether the new verdict is a clearer result than `small_angle`'s original
`"inconclusive"` (ROADMAP.md success criterion 4) -- `"clearer (exp)"`, `"clearer (poly)"`, or
`"still inconclusive"`.

Print a concise final summary to stdout covering both analyses' headline findings (which cells
survived/weakened/disappeared for TRAIN-09; which direction, if any, TRAIN-10 resolved).
  </action>
  <verify>`./venv/Scripts/python.exe trainability_analysis_1701.py` runs cleanly and produces both summary CSVs (24 rows / 2 rows respectively) and both plots. Manually inspect the printed survives/weakens/disappears and clearer/still-inconclusive statements for `weight1/uniform`, `mixed/uniform`, and both TRAIN-10 cells -- each must be one of the defined labels, never left blank or hedged.</verify>
  <done>trainability_analysis_1701.py exists, runs cleanly, and produces explicit, labeled verdicts for every TRAIN-09 sigma cell and every TRAIN-10 cell, without modifying trainability_analysis.py.</done>
</task>

</tasks>

<verification>
1. Both TRAIN-10 CSVs exist with correct row counts and finite `var` values.
2. `results/phase171_train09_curve_fit_summary.csv` has 24 rows; `results/phase171_train10_curve_fit_summary.csv` has 2 rows.
3. Both sigma-sensitivity plots exist and are non-empty.
4. `trainability_analysis.py` (Phase 17's original) is unchanged -- `git diff --stat trainability_analysis.py` shows no changes.
5. `./venv/Scripts/python.exe -m pytest` (full suite) -- no regressions.
</verification>

<success_criteria>
- TRAIN-10's real dataset exists at the owner-decided hyperparameters (scale_factor=1.0, n_draws=1).
- ROADMAP.md Phase 17.1 success criteria 2 and 4 are directly satisfiable from this plan's output: explicit survives/weakens/disappears (TRAIN-09) and clearer/still-inconclusive (TRAIN-10) statements exist for every required cell.
- Phase 17's own trainability_analysis.py remains untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/17.1-trainability-follow-up-bandwidth-init-sensitivity/17.1-05-SUMMARY.md`
</output>
