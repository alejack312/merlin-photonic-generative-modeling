---
phase: 17.1-trainability-follow-up-bandwidth-init-sensitivity
plan: 01
subsystem: testing
tags: [numpy, tdd, trainability, init-scheme, barren-plateau, recio-armengol]

# Dependency graph
requires:
  - phase: 17-trainability-barren-plateau-study
    provides: "trainability/target_grid.py's make_target_grid(n) -> (centers, p_real, bin_index_fn), the K=2^n grid-bin target representation this plan's p_real inputs come from"
provides:
  - "trainability/data_dependent_init.py: bit_k, empirical_mean_bit, weight1_data_dependent_theta, empirical_pm1_covariance, weight2_data_dependent_theta -- Recio-Armengol et al. (arXiv:2503.02934 Sec 8.1.2) data-dependent init recipe, translated onto this project's grid-bin p_real representation"
  - "Bit-ordering convention (bit_k as the exact inverse of target_grid.py's bin_index_fn = int(bitstring,2)) proven correct by a dedicated unit test across n in {2,3,4} and every qubit position"
affects: ["17.1-02", "17.1-03 (TRAIN-10 sweep wiring)"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Pure-numpy, no-cross-import, single-concern module (matches mmd_exact.py/param_shift.py/target_grid.py/rng.py/stats.py/curve_fit.py convention) -- callers pass p_real in directly rather than the module importing target_grid itself"

key-files:
  created:
    - trainability/data_dependent_init.py
    - tests/test_data_dependent_init.py

key-decisions:
  - "Implemented exactly as the plan's <implementation> block specified -- no deviation from the reference formulas or bit-ordering derivation"

patterns-established:
  - "Data-dependent init functions take p_real (the grid-bin target distribution) as input rather than raw training data, so they compose directly with trainability/target_grid.py's existing K=2^n grid machinery without a new data-loading path"

# Metrics
duration: ~2min (module + test already scaffolded from a prior interrupted attempt; this run validated, staged, and committed)
completed: 2026-08-12
---

# Phase 17.1 Plan 01: Data-Dependent Init Primitives (TRAIN-10) Summary

**`trainability/data_dependent_init.py` implementing Recio-Armengol et al.'s (arXiv:2503.02934 Sec 8.1.2) data-dependent init recipe on this project's grid-bin `p_real`, with the bitstring-index-to-qubit bit-ordering convention proven correct by a dedicated test rather than assumed.**

## Performance

- **Duration:** ~2 min of active execution (RED confirm -> GREEN implement -> commit); the test file existed as an untracked draft from a prior interrupted session and was validated against the plan's spec before being trusted
- **Completed:** 2026-08-12T17:23:29+02:00 (final commit)
- **Tasks:** 1 (TDD: RED, GREEN; no REFACTOR needed)
- **Files modified:** 2 (both new)

## Accomplishments
- `bit_k(bin_index, k, n)` proven the exact inverse of `target_grid.py`'s `bin_index_fn(bitstring) = int(bitstring, 2)` for every qubit position `k` at `n in {2,3,4}` (17.1-RESEARCH.md Pitfall 3), closing the silent-wrong-qubit failure mode the plan flagged as this task's real risk.
- `weight1_data_dependent_theta` (single-qubit init, `arcsin(sqrt(<x_k>))`) and `weight2_data_dependent_theta` (two-qubit init, `scale_factor * Cov(z_i,z_j)` in the `{-1,+1}` convention) both match hand-computed worked-example values exactly.
- Confirmed by direct construction (not just documentation) that `weight1_data_dependent_theta` never raises at the arcsin domain boundary (bit-marginal exactly 0.0 or 1.0) -- both are valid probabilities.

## Task Commits

TDD task, two commits (RED -> GREEN; no REFACTOR was needed -- the implementation matched the plan's reference code with no cleanup required):

1. **RED: failing test for data-dependent init primitives** - `c214405` (test)
2. **GREEN: implement data-dependent init primitives** - `e972855` (feat)

## Files Created/Modified
- `trainability/data_dependent_init.py` - `bit_k`, `empirical_mean_bit`, `weight1_data_dependent_theta`, `empirical_pm1_covariance`, `weight2_data_dependent_theta` (70 lines, pure numpy, no cross-imports)
- `tests/test_data_dependent_init.py` - 8 tests: Pitfall-3 bit-ordering spot-check (parametrized over n in {2,3,4}), worked-example checks for every function, arcsin domain-boundary safety check (94 lines)

## Decisions Made
None beyond the plan's own spec -- implemented the formulas, bit-ordering derivation, and test cases exactly as written in the plan's `<implementation>`/`<behavior>` blocks. No architectural or interpretive choices were needed.

## Deviations from Plan

None — plan executed exactly as written. One process note, not a deviation from the plan's content: the task prompt flagged that `tests/test_data_dependent_init.py` existed as an uncommitted, untracked file from a prior interrupted attempt. It was read and compared line-by-line against the plan's `<behavior>` specification before being trusted; it matched exactly (all 6 required cases, correct worked-example values, correct Pitfall-3 spot-check structure), so it was kept as-is rather than rewritten, then used as the RED-phase commit.

## Issues Encountered

**Concurrent session activity in the same working tree.** During execution, files unrelated to this plan's scope (`gradient_variance_sweep.py`, `results/_tmp_sigma_test.csv`, and at different points `trainability/sweep.py`, `trainability/target_grid.py`, `tests/test_sweep.py`) appeared, disappeared, and changed content between shell calls — evidence of another active session doing TRAIN-09-adjacent work in this same repo (per `~/.claude/CLAUDE.md`'s Fleet Operations note: "I run multiple parallel sessions"). Handled by staging files individually and re-verifying `git status --short` immediately before each commit, never using `git add -A`/`git add .` — only `tests/test_data_dependent_init.py` and `trainability/data_dependent_init.py` were ever staged or committed by this execution. No files belonging to other in-flight work were touched, staged, or committed.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- `trainability/data_dependent_init.py` is a trusted, independently-tested primitive ready for Plan 17.1-03 to wire into the actual gradient-variance sweep as TRAIN-10's data-dependent init scheme.
- Full 231-test repo suite passes with zero regressions as of this plan's final commit.
- Other uncommitted, in-progress work in the working tree (apparently TRAIN-09/sigma-bandwidth related, under `trainability/sweep.py`, `trainability/target_grid.py`, `tests/test_sweep.py`, `gradient_variance_sweep.py`) belongs to a separate concurrent session/plan and was deliberately left untouched — not committed, not evaluated for correctness, and not part of this plan's scope. Whoever owns that work should commit or resolve it separately.

---
*Phase: 17.1-trainability-follow-up-bandwidth-init-sensitivity*
*Completed: 2026-08-12*
