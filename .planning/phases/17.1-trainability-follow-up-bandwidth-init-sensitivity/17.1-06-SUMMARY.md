---
phase: 17.1-trainability-follow-up-bandwidth-init-sensitivity
plan: 06
subsystem: docs
tags: [trainability, barren-plateau, gaussian-kernel, bandwidth-sensitivity, data-dependent-init, documentation]

# Dependency graph
requires:
  - phase: 17.1-05
    provides: "results/phase171_train09_curve_fit_summary.csv (24 rows, TRAIN-09 sigma-grid curve-fit verdicts) and results/phase171_train10_curve_fit_summary.csv (2 rows, TRAIN-10 data-dependent-init curve-fit verdicts)"
provides:
  - "docs/trainability-study.md gains two new top-level subsections: Bandwidth sensitivity follow-up (TRAIN-09) and Data-dependent initialization follow-up (TRAIN-10), plus a short addendum to the closing 'What this does/doesn't establish' section"
  - "TRAIN-09 and TRAIN-10 requirements now fully satisfiable end to end from shipped artifacts (results CSVs + this doc), closing ROADMAP.md Phase 17.1 success criterion 5"
affects: ["20-technical-write-up"]

# Tech tracking
tech-stack:
  added: []
  patterns: ["Follow-up findings folded into an existing canonical doc as new, clearly-scoped top-level sections rather than editing the original content in place -- same pattern the doc's own 'Independent cross-check' section already established"]

key-files:
  created: []
  modified: ["docs/trainability-study.md"]

key-decisions:
  - "Both new sections positioned strictly after Cross-reference verdict (TRAIN-07) and before Independent cross-check, per 17.1-RESEARCH.md's explicit insertion-point guidance -- keeps Phase 17's own core content and the later supplementary dual-rail work each contiguous."
  - "TRAIN-09's results split into two tables (weight1, mixed) rather than one 24-row table, for readability, with sigma as the leading row-differentiator per init_scheme -- plan explicitly allowed this alternative to a single dense table."
  - "Reported the weight1/uniform non-monotonic re-emergence at sigma in {3.0, 9.0} as its own finding, not smoothed into a simple 'artifact fades with bandwidth' narrative -- the data doesn't support that simpler story and this project's convention is to report the finding as measured."

patterns-established:
  - "When multiple owner-facing verdict tables exist across a document, extend/reuse the original table's exact column shape rather than inventing a new one, so any reader can cross-reference rows directly."

# Metrics
duration: 12min
completed: 2026-08-13
---

# Phase 17.1 Plan 06: Bandwidth and Init Follow-Up Write-Up Summary

**Wrote docs/trainability-study.md's TRAIN-09 (bandwidth sensitivity) and TRAIN-10 (data-dependent init) follow-up sections, reporting both as honest either-direction findings: the original "exp" verdict is NOT robust across the sigma grid (non-monotonic for weight1/uniform), and data-dependent init did NOT resolve small_angle's inconclusive verdict.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-08-13T16:08:00Z
- **Completed:** 2026-08-13T16:20:00Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- `## Bandwidth sensitivity follow-up (TRAIN-09)` section added: methodology (why the fixed sigma-grid design was chosen over a per-n schedule), full 24-cell results split into weight1/mixed tables, sigma=0.1 consistency-check footnote (including the ~1e-13-to-1e-16 mixed/small_angle discrepancy diagnosed in Plan 17.1-04), and a plain verdict — the original "exp" finding for `weight1/uniform` and `mixed/uniform` is not robust across sigma, surviving only near 0.1 and (for weight1 only) non-monotonically re-emerging at 3.0/9.0.
- `## Data-dependent initialization follow-up (TRAIN-10)` section added: methodology (the grid-bin translation of Recio-Armengol et al.'s recipe, the mixed-scope weight-2-pair override design decision restated explicitly, `scale_factor=1.0`/`n_draws=1` rationale), results table, and comparison-against-`small_angle` table — plain verdict: the literature-sourced init did not resolve the inconclusive verdict in either scope, a genuine negative result.
- Addendum added to the closing `## What this does/doesn't establish` section pointing to both new subsections without altering any of that section's original language.
- Confirmed via `git diff` that the edit is strictly additive (175 insertions, 0 deletions) — Phase 17's original content is byte-for-byte unchanged.

## Task Commits

Each task was committed atomically:

1. **Task 1: Write the TRAIN-09 and TRAIN-10 subsections into docs/trainability-study.md** - `0ac222c` (docs)

**Plan metadata:** (this commit, following SUMMARY.md + STATE.md update)

## Files Created/Modified
- `docs/trainability-study.md` - Added two new top-level sections (TRAIN-09, TRAIN-10 follow-ups) plus a short addendum to the closing section; no existing content altered.

## Decisions Made
- Split TRAIN-09's results into two tables (weight1, mixed) instead of one dense 24-row table, per the plan's explicitly-offered alternative, for readability.
- Reported weight1/uniform's non-monotonic re-emergence (exp at sigma=3.0/9.0 after disappearing at 0.3/1.0) as its own distinct finding rather than folding it into a simpler "fixed-bandwidth artifact fades away" narrative, since the data does not support that simpler story.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 17.1 is now fully complete (all 6 plans shipped): TRAIN-09 and TRAIN-10 are satisfiable end to end from shipped artifacts (`results/phase171_train09_curve_fit_summary.csv`, `results/phase171_train10_curve_fit_summary.csv`, and this document).
- Phase 20 (Technical Write-Up) can draw on `docs/trainability-study.md` directly, including both follow-up sections, without needing to re-derive anything from raw CSVs.
- No blockers. The `> Owner interpretation: [pending]` blockquote in the Cross-reference verdict (TRAIN-07) section remains open (pre-existing, untouched by this plan, tracked separately in STATE.md's Pending Todos).

---
*Phase: 17.1-trainability-follow-up-bandwidth-init-sensitivity*
*Completed: 2026-08-13*
