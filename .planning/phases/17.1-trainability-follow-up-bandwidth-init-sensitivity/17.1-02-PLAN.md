---
phase: 17.1-trainability-follow-up-bandwidth-init-sensitivity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - trainability/sweep.py
  - trainability/target_grid.py
  - gradient_variance_sweep.py
  - tests/test_sweep.py
autonomous: true

must_haves:
  truths:
    - "The MMD kernel bandwidth (sigma) used by pooled_gradients_for_cell/run_gradient_variance_sweep is an explicit, caller-controllable parameter -- not a hardcoded module constant -- while every existing zero-argument-changed caller (Phase 17's own tests and CLI flows) keeps producing bit-identical results (17.1-RESEARCH.md Pitfall 1)."
    - "gradient_variance_sweep.py's CLI accepts a --sigma flag and records both the sigma value and this project's own target-grid bin spacing at that n explicitly in its CSV output -- not just implied by a formula (ROADMAP.md Phase 17.1 success criterion 1)."
    - "Draw-chunk files and their later combine-chunks call cannot silently mix gradients computed at different sigma values -- sigma is embedded in the chunk filename itself, so a sigma mismatch between chunk and combine calls produces a clean, loud error (no matching chunk files found), never a silent semantic mismatch (17.1-RESEARCH.md Pitfall 2)."
  artifacts:
    - path: "trainability/sweep.py"
      provides: "pooled_gradients_for_cell(..., sigma: float = SIGMA) and run_gradient_variance_sweep(..., sigma: float = SIGMA), sigma passed straight through to mmd_exact.gaussian_kernel_matrix_np(centers, sigma) in place of the bare module global"
      min_lines: 5
    - path: "trainability/target_grid.py"
      provides: "bin_spacing(n, lo=-0.1, hi=1.1) -> float, the nearest-neighbor spacing between adjacent target-grid bin centers at a given n"
      min_lines: 8
    - path: "gradient_variance_sweep.py"
      provides: "--sigma CLI flag (default 0.1, backward compatible), sigma + bin_spacing columns in every CSV row, sigma embedded in chunk filenames"
      min_lines: 5
  key_links:
    - from: "gradient_variance_sweep.py"
      to: "trainability/sweep.py::run_gradient_variance_sweep"
      via: "args.sigma threaded through as an explicit keyword argument on every call path (run(), run_chunk(), combine_chunks())"
      pattern: "sigma=args\\.sigma"
    - from: "gradient_variance_sweep.py::_chunk_path"
      to: "gradient_variance_sweep.py::combine_chunks's glob pattern"
      via: "sigma value embedded in the chunk filename itself, so chunk and combine calls at mismatched sigma cannot silently combine"
      pattern: "sigma\\{sigma"
---

<objective>
Thread `sigma` as an explicit, backward-compatible parameter through the existing gradient-variance sweep pipeline, and fix the one real correctness risk that comes with retrofitting a second per-cell parameter onto the existing draw-chunking machinery (17.1-RESEARCH.md Pitfall 2: chunk files store raw gradients but not which sigma produced them). This is the "textbook add-an-optional-parameter-with-the-old-behavior-as-default" refactor 17.1-RESEARCH.md's Minimal-diff plan calls for -- no new statistics or simulation logic, only plumbing.

Purpose: gives Plan 17.1-04 the CLI surface it needs to actually run TRAIN-09's fixed-sigma-grid sweep (`{0.03, 0.1, 0.3, 1.0, 3.0, 9.0}`, owner-decided per 17.1-RESEARCH.md's "Owner Decisions" section), with sigma and bin-spacing values recorded explicitly in every output row.
Output: `trainability/sweep.py`, `trainability/target_grid.py`, `gradient_variance_sweep.py` updated; `tests/test_sweep.py` extended to prove both backward compatibility and the new sigma behavior.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17.1-trainability-follow-up-bandwidth-init-sensitivity/17.1-RESEARCH.md
@trainability/sweep.py
@trainability/target_grid.py
@trainability/mmd_exact.py
@gradient_variance_sweep.py
@tests/test_sweep.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread sigma through trainability/sweep.py and add target_grid.py's bin_spacing helper</name>
  <files>trainability/sweep.py, trainability/target_grid.py, tests/test_sweep.py</files>
  <action>
**`trainability/sweep.py`**: add `sigma: float = SIGMA` as an explicit keyword parameter to both
`pooled_gradients_for_cell` and `run_gradient_variance_sweep`. Replace the current
`K = mmd_exact.gaussian_kernel_matrix_np(centers, SIGMA)` call (which reads the bare module
global) with `K = mmd_exact.gaussian_kernel_matrix_np(centers, sigma)` (reads the parameter,
which defaults to the same module constant `SIGMA`). `run_gradient_variance_sweep` passes its own
`sigma` parameter straight through to its `pooled_gradients_for_cell(...)` call. The module-level
`SIGMA = 0.1` constant itself stays -- it is still the correct default and still used directly by
any caller that doesn't pass `sigma` explicitly (Pitfall 1: this MUST remain a zero-behavior-change
default for every existing caller, including `tests/test_sweep.py`'s current suite and Phase 17's
already-shipped CSVs -- do not make `sigma` a required parameter).

**`trainability/target_grid.py`**: add a small new function, purely additive (does not touch
`make_target_grid` or `bitstring_dict_to_vector`):

```python
def bin_spacing(n: int, lo: float = -0.1, hi: float = 1.1) -> float:
    """Nearest-neighbor spacing between adjacent target-grid bin centers at this n -- the
    min(dx, dy) of make_target_grid's axis-aligned rows x cols layout. Purely a reporting/context
    value (17.1-RESEARCH.md: TRAIN-09's final design uses a FIXED sigma per sweep run, not a
    per-n-derived schedule, so this is NOT used to compute sigma -- only to record explicitly, in
    the sweep's own CSV output, how sharp/blunt a given fixed sigma is relative to the grid at
    each n, per ROADMAP.md Phase 17.1 success criterion 1)."""
    rows = 2 ** ((n + 1) // 2)
    cols = 2 ** (n // 2)
    dx = (hi - lo) / (rows - 1) if rows > 1 else float("inf")
    dy = (hi - lo) / (cols - 1) if cols > 1 else float("inf")
    return min(dx, dy)
```

Add tests to `tests/test_sweep.py`: (a) confirm every existing test in the file still passes
unchanged (proves the default-preserving refactor); (b) a new test that
`pooled_gradients_for_cell(2, "weight1", "uniform", draw_start=0, draw_count=3, sigma=0.1)`
produces the SAME pooled gradients as the equivalent call with `sigma` omitted (proves the
default really does equal the old hardcoded behavior, not just "close enough"); (c) a new test
that a materially different `sigma` (e.g. `sigma=5.0`) produces DIFFERENT pooled gradients than
`sigma=0.1` at the same n/draws (proves sigma is actually wired to the kernel matrix, not a
no-op parameter). Add a short `tests/test_target_grid.py`-style check (in `test_sweep.py` or
inline) that `target_grid.bin_spacing(2) == pytest.approx(1.2)` and
`target_grid.bin_spacing(6) == pytest.approx(0.1714, abs=1e-3)`, matching 17.1-RESEARCH.md's
directly-computed bin-spacing table.
  </action>
  <verify>`./venv/Scripts/python.exe -m pytest tests/test_sweep.py -v` -- all tests pass, including the new sigma-default-equivalence, sigma-actually-changes-output, and bin_spacing tests.</verify>
  <done>sigma is an optional, backward-compatible parameter on both sweep.py functions; bin_spacing(n) matches the hand-verified table in 17.1-RESEARCH.md.</done>
</task>

<task type="auto">
  <name>Task 2: Wire --sigma through gradient_variance_sweep.py's CLI, CSV output, and chunk filenames</name>
  <files>gradient_variance_sweep.py</files>
  <action>
Add `parser.add_argument("--sigma", type=float, default=0.1, help="MMD kernel bandwidth, held
fixed across the whole n-range for this invocation (default: 0.1, Phase 17's original value).")`
to `parse_args()`.

Add `"sigma"` and `"bin_spacing"` to `FIELDNAMES` (append after the existing columns, so any
script reading the CSV positionally -- there are none currently, `csv.DictReader` is used
everywhere -- stays unaffected). In `run()`, `run_chunk()`, and `combine_chunks()`: after building
each `result` dict, add `result["sigma"] = args.sigma` and
`result["bin_spacing"] = target_grid.bin_spacing(n)` before writing/returning it (import
`from trainability import target_grid` at the top). Thread `sigma=args.sigma` into every
`pooled_gradients_for_cell(...)` and `run_gradient_variance_sweep(...)` call site in this file
(there are three: `run_chunk`, `combine_chunks`'s `n_tracked`-re-derivation call, and `run`).

**Pitfall 2 fix (chunk/combine sigma consistency):** update `_chunk_path` to embed sigma in the
filename: `f"{scope}_n{n}_{init_scheme}_sigma{sigma:g}_{draw_start}-{draw_start + draw_count}.npy"`
(the `_chunk_path` function signature gains a `sigma` parameter; update both call sites --
`run_chunk` and `combine_chunks`'s glob-pattern construction -- to pass `args.sigma`). Update
`combine_chunks`'s glob pattern from `f"{args.scope}_n{n}_{init_scheme}_*.npy"` to
`f"{args.scope}_n{n}_{init_scheme}_sigma{args.sigma:g}_*.npy"` so a `combine-chunks` call at one
sigma can ONLY ever find chunk files written by `run_chunk` calls at that SAME sigma -- a
forgotten or mismatched `--sigma` between chunking and combining now produces a clean
`FileNotFoundError: no chunk files found matching ...` instead of silently mixing gradients
computed under different kernels (17.1-RESEARCH.md Pitfall 2's exact failure mode).

Update the module docstring's usage examples to show `--sigma` in at least one example, and note
in a short comment near `FIELDNAMES` that `sigma`/`bin_spacing` are per-row reporting columns, not
inputs to the summary statistics themselves.
  </action>
  <verify>`./venv/Scripts/python.exe gradient_variance_sweep.py --scope weight1 --n-values 2 3 --init-schemes uniform --sigma 0.3 --n-draws 3 --out results/_tmp_sigma_test.csv` runs cleanly and the output CSV has `sigma=0.3` and a correct `bin_spacing` value in every row; a chunk run at `--sigma 0.3` followed by a `--combine-chunks` call at a DIFFERENT `--sigma` (e.g. `0.1`) raises `FileNotFoundError`, not a silently-wrong combined row. Delete the temp CSV/chunks dir after verifying.</verify>
  <done>--sigma is a working CLI flag threaded consistently through run/run_chunk/combine_chunks; every output row records its own sigma and bin_spacing; mismatched chunk/combine sigma fails loudly, not silently.</done>
</task>

</tasks>

<verification>
1. `./venv/Scripts/python.exe -m pytest tests/test_sweep.py -v` -- all tests pass.
2. `./venv/Scripts/python.exe -m pytest` (full suite) -- no regressions; Phase 17's own already-shipped CSVs are untouched (this plan only adds capability, never re-runs or overwrites `results/phase17_*`).
3. Manual CLI smoke test (Task 2's verify step) confirms sigma/bin_spacing appear correctly in CSV output and chunk/combine sigma-mismatch fails loudly.
</verification>

<success_criteria>
- sigma is a fully optional, default-preserving parameter throughout the pipeline (zero regression risk for Phase 17's existing artifacts).
- Every TRAIN-09 sweep row will explicitly record its own sigma and bin_spacing, satisfying ROADMAP.md's Phase 17.1 success criterion 1 ahead of the actual sweep run (Plan 17.1-04).
- Chunk/combine sigma mismatches are structurally caught, not just documented as a risk.
</success_criteria>

<output>
After completion, create `.planning/phases/17.1-trainability-follow-up-bandwidth-init-sensitivity/17.1-02-SUMMARY.md`
</output>
