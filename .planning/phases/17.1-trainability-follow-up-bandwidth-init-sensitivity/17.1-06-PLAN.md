---
phase: 17.1-trainability-follow-up-bandwidth-init-sensitivity
plan: 06
type: execute
wave: 5
depends_on: ["17.1-05"]
files_modified:
  - docs/trainability-study.md
autonomous: true

must_haves:
  truths:
    - "docs/trainability-study.md gains two new top-level subsections -- bandwidth sensitivity (TRAIN-09) and data-dependent init (TRAIN-10) -- inserted after the existing Cross-reference verdict (TRAIN-07) section and before the existing Independent cross-check section, per 17.1-RESEARCH.md's explicit insertion-point guidance."
    - "Phase 17's original CORE dataset, table, and verdict language are NOT silently replaced or altered -- both new sections are presented explicitly as follow-up checks, honoring this project's honesty-over-narrative convention (matching how the existing Independent cross-check section was itself folded in without altering Phase 17's original verdict language)."
    - "Both new sections state their result honestly in whichever direction the data pointed -- 'the finding holds' is reported exactly as plainly as 'it doesn't', per this phase's own stated goal."
  artifacts:
    - path: "docs/trainability-study.md"
      provides: "## Bandwidth sensitivity follow-up (TRAIN-09) and ## Data-dependent initialization follow-up (TRAIN-10) sections, plus a short addendum to the closing 'What this does/doesn't establish' section"
      min_lines: 40
---

<objective>
Fold Plan 17.1-04's TRAIN-09 sigma-grid results and Plan 17.1-05's TRAIN-10 data-dependent-init results into `docs/trainability-study.md`, following that document's own established structure and voice (methodology/what-changed statement, results table, explicit comparison-against-original table, plain honest either-direction statement) -- the same pattern the document's existing "Independent cross-check" section already set as precedent.

Purpose: closes out Phase 17.1's ROADMAP.md success criterion 5 -- both follow-up results recorded where Phase 20 (Technical Write-Up) will draw on them, without disturbing Phase 17's own already-shipped, already-verified content.
Output: `docs/trainability-study.md`, updated in place.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17.1-trainability-follow-up-bandwidth-init-sensitivity/17.1-RESEARCH.md
@docs/trainability-study.md
@results/phase171_train09_curve_fit_summary.csv
@results/phase171_train10_curve_fit_summary.csv
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the TRAIN-09 and TRAIN-10 subsections into docs/trainability-study.md</name>
  <files>docs/trainability-study.md</files>
  <action>
Insert two new top-level `##` sections into `docs/trainability-study.md`, positioned immediately
AFTER the existing `## Cross-reference verdict (TRAIN-07)` section (including its
`> Owner interpretation: [pending]` blockquote) and BEFORE the existing
`## Independent cross-check: dual-rail encoding + MerLin native autograd` section -- per
17.1-RESEARCH.md's explicit "Where New Subsections Should Go" guidance (right after Phase 17's own
core content ends, before the later supplementary dual-rail work begins).

**`## Bandwidth sensitivity follow-up (TRAIN-09)`:**
1. One-paragraph methodology/what-changed statement: Phase 17's CORE sweep re-run at a fixed sigma
   grid `{0.03, 0.1, 0.3, 1.0, 3.0, 9.0}` (owner-decided, directly modeled on the sibling project's
   own `AC12` bandwidth sweep), each value held constant across the whole n-range, rather than the
   originally-considered per-n sigma schedule -- state why the schedule approach was dropped (it
   requires picking one arbitrary anchor point; the fixed-grid approach avoids that entirely and
   matches this project's own convention of sweeping one fixed hyperparameter across the full
   n-range per run).
2. A results table (same shape as the existing `## Results` table, extended with a `sigma` column)
   covering all 24 `(generator_scope, init_scheme, sigma)` cells from
   `results/phase171_train09_curve_fit_summary.csv` -- or, if that's too dense for one table,
   split into two (weight1, mixed), each with `sigma` as a row per init_scheme.
3. The explicit survives/weakens/disappears comparison for `weight1/uniform` and `mixed/uniform`
   specifically (the two cells with a definite original "exp" verdict), stated plainly: does the
   exponential-decay signature hold across the whole sigma grid, or only near sigma=0.1?
4. State the sigma=0.1 consistency check's outcome (Plan 17.1-04 Task 3) as a one-line footnote --
   this is the built-in sanity check that the new pipeline reproduces the original result exactly
   at the original bandwidth.
5. A plain honesty statement in the same voice as the rest of this document: this result either
   strengthens confidence that the exp-decay signature is a genuine circuit/init property (if it
   survives across the grid) or reveals it was partly/wholly a fixed-bandwidth artifact (if it
   only holds near sigma=0.1) -- state which one actually happened, no hedging.

**`## Data-dependent initialization follow-up (TRAIN-10)`:**
1. One-paragraph methodology statement: Recio-Armengol et al.'s (arXiv:2503.02934, Sec. 8.1.2)
   data-dependent init recipe, translated onto this project's grid-bin target representation
   (weight-1 angles from `p_real`'s per-bit marginals via `arcsin(sqrt(...))`; for the `mixed`
   scope, the weight-2 pair's two qubits use the covariance-based rule instead of their own
   per-qubit value -- state the design decision from Plan 17.1-03 explicitly here too, since a
   reader of this document won't have seen the plan file). State `scale_factor=1.0` and
   `n_draws=1` (both owner-decided, and why `n_draws=1` is valid here -- the init is fully
   deterministic, so more draws would be redundant, not more rigorous).
2. A results table (same shape as the existing `## Results` table) for the 2 new
   `(generator_scope, data_dependent)` cells from `results/phase171_train10_curve_fit_summary.csv`.
3. An explicit comparison table against the original `small_angle` verdict for each scope
   (`inconclusive` -> `?`), stating plainly whether this recipe produced a clearer result and, if
   so, which direction (exp or poly) -- or whether it, too, remained inconclusive.
4. A plain honesty statement: does a literature-sourced init scheme resolve `small_angle`'s
   inconclusive result, or does the inconclusiveness persist regardless of init strategy?

Add a short addendum (not a rewrite) to the existing closing `## What this does/doesn't establish`
section, one or two sentences, noting these two follow-up results exist and pointing to the new
subsections -- matching exactly how the "Independent cross-check" section was itself folded in
without altering Phase 17's original verdict language (see that section's own precedent in this
same document).

Do not alter any existing section's content, numbers, or verdict language above the insertion
point -- this is a strictly additive edit.
  </action>
  <verify>Read back `docs/trainability-study.md`: confirm the two new sections are present, correctly positioned (after Cross-reference verdict, before Independent cross-check), state a concrete honest verdict in each direction (not hedged), and that every existing section's original content is byte-for-byte unchanged above the insertion point (`git diff docs/trainability-study.md` shows only additions, no deletions/modifications to pre-existing lines).</verify>
  <done>docs/trainability-study.md contains both new subsections, correctly positioned, with concrete honest verdicts, and Phase 17's original content is fully preserved.</done>
</task>

</tasks>

<verification>
1. `git diff docs/trainability-study.md` shows only additions (no modified/deleted pre-existing lines).
2. Both new sections are present, correctly positioned, and state concrete verdicts (survives/weakens/disappears for TRAIN-09; clearer/still-inconclusive for TRAIN-10).
3. `./venv/Scripts/python.exe -m pytest` (full suite) -- no regressions (docs-only change).
</verification>

<success_criteria>
- ROADMAP.md Phase 17.1 success criterion 5 is satisfied: both results are folded into docs/trainability-study.md as new subsections, not silently replacing Phase 17's original reported numbers.
- TRAIN-09 and TRAIN-10 are now fully satisfiable end to end from this phase's shipped artifacts, matching Phase 17's own precedent of "all requirements satisfiable from shipped artifacts."
</success_criteria>

<output>
After completion, create `.planning/phases/17.1-trainability-follow-up-bandwidth-init-sensitivity/17.1-06-SUMMARY.md`
</output>
