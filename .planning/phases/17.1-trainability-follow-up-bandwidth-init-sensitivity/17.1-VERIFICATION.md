---
phase: 17.1-trainability-follow-up-bandwidth-init-sensitivity
verified: 2026-08-13T00:00:00Z
status: passed
score: 5/5 must-haves verified
---

# Phase 17.1: Trainability Follow-Up (Bandwidth & Init Sensitivity) Verification Report

**Phase Goal:** Resolve two literature-motivated open questions about Phase 17's already-shipped trainability result -- (1) is the measured exponential gradient-decay signature partly/wholly a fixed-bandwidth artifact, and (2) does a literature-sourced data-dependent init (Recio-Armengol et al.) resolve Phase 17's inconclusive small_angle result -- using the existing sweep infrastructure, both measured and reported honestly either direction.

**Verified:** 2026-08-13
**Status:** passed
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | TRAIN-09's fixed-sigma-grid sweep produces a real, complete dataset at Phase 17's CORE n-range, both init schemes, all 6 sigma values, with sigma/bin_spacing recorded per row | VERIFIED | results/phase171_train09_weight1_gradient_variance.csv has exactly 60 data rows (10 per sigma x 6 sigma), results/phase171_train09_mixed_gradient_variance.csv has exactly 48 (8 per sigma x 6 sigma). All var values finite (0 NaN/invalid found by direct scan). sigma and bin_spacing columns present and populated in every row. |
| 2 | TRAIN-09's poly-vs-exp verdict machinery (fit_and_compare, unmodified) is applied at every sigma value with an explicit survives/weakens/disappears statement for weight1/uniform and mixed/uniform | VERIFIED | results/phase171_train09_curve_fit_summary.csv has 24 rows (2 scopes x 2 init x 6 sigma), each with a survival_vs_baseline column populated for uniform rows (survives/disappears, no blanks/hedges). docs/trainability-study.md's TRAIN-09 section states the finding plainly: original exp verdict survives only near sigma in 0.03/0.1, disappears at 0.3/1.0, and non-monotonically re-emerges for weight1 (not mixed) at 3.0/9.0 -- reported as not robust, no hedging. |
| 3 | TRAIN-10's data-dependent init recipe (Recio-Armengol et al., weight-1 arcsin(sqrt(mean)), weight-2 covariance-proportional, scale_factor=1.0) is implemented as a new init scheme alongside small_angle/uniform | VERIFIED | trainability/data_dependent_init.py implements weight1_data_dependent_theta/weight2_data_dependent_theta exactly per the paper's formulas, hand-verified by 6 worked-example/edge-case tests (all pass). trainability/sweep.py's pooled_gradients_for_cell dispatches init_scheme=data_dependent, with a dedicated test proving the mixed-scope weight-2-pair override actually changes the gradients computed (not merely present). gradient_variance_sweep.py's --init-schemes CLI accepts data_dependent, --scale-factor is threaded through. |
| 4 | TRAIN-10's sweep is re-run under data_dependent at n_draws=1, with an explicit statement of whether it produces a clearer (non-inconclusive) verdict than small_angle | VERIFIED | results/phase171_train10_weight1_gradient_variance.csv (5 rows) and mixed variant (4 rows) exist, n_samples equals n_tracked_params confirming n_draws=1 was honored (not silently multiplied). results/phase171_train10_curve_fit_summary.csv (2 rows) has a vs_small_angle_verdict column, both reading "still inconclusive". docs/trainability-study.md's TRAIN-10 section states plainly: a genuine negative result. |
| 5 | Both results are folded into docs/trainability-study.md as new subsections without silently altering Phase 17's original reported numbers | VERIFIED | Bandwidth sensitivity follow-up (TRAIN-09) and Data-dependent initialization follow-up (TRAIN-10) sections exist, correctly positioned between Cross-reference verdict (TRAIN-07) and Independent cross-check. git diff --stat across this phase's full commit range on this file shows 175 insertions, 0 deletions -- strictly additive. A short addendum was also added to the closing What this does/doesn't establish section. |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| trainability/data_dependent_init.py | 5 pure-numpy functions per Recio-Armengol recipe, bit-ordering-verified | VERIFIED | 71 lines, all 5 functions present (bit_k, empirical_mean_bit, weight1_data_dependent_theta, empirical_pm1_covariance, weight2_data_dependent_theta), no stubs/TODOs, no cross-imports beyond numpy per convention. |
| tests/test_data_dependent_init.py | TDD tests incl. Pitfall-3 bit-ordering spot-check + worked examples | VERIFIED | 95 lines, 6 tests, all pass. Bit-ordering test parametrized across n in 2,3,4 x every qubit position (not just endpoints). |
| trainability/sweep.py | sigma/scale_factor explicit kwargs, data_dependent dispatch | VERIFIED | 224 lines. Both params threaded with backward-compatible defaults (sigma=SIGMA, scale_factor=1.0). data_dependent branch computes fixed_thetas once per cell (outside draw loop), mixed-scope override implemented exactly as documented. |
| trainability/target_grid.py bin_spacing | bin_spacing(n, lo, hi) helper | VERIFIED | Implemented exactly per plan; bin_spacing(2)==1.2, bin_spacing(6) approx 0.1714 confirmed by passing test. |
| gradient_variance_sweep.py | --sigma, --scale-factor, data_dependent CLI choice, sigma in chunk filenames | VERIFIED | All present: --sigma (default 0.1), --scale-factor (default 1.0), data_dependent in --init-schemes choices, sigma/bin_spacing in FIELDNAMES, _chunk_path embeds sigma value in filename, combine_chunks glob pattern includes sigma (mismatch causes loud FileNotFoundError, not silent mixing). |
| results/phase171_train09_weight1_gradient_variance.csv | 60 data rows | VERIFIED | Exactly 60 rows, 6 distinct sigma values x 10 rows each, all var finite. |
| results/phase171_train09_mixed_gradient_variance.csv | 48 data rows | VERIFIED | Exactly 48 rows, 6 distinct sigma values x 8 rows each, all var finite. |
| results/phase171_train10_weight1_gradient_variance.csv | 5 rows | VERIFIED | Exactly 5 rows (n=2..6), n_samples equals n_tracked_params confirms n_draws=1. |
| results/phase171_train10_mixed_gradient_variance.csv | 4 rows | VERIFIED | Exactly 4 rows (n=2..5), same n_draws=1 confirmation. |
| trainability_analysis_1701.py | New, separate script reusing fit_and_compare unchanged | VERIFIED | 360 lines. Imports fit_and_compare directly from trainability.curve_fit (unmodified); does not import from or touch trainability_analysis.py. Produces both summary CSVs and both plots. |
| results/phase171_train09_curve_fit_summary.csv | 24 rows | VERIFIED | Exactly 24 rows (2 scopes x 2 init x 6 sigma). |
| results/phase171_train10_curve_fit_summary.csv | 2 rows | VERIFIED | Exactly 2 rows (weight1, mixed). |
| results/phase171_train09_weight1_sigma_sensitivity.png and mixed variant | Sigma-sensitivity plots | VERIFIED | Both PNG files exist (32KB, 41KB), non-empty. |
| docs/trainability-study.md new sections | TRAIN-09/TRAIN-10 subsections plus addendum | VERIFIED | Both sections present, correctly positioned, concrete honest verdicts stated, 175 insertions / 0 deletions across the phase. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| data_dependent_init.py bit_k | target_grid.py bin_index_fn convention | Inverse-function relationship | WIRED | Dedicated parametrized test proves bit_k is int(bitstring,2)'s exact inverse for every qubit position n=2,3,4. |
| gradient_variance_sweep.py | trainability/sweep.py run_gradient_variance_sweep and pooled_gradients_for_cell | sigma=args.sigma, scale_factor=args.scale_factor | WIRED | Confirmed threaded through run(), run_chunk(), combine_chunks() -- all 3 call sites. |
| gradient_variance_sweep.py _chunk_path | combine_chunks's glob pattern | sigma embedded in filename | WIRED | _chunk_path includes the sigma value; combine_chunks's glob pattern includes it too -- mismatch produces FileNotFoundError, not silent mixing. |
| trainability/sweep.py pooled_gradients_for_cell | data_dependent_init weight1/weight2 functions | Direct call, once per cell | WIRED | Confirmed in source; confirmed by test_pooled_gradients_mixed_data_dependent_overrides_pair_indices, which independently recomputes expected gradients with/without the override and asserts the actual sweep output matches only the with-override version -- the strongest possible proof this isn't a no-op wiring. |
| trainability_analysis_1701.py | trainability.curve_fit.fit_and_compare | Direct call per cell | WIRED | Confirmed via source read; called once per (scope, init_scheme, sigma) for TRAIN-09 (24x) and once per scope for TRAIN-10 (2x). |
| trainability_analysis_1701.py | results/phase17_curve_fit_summary.csv | Loaded as survival-classification baseline | WIRED | load_phase17_baseline() reads this file directly; used only for uniform cells' survival classification, consistent with the plan's stated scope. |

### Requirements Coverage

| Requirement | Status | Blocking Issue |
|---|---|---|
| TRAIN-09 | SATISFIED | Real 6-sigma-grid dataset produced, poly-vs-exp verdicts computed and compared, honest non-robust finding reported. |
| TRAIN-10 | SATISFIED | Real data-dependent-init dataset produced, verdicts compared against small_angle, honest negative-result finding reported. |

Note: .planning/REQUIREMENTS.md's traceability checkboxes for TRAIN-09/TRAIN-10 are still marked Pending as of this verification -- this is expected to be updated by the orchestrator as part of phase closure, not a gap in this phase's own shipped artifacts.

### Anti-Patterns Found

None blocking. No TODO/FIXME/placeholder patterns found in any of the phase's new source files (trainability/data_dependent_init.py, tests/test_data_dependent_init.py, trainability_analysis_1701.py, the modified trainability/sweep.py, target_grid.py, gradient_variance_sweep.py).

One honestly-documented, non-blocking data discrepancy (informational, not an anti-pattern in the code): Plan 17.1-04's Task 3 consistency check (sigma=0.1 subset should reproduce Phase 17's original CORE CSVs bit-for-bit) passes exactly for weight1 (10/10 rows) and for mixed/uniform (2/2 rows), but shows a roughly 1e-13 to 1e-16 relative-magnitude floating-point difference for mixed/small_angle at n=2,3,4 (6 of 18 total compared rows) -- diagnosed in 17.1-04-SUMMARY.md as a deterministic environment/floating-point-summation-order difference (re-running the affected cell reproduced this pipeline's own value bit-for-bit; weight1's identical sigma-threading code shows zero drift), not a sigma-threading logic bug. Independently confirmed here: this discrepancy only affects small_angle cells, which are never used in the survives/weakens/disappears classification (only uniform cells are, and those matched exactly) -- so it has zero effect on this phase's headline TRAIN-09 finding. Both the SUMMARY and docs/trainability-study.md report this openly rather than hiding it, consistent with the phase's own "measured and reported honestly either direction" goal.

### Human Verification Required

None. This phase's outputs are entirely numerical/data-analysis artifacts (CSVs, a script reusing already-verified statistical machinery, and a documentation fold-in) -- all claims are mechanically checkable against the CSVs and source code, which was done above. Per this project's CLAUDE.md, final interpretation of the measured results (e.g., whether the sigma-sensitivity finding changes the owner's broader trainability narrative) is explicitly the owner's job, not something a verifier certifies -- but that is a downstream interpretive step, not a gap in whether the phase's deliverables exist and are correct.

### Gaps Summary

No gaps. All 5 observable truths verified, all 14 required artifacts exist/substantive/wired, all key links confirmed wired (including the one link most likely to hide a stub -- the mixed-scope data-dependent override -- proven by a test that independently recomputes expected gradients rather than merely checking the code path executes). Full test suite (234 tests) passes with no regressions. trainability_analysis.py (Phase 17's canonical script) confirmed unmodified via git diff --stat across the phase's full commit range. docs/trainability-study.md's original content confirmed byte-for-byte preserved (175 insertions, 0 deletions). Both TRAIN-09 and TRAIN-10 questions were answered with concrete, non-hedged verdicts in the direction the data actually pointed (TRAIN-09: original exp signature is NOT robust to bandwidth changes; TRAIN-10: data-dependent init did NOT resolve the inconclusive verdict) -- exactly the honest-either-direction goal this phase was scoped around.

---

*Verified: 2026-08-13*
*Verifier: Claude (gsd-verifier)*
