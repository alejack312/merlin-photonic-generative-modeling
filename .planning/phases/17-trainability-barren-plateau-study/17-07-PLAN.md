---
phase: 17-trainability-barren-plateau-study
plan: 07
type: execute
wave: 4
depends_on: ["17-06", "17-04"]
files_modified:
  - trainability_analysis.py
  - results/phase17_weight1_curve_fit.png
  - results/phase17_mixed_curve_fit.png
  - results/phase17_curve_fit_summary.csv
  - docs/trainability-study.md
autonomous: true

must_haves:
  truths:
    - "Plan 17-04's poly-vs-exponential model comparison (curve fit + R^2 + AIC) has been run against the real weight-1 and mixed gradient-variance-vs-n data from Plan 17-06, with results plotted, not eyeballed (TRAIN-02)."
    - "docs/trainability-study.md states, explicitly and separately: the parameter-init distributions and per-circuit normalization convention used (TRAIN-03), the generator-scope choice and reasoning (TRAIN-04), the honest max-n reached and its relationship to both docs/iqp-baseline.md's n>=6 threshold and the N=20-24 literature fit-flip threshold (TRAIN-05, TRAIN-08), and the cross-reference verdict against docs/iqp-baseline.md's empirical plateau rule (TRAIN-07)."
    - "The cross-reference against docs/iqp-baseline.md's rule (plateau if small_angle or (uniform and n>=6 and not complete_graph_like)) is applied concretely to this project's own measured data and its agreement/disagreement is stated plainly, in the same direct honest-negative-result language this project used for GEN-07/Phase 7's neighbor-locality verdict."
  artifacts:
    - path: "trainability_analysis.py"
      provides: "Root-level script: loads both gradient-variance CSVs, runs trainability.curve_fit.fit_and_compare, plots, applies the iqp-baseline.md rule, writes results/phase17_curve_fit_summary.csv"
      min_lines: 30
    - path: "docs/trainability-study.md"
      provides: "The phase's canonical write-up: methodology, generator scope + init/normalization statement, curve-fit results, honest max-n statement, cross-reference verdict"
      min_lines: 60
    - path: "results/phase17_curve_fit_summary.csv"
      provides: "Per (generator_scope, init_scheme): exp/poly params, r2, aic, verdict"
  key_links:
    - from: "trainability_analysis.py"
      to: "trainability.curve_fit.fit_and_compare"
      via: "direct call per (generator_scope, init_scheme) on the n-vs-variance series loaded from Plan 17-06's CSVs"
      pattern: "fit_and_compare\\("
---

<objective>
Close out Phase 17: apply Plan 17-04's curve-fit machinery to the real dataset Plan 17-06 produced, cross-reference the result against `docs/iqp-baseline.md`'s qubit-side empirical plateau rule (TRAIN-07), and write the phase's canonical results document stating everything TRAIN-03/04/05/07/08 require to be stated explicitly -- honestly, in whichever direction the data actually points.

Purpose: this is where Phase 17's raw numbers become the actual measured claim the phase exists to produce; Phase 20 (Technical Write-Up) will draw on this document rather than re-deriving it.
Output: `trainability_analysis.py`, two curve-fit plots, a summary CSV, and `docs/trainability-study.md` -- the phase's permanent reference document, matching `docs/iqp-baseline.md`/`docs/mmd-loss.md`'s existing documentation convention.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-trainability-barren-plateau-study/17-CONTEXT.md
@.planning/phases/17-trainability-barren-plateau-study/17-RESEARCH.md
@trainability/curve_fit.py
@docs/iqp-baseline.md
@docs/iqp-photonic-encoding.md
@results/phase17_weight1_gradient_variance.csv
@results/phase17_mixed_gradient_variance.csv
</context>

<tasks>

<task type="auto">
  <name>Task 1: Curve-fit analysis, cross-reference, and plots</name>
  <files>trainability_analysis.py, results/phase17_weight1_curve_fit.png, results/phase17_mixed_curve_fit.png, results/phase17_curve_fit_summary.csv</files>
  <action>
Create `trainability_analysis.py` at the repo root. For each `generator_scope in {"weight1",
"mixed"}` and each `init_scheme in {"small_angle", "uniform"}`:

1. Load the corresponding rows from `results/phase17_weight1_gradient_variance.csv` /
   `results/phase17_mixed_gradient_variance.csv` (and the stretch CSVs too, IF they exist and
   completed -- check with `os.path.exists` and merge in any additional n-values found there,
   since more data points only strengthen the curve fit; do not fail if they're missing/partial,
   per Plan 17-06's explicitly-authorized outcome).
2. Extract the `(n, var)` series (use `var`, not `mean` -- barren-plateau claims are about
   gradient VARIANCE, per `docs/iqp-baseline.md`'s formal definition
   `Var_theta~D[d_theta L] = O(2^{-alpha*n})`).
3. Call `trainability.curve_fit.fit_and_compare(ns, variances)`.
4. Apply `docs/iqp-baseline.md`'s empirical rule directly to this project's own setting: `plateau
   if init_scheme == "small_angle" or (init_scheme == "uniform" and max(ns) >= 6)` (this project's
   circuits are not naturally "complete-graph-like" in the qubit-side sense that rule's
   `not complete_graph_like` clause refers to -- state this mapping-ambiguity explicitly in the
   write-up rather than silently assuming it doesn't matter) -- compare this RULE-PREDICTED
   plateau/no-plateau verdict against what the actual `fit_and_compare` verdict says (does the
   variance data show exponential decay, i.e. a shrinking-with-n signature consistent with a
   plateau, or not).
5. Save all four `(generator_scope, init_scheme)` results as rows in
   `results/phase17_curve_fit_summary.csv` (columns: generator_scope, init_scheme, n_min, n_max,
   n_points, exp_params, exp_r2, exp_aic, poly_params, poly_r2, poly_aic, verdict,
   baseline_rule_prediction, agrees_with_baseline_rule).
6. Plot two PNGs (`results/phase17_weight1_curve_fit.png`, `results/phase17_mixed_curve_fit.png`,
   `matplotlib.use("Agg")` before importing pyplot, per `cp_alpha_sweep.py`'s established
   convention): each figure has 2 subplots (one per init_scheme), y-axis log-scale (variance),
   x-axis = n, scatter of the real `(n, var)` points plus BOTH fitted curves (exp and poly)
   overlaid as smooth lines across the plotted n range, legend distinguishing them, title stating
   the winning verdict.

Run `./venv/Scripts/python.exe trainability_analysis.py` and confirm it completes and all four
output artifacts are produced before moving to Task 2.
  </action>
  <verify>`results/phase17_curve_fit_summary.csv` has exactly 4 data rows (2 generator_scope x 2 init_scheme) plus header; both PNGs exist and are non-empty.</verify>
  <done>trainability_analysis.py runs cleanly, producing a complete curve-fit summary CSV and two plots covering all four (generator_scope, init_scheme) combinations.</done>
</task>

<task type="auto">
  <name>Task 2: Write docs/trainability-study.md</name>
  <files>docs/trainability-study.md</files>
  <action>
Create `docs/trainability-study.md`, matching `docs/iqp-baseline.md`/`docs/iqp-photonic-encoding.md`'s
existing terse, load-bearing-only documentation style (no textbook padding, per this milestone's
explicit REQUIREMENTS.md "Out of Scope" entry on generic QML background padding). Required
sections:

1. **Methodology** (stated before results, per WRITE-01's precedent this phase should already
   follow even though WRITE-01 itself is Phase 20's requirement): exact parameter-shift
   (shift=pi/4, no division) on `photonic_iqp_distribution`/`photonic_weight2_iqp_distribution`,
   NOT MerLin `QuantumLayer` autograd (state why, one line, citing the polarization-annotation
   rejection already documented in STATE.md); MMD² between the circuit's output and v1.0's
   K=2^n-generalized target distribution (cite Plan 17-03's design, and explicitly state the
   deliberate choice to skip a Monte-Carlo fallback since exact enumeration suffices across this
   project's entire reachable n range).
2. **Parameter-initialization and normalization (TRAIN-03)**: state the exact `small_angle`
   (`Uniform(-0.1,0.1)`) and `uniform` (`Uniform(0,2*pi)`) sampling used, and state the
   normalization convention plainly: this circuit's gates are all passive/unitary (phase shifters
   and beamsplitters), so total photon number is conserved by construction with no separate
   energy-normalization parameter -- `theta` is a bare phase-gate angle in radians.
3. **Generator scope (TRAIN-04)**: state the weight-1-only vs. mixed (weight-1 on all n qubits
   plus one fixed weight-2 Z_i*Z_j pair at (i,j)=(0,1), reusing Phase 13's validated
   `photonic_weight2_iqp_distribution` composability) choice and why (direct reuse of the only
   already-shipped, already-validated multi-generator composition this project has -- no new
   circuit topology invented for this study).
4. **Results**: embed both PNGs, present `results/phase17_curve_fit_summary.csv`'s content as a
   markdown table (generator_scope, init_scheme, n range, winning model, R², AIC).
5. **Honest max-n statement (TRAIN-05, TRAIN-08)**: state the actual n range reached for each
   generator scope (from Task 1's data, including whatever the stretch attempts did or didn't
   achieve per Plan 17-06's SUMMARY), explicitly relative to (a) `docs/iqp-baseline.md`'s n>=6
   qubit-baseline threshold -- reached for weight-1, and (b) the N=20-24 literature fit-flip
   threshold (arXiv:2605.11879) -- NOT reached, state the measured/extrapolated compute cost from
   17-RESEARCH.md as the reason why (this repo's photonic Fock-space output scales as
   `C(3n-1,n) ~ 6.75^n`, not `2^n`), in the same direct, un-hedged language this project used for
   GEN-07/Phase 7's honest negative results -- do not soften this into "future work" framing.
6. **Cross-reference verdict (TRAIN-07)**: state plainly whether Task 1's `fit_and_compare`
   verdict agrees or disagrees with `docs/iqp-baseline.md`'s empirical rule's prediction, for each
   of the 4 (generator_scope, init_scheme) cells, and give the owner's OWN interpretation a
   labeled placeholder to fill in (`> Owner interpretation: [pending]`) -- matching this project's
   established self-explanation-checkpoint convention (per this repo's CLAUDE.md and Phase
   7/GEN-07's "measured, not interpreted" precedent) rather than asserting a final conclusion on
   the owner's behalf.
7. **What this does/doesn't establish**: one explicit paragraph (WRITE-04's precedent, reasonable
   to include now even though WRITE-04 itself belongs to Phase 20) -- this is an empirical
   measurement at this project's own small, compute-bound n range, not a proof of any asymptotic
   trainability property, and does not by itself establish that the photonic realization
   inherits (or fails to inherit) the qubit-side rule's behavior at hardware-relevant scale.
  </action>
  <verify>Read back `docs/trainability-study.md`: confirm all 7 sections are present, both PNGs are correctly referenced, the summary CSV's content is represented as a table, and the honest max-n/cross-reference sections state a concrete verdict rather than hedging.</verify>
  <done>docs/trainability-study.md exists, is complete per the 7 required sections, and accurately reflects Task 1's real computed results.</done>
</task>

</tasks>

<verification>
1. `results/phase17_curve_fit_summary.csv` has 4 rows covering all (generator_scope, init_scheme) combinations.
2. Both curve-fit PNGs exist.
3. `docs/trainability-study.md` contains all 7 required sections with concrete, non-hedged content.
4. `./venv/Scripts/python.exe -m pytest` (full suite) -- no regressions.
</verification>

<success_criteria>
- TRAIN-01 through TRAIN-08 are all satisfiable end to end: real gradient-variance data (TRAIN-01), curve fit + R²/AIC (TRAIN-02), init/normalization stated (TRAIN-03), generator scope stated with reasoning (TRAIN-04), honest max-n statement (TRAIN-05), mixed sweep (TRAIN-06), cross-reference against docs/iqp-baseline.md (TRAIN-07), honest stretch-toward-N=20-24 reporting (TRAIN-08).
- `docs/trainability-study.md` is a complete, honest, self-contained reference for Phase 20's later write-up.
</success_criteria>

<output>
After completion, create `.planning/phases/17-trainability-barren-plateau-study/17-07-SUMMARY.md`
</output>
