---
phase: 17-trainability-barren-plateau-study
plan: 06
type: execute
wave: 3
depends_on: ["17-05"]
files_modified:
  - gradient_variance_sweep.py
  - results/phase17_weight1_gradient_variance.csv
  - results/phase17_mixed_gradient_variance.csv
autonomous: true

must_haves:
  truths:
    - "A real gradient-variance-vs-n dataset exists for the weight-1-only generator across >=3 system sizes (n=2..6, 5 sizes) and >=100 independent random parameter draws each, for BOTH the small-angle and uniform initialization regimes (TRAIN-01, TRAIN-03)."
    - "A real gradient-variance-vs-n dataset exists for the mixed weight-1+weight-2 generator (reusing Phase 13's validated composability, TRAIN-06) across >=3 system sizes (n=2..5, 4 sizes), both init regimes (TRAIN-04)."
    - "An honest, best-effort, no-time-box attempt is made at the compute-bound stretch sizes (n=7 weight-1, n=6 mixed) toward TRAIN-08's N~20-24 target, run across sessions rather than abandoned within a short window, with the outcome (completed, still running as of this plan's completion, or genuinely stopped later at/after the 2026-08-20 mid-milestone checkpoint) recorded plainly rather than silently omitted."
  artifacts:
    - path: "gradient_variance_sweep.py"
      provides: "Root-level CLI script wrapping trainability.sweep.run_gradient_variance_sweep, writes CSV output"
      min_lines: 40
    - path: "results/phase17_weight1_gradient_variance.csv"
      provides: "Columns: n, generator_scope, init_scheme, n_tracked_params, n_samples, mean, var, std, median, abs_mean, rms -- weight-1-only, n=2..6, both init schemes"
    - path: "results/phase17_mixed_gradient_variance.csv"
      provides: "Same columns, mixed weight-1+weight-2, n=2..5, both init schemes"
  key_links:
    - from: "gradient_variance_sweep.py"
      to: "trainability.sweep.run_gradient_variance_sweep"
      via: "direct call per (generator_scope, init_scheme) combination, results appended to a CSV"
      pattern: "run_gradient_variance_sweep\\("
---

<objective>
Run the actual gradient-variance sweep and produce the real dataset this phase's central measured claim depends on -- the compute-bound step 17-RESEARCH.md's benchmarks and cost model exist specifically to plan around. CONTEXT.md's locked decision: push toward TRAIN-08's N~20-24 target until compute genuinely can't go further, no hard time-box, with the ~2026-08-20 mid-milestone checkpoint as the accepted safety net -- this plan operationalizes that by running a comfortably-affordable CORE range synchronously (the part that must complete for the phase to succeed) and a separate, explicitly best-effort STRETCH attempt toward n=7 (weight-1) / n=6 (mixed) that runs in the background across sessions for as long as it takes, without an artificial short-window cutoff, and without blocking anything else in the phase.

Purpose: produces the raw numbers Plan 17-07 will curve-fit and cross-reference against `docs/iqp-baseline.md`.
Output: `gradient_variance_sweep.py` (root-level script, matching this repo's `cp_alpha_sweep.py`/`sigma_resweep.py` naming convention) and two core-range CSVs in `results/`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-trainability-barren-plateau-study/17-CONTEXT.md
@.planning/phases/17-trainability-barren-plateau-study/17-RESEARCH.md
@trainability/sweep.py
@cp_alpha_sweep.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write gradient_variance_sweep.py and run the weight-1-only core sweep</name>
  <files>gradient_variance_sweep.py, results/phase17_weight1_gradient_variance.csv</files>
  <action>
Create `gradient_variance_sweep.py` at the repo root (matching `cp_alpha_sweep.py`'s existing
root-level script convention). CLI shape (using `argparse`, so this same script also serves the
stretch runs in Task 2 and any later re-runs without editing code):

```
python gradient_variance_sweep.py --scope weight1 --n-values 2 3 4 5 6 --init-schemes small_angle uniform --out results/phase17_weight1_gradient_variance.csv
```

Behavior: for each `init_scheme` in `--init-schemes`, call
`trainability.sweep.run_gradient_variance_sweep(n_values=<parsed --n-values>,
generator_scope=<--scope>, init_scheme=init_scheme, n_draws=100, max_tracked_params=3,
weight2_pair=(0,1), seed_base=170917)`, collect all resulting per-n dicts across both init
schemes into one list, and write them to `--out` via `csv.DictWriter` (one row per (n,
init_scheme) combination, full-overwrite write -- matches `cp_alpha_sweep.py`'s single-pass
convention, no resumable-merge complexity needed here). Print per-row progress to stdout as each
`(n, init_scheme)` cell completes (e.g. `f"n={n} init={init_scheme}: mean={mean:.3e}
var={var:.3e} ({elapsed:.1f}s)"`), including a wall-clock timer per cell -- this sweep's cells
range from seconds to tens of minutes (17-RESEARCH.md's measured costs), and visible per-cell
progress is how a human (or a later background-job check) can tell it's alive and how far along
it is, not just a black box.

Run the CORE weight-1 sweep now (synchronously, as part of this task):
```
./venv/Scripts/python.exe gradient_variance_sweep.py --scope weight1 --n-values 2 3 4 5 6 --init-schemes small_angle uniform --out results/phase17_weight1_gradient_variance.csv
```
Expected wall-clock: roughly 30-40 minutes total (17-RESEARCH.md's measured per-eval costs at
n<=6, capped at 3 tracked params x 100 draws x 2 shifts x 2 init schemes). This is expected and
acceptable -- do not shorten the draw count or n range to make it faster; if it is taking
dramatically longer than this estimate (e.g. >2 hours), stop and report the discrepancy rather
than silently waiting indefinitely, since that would indicate a real bug (e.g. accidentally
tracking all n parameters instead of the capped 3) rather than expected compute cost.
  </action>
  <verify>`results/phase17_weight1_gradient_variance.csv` exists with 10 data rows (5 n-values x 2 init schemes) plus header; every row has a finite, non-NaN `var` value.</verify>
  <done>gradient_variance_sweep.py runs cleanly for scope=weight1 across n=2..6 and both init schemes, producing a complete CSV.</done>
</task>

<task type="auto">
  <name>Task 2: Run the mixed weight-1+weight-2 core sweep, then attempt both stretch sizes best-effort with no time-box</name>
  <files>results/phase17_mixed_gradient_variance.csv</files>
  <action>
Run the CORE mixed sweep using the same script:
```
./venv/Scripts/python.exe gradient_variance_sweep.py --scope mixed --n-values 2 3 4 5 --init-schemes small_angle uniform --out results/phase17_mixed_gradient_variance.csv
```
Expected wall-clock: roughly 1.5-2 hours total (weight-2's `photonic_weight2_iqp_distribution`
is far more expensive per call than weight-1's, per 17-RESEARCH.md's measured table -- n=5
dominates at ~49 minutes per init scheme). This is expected; do not reduce scope to make it
faster.

After the core mixed sweep completes and is saved, ATTEMPT the two stretch points CONTEXT.md
authorizes pushing toward (n=7 weight-1, n=6 mixed) as an explicitly best-effort step -- not a
blocking requirement for this plan's own done-criteria, but also NOT subject to any short-window
cutoff:

```
./venv/Scripts/python.exe gradient_variance_sweep.py --scope weight1 --n-values 7 --init-schemes small_angle uniform --out results/phase17_weight1_gradient_variance_stretch.csv
./venv/Scripts/python.exe gradient_variance_sweep.py --scope mixed --n-values 6 --init-schemes small_angle uniform --out results/phase17_mixed_gradient_variance_stretch.csv
```

Per 17-RESEARCH.md's extrapolated costs, these may take multiple hours (n=7 weight-1: ~6 hours;
n=6 mixed: ~24 hours) -- run each with `run_in_background` (or equivalent) and check back at
natural session boundaries (start/end of a work session, or after other Phase 17 tasks complete)
rather than blocking the rest of Phase 17 on them. Per CONTEXT.md's locked decision, there is
**no time-box** on this push, and the ~2026-08-20 mid-milestone checkpoint is the accepted safety
net for this risk -- do NOT stop a stretch run early because it has been running for "a few
hours" or any other short session-length window; let it keep running in the background across
sessions. The point to actually reassess whether to keep pushing or stop is at/after the
2026-08-20 checkpoint, and even then that call belongs to the owner, not an automatic cutoff
baked into this plan.

Whatever the state of these stretch runs when this plan needs to wrap up (complete, still
running in the background, or -- only if genuinely stopped later at/after the checkpoint --
abandoned with a stated reason), record the exact status plainly in this plan's SUMMARY.md --
this is the honest-reporting outcome TRAIN-05/TRAIN-08 explicitly call for, not a failure of this
plan. Do NOT let an in-progress, not-yet-complete stretch run block Plan 17-07's completion: Plan
17-07 can proceed with whatever core-range data exists and pick up stretch results later if/when
they land (its own loading step already checks for the stretch CSVs and merges them in if
present). A partial or still-running stretch CSV at the time Plan 17-07 runs is an acceptable,
expected, and explicitly authorized outcome (CONTEXT.md's owner-approved risk), not a reason to
abandon it.
  </action>
  <verify>`results/phase17_mixed_gradient_variance.csv` exists with 8 data rows (4 n-values x 2 init schemes) plus header, all `var` values finite. Stretch CSVs may or may not exist/be complete at the time this task wraps up -- their state (done / still running in background / genuinely stopped-with-reason at or after the checkpoint) is recorded in this plan's SUMMARY.md, not silently unmentioned either way.</verify>
  <done>The mixed core sweep (n=2..5, both init schemes) is complete and saved. The n=7 weight-1 / n=6 mixed stretch attempts were made in good faith with no artificial time-box, and their outcome (complete, still running in the background, or genuinely stopped later at/after the 2026-08-20 checkpoint with reason recorded) is explicitly documented.</done>
</task>

</tasks>

<verification>
1. `results/phase17_weight1_gradient_variance.csv` has 10 rows (n=2..6 x 2 init schemes), all `var` finite.
2. `results/phase17_mixed_gradient_variance.csv` has 8 rows (n=2..5 x 2 init schemes), all `var` finite.
3. Stretch attempt outcome (n=7 weight-1, n=6 mixed) is explicitly documented in the plan's SUMMARY, whatever it turned out to be -- including "still running in the background" as a valid, non-final outcome.
</verification>

<success_criteria>
- Real gradient-variance data exists for weight-1-only (n=2..6) and mixed (n=2..5), both init schemes, satisfying TRAIN-01/TRAIN-03/TRAIN-04/TRAIN-06's floor requirements.
- The compute-bound stretch toward TRAIN-08's N~20-24 target was genuinely attempted with no time-box and its outcome honestly recorded, without blocking the phase on it and without abandoning it before the 2026-08-20 mid-milestone checkpoint.
</success_criteria>

<output>
After completion, create `.planning/phases/17-trainability-barren-plateau-study/17-06-SUMMARY.md`
</output>
