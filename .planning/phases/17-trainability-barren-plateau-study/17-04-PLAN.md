---
phase: 17-trainability-barren-plateau-study
plan: 04
type: tdd
wave: 1
depends_on: []
files_modified:
  - trainability/curve_fit.py
  - tests/test_curve_fit.py
autonomous: true

must_haves:
  truths:
    - "Given synthetic gradient-variance-vs-n data generated from a known exponential decay model (plus small noise), the fit routine recovers parameters close to the true ones and its model-comparison verdict correctly identifies 'exponential' as the better-fitting model."
    - "Given synthetic data generated from a known polynomial/power-law decay model (plus small noise), the same routine's verdict correctly identifies 'polynomial' as the better-fitting model."
    - "Both R^2 and AIC are computed and reported for each candidate model -- not just an eyeballed plot, satisfying TRAIN-02's explicit ask."
  artifacts:
    - path: "trainability/curve_fit.py"
      provides: "exp_model, poly_model, aic(residuals, k_params), fit_and_compare(ns, variances) -> dict with per-model params/r2/aic and a verdict"
      min_lines: 30
    - path: "tests/test_curve_fit.py"
      provides: "TDD tests: exponential-data recovers exp verdict, polynomial-data recovers poly verdict, R^2/AIC both present for both models"
      min_lines: 30
  key_links:
    - from: "trainability/curve_fit.py"
      to: "scipy.optimize.curve_fit"
      via: "direct call, exp_model and poly_model as the two candidate fit functions"
      pattern: "curve_fit\\("
---

<objective>
Build the poly-vs-exponential model-comparison routine TRAIN-02 requires: "explicit poly-vs-exponential model comparison (curve fit + goodness-of-fit, e.g. R²/AIC) — not an eyeballed plot." Extends the sibling project's log-linear R²-only pattern (`iqp_bp/experiments/scaling_summary.py::_fit_scaling_groups`) with `scipy.optimize.curve_fit` plus AIC, per 17-RESEARCH.md's explicit recommendation (the sibling's version doesn't compute AIC and TRAIN-02 asks for it as an option).

Purpose: this is the analysis routine Plan 17-07 will run against the real gradient-variance-vs-n data Plan 17-06 produces -- built and proven correct here, against synthetic data with a KNOWN ground-truth model, so any later "which model wins" verdict on real data is backed by a routine already shown to recover the right answer when the answer is known.
Output: `trainability/curve_fit.py`, with a passing TDD test suite proving the routine correctly distinguishes exponential from polynomial decay on synthetic data.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-trainability-barren-plateau-study/17-CONTEXT.md
@.planning/phases/17-trainability-barren-plateau-study/17-RESEARCH.md
</context>

<feature>
  <name>Poly-vs-exponential curve fit with R^2 and AIC model comparison</name>
  <files>trainability/curve_fit.py, tests/test_curve_fit.py</files>
  <behavior>
    Given a sequence of system sizes `ns` and corresponding gradient-variance values `variances`,
    fit two competing models -- `exp_model(n,a,b,c) = a*exp(-b*n)+c` (barren-plateau signature:
    variance decays exponentially in n) and `poly_model(n,a,b,c) = a*n**(-b)+c` (polynomial/power-
    law decay, the non-barren-plateau alternative) -- via `scipy.optimize.curve_fit`, then compute
    R^2 and AIC for each, and report which model is better-supported (lower AIC; a delta-AIC
    threshold, e.g. >2, is the conventional "meaningfully better" bar -- state whichever threshold
    convention is used explicitly in the docstring).

    Cases (input -> expected output), both as pytest tests with a FIXED random seed for the noise:
    1. **Exponential ground truth**: generate `ns = [2,3,4,5,6]` (or similar), `variances =
       a_true*np.exp(-b_true*ns)+c_true + small_gaussian_noise` for some fixed
       `a_true,b_true,c_true` (e.g. `2.0, 0.8, 0.001`) and noise std small relative to the signal
       (e.g. 1% of the signal range). Assert `fit_and_compare(ns, variances)`'s recovered
       exponential-model params are close to `(a_true, b_true, c_true)` (loose tolerance, e.g.
       `atol=0.5` on `b` -- curve fits with 5-6 points and 3 free params have real uncertainty,
       don't over-tighten) AND that the returned verdict/lower-AIC model is `"exp"`.
    2. **Polynomial ground truth**: same shape, `variances = a_true*ns**(-b_true)+c_true +
       small_gaussian_noise`, assert the verdict is `"poly"`.
    3. **Both models always report both metrics**: for either synthetic case, assert the returned
       dict has `r2` and `aic` keys present and finite for BOTH `"exp"` and `"poly"` entries (not
       just the winning model) -- TRAIN-02 requires a genuine comparison, not just a single
       verdict with no visible alternative.
  </behavior>
  <implementation>
    `trainability/curve_fit.py` (extends 17-RESEARCH.md's provided code example -- use it as the
    starting point, adjust as needed for real numerical behavior found while writing the tests):

    ```python
    import numpy as np
    from scipy.optimize import curve_fit

    def exp_model(n, a, b, c):
        return a * np.exp(-b * n) + c

    def poly_model(n, a, b, c):
        return a * np.power(n, -b) + c

    def aic(residuals, k_params):
        n_obs = len(residuals)
        rss = np.sum(residuals ** 2)
        rss = max(rss, 1e-300)  # guard against log(0) if a fit is ~perfect
        return n_obs * np.log(rss / n_obs) + 2 * k_params

    def fit_and_compare(ns, variances):
        ...  # fit both models, compute r2/aic for both, return a dict including a "verdict" key
    ```

    Handle `curve_fit` convergence failures gracefully (e.g. wrap in try/except, use a reasonable
    `p0` guess derived from the data itself -- e.g. `p0=[variances[0]-variances[-1], 0.5,
    variances[-1]]` for both models -- and `maxfev=10000`) -- if a fit genuinely fails to converge
    on the SYNTHETIC ground-truth data in this plan's own tests, that's a bug in the initial guess
    or model form to fix now, not something to catch-and-ignore; real sweep data (Plan 17-07) may
    still fail to converge for one model, in which case surface that failure explicitly in the
    returned dict (e.g. `{"exp": {"converged": False, ...}}`) rather than crashing the whole
    analysis.

    The returned dict's shape (exact keys) is this module's own choice -- document it clearly in
    the docstring, since Plan 17-07 will consume it directly. A reasonable shape:
    `{"exp": {"params": ..., "r2": ..., "aic": ..., "converged": True}, "poly": {...}, "verdict":
    "exp" | "poly" | "inconclusive"}` (use `"inconclusive"` if the AIC difference is below the
    stated significance threshold, e.g. `abs(aic_exp - aic_poly) < 2`).

    Follow RED -> GREEN -> REFACTOR: write the tests first with the synthetic-data generation
    inline (seeded `np.random.default_rng`), confirm they fail against a stub/missing module,
    implement `trainability/curve_fit.py`, confirm green.
  </implementation>
</feature>

<verification>
1. `./venv/Scripts/python.exe -m pytest tests/test_curve_fit.py -v` -- all tests pass.
2. `./venv/Scripts/python.exe -m pytest` (full suite) -- no regressions.
</verification>

<success_criteria>
- `trainability/curve_fit.py` correctly distinguishes exponential from polynomial decay on synthetic ground-truth data.
- Both R² and AIC are reported for both candidate models on every call.
- Fit-convergence failures are surfaced explicitly, never silently swallowed.
</success_criteria>

<output>
After completion, create `.planning/phases/17-trainability-barren-plateau-study/17-04-SUMMARY.md`
</output>
