---
phase: 23-ancilla-lifecycle-safety-formal-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - forge/ancilla_lifecycle_safety.frg
autonomous: true
requirements: [LIFE-01, LIFE-02, LIFE-03, LIFE-04]

must_haves:
  truths:
    - "A bounded relational Forge model represents individual ancilla modes and grouped four-mode blocks across an ordered State.next trace."
    - "The gate protocol exposes allocate, begin/use, finish, terminal post-selection, and release transitions rather than hiding lifecycle changes in a scalar predicate."
    - "Strict deferred post-selection keeps a live block unavailable for reuse until the terminal release sequence."
    - "An unsafe same-trace reuse is surfaced as a trace-shaped counterexample, while a safe cross-epoch reuse witness satisfies the strengthened non-vacuity requirement."
  artifacts:
    - path: forge/ancilla_lifecycle_safety.frg
      provides: Explicit-State Forge lifecycle model and bounded test-expect suite
      contains: test expect
  key_links:
    - from: forge/ancilla_lifecycle_safety.frg
      to: .planning/phases/23-ancilla-lifecycle-safety-formal-verification/23-CONTEXT.md
      via: Locked D-01 through D-10 semantics encoded in model comments and predicates
      pattern: deferred post-selection
    - from: forge/ancilla_lifecycle_safety.frg
      to: C:/Users/cuqui/cs1710/hw/cs1710-memory-management-alejack312/stop_and_copy.frg
      via: Explicit State snapshots, next relation, and liveness-style transitions
      pattern: State
---

<objective>
Build the Phase 23 lifecycle model before any execution or prose synthesis. The model must make the temporal claim inspectable: a mode/block is allocated, used, remains live under deferred post-selection, and is released only at the terminal event. It must contain both a valid cross-epoch reuse witness and an unsafe same-trace reuse counterexample shape.

<context>
Use `23-CONTEXT.md` as the authority. The owner locked explicit relational `State` snapshots, one bounded `next` trace, both individual modes and four-mode blocks, expanded allocate -> begin/use -> finish events, strict deferred liveness, and n=4 as the first meaningful domain. Reuse the local style from `forge/pooled_ancilla_allocation.frg`, `forge/ancilla_mapping.frg`, and the CS1710 memory-management models, but do not change those precedents.

The delegated research artifact is `23-RESEARCH.md`; it confirms the local Forge 5.2/Racket 8.15 approach and warns that this model establishes bounded structural lifecycle behavior, not Perceval amplitudes or physical unitary equivalence.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the explicit State trace and lifecycle vocabulary</name>
  <files>forge/ancilla_lifecycle_safety.frg</files>
  <read_first>
    - `.planning/phases/23-ancilla-lifecycle-safety-formal-verification/23-CONTEXT.md`
    - `23-RESEARCH.md`
    - `forge/pooled_ancilla_allocation.frg`
    - `forge/ancilla_mapping.frg`
    - `C:\Users\cuqui\cs1710\hw\cs1710-memory-management-alejack312\stop_and_copy.frg`
    - `C:\Users\cuqui\cs1710\hw\cs1710-memory-management-alejack312\mark_and_sweep.frg`
  </read_first>
  <action>
Create a new `#lang forge` model with Sterling disabled, leaving all existing Forge files untouched. Define finite atoms for Pair, Mode, Block, and State. Give State an ordered `next` relation and explicit fields for event kind, active pair/block, allocated blocks, in-use blocks, releasable blocks, free blocks, and mode-level status. Keep a block-to-four-modes relation so the grouped allocation unit and individual mode safety can both be checked.

Encode a bounded trace predicate with an up-to-N depth and a concrete n=4 initial domain. State in comments that this is a bounded model and that the shortest satisfying/violating trace within the declared scope is evidence, not an unbounded theorem.
  </action>
  <verify>
    <automated>racket forge/ancilla_lifecycle_safety.frg</automated>
  </verify>
  <acceptance_criteria>
    - The new file parses under the repository's Forge toolchain.
    - The file contains an explicit `State` signature with `next`, not `#lang forge/temporal`.
    - Both block-level and individual-mode lifecycle fields are present.
    - The header states that no Python k-pair implementation is being verified and that this is a bounded structural model.
  </acceptance_criteria>
  <done>The model skeleton runs and exposes the complete state vocabulary without modifying Phase 22 models.</done>
</task>

<task type="auto">
  <name>Task 2: Encode transitions, strict liveness, and trace-shaped safety</name>
  <files>forge/ancilla_lifecycle_safety.frg</files>
  <read_first>
    - `forge/ancilla_lifecycle_safety.frg` from Task 1
    - `23-CONTEXT.md` decisions D-05 through D-10
    - `results/phase22_reuse_gate.md`
  </read_first>
  <action>
Add predicates for valid initial/final states and each event transition: allocate, begin/use, finish, terminal post-selection, and release. Enforce monotonic liveness within an epoch: finish does not make a block free; only terminal post-selection may make it releasable, followed by an explicit free transition. Prevent mode/block allocation when any mode in the block remains live.

Define the unsafe-reuse predicate so it identifies a later allocation of a still-live block and retains the relevant State sequence, pair identities, block identity, and event names needed to read the clobber point as a trace. Define a safe witness with pair (0,1) using block 0 through terminal release and pair (2,3) reusing block 0 in a later epoch. Conjoin the safe witness with the lifecycle safety predicate so LIFE-04 cannot pass through a witness that reuses incorrectly.
  </action>
  <verify>
    <automated>racket forge/ancilla_lifecycle_safety.frg</automated>
  </verify>
  <acceptance_criteria>
    - `allocate`, `begin/use`, `finish`, terminal post-selection, and release are distinguishable in the model.
    - A same-trace allocation before terminal post-selection is rejected or returned as the named unsafe trace, never silently treated as safe.
    - The safe witness includes at least two gates and genuine block reuse after release.
    - No model predicate claims physical amplitude equivalence or replaces Phase 22's numerical evidence.
  </acceptance_criteria>
  <done>Lifecycle transitions and the two required witness shapes are encoded with strict deferred-postselection semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Add bounded sat/unsat tests and counterexample comments</name>
  <files>forge/ancilla_lifecycle_safety.frg</files>
  <read_first>
    - `forge/ancilla_lifecycle_safety.frg`
    - `forge/ancilla_mapping.frg` test-expect structure
    - `23-RESEARCH.md` verification architecture
  </read_first>
  <action>
Add a test-expect suite at n=4 with an explicit integer bitwidth justified against the largest mode/block value actually computed. Include: a safe cross-epoch reuse `sat` witness; a genuine two-gate safe witness conjoined with safety; an unsafe same-trace reuse query that is `sat` as a counterexample to the unsafe behavior being possible; and a no-live-reallocation safety query that is `unsat` when the model is constrained to valid traces. Make the distinction between “unsafe trace exists” and “safe traces cannot contain unsafe reuse” explicit in names/comments so a green Forge run cannot be misread.

Include a comment specifying what the unsafe counterexample must contain: ordered states, two vertex-disjoint pairs, the same block, second allocation before terminal post-selection, and the exact still-live mode/block. Keep the first run bounded to n=4 and a small configurable depth; do not add a joint minimum-K search.
  </action>
  <verify>
    <automated>racket forge/ancilla_lifecycle_safety.frg</automated>
  </verify>
  <acceptance_criteria>
    - Every live test-expect block passes with the intended sat/unsat verdict.
    - At least one raw solver witness includes enough State/event structure to reconstruct the unsafe trace.
    - The model documents the chosen `for N Int` bitwidth and why it is safe.
    - `forge/pooled_ancilla_allocation.frg` and `forge/ancilla_mapping.frg` remain unmodified.
  </acceptance_criteria>
  <done>Phase 23's lifecycle model is executable, bounded, non-vacuous, and ready for evidence capture.</done>
</task>

</tasks>

<verification>
- `racket forge/ancilla_lifecycle_safety.frg` exits successfully with the named sat/unsat test expectations.
- The output/model contains both a safe cross-epoch reuse witness and a trace-shaped unsafe same-trace witness.
- `git diff -- forge/pooled_ancilla_allocation.frg forge/ancilla_mapping.frg` is empty.
</verification>

<success_criteria>
LIFE-01 through LIFE-04 have an executable, bounded relational model with explicit lifecycle transitions, strict deferred post-selection, trace-shaped unsafe reuse, and a strengthened safe-reuse witness.
</success_criteria>
