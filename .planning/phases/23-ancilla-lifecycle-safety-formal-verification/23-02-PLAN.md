---
phase: 23-ancilla-lifecycle-safety-formal-verification
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - results/phase23_lifecycle_run_log.md
  - results/phase23_lifecycle_traces.md
autonomous: true
requirements: [LIFE-02, LIFE-03, LIFE-04, LIFE-05]

must_haves:
  truths:
    - "The Phase 23 Forge model has been run at the declared n=4/depth bound and its actual sat/unsat output is preserved."
    - "The unsafe lifecycle behavior is documented as an ordered trace, not reduced to a scalar verdict."
    - "The safe cross-epoch reuse witness is documented separately from unsafe same-trace reuse."
    - "The comparison with Phase 22's numerical pooled-vs-dedicated result preserves both abstraction layers and does not reconcile disagreement by assumption."
  artifacts:
    - path: results/phase23_lifecycle_run_log.md
      provides: Actual Forge commands, bounds, solver output, and runtimes
      contains: n=4
    - path: results/phase23_lifecycle_traces.md
      provides: Human-readable safe and unsafe state-by-state traces
      contains: post-selection
  key_links:
    - from: results/phase23_lifecycle_run_log.md
      to: forge/ancilla_lifecycle_safety.frg
      via: Exact command and bound record
      pattern: ancilla_lifecycle_safety.frg
    - from: results/phase23_lifecycle_traces.md
      to: results/phase22_reuse_gate.md
      via: Explicit comparison boundary
      pattern: Phase 22
---

<objective>
Run the lifecycle model, capture real solver evidence, and translate the raw Forge instances into readable traces. This plan is the evidence gate for LIFE-02 through LIFE-05; it must preserve the difference between a structural trace finding and Phase 22's numerical physics result.

<context>
The model from 23-01 is intentionally bounded and starts at n=4. Do not expand scope into a full n<=8 static allocation search or a Python multi-pair circuit. Use the same honesty standard as `results/phase22_forge_run_log.md`: record attempted bounds, actual runtimes, and timeouts rather than reporting only successful cases.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the Forge model and preserve actual output</name>
  <files>results/phase23_lifecycle_run_log.md</files>
  <read_first>
    - `forge/ancilla_lifecycle_safety.frg`
    - `results/phase22_forge_run_log.md`
    - `23-RESEARCH.md`
  </read_first>
  <action>
Run `racket forge/ancilla_lifecycle_safety.frg` with the model's declared n=4 and trace-depth bounds. If the model supports a small depth sweep, run the declared depths in ascending order and stop at a clearly recorded solver ceiling. Capture the exact command, date, Forge/Racket versions, bound clauses, each test-expect verdict, solver timing lines, and any timeout/error. Do not infer a pass from a stale file or from a model that did not execute.
  </action>
  <verify>
    <automated>racket forge/ancilla_lifecycle_safety.frg</automated>
  </verify>
  <acceptance_criteria>
    - The run log contains the exact command and actual output for every attempted bound.
    - Sat/unsat verdicts are attributed to named tests and match the model's intended semantics.
    - Any non-convergence or syntax issue is retained as a finding with its bound and elapsed time.
  </acceptance_criteria>
  <done>There is a reproducible Phase 23 execution record grounded in a real Forge run.</done>
</task>

<task type="auto">
  <name>Task 2: Translate raw instances into safe and unsafe lifecycle traces</name>
  <files>results/phase23_lifecycle_traces.md</files>
  <read_first>
    - `results/phase23_lifecycle_run_log.md`
    - `forge/ancilla_lifecycle_safety.frg`
    - `23-CONTEXT.md` decisions D-07 through D-10
  </read_first>
  <action>
Preserve the raw Forge witness identifiers/output or a verbatim excerpt, then add a human-readable table for the unsafe trace and a separate table for safe cross-epoch reuse. Each row must identify State/event, pair, block, individual modes, block status, and whether terminal post-selection has occurred. Mark the exact clobber point in the unsafe trace and the exact release point in the safe trace.

If the solver chooses a trace with irrelevant extra states, explain the projection used to make the table readable without changing the witness. If the required witness cannot be obtained at the initial bound, record the failure and the smallest bound attempted; do not fabricate a trace.
  </action>
  <verify>
    <automated>rg -n "unsafe|safe|allocate|begin|finish|post-selection|release|clobber" results/phase23_lifecycle_traces.md</automated>
  </verify>
  <acceptance_criteria>
    - Both witness types are separately labeled.
    - The unsafe table demonstrates reuse before terminal post-selection; the safe table demonstrates reuse only after release.
    - The artifact preserves enough state/event data for another reader to reconstruct the trace.
  </acceptance_criteria>
  <done>The solver evidence is inspectable by a human without collapsing lifecycle behavior into a single number.</done>
</task>

<task type="auto">
  <name>Task 3: Record the LIFE-05 cross-check boundary</name>
  <files>results/phase23_lifecycle_run_log.md</files>
  <read_first>
    - `results/phase22_reuse_gate.md`
    - `results/phase23_lifecycle_traces.md`
    - `.planning/phases/22-multi-pair-ancilla-allocation-formal-verification/22-VERIFICATION.md`
  </read_first>
  <action>
Add a short comparison section stating exactly what Phase 22's MPAIR-07 numerical probe measured (pooled versus dedicated physical distribution behavior) and what Phase 23 measures (bounded state/liveness structure). Make the same-trace strict-liveness comparison primary and cross-epoch reuse a separate sanity check, per D-11. If the results appear to disagree, preserve both results and label the disagreement unresolved; do not choose a universal winner or silently change the model semantics.
  </action>
  <verify>
    <automated>rg -n "MPAIR-07|numerical|structural|same-trace|cross-epoch|unresolved" results/phase23_lifecycle_run_log.md</automated>
  </verify>
  <acceptance_criteria>
    - The cross-check names both methods, their inputs, and their distinct claims.
    - The document does not say that Forge proves physical reuse validity.
    - Any agreement or disagreement is stated as a measured finding with a caveat.
  </acceptance_criteria>
  <done>LIFE-05 has a traceable, honest comparison record rather than an inferred reconciliation.</done>
</task>

</tasks>

<verification>
- `racket forge/ancilla_lifecycle_safety.frg` was run and its output is preserved.
- Both readable traces link back to raw solver evidence.
- The Phase 22 numerical and Phase 23 structural claims are explicitly separated.
</verification>

<success_criteria>
LIFE-02 through LIFE-05 have real run evidence, readable traces, and an honest cross-method comparison record.
</success_criteria>
