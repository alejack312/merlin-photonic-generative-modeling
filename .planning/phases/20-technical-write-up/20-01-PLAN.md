---
phase: 20-technical-write-up
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/trainability-study.md
autonomous: false

must_haves:
  truths:
    - "TRAIN-07's `> Owner interpretation: [pending]` placeholder at docs/trainability-study.md:174 is replaced with the owner's own unaided explanation of why weight1/uniform agrees with iqp-baseline.md's empirical rule while mixed/uniform disagrees -- transcribed, not authored, by Claude."
    - "docs/trainability-study.md's 'What this does/doesn't establish' section contains a literature comparison table stating consistent/inconsistent/silent for all 11 named WRITE-02 baselines (6 with substantive TRAIN-specific reasoning, 5 briefly marked silent with a one-line reason)."
    - "docs/trainability-study.md contains a short cross-reference note pointing at docs/hardness-under-loss-study.md's HARD-06 section, stating plainly whether TRAIN's measured result is consistent or inconsistent with Herbst et al.'s anticoncentration-tradeoff prediction."
    - "The McClean et al. baseline's citation confidence is stated honestly -- either confirmed via a primary-source fetch during this plan, or explicitly caveated as secondary-source-only -- never silently upgraded to full confidence."
  artifacts:
    - path: "docs/trainability-study.md"
      provides: "Filled TRAIN-07 owner interpretation, TRAIN literature comparison table, Herbst cross-reference note"
      min_lines: 548
  key_links:
    - from: "docs/trainability-study.md's Herbst cross-reference note"
      to: "docs/hardness-under-loss-study.md's HARD-06 section"
      via: "prose pointer"
      pattern: "hardness-under-loss-study"
---

<objective>
Close TRAIN-07's genuinely open self-explanation gap (owner explains, unaided, why the two `uniform`-init cross-reference rows agree/disagree with `docs/iqp-baseline.md`'s empirical rule), then add TRAIN's literature comparison table (WRITE-02) and its Herbst et al. cross-reference note (success criterion 6) to `docs/trainability-study.md`.

**This plan has a mandatory human checkpoint (Task 1) — do not skip to Task 2 without it.** Per this project's `CLAUDE.md` self-explanation-checkpoint convention and `20-CONTEXT.md`'s explicit lock: TRAIN-07's interpretation is a real, currently-open gap (the literal placeholder text `> Owner interpretation: [pending]` at `docs/trainability-study.md:174`), not a formality — Claude must NOT write this interpretation itself.

Purpose: this is the one genuinely open item Phase 20 must close via live owner input; everything else in this plan (the literature table, the Herbst note) is mechanical documentation synthesis that can proceed once the checkpoint is on record.
Output: `docs/trainability-study.md`, updated in place — TRAIN-07's interpretation filled, plus two new subsections in its "What this does/doesn't establish" section.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-technical-write-up/20-CONTEXT.md
@.planning/phases/20-technical-write-up/20-RESEARCH.md
@docs/trainability-study.md
@docs/iqp-baseline.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: TRAIN-07 self-explanation checkpoint -- owner explains the agree/disagree split before Claude writes anything</name>
  <what-to-present>
Before asking the owner to attempt anything, present these confirmed facts (already established by prior phases -- do not re-derive them, cite where each comes from):

1. `docs/iqp-baseline.md`'s empirical rule (97.9% accuracy, 283 rows, sibling project): `plateau if small_angle OR (uniform AND n>=6 AND not complete_graph_like)`. The `not complete_graph_like` escape hatch is a qubit-side structural notion this project's weight-1/mixed photonic circuits have no established mapping onto -- already stated in the doc as "treated as inapplicable here, not silently assumed to hold."
2. TRAIN-07's cross-reference table (`docs/trainability-study.md:167-172`), read verbatim:

   | generator_scope | init_scheme | baseline rule predicts | measured fit verdict | agreement |
   |---|---|---|---|---|
   | weight1 | small_angle | plateau | inconclusive (weak fit both ways, R^2~0.4-0.5) | inconclusive |
   | weight1 | uniform | plateau (n_max=6 >= 6) | plateau (exp wins, R^2=0.999, decaying) | **agree** |
   | mixed | small_angle | plateau | inconclusive (both R^2~0, no discernible trend) | inconclusive |
   | mixed | uniform | no_plateau (n_max=5 < 6) | plateau (exp wins, R^2=0.910, decaying) | **disagree** |

3. Both `small_angle` rows are already flagged "inconclusive" on both sides -- there is no verdict to explain there. The interpretive question is specifically about the two `uniform` rows' split outcome: `weight1/uniform` **agrees** with the rule (predicts plateau, measures plateau); `mixed/uniform` **disagrees** (rule predicts no_plateau since n_max=5<6, but measured result is still plateau).
4. Context the owner may want to draw on, already on record elsewhere in the same document (do not supply this as the answer, just point at it if asked):
   - Both `uniform` exponential fits carry a "near-cancelling a/c pair, poorly-identified parameterization at only 4-5 points" caveat (Results section, `docs/trainability-study.md` lines 88-99).
   - TRAIN-09's bandwidth-sensitivity follow-up (`docs/trainability-study.md` lines 176-282) found neither `uniform` row's "exp" verdict is robust across a six-point sigma grid -- both flip to "inconclusive" at intermediate sigma, `weight1/uniform` non-monotonically re-emerges as "exp" at high sigma, `mixed/uniform` does not.
   - The `not complete_graph_like` escape hatch is explicitly "treated as inapplicable" for this photonic circuit family -- the owner may need to reason about whether that structural gap in the baseline rule's applicability, rather than a genuine trainability discrepancy, explains the `mixed/uniform` disagreement.
  </what-to-present>
  <owner-task>
Ask the owner to explain, unaided, in their own words: why does `weight1/uniform` agree with `docs/iqp-baseline.md`'s empirical rule while `mixed/uniform` disagrees? Record the actual answer -- including any wrong turns or partial reasoning -- matching the style already used throughout this document (see the TRAIN-09/TRAIN-10 sections' own interpretive framing) and this project's other self-explanation checkpoints (`docs/iqp-photonic-encoding.md`'s ARB-01/ARB-02 "Owner's Attempt" transcript, `docs/hardness-under-loss-study.md`'s HARD-04 "Owner's attempt-first response"). Do not silently polish or complete the owner's reasoning -- transcribe what they actually say.
  </owner-task>
  <resume-signal>Owner has shared their interpretation (or explicitly says "just write something reasonable, I've got the concept already" per CLAUDE.md's stated exception) -- proceed to Task 2 only after this.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Transcribe TRAIN-07's interpretation, then add the TRAIN literature comparison table and Herbst cross-reference note</name>
  <files>docs/trainability-study.md</files>
  <action>
**Part A -- close TRAIN-07 (do this first):** Replace the `> Owner interpretation: [pending]` placeholder at line 174 with the owner's actual words from Task 1, formatted as a `> Owner interpretation:` blockquote (matching the existing single-line-blockquote style already scaffolded at that line; extend to a short multi-paragraph blockquote if the dialogue took more than one exchange, matching the precedent style already used elsewhere in this document, e.g. the TRAIN-09/TRAIN-10 sections' own interpretive framing). Do not add any interpretation beyond what the owner actually said.

**Part B -- TRAIN literature comparison table (WRITE-02):** Append a new `### Literature comparison table (WRITE-02)` subsection after the existing "Phase 17.1 addendum" paragraph (end of `## What this does/doesn't establish`, currently ending at line 548). Follow `docs/iqp-baseline.md`'s "Fresh Primary-Source Verification" section's established citation-precision style (cite theorem/section/page inline, then state relevance) rather than a terse table-only format. Cover all 11 WRITE-02 baselines; the following per-baseline assignment (planner's decision, made per `20-CONTEXT.md`'s delegated discretion) keeps the table TRAIN-focused and avoids padding it with irrelevant rows:

  Substantive rows (state consistent/inconsistent/silent with reasoning, citing the theorem/section already used elsewhere in this repo's docs):
  - **McClean et al.** (barren plateaus in QNN training, Nature Communications 2018) -- **before drafting this row**, use WebSearch/WebFetch to confirm the paper's actual arXiv ID and core claim (this repo has never independently verified it -- prior citations in `.planning/research/` are WebSearch-sourced summaries only, per `20-RESEARCH.md`'s flagged gap). If the fetch confirms the standard gradient-variance-vs-system-size protocol (the same diagnostic shape Phase 17's own methodology uses), state the row as "consistent with the well-known protocol shape" and note explicitly whether this was confirmed via a primary-source read or remains a secondary/WebSearch-level confirmation -- do not silently present it at the same confidence as the other 10 baselines, all of which have a downloaded PDF in `docs/papers/`.
  - **`docs/iqp-baseline.md`'s own empirical rule** -- point back to the TRAIN-07 table above (Part A) rather than re-deriving; state the split verdict plainly (agree on `weight1/uniform`, disagree on `mixed/uniform`, both `small_angle` rows inconclusive on both sides).
  - **Rudolph et al.** (arXiv:2305.02881, Theorem 2) -- state as directionally consistent with TRAIN-09's bandwidth-sensitivity finding (the original fixed-`SIGMA=0.1` "exp" verdict for both `uniform` rows is not robust across the sigma grid, weakening/disappearing at intermediate sigma) even though the paper's own bitstring-Hamming-distance bodyness decomposition doesn't mechanically transfer to this project's Euclidean-grid kernel (already stated in the TRAIN-09 section and in `docs/iqp-baseline.md`) -- state both halves (directional consistency of the *risk*, mechanistic non-transfer of the *proof*) rather than collapsing to one word.
  - **Mhiri et al.** (arXiv:2502.07889, p.5-6, Appendix H) -- consistent: their proof that small-angle/warm-start guarantees are not general, with structured/commuting circuits flagged as the specific risk case, is a citable theoretical reason Phase 17's `small_angle` rows came out inconclusive in both generator scopes rather than confirming a clean plateau either way.
  - **Recio-Armengol et al.** (arXiv:2503.02934, Sec. 9.3 and Sec. 8.1.2) -- consistent for the uniform-init exponential-concentration finding (their own analytical derivation matches this project's empirical `uniform`-init signature); their proposed fix (data-dependent init) was directly implemented and tested as TRAIN-10 and found NOT to resolve `small_angle`'s inconclusive verdict in either scope -- report this as the genuine negative result it is, not softened.
  - **Herbst et al.** (arXiv:2512.24801) -- this baseline's substantive content is the cross-reference note in Part C below; in this table row, state "see Cross-reference note below" plus a one-clause summary of the verdict reached there.

  Silent rows (one line each, stating why the baseline doesn't bear on TRAIN specifically): Aaronson-Brod (arXiv:1510.05245, lost-photon hardness -- HARD-specific, not a trainability claim), arXiv:2510.24137/Park & Oh (MPS-simulability/noisy-IQP hardness -- HARD-specific), arXiv:2405.01395 (two-photon gate construction -- ARB-specific), Bremner-Montanaro-Shepherd 2015 (arXiv:1504.07999, hardness threshold -- no trainability claim), Bremner-Montanaro-Shepherd 2017 (arXiv:1610.01808, depolarizing-noise hardness threshold -- HARD-specific, no direct trainability claim of its own).

**Part C -- Herbst et al. cross-reference note (success criterion 6):** In the same `## What this does/doesn't establish` section (after Part B's table, or as its own short `### Cross-reference: Herbst et al.'s anticoncentration-tradeoff prediction` subsection), add a short note stating plainly whether TRAIN's measured result is consistent or inconsistent with Herbst et al.'s prediction that anticoncentration drives both sampling hardness and MMD-loss concentration (`docs/iqp-baseline.md`'s existing citation, "Fresh Primary-Source Verification" section). State the actual measured facts (do not invent a stronger claim than the data supports):
  - TRAIN's `uniform`-init result shows an exponential gradient-decay signature at Phase 17's original fixed bandwidth (R^2=0.999 weight1, R^2=0.910 mixed) -- but TRAIN-09 found this signature is NOT robust across a wider sigma grid (flips to inconclusive at intermediate sigma). State this caveat alongside the headline result, not instead of it.
  - Point at `docs/hardness-under-loss-study.md`'s equivalent note (added by a parallel plan in this phase) for the HARD-side half of this cross-reference -- do not restate HARD's own measured alpha(eta) trend here, only point at it.
  - This is a genuinely interpretive connection between two separately-measured trends across two different independent variables (TRAIN sweeps n at fixed eta=1; HARD sweeps eta at small fixed n) -- state the comparison with appropriate hedging (consistent with this project's CLAUDE.md rule that Claude may organize/compute but the owner interprets; where a strong claim would require judgment beyond stating measured facts and the literature's own prediction, flag it as such rather than asserting confidently).
  </action>
  <verify>Read back docs/trainability-study.md: confirm (a) line 174's placeholder is replaced with the owner's actual transcribed words, (b) a new literature comparison table exists covering all 11 baselines (6 substantive + 5 one-line silent), (c) a Herbst cross-reference note exists pointing at docs/hardness-under-loss-study.md, (d) every other existing section of the document is unmodified (`git diff docs/trainability-study.md` shows only additions/the line-174 replacement, no deletions of prior content).</verify>
  <done>docs/trainability-study.md has TRAIN-07's real owner interpretation on record, a literature comparison table for all 11 WRITE-02 baselines, and a Herbst cross-reference note -- WRITE-02/04/05 (TRAIN) and success criterion 6 (TRAIN half) are satisfied.</done>
</task>

</tasks>

<verification>
1. Task 1's checkpoint actually occurred -- the owner's own words are on record before Task 2 started (not Claude-authored interpretation).
2. `docs/trainability-study.md:174` no longer contains the literal string `[pending]`.
3. A literature comparison table exists in `## What this does/doesn't establish` covering all 11 WRITE-02 baselines with a stated consistent/inconsistent/silent verdict (or equivalent reasoning) each.
4. A Herbst et al. cross-reference note exists, pointing at `docs/hardness-under-loss-study.md`.
5. `git diff docs/trainability-study.md` shows only additions to the file -- no prior content deleted or altered beyond the line-174 replacement.
</verification>

<success_criteria>
- TRAIN-07 closed with a genuine, owner-authored (not Claude-authored) interpretation.
- WRITE-02 satisfied for TRAIN's table (all 11 baselines addressed).
- WRITE-04 already satisfied structurally for TRAIN (pre-existing section); WRITE-05 satisfied by Task 1's recorded checkpoint.
- Success criterion 6 (TRAIN half) satisfied: explicit statement of whether TRAIN's result is consistent/inconsistent with Herbst et al.'s prediction.
</success_criteria>

<output>
After completion, create `.planning/phases/20-technical-write-up/20-01-SUMMARY.md`
</output>
