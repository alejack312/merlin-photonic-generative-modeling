---
phase: 20-technical-write-up
plan: 04
type: execute
wave: 2
depends_on: ["20-01", "20-02", "20-03"]
files_modified:
  - docs/technical-findings.md
autonomous: true

must_haves:
  truths:
    - "docs/technical-findings.md exists as a new document, synthesizing TRAIN (Phase 17/17.1), HARD (Phase 18), and ARB-01/ARB-02 (Phases 15-16) findings, each with methodology-stated-before-results structure."
    - "All three source docs' literature comparison tables are mirrored into docs/technical-findings.md, with the same consistent/inconsistent/silent verdicts as their canonical source (no re-derivation, no drift)."
    - "docs/technical-findings.md links to each source doc's own scope ('what this does/doesn't establish') section rather than re-deriving that content."
    - "docs/technical-findings.md surfaces the Herbst et al. TRAIN<->HARD connection (pointing at both source docs' cross-reference notes) without re-deriving the analysis itself."
    - "Every reported number in docs/technical-findings.md is traceable to the same script/CSV/test its source doc already cites -- no new, uncited numbers introduced."
  artifacts:
    - path: "docs/technical-findings.md"
      provides: "Project-level synthesis of STUDY-01, STUDY-02, and ARB-01 findings with mirrored literature tables and links to the three source docs"
      min_lines: 80
  key_links:
    - from: "docs/technical-findings.md"
      to: "docs/trainability-study.md"
      via: "markdown link/reference"
      pattern: "trainability-study\\.md"
    - from: "docs/technical-findings.md"
      to: "docs/hardness-under-loss-study.md"
      via: "markdown link/reference"
      pattern: "hardness-under-loss-study\\.md"
    - from: "docs/technical-findings.md"
      to: "docs/iqp-photonic-encoding.md"
      via: "markdown link/reference"
      pattern: "iqp-photonic-encoding\\.md"
---

<objective>
Write `docs/technical-findings.md` -- the project's synthesis document for STUDY-01 (trainability, Phase 17/17.1), STUDY-02 (hardness-under-loss, Phase 18), and ARB-01 (tunable weight-2 gate, Phases 15-16). Mirrors all three source docs' literature comparison tables and links to their scope sections, per `20-CONTEXT.md`'s locked deliverable shape: "the synthesis doc's job is pointing the reader at [the detailed analysis], not re-deriving it."

This is the final wave of Phase 20 -- it depends on Plans 20-01/20-02/20-03 having already added each source doc's literature table, scope subsection, and (for TRAIN/HARD) Herbst cross-reference note, since this plan mirrors that content rather than authoring it fresh.

Purpose: satisfies WRITE-01 (methodology-before-results structure, now at the project level, not just per-source-doc) and completes WRITE-02/04/05/06 at the synthesis level.
Output: new file `docs/technical-findings.md`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-technical-write-up/20-CONTEXT.md
@.planning/phases/20-technical-write-up/20-RESEARCH.md
@docs/trainability-study.md
@docs/hardness-under-loss-study.md
@docs/iqp-photonic-encoding.md
@docs/julia-cross-check-study.md
@trainability/rng.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft docs/technical-findings.md</name>
  <files>docs/technical-findings.md</files>
  <action>
Create `docs/technical-findings.md` following this project's established `docs/*.md` convention (kebab-case filename, `# Title` as line 1, no YAML front-matter). Structure and exact prose/ordering are Claude's discretion per `20-CONTEXT.md`, within this locked shape:

1. **Title and brief framing paragraph** stating this is the project's synthesis document for the v3.0 milestone's three research questions (trainability, hardness-under-loss, tunable weight-2 gate validation), and that it organizes and cross-references already-published, already-verified findings from `docs/trainability-study.md`, `docs/hardness-under-loss-study.md`, and `docs/iqp-photonic-encoding.md` -- it does not introduce new numbers or re-run any analysis.

2. **Three sections, one per study**, each with methodology-stated-before-results structure (WRITE-01):
   - **Trainability (STUDY-01, TRAIN-01..10)**: brief methodology summary (exact parameter-shift gradients, n=2-6 weight1 / n=2-5 mixed, both init schemes), headline results (uniform-init exponential-decay signature at Phase 17's original bandwidth, R^2=0.999/0.910; TRAIN-09's finding that this signature is NOT robust across the sigma grid; TRAIN-10's finding that data-dependent init did not resolve `small_angle`'s inconclusive verdict), the TRAIN-07 cross-reference verdict (now including the owner's real interpretation from Plan 20-01, transcribed or summarized -- do not re-interpret it, quote or closely paraphrase what the owner actually said), a link to `docs/trainability-study.md#what-this-doesdoesnt-establish` for the full scope statement, and the mirrored literature table (see Task 2).
   - **Hardness-under-loss (STUDY-02, HARD-01..07)**: brief methodology summary (`pcvl.LC(loss)` + `min_detected_photons_filter(0)`, eta grid, weight1 n=2-6 / mixed n=2-4), headline results (TVD-vs-eta, anticoncentration alpha(eta) decreasing as eta decreases, weight-2 herald compounding), HARD-04's positioning (no eta-to-epsilon translation attempted, by explicit owner decision -- link to the "Owner's attempt-first response" section rather than restating it in full), a link to `docs/hardness-under-loss-study.md#hard-06-what-this-phase-does-and-does-not-establish`, and the mirrored literature table.
   - **ARB-01/ARB-02 (tunable weight-2 gate, ARB-01..09)**: brief methodology summary (`PostProcessedControlledRotationsItem`/`CP(alpha)`, general operator identity alpha=4*theta, closed-form success probability), headline results (16-point alpha sweep matching closed form to ~1e-6, full-pipeline TVD at floating-point-noise level, n=3 mixed composability, Forge-verified injective mode-mapping), a link to `docs/iqp-photonic-encoding.md`'s new "What ARB-01/ARB-02 does/doesn't establish" subsection (added by Plan 20-03), and the mirrored literature table.

3. **A short "Trainability and hardness: are they connected?" section** (or similar heading, Claude's discretion) that surfaces the Herbst et al. cross-thread (success criterion 6) by pointing at BOTH source docs' own cross-reference notes (added by Plans 20-01/20-02) -- state the one-sentence headline (TRAIN's uniform-init decay signature is not robust to bandwidth; HARD's loss increases anticoncentration, the opposite direction from `docs/iqp-baseline.md`'s original speculative guess) and link to both notes for the full reasoning. Do not re-derive the analysis here -- this section's job is pointing the reader at it, per `20-CONTEXT.md`'s explicit lock.

4. **An "Independent verification" pointer** (brief, supplementary): note that `docs/julia-cross-check-study.md` independently cross-checked the underlying exact and lossy distributions this project's TRAIN/HARD/ARB findings are built on (Yao.jl qubit-side, BosonSampling.jl weight-1/weight-2, BosonSampling.jl native loss API) via a separate toolchain, with all four legs reaching a genuine GO -- link to it, do not restate its numbers or force it into the TRAIN/HARD/ARB structure (per `20-CONTEXT.md`'s explicit "supplementary only" framing).

5. **What this project does not establish, at the milestone level** (brief, pointing at each source doc's own scope statement rather than re-deriving): no complexity-theoretic proof, no asymptotic-scale demonstration, ARB-01/ARB-02 and HARD-01..07 use two DIFFERENT weight-2 gate families (`heralded_cz` fixed-angle vs. `CP(alpha)` tunable) that were never cross-tested under loss together -- state this explicitly, since it's easy to conflate the two constructions when reading the synthesis doc in isolation.
  </action>
  <verify>Read back docs/technical-findings.md: confirm all three study sections exist with methodology-before-results structure, each links to its source doc's scope section, the Herbst cross-thread section points at both source notes without re-deriving them, and the Independent verification pointer links to docs/julia-cross-check-study.md without restating its numbers.</verify>
  <done>docs/technical-findings.md exists with the full locked structure (three study sections, Herbst cross-thread pointer, independent-verification pointer, milestone-level scope statement).</done>
</task>

<task type="auto">
  <name>Task 2: Mirror the three literature tables, verify traceability, and resolve the TRAIN seed question</name>
  <files>docs/technical-findings.md</files>
  <action>
**Part A -- mirror the tables:** Copy each source doc's literature comparison table (added by Plans 20-01/20-02/20-03) into its corresponding section in `docs/technical-findings.md`, verbatim or lightly reformatted for the synthesis doc's own prose style -- the consistent/inconsistent/silent verdicts and per-baseline reasoning must match the source doc exactly (no drift, no re-interpretation). Each mirrored table should carry a one-line note ("full reasoning and citations: see `docs/<source-doc>.md`") pointing back to the canonical version.

**Part B -- WRITE-06 traceability pass:** Re-read every number stated in `docs/technical-findings.md` (R^2 values, TVD figures, success probabilities, alpha values, n-ranges) and confirm each one traces to the exact same script/CSV/test its source doc already cites (e.g. `results/phase17_curve_fit_summary.csv`, `results/phase18_weight1_loss_sweep.csv`, `results/phase16_alpha_sweep.csv`). Do not introduce any new number that isn't already cited in one of the three source docs -- if a number needs restating for the synthesis doc's own flow, copy the source doc's own citation alongside it.

**Part C -- resolve the TRAIN RNG-seed traceability question (20-RESEARCH.md Open Question 3):** TRAIN's randomness uses `trainability/rng.py::derive_seed`/`get_rng` -- a deterministic, reorder-safe hash of each cell's own full labeled coordinate tuple `(n, generator_scope, init_scheme, draw_index)`, not one single global seed constant (confirmed by reading `trainability/rng.py` directly). This is architecturally different from HARD's single literal `seed_base=180814`. **Decision (stated here, not silently made): do not fabricate a single literal seed number for TRAIN to force parity with HARD's style** -- doing so would misrepresent TRAIN's actual (and more robust) per-coordinate-hashed design as a single-seed design it isn't. Instead, state in `docs/technical-findings.md`'s Trainability section: "randomness uses a deterministic, reorder-safe hashed-seed scheme (`trainability/rng.py::derive_seed`), not a single global seed constant -- each `(n, generator_scope, init_scheme, draw_index)` coordinate gets its own reproducible seed, so results are exactly reproducible without needing one literal number quoted here." This satisfies WRITE-06's "fixed seed where randomness is involved" via an accurate mechanism citation rather than an invented number.

**Part D -- final consistency read-through:** confirm no heading/link in `docs/technical-findings.md` is broken (all `docs/*.md` references resolve to real files/sections), and that nothing in the synthesis doc states a stronger or more confident conclusion than its source doc does (in particular: TRAIN-09's "not robust" finding, TRAIN-10's negative result, and HARD-04's "no translation attempted" decision must read with the same honest, unsoftened framing here as in their source docs -- per `20-CONTEXT.md`'s explicit instruction not to re-narrate or soften Phase 17/17.1/18's honest findings).
  </action>
  <verify>Read back docs/technical-findings.md: confirm all three tables are mirrored with matching verdicts, every number has a traceable citation, the TRAIN seed question is resolved via the stated mechanism-citation (not a fabricated number), and a final read-through confirms no softened or overstated claims relative to the source docs.</verify>
  <done>docs/technical-findings.md is complete, internally consistent, and fully traceable -- WRITE-02 (mirrored), WRITE-03, and WRITE-06 are satisfied at the synthesis level.</done>
</task>

</tasks>

<verification>
1. `docs/technical-findings.md` exists and contains three study sections (TRAIN, HARD, ARB), each with methodology-before-results structure and a link to its source doc's scope section.
2. All three literature tables are mirrored with verdicts matching their canonical source docs exactly.
3. The Herbst cross-thread section points at both source notes without re-deriving the analysis.
4. Every number in the doc traces to an already-cited script/CSV/test; the TRAIN seed question is resolved via an accurate mechanism citation, not a fabricated literal number.
5. No claim in the synthesis doc is softer or stronger than its source doc's own framing.
</verification>

<success_criteria>
- WRITE-01 satisfied at the project/synthesis level.
- WRITE-02 satisfied: all three tables mirrored into one document, matching their canonical sources.
- WRITE-03 satisfied: honest negative/inconclusive framing preserved, not softened.
- WRITE-05 satisfied: TRAIN-07's real owner interpretation is reflected (via quote/summary, not re-authored).
- WRITE-06 satisfied: every number traceable, TRAIN's seed mechanism accurately described.
- This closes Phase 20 -- all of WRITE-01 through WRITE-06 are now satisfiable from this phase's shipped artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/20-technical-write-up/20-04-SUMMARY.md`
</output>
