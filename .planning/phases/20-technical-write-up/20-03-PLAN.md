---
phase: 20-technical-write-up
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/iqp-photonic-encoding.md
autonomous: true

must_haves:
  truths:
    - "docs/iqp-photonic-encoding.md has a new, ARB-01/ARB-02-scoped 'what this does/doesn't establish' subsection -- the one structural gap this section had relative to TRAIN's and HARD's already-existing per-section scope subsections."
    - "The new subsection contains a literature comparison table addressing all 11 named WRITE-02 baselines, with only arXiv:2405.01395 given a substantive consistent/inconsistent/silent verdict (already-implemented gate-construction source) and the other 10 explicitly noted as not applicable via prose, per this section's actual subject (gate-mechanics validation, not a hardness or trainability claim)."
  artifacts:
    - path: "docs/iqp-photonic-encoding.md"
      provides: "New ARB-01/ARB-02-scoped scope statement + literature comparison table"
      min_lines: 500
  key_links: []
---

<objective>
Add the one real structural gap Phase 20's research identified in `docs/iqp-photonic-encoding.md`: unlike `docs/trainability-study.md` and `docs/hardness-under-loss-study.md`, the ARB-01/ARB-02 section has no section-scoped "what this does/doesn't establish" subsection of its own (only a document-wide `## Conclusion and Open Questions` covering the entire doc, ENC-01 through ARB-09). Add a new, narrowly-scoped subsection plus ARB's literature comparison table (WRITE-02).

This plan is independent of Plan 20-01 (TRAIN) and Plan 20-02 (HARD) -- three different files, no dependency between them, per `20-RESEARCH.md`'s explicit sequencing analysis. Per `20-CONTEXT.md`'s locked decision, Herbst et al.'s cross-thread is NOT required in this document (it is a trainability/hardness prediction, not a gate-mechanics claim) -- do not add a Herbst note here.

Purpose: satisfies WRITE-04 for ARB (the one section genuinely missing it) and WRITE-02 for ARB's table.
Output: `docs/iqp-photonic-encoding.md`, updated in place -- one new subsection added between the existing "Forge Verification" subsection and the document-level "Conclusion and Open Questions" section.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-technical-write-up/20-CONTEXT.md
@.planning/phases/20-technical-write-up/20-RESEARCH.md
@docs/iqp-photonic-encoding.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the ARB-01/ARB-02-scoped "what this does/doesn't establish" subsection</name>
  <files>docs/iqp-photonic-encoding.md</files>
  <action>
Insert a new `### What ARB-01/ARB-02 does/doesn't establish` subsection immediately after the existing `### Forge Verification of the Ancilla Mode-Mapping (Phase 16)` subsection (ends around line 486) and before `## Conclusion and Open Questions` (starts at line 488) -- scoped narrowly to ARB-01/ARB-02 only, matching TRAIN's and HARD's per-section pattern (each has a dedicated, section-scoped scope subsection, not a document-level one), rather than folding into the existing document-wide Conclusion.

Content required, drawing on what this section (ARB-01/ARB-02) has already established elsewhere in this document -- do not re-derive, cite the existing subsections directly:

**What ARB-01/ARB-02 establishes:** a continuously-tunable two-qubit diagonal-phase gate (`PostProcessedControlledRotationsItem`/`CP(alpha)`), validated to the same rigor bar `heralded_cz` cleared -- general operator identity (`alpha=4*theta`), closed-form success probability derived and confirmed against measurement (Phase 15), a 16-point alpha sweep matching the closed form to ~1e-6 (Phase 16, ARB-08), full-pipeline TVD validation at floating-point-noise level for n=2,3 across multiple non-trivial alpha values plus a direct boundary-agreement confirmation against `heralded_cz` at alpha=pi (Phase 15), n=3 mixed weight-1+arbitrary-theta-weight-2 composability (Phase 16, ARB-07), and a Forge-based formal check that the gate's ancilla mode-mapping dict is injective/non-aliasing for all valid (n,i,j), n<=8 (Phase 16, ARB-09, no bug found).

**What it does not establish** (draw on the existing document-level Conclusion's bullets at lines 494/497/498, but restate scoped specifically to ARB-01/ARB-02 rather than the whole document):
- This is a design/mapping and gate-validation exercise, not a hardness or trainability claim -- it says nothing about whether circuits using `CP(alpha)` are classically hard to sample from or trainable; those are TRAIN's and HARD's separate, distinct questions (already answered on their own terms elsewhere in this project, using the FIXED-angle `heralded_cz` construction, not `CP(alpha)`).
- Toy-check scope: validated at n=2-3 only, under idealized lossless SLOS/Processor simulation -- nothing here shows the construction's behavior at larger n or under photon loss (HARD's loss model was built and tested against `heralded_cz`, not `CP(alpha)` -- the two weight-2 gate families were never cross-tested under loss).
- General-n scaling is stated (the operator identity and success-probability formula are written for general n), but only concretely instantiated and checked at n=2.
- Success probability is a real, alpha-dependent, non-monotonic quantity (never a fixed constant) -- reported as a full table/curve, never collapsed to a single number, but this also means `CP(alpha)`'s resource cost genuinely varies with the chosen angle, unlike `heralded_cz`'s fixed 2/27.

**Literature comparison table (WRITE-02):** immediately following the "does not establish" content above, add a literature comparison table. Per `20-CONTEXT.md`'s locked decision ("each table lists only the baselines actually relevant to that section, avoids rows full of silent"), and because ARB-01/ARB-02 genuinely has only one directly-relevant baseline among the 11, use a SHORT table plus a brief prose note rather than 10 padding rows:

  - **arXiv:2405.01395** ("Simple rules for two-photon state preparation with linear optics"), Section V-B -- **consistent**: this is the primary source `PostProcessedControlledRotationsItem`'s own construction is built from; its closed-form success-probability formula (`p_success(alpha) = 1/sigma_max^(2n)`) is already independently verified against this project's own measured amplitudes to ~1e-7 (see the "Closed-Form Success Probability" and "Verification" subsections above) -- note explicitly that this is a stronger form of verification than a citation check (a direct empirical match against a construction paper's own formula), and that this project's own confidence in this citation was never downgraded (unlike McClean et al. or arXiv:2510.24137, this paper's formula was directly implemented and tested, not just cited).
  - **The other 10 named WRITE-02 baselines** (McClean et al., Aaronson-Brod, arXiv:2510.24137/Park & Oh, `docs/iqp-baseline.md`'s empirical rule, Bremner-Montanaro-Shepherd 2015 and 2017, Rudolph et al., Mhiri et al., Recio-Armengol et al., Herbst et al.) -- state in one short paragraph (not 10 table rows) that these are trainability and/or hardness-under-noise papers, none of which make a claim about gate construction, success probability, or postselection mechanics -- the actual subject of ARB-01/ARB-02. None are silently omitted; all are explicitly named as not applicable to this section's claim, with the reader pointed at `docs/trainability-study.md`'s and `docs/hardness-under-loss-study.md`'s own literature tables (added by parallel plans in this phase) for where those 10 baselines DO apply.
  </action>
  <verify>Read back the new subsection: confirm it sits between "Forge Verification" and "Conclusion and Open Questions", contains a scoped "what this does/doesn't establish" statement citing existing ARB-01/ARB-02 content (not re-deriving it), and a literature comparison table with arXiv:2405.01395's substantive verdict plus the other 10 baselines explicitly addressed via prose. Confirm the existing document-level "Conclusion and Open Questions" section is unmodified.</verify>
  <done>docs/iqp-photonic-encoding.md has a new ARB-01/ARB-02-scoped "what this does/doesn't establish" subsection with a literature comparison table -- WRITE-02 and WRITE-04 (ARB) are satisfied.</done>
</task>

</tasks>

<verification>
1. A new `### What ARB-01/ARB-02 does/doesn't establish` subsection exists, placed between "Forge Verification of the Ancilla Mode-Mapping" and "Conclusion and Open Questions".
2. The subsection states what ARB-01/ARB-02 establishes and does not establish, scoped to that section specifically (not restating the whole-document Conclusion).
3. A literature comparison table exists, with arXiv:2405.01395 given a substantive verdict and all 10 other WRITE-02 baselines explicitly addressed (via prose, not padding rows) as not applicable to this section's gate-construction claim.
4. `git diff docs/iqp-photonic-encoding.md` shows only the new subsection added -- no existing content (Ingredient 2, ARB-01/ARB-02's existing subsections, or the document-level Conclusion) altered.
</verification>

<success_criteria>
- WRITE-04 satisfied for ARB: a section-scoped "what this does/doesn't establish" statement now exists (the one structural gap this document had relative to TRAIN/HARD).
- WRITE-02 satisfied for ARB's table: all 11 baselines addressed, with the one genuinely relevant baseline given a real verdict and the rest explicitly (not silently) marked not applicable.
</success_criteria>

<output>
After completion, create `.planning/phases/20-technical-write-up/20-03-SUMMARY.md`
</output>
