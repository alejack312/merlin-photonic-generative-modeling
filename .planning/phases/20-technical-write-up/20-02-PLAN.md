---
phase: 20-technical-write-up
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/hardness-under-loss-study.md
autonomous: true

must_haves:
  truths:
    - "docs/hardness-under-loss-study.md's HARD-04/HARD-06 section contains a literature comparison table stating consistent/inconsistent/silent for all 11 named WRITE-02 baselines (5 with substantive HARD-specific reasoning, 6 briefly marked silent with a one-line reason)."
    - "docs/hardness-under-loss-study.md contains a short cross-reference note pointing at docs/trainability-study.md's TRAIN section, stating plainly whether HARD's measured result is consistent or inconsistent with Herbst et al.'s anticoncentration-tradeoff prediction, using the actual measured direction of alpha(eta) (decreasing as eta decreases -- MORE anticoncentrated under loss, not less)."
    - "The doc's stale header note (lines 9-12, claiming HARD-04/HARD-06 sections don't yet exist) is corrected -- those sections were added by Plan 18-08 and are present."
  artifacts:
    - path: "docs/hardness-under-loss-study.md"
      provides: "HARD literature comparison table, Herbst cross-reference note, corrected header"
      min_lines: 569
  key_links:
    - from: "docs/hardness-under-loss-study.md's Herbst cross-reference note"
      to: "docs/trainability-study.md's What this does/doesn't establish section"
      via: "prose pointer"
      pattern: "trainability-study"
---

<objective>
Add HARD's literature comparison table (WRITE-02) and Herbst et al. cross-reference note (success criterion 6) to `docs/hardness-under-loss-study.md`, and fix a stale header note left over from before Plan 18-08 completed the doc. Pure documentation synthesis -- no new experiments, no new numbers; every claim here already exists somewhere in this project's shipped docs/papers and is being organized/cross-referenced, not re-derived.

This plan is independent of Plan 20-01 (TRAIN) and Plan 20-03 (ARB) -- three different files, no dependency between them, per `20-RESEARCH.md`'s explicit sequencing analysis.

Purpose: satisfies WRITE-02 (HARD's table) and success criterion 6 (HARD half of the Herbst cross-thread).
Output: `docs/hardness-under-loss-study.md`, updated in place.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-technical-write-up/20-CONTEXT.md
@.planning/phases/20-technical-write-up/20-RESEARCH.md
@docs/hardness-under-loss-study.md
@docs/iqp-baseline.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add HARD's literature comparison table</name>
  <files>docs/hardness-under-loss-study.md</files>
  <action>
Insert a new `### Literature comparison table (WRITE-02)` subsection immediately before `### HARD-06: What this phase does and does not establish` (currently at line 534), i.e. after the "Dual/triple positioning" subsection ends (around line 532). Follow `docs/iqp-baseline.md`'s "Fresh Primary-Source Verification" section's established citation-precision style (cite theorem/section/page inline, then state relevance) rather than a terse table-only format. Cover all 11 WRITE-02 baselines; the following per-baseline assignment (planner's decision, made per `20-CONTEXT.md`'s delegated discretion) keeps the table HARD-focused and avoids padding it with irrelevant rows:

  Substantive rows (state consistent/inconsistent/silent with reasoning, citing the theorem/section already used elsewhere in this doc):
  - **Aaronson-Brod** (arXiv:1510.05245, Theorem 1, p.5) -- already extensively engaged in the "Dual/triple positioning" section above (fixed-loss-count regime); state the verdict this doc already reaches: this project's fractional-rate `eta` loss model structurally sits in the weak/constant-fraction regime the paper's own text calls insufficient for strong complexity claims -- point at the existing prose rather than re-deriving.
  - **arXiv:2510.24137 (Park & Oh), Theorem 1** -- already engaged (`docs/iqp-baseline.md`, this doc's positioning section); state as the closest structural match to this project's own per-mode-transmittance loss channel (both use a transmittance-`eta` beamsplitter-loss model), while explicitly noting Theorem 1 is an upper bound on ONE classical simulation method's efficiency, not a hardness lower bound -- never conflate with Section V's separate qubit-level-depolarizing "Noisy IQP" result, which is NOT the one being cited here.
  - **Bremner-Montanaro-Shepherd 2017** (arXiv:1610.01808, Theorem 4) -- state as "silent by owner decision" rather than a forced consistent/inconsistent verdict: this doc's own HARD-04 section already records the owner's explicit choice not to fabricate an eta-to-epsilon depolarizing-rate translation, so no numeric comparison against Theorem 4 can be honestly stated. Cite that decision directly (`### HARD-04/HARD-06` section, "Against BMS's depolarizing regime" subsection) rather than re-litigating it.
  - **Bremner-Montanaro-Shepherd 2015** (arXiv:1504.07999, Theorem 1, p.1) -- silent-with-context: this is the foundational (noiseless) IQP hardness threshold BMS-2017's noise extension builds on; it makes no noise/loss claim of its own to compare against HARD's loss sweep, so it is noted as background context rather than assigned a consistent/inconsistent verdict.
  - **Herbst et al.** (arXiv:2512.24801) -- this baseline's substantive content is the cross-reference note in Task 2 below; in this table row, state "see Cross-reference note below" plus a one-clause summary of the verdict reached there.

  Silent rows (one line each, stating why the baseline doesn't bear on HARD specifically): McClean et al. (barren-plateau protocol -- TRAIN-specific, no hardness/loss claim), arXiv:2405.01395 (two-photon gate construction -- ARB-specific), `docs/iqp-baseline.md`'s own empirical rule (qubit-side trainability rule -- TRAIN-specific), Rudolph et al. (arXiv:2305.02881, MMD bandwidth trainability -- TRAIN-specific), Mhiri et al. (arXiv:2502.07889, warm-start guarantees -- TRAIN-specific), Recio-Armengol et al. (arXiv:2503.02934, data-dependent init -- TRAIN-specific).
  </action>
  <verify>Read back the new subsection: confirm all 11 baselines are addressed (5 substantive + 6 one-line silent), each with a stated verdict, and that no existing content in the "Dual/triple positioning" or "HARD-06" sections was altered.</verify>
  <done>docs/hardness-under-loss-study.md has a literature comparison table covering all 11 WRITE-02 baselines with HARD-specific relevance framing.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Herbst cross-reference note and fix the stale header note</name>
  <files>docs/hardness-under-loss-study.md</files>
  <action>
**Part A -- Herbst et al. cross-reference note (success criterion 6):** Add a new short subsection at the very end of the document (after `### HARD-06`'s closing sentence "This closes HARD-04... completing all of HARD-01 through HARD-07 for Phase 18.") titled `### Cross-reference: Herbst et al.'s anticoncentration-tradeoff prediction`. State plainly whether HARD's measured result is consistent or inconsistent with Herbst et al.'s prediction that anticoncentration drives both sampling hardness and MMD-loss concentration (`docs/iqp-baseline.md`'s existing citation, "Fresh Primary-Source Verification" section). Use the actual measured direction, already reported in this doc's own Anticoncentration section (lines 213-243) -- do not invent or assume a direction:

  - `alpha(eta)` **decreases monotonically as eta decreases** (i.e. as photon loss increases) for both weight-1 and mixed scope, crossing below the `alpha=1` uniform-reference line at every measured `n` -- meaning the output distribution becomes MORE anticoncentrated (not less) as loss increases.
  - `docs/iqp-baseline.md`'s earlier speculative framing (written 2026-08-12, before this phase's real sweep existed) guessed the opposite direction ("if Phase 18 finds photon loss erodes anticoncentration... trainability should correspondingly improve at higher loss") -- state explicitly that the real measured direction is the reverse of that speculation (loss INCREASES anticoncentration, doesn't erode it), so the speculative note's premise does not hold as originally phrased. Correct this rather than silently ignoring the mismatch, matching this project's established pattern of catching and correcting its own earlier statements once real data exists.
  - Under Herbst et al.'s framework (anticoncentration predicted to drive BOTH increased classical-simulability-under-noise AND increased MMD-loss concentration, not a trade-off), the measured direction here (loss increases anticoncentration) would predict trainability should, if anything, get WORSE (not better) as loss increases -- the opposite conclusion from `docs/iqp-baseline.md`'s original speculative guess.
  - Point at `docs/trainability-study.md`'s equivalent note (added by a parallel plan in this phase) for the TRAIN-side half of this cross-reference -- do not restate TRAIN's own gradient-variance findings here, only point at them.
  - State explicitly that TRAIN and HARD do not share a common independent variable (TRAIN sweeps `n` at `eta=1` i.e. no loss; HARD sweeps `eta` at small fixed `n`) -- so this project cannot directly test Herbst's co-occurrence prediction on a single combined dataset. This is a qualitative, hedged cross-reference between two separately-measured trends, not a joint experiment -- state it with that hedge explicit (per this project's CLAUDE.md rule that Claude organizes/computes but does not assert interpretive conclusions the owner hasn't reviewed; flag this as requiring care rather than asserting it confidently).

**Part B -- fix the stale header note:** The document's own header (lines 9-12) currently states "This document does not yet contain HARD-04's depolarizing-translation positioning or HARD-06's final scope statement -- those sections are appended by Plan 18-08 after the owner's attempt-first checkpoint (see the placeholder heading at the end of this document)." This is stale -- Plan 18-08 already added both sections (visible at lines 362-569). Update this paragraph to state plainly that the document is complete, listing all sections it now contains (methodology, HARD-01/02/05/07 results, anticoncentration, HARD-04/HARD-06 positioning and scope statement, and this phase's added literature table + Herbst cross-reference note).
  </action>
  <verify>Read back the document: confirm (a) a Herbst cross-reference note exists at the end of the document using the correct measured direction (alpha decreases as eta decreases), (b) it explicitly corrects the iqp-baseline.md speculative note's direction, (c) it points at docs/trainability-study.md, (d) the stale header paragraph (lines 9-12) no longer claims HARD-04/HARD-06 are missing.</verify>
  <done>docs/hardness-under-loss-study.md has a Herbst cross-reference note using the real measured direction and an accurate, non-stale header -- success criterion 6 (HARD half) is satisfied.</done>
</task>

</tasks>

<verification>
1. A literature comparison table exists in the `## HARD-04/HARD-06` section covering all 11 WRITE-02 baselines with a stated consistent/inconsistent/silent verdict (or equivalent reasoning) each.
2. A Herbst et al. cross-reference note exists at the end of the document, using the doc's own already-measured alpha(eta) direction (decreasing as eta decreases), explicitly correcting `docs/iqp-baseline.md`'s earlier speculative direction, and pointing at `docs/trainability-study.md`.
3. The stale header note (lines 9-12) is corrected.
4. `git diff docs/hardness-under-loss-study.md` shows only additions/the header correction -- no prior HARD-01..07 content deleted or altered.
</verification>

<success_criteria>
- WRITE-02 satisfied for HARD's table (all 11 baselines addressed).
- Success criterion 6 (HARD half) satisfied: explicit statement of whether HARD's result is consistent/inconsistent with Herbst et al.'s prediction, using the real measured direction, correcting the earlier speculative note.
- Stale header note fixed for internal document consistency.
</success_criteria>

<output>
After completion, create `.planning/phases/20-technical-write-up/20-02-SUMMARY.md`
</output>
