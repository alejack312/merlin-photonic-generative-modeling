---
phase: 08-literature-scoping-prerequisites
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/iqp-baseline.md
autonomous: true

must_haves:
  truths:
    - "A single reference doc exists compiling prior IQP circuit-structure/hardness notes and barren-plateau trainability findings as the qubit-side baseline for Phase 9"
    - "Both IQP structure/hardness and barren-plateau trainability are covered with roughly equal weight, per 08-CONTEXT.md"
    - "The doc states which specific source files it draws from, matching this repo's grounding-statement convention"
  artifacts:
    - path: "docs/iqp-baseline.md"
      provides: "1-2 page qubit-side IQP + barren-plateau reference doc for Phase 9 comparison"
      min_lines: 40
      contains: "## "
  key_links:
    - from: "docs/iqp-baseline.md"
      to: "C:\\Users\\cuqui\\iqp-mmd-barren-plateau\\docs\\technical\\iqp-classical-sampling.md and the iqp-mmd-barren-plateau-vault Theory/Report files"
      via: "explicit source citation in the grounding statement"
      pattern: "iqp-classical-sampling|Barren Plateaus|Final Findings"
---

<objective>
Compile prior IQP circuit-structure/hardness and barren-plateau-trainability material from the sibling `iqp-mmd-barren-plateau` project into a single short reference doc, `docs/iqp-baseline.md` — the qubit-side baseline Phase 9 will compare its photonic encoding design against.

Purpose: Phase 9's encoding design needs a compact, scannable statement of "what does IQP look like / how trainable is it on the qubit side" without re-deriving it from scratch or re-reading the sibling project's full paper stash each time.
Output: `docs/iqp-baseline.md`, 1-2 pages, following this repo's existing doc house style.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-literature-scoping-prerequisites/08-CONTEXT.md
@.planning/phases/08-literature-scoping-prerequisites/08-RESEARCH.md
@docs/mmd-loss.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read the primary source material</name>
  <files>(no files modified — reading only)</files>
  <action>
    Read, in full, these three files from the sibling project (paths verified present in `08-RESEARCH.md`):
    - `C:\Users\cuqui\iqp-mmd-barren-plateau\docs\technical\iqp-classical-sampling.md` — IQP circuit structure (Hadamard-sandwiched commuting diagonal gates), the classical-training trick (Van den Nest's cosine formula for `⟨Z_a⟩`), and why IQP's commuting structure makes this tractable.
    - `C:\Users\cuqui\iqp-mmd-barren-plateau\iqp-mmd-barren-plateau-vault\Theory\Barren Plateaus.md` — the barren-plateau definition (`Var[∂L/∂θᵢ] = O(2^{-αn})`), the "does IQP's structured gate set protect trainability" framing, average-case-vs-pointwise framing (Larocca et al. 2024).
    - `C:\Users\cuqui\iqp-mmd-barren-plateau\iqp-mmd-barren-plateau-vault\Report\Final Findings - IQP MMD Barren Plateaus.md` — the empirical answer: structure alone doesn't predict plateaus; best empirical rule `plateau if small_angle OR (uniform AND n>=6 AND NOT complete_graph_like)` (97.9% accuracy across 283 rows); anti-concentration alone is an insufficient success criterion.

    Extract, for each of the two halves (IQP structure/hardness; barren-plateau trainability), the 4-6 key facts/formulas/findings worth carrying into a 1-2 page summary — don't just re-read passively, identify what's load-bearing for a Phase 9 reader who needs to know "what does IQP look like on the qubit side, and is it trainable."
  </action>
  <verify>All three source files were read in full (not skimmed); a clear list of extracted key facts for each half exists (can be informal working notes, not a deliverable itself).</verify>
  <done>The two halves of source material (structure/hardness, barren-plateau trainability) are understood well enough to write a tight, accurate 1-2 page summary without re-opening the source files.</done>
</task>

<task type="auto">
  <name>Task 2: Write docs/iqp-baseline.md</name>
  <files>docs/iqp-baseline.md</files>
  <action>
    Write `docs/iqp-baseline.md` following this repo's house doc style (see `docs/mmd-loss.md`: grounding statement up top, section-per-topic headers, code/quote blocks with source attribution when quoting formulas). Target 1-2 pages (roughly 80-150 lines) — a scannable reference doc, not a deep-dive like `docs/mmd-loss.md` or `docs/raster-order.md`.

    Structure:
    - Opening grounding statement: name the exact three source files read in Task 1 (both repo-relative sibling-project paths), stating this is compiled from them, not re-derived from memory or a fresh literature pass.
    - `## IQP Circuit Structure & Hardness` — cover: qubits start in a fixed basis, a layer of commuting Z-diagonal gates is applied (no fixed ordering needed — "instantaneous"), measurement in the Hadamard-conjugate (X) basis; the classical-simulability trick for computing `⟨Z_a⟩` exactly (Van den Nest's cosine formula) and why commuting structure enables it; briefly note this is the same structure Douce et al. builds its CV analogue from (cross-reference `docs/iqp-lit-scoping.md` by name if useful, but don't duplicate its content).
    - `## Barren-Plateau Trainability` — cover: the formal definition (`Var[∂L/∂θᵢ] = O(2^{-αn})`), the open question the sibling project investigated (does IQP's structured/commuting gate set protect against the plateaus that random deep circuits suffer), and the empirical resolution found (structure alone doesn't predict plateaus; the specific empirical rule and its accuracy/precision/recall numbers; anti-concentration alone being an insufficient success criterion).
    - `## Why This Matters for Phase 9` — a short (3-5 sentence) bridging paragraph: Phase 9's photonic DV encoding design should keep both qubit-side properties (structural hardness argument AND known trainability behavior) in mind as the target to preserve or knowingly trade off when mapping to Fock-space primitives.

    Keep prose tight — this is a reference glossary for a future reader (including the owner, unaided), not a persuasive essay.
  </action>
  <verify>`docs/iqp-baseline.md` exists, opens with a grounding statement naming the three source files, contains the three `##` sections listed above, and is roughly 80-150 lines (1-2 page cap respected).</verify>
  <done>PREQ-02 is satisfied: prior IQP structure/hardness and barren-plateau notes are compiled into one short reference doc covering both with roughly equal weight, as the qubit-side baseline for Phase 9.</done>
</task>

</tasks>

<verification>
- `docs/iqp-baseline.md` exists, under the 1-2 page cap, with a grounding statement citing its three source files by exact path.
- Both IQP structure/hardness and barren-plateau trainability sections are present and roughly balanced in length.
- The doc follows this repo's established house style (see `docs/mmd-loss.md`, `docs/raster-order.md` precedent).
</verification>

<success_criteria>
PREQ-02 is satisfied: `docs/iqp-baseline.md` exists as a compact, grounded qubit-side reference doc Phase 9 can cite directly for comparison, without needing to re-read the sibling project's full paper stash.
</success_criteria>

<output>
After completion, create `.planning/phases/08-literature-scoping-prerequisites/08-03-SUMMARY.md`
</output>
