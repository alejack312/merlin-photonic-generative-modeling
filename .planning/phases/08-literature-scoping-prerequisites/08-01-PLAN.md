---
phase: 08-literature-scoping-prerequisites
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/iqp-lit-scoping.md
autonomous: false

must_haves:
  truths:
    - "A go/no-go verdict on Phase 9 (encoding design) is written down explicitly, citing both a constructive and a disconfirming search pass, with the owner's own stated reasoning recorded"
    - "Douce et al.'s CV construction is summarized from a full-text read (not abstract-level) well enough to correctly position a DV/Fock-space design against it"
    - "The verdict and summary explicitly distinguish CV-quadrature (squeezed light + homodyne) from Fock-space/photon-number encoding, so the two are never conflated"
    - "MerLin's reproduced-papers catalog check (LIT-03, already complete) is cited in the writeup for coverage, not silently omitted"
  artifacts:
    - path: "docs/iqp-lit-scoping.md"
      provides: "Combined LIT-01/LIT-02/LIT-04 deliverable: Douce et al. summary, literature search methodology + findings, go/no-go verdict"
      min_lines: 60
      contains: "## Go/No-Go Verdict"
  key_links:
    - from: "docs/iqp-lit-scoping.md's verdict section"
      to: "the search findings and Douce et al. summary sections in the same doc"
      via: "explicit citation (e.g. 'per the search findings above' / 'per the Douce et al. summary above')"
      pattern: "above"
---

<objective>
Produce the literature-scoping deliverable for Phase 8: a full-text-grounded summary of Douce et al. (2017)'s CV-IQP hardness construction (LIT-02), an extended/independent literature search pass for any existing discrete-variable (DV)/Fock-space linear-optical IQP construction (LIT-01, building on but not merely repeating `08-RESEARCH.md`'s WebSearch pass), and an explicit, owner-authored go/no-go verdict on whether Phase 9 (encoding design) should proceed (LIT-04).

Purpose: This is the milestone's core gating decision — Phase 9 does not start unless this verdict says "go." Per this repo's CLAUDE.md, core interpretive decisions are the owner's, not Claude's to make unilaterally; Claude's job here is to compile and present the evidence, then record the owner's own stated verdict.
Output: `docs/iqp-lit-scoping.md`, containing the Douce et al. summary, the literature search record, and the recorded go/no-go verdict with reasoning.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-literature-scoping-prerequisites/08-CONTEXT.md
@.planning/phases/08-literature-scoping-prerequisites/08-RESEARCH.md
@docs/mmd-loss.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Douce et al. full-text summary (LIT-02)</name>
  <files>docs/iqp-lit-scoping.md</files>
  <action>
    Create `docs/iqp-lit-scoping.md` following this repo's doc house style (see `docs/mmd-loss.md` for the pattern: grounding statement up top citing exactly what was read, section-per-topic headers, code/quote blocks with source attribution).

    Open with a grounding statement: this doc is grounded in a full-text read of Douce et al. (PRL 118, 070503, 2017, arXiv:1607.07605) — cite `08-RESEARCH.md`'s LIT-02 section as the source of the verified full-text findings (already read and confirmed in this repo's research pass; re-use its content directly rather than re-deriving from memory).

    Write a "## Douce et al. (2017) — CV-IQP Hardness Construction" section covering, in your own words (not copy-pasted verbatim from RESEARCH.md):
    - The DV-IQP recap the paper builds from (qubits in `|+⟩`, commuting Z-diagonal gates, X-basis measurement).
    - The CV analogue: squeezed states in the p̂ quadrature, gates diagonal in q̂ (`Ẑ`, `ĈZ`, `T̂`), finite-resolution homodyne detection of p̂. State explicitly that "Hadamard-basis conjugation" here is realized via the Fourier transform operator sandwiching the q̂-diagonal gates, implemented through a measurement-based Fourier gadget (not a literal Hadamard gate).
    - The 3-4 step hardness argument (Fourier gadget → universal gate set; GKP error correction → `CVrMBQC = BQP`; post-selection maps `PostBQP`; finite-squeezing scaling requirement).
    - A dedicated, explicit paragraph stating why this does NOT transfer to a Fock-space/photon-number linear-optical construction: this paper's "CV" means continuous-quadrature (squeezed light + homodyne), a fundamentally different formalism from Fock-space/photon-counting linear optics (what Perceval/MerLin actually simulate) — the paper contains no mention of photon-number/Fock-basis measurement or discrete linear-optical networks. Use this to explicitly guard against conflating the two senses of "CV" (per `08-RESEARCH.md`'s flagged pitfall).
  </action>
  <verify>File `docs/iqp-lit-scoping.md` exists, opens with a grounding statement naming the specific source (Douce et al. full-text read, arXiv:1607.07605), and contains a section explicitly distinguishing CV-quadrature from Fock-space/photon-number encoding.</verify>
  <done>Douce et al.'s construction is summarized accurately enough that a reader unfamiliar with the paper could correctly explain why it doesn't hand you a DV/Fock-space IQP construction.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the literature search for an existing DV/Fock-space IQP construction (LIT-01)</name>
  <files>docs/iqp-lit-scoping.md</files>
  <action>
    Append a "## Literature Search (LIT-01)" section to `docs/iqp-lit-scoping.md`.

    First, cite `08-RESEARCH.md`'s existing WebSearch-based scan as a starting point: list the query strings it already tried (direct-keyword and structural-analogue queries) and what surfaced (boson sampling, Gaussian boson sampling, Jabbour & Novo arXiv:2310.06034, "Unified boson sampling" PRR 7 L042068, standard DV-IQP theory papers) — none of which matched.

    Then run a genuinely independent second pass using WebSearch with DIFFERENT phrasing than what's already listed in `08-RESEARCH.md` — per its own caveat, a single WebSearch pass is not sufficient evidence for LIT-04's "citing both constructive and disconfirming search passes" bar. Try at minimum:
    - Citation-chase framing: search for what cites Douce et al. (arXiv:1607.07605) or what it cites for the DV-IQP definition, to see if any citing/cited work proposes a linear-optical Fock-space analogue.
    - Alternate terminology: "sampling complexity linear optical network commuting gates", "photon number IQP-like circuit hardness", "discrete variable analogue continuous variable instantaneous quantum computing".
    - A targeted check of arXiv listings (via WebSearch site-scoped queries) for very recent (2024-2026) preprints using "IQP" + "linear optic" or "IQP" + "Perceval"/"boson sampling" together.

    Record: exact queries run, what surfaced (title + one-line relevance note for each), and whether anything constitutes (a) a matching construction, (b) an impossibility/no-go result, or (c) neither (consistent with `08-RESEARCH.md`'s finding). State the result of this second pass explicitly — do not silently merge it into the first pass's conclusion without noting whether it corroborated or changed anything.

    Also cite LIT-03 (MerLin's reproduced-papers catalog, already verified 2026-07-30 per REQUIREMENTS.md — all 21 titles checked, none IQP-adjacent) as a one-line coverage note in this section.
  </action>
  <verify>The section lists at least 3 new/differently-phrased queries beyond `08-RESEARCH.md`'s original list, states what surfaced for each, and gives an explicit constructive-vs-disconfirming conclusion. LIT-03 is cited by name.</verify>
  <done>The doc now has a second, independently-conducted search pass on record (not just a restatement of `08-RESEARCH.md`'s pass), satisfying LIT-04's evidentiary requirement to cite more than one pass.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>LIT-04: go/no-go verdict on proceeding to Phase 9 (encoding design)</decision>
  <context>
    Tasks 1 and 2 above compiled the evidence: Douce et al.'s CV construction does not transfer to Fock-space (Task 1), and two independent search passes (RESEARCH.md's original + Task 2's extension) found no DV/Fock-space linear-optical IQP construction and no impossibility result (Task 2).

    Per `08-CONTEXT.md`'s locked bar:
    - **Go**: no blocking impossibility result found. Deliberately low bar — a full constructive mapping is NOT required (that's Phase 9's job).
    - **Not-ready**: only if an explicit impossibility/no-go finding was found (a real argument/result that DV linear optics cannot realize IQP's structure).
    - **Promising but needs more time**: only for a genuine partial/ambiguous lead worth deeper follow-up — not a catch-all for "found nothing." A time-box expiring with nothing found defaults to "go," not this option.

    Per this repo's CLAUDE.md ("Do not shortcut" — core design decisions and anything the owner must explain unaided are the owner's call, not Claude's to make silently), this verdict should be stated by the owner, in their own words, informed by the evidence above — not pre-written by Claude for rubber-stamp approval.
  </context>
  <options>
    <option id="go">
      <name>Go — proceed to Phase 9</name>
      <pros>No blocking impossibility found across two independent search passes; matches CONTEXT.md's stated default when the time-box expires with nothing found</pros>
      <cons>Neither search pass was an exhaustive manual citation-chasing crawl — some residual risk a deeper pass would surface something (worth stating as a caveat regardless of verdict)</cons>
    </option>
    <option id="not-ready">
      <name>Not ready — do not proceed to Phase 9</name>
      <pros>Correct only if Task 2 surfaced an explicit impossibility/no-go argument</pros>
      <cons>Not supported by current evidence unless such a finding exists — must cite the specific obstruction if chosen</cons>
    </option>
    <option id="promising-needs-more-time">
      <name>Promising but needs more time — deeper follow-up pass before Phase 9</name>
      <pros>Appropriate only if Task 2 surfaced a genuine partial/ambiguous lead worth chasing</pros>
      <cons>Not valid for "found nothing" per CONTEXT.md — only choose this if a specific lead is named</cons>
    </option>
  </options>
  <resume-signal>State your verdict (go / not-ready / promising-but-needs-more-time) and your reasoning, in your own words, referencing the evidence above. Claude will record it as the "## Go/No-Go Verdict" section of `docs/iqp-lit-scoping.md`, attributed to you, lightly edited only for clarity (not substance).</resume-signal>
</task>

</tasks>

<verification>
- `docs/iqp-lit-scoping.md` exists and contains three sections: Douce et al. summary, literature search record, and go/no-go verdict.
- The verdict section is attributed to the owner's own stated reasoning, not a Claude-authored conclusion the owner merely approved.
- The CV-quadrature vs. Fock-space distinction is stated explicitly at least once, unambiguously.
- LIT-03's completion is cited for coverage.
</verification>

<success_criteria>
LIT-01, LIT-02, and LIT-04 requirements are satisfied: a go/no-go verdict exists citing both a constructive and disconfirming search pass, Douce et al. is summarized from a full-text read well enough to position a DV design against it, and the verdict's reasoning is explicit and owner-authored.
</success_criteria>

<output>
After completion, create `.planning/phases/08-literature-scoping-prerequisites/08-01-SUMMARY.md`
</output>
