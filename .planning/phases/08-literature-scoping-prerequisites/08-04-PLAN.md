---
phase: 08-literature-scoping-prerequisites
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - perceval_fluency_demo.py
  - tests/test_perceval_fluency_demo.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "perceval_fluency_demo.py instantiates and uses pcvl.PS (phase shifter) to build a circuit, not just Circuit/BS/BasicState/Processor/Analyzer"
    - "The PS-based construction's output is checked against a closed-form prediction programmatically (not eyeballed)"
    - "The demo shows the PS phase value visibly and predictably changing the output distribution via interference (Mach-Zehnder-style BS->PS->BS), demonstrating why PS matters rather than a case where phase is invisible to Fock-basis measurement"
  artifacts:
    - path: "perceval_fluency_demo.py"
      provides: "Third example: BS.H()->PS(theta)->BS.H() Mach-Zehnder-style construction with a closed-form probability check that varies by phase, run across multiple theta values"
      contains: "pcvl.PS("
    - path: "tests/test_perceval_fluency_demo.py"
      provides: "Automated test(s) asserting the MZI output distribution matches the cos^2(theta/2)/sin^2(theta/2) closed form across multiple theta values"
  key_links:
    - from: "perceval_fluency_demo.py"
      to: "perceval.PS"
      via: "circuit.add(0, pcvl.PS(theta)) placed between two BS.H() components, forming a BS-PS-BS Mach-Zehnder interferometer"
      pattern: "pcvl\\.PS\\("
---

<objective>
Close the one gap `08-VERIFICATION.md` found in Phase 8's PREQ-01 deliverable: `perceval_fluency_demo.py` never uses `pcvl.PS` (phase shifter), despite `PS` being explicitly named in `08-CONTEXT.md`'s locked scope and ROADMAP.md's Phase 8 success criterion 4 as one of the five primitives the fluency demo must exercise.

Add a third example to the existing, already-passing demo: a Mach-Zehnder-style `BS.H() -> PS(theta) -> BS.H()` construction, where the phase shifter's angle visibly and predictably changes the output distribution via interference — a more compelling demonstration of `PS` fluency than a "phase is invisible without interference" case, and directly relevant groundwork for Phase 9's encoding design (which will need to reason about phase-driven interference).

This is boilerplate/API-lookup work on an already-attempted, already-implemented demo (per this repo's CLAUDE.md, MerLin/Perceval API syntax lookups are offloadable), not a new conceptual decision — no attempt-first checkpoint needed.

Purpose: Fully satisfy PREQ-01 and ROADMAP.md's Phase 8 success criterion 4 (a working manual Perceval circuit exercising `Circuit`/`PS`/`BS`/`BasicState`/`Analyzer`), closing Phase 8's only outstanding verification gap.

Output: Updated `perceval_fluency_demo.py` (third example + closed-form check) and updated `tests/test_perceval_fluency_demo.py` (test coverage for the new example).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-literature-scoping-prerequisites/08-CONTEXT.md
@.planning/phases/08-literature-scoping-prerequisites/08-VERIFICATION.md
@perceval_fluency_demo.py
@tests/test_perceval_fluency_demo.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a BS-PS-BS Mach-Zehnder example exercising PS, with a closed-form interference check</name>
  <files>perceval_fluency_demo.py, tests/test_perceval_fluency_demo.py</files>
  <action>
    In `perceval_fluency_demo.py`:

    - Remove the existing dead `phase_shift(circuit, angle)` helper (lines ~56-59) — it is currently unused stub code (`pcvl.PS(angle)` added but never called from anywhere), and is being replaced by a real, exercised construction below rather than left as duplicate/confusing dead code.

    - Add `build_mzi_circuit(theta)`: a 2-mode `pcvl.Circuit(2)` built as `BS.H() -> PS(theta) on mode 0 -> BS.H()`:
      ```python
      def build_mzi_circuit(theta):
          """Mach-Zehnder-style circuit: BS.H() -> PS(theta) on mode 0 -> BS.H().
          Demonstrates that PS's phase value visibly and predictably changes the
          output distribution once there's a second beamsplitter to interfere
          against (unlike a bare PS, whose phase is invisible to a Fock-basis
          photon-number measurement with no interferometer)."""
          circuit = pcvl.Circuit(2)
          circuit.add(0, pcvl.BS.H())
          circuit.add(0, pcvl.PS(theta))
          circuit.add(0, pcvl.BS.H())
          return circuit
      ```

    - Add `run_mzi_analyzer(theta)`, following the same Processor+Analyzer pattern as `run_analyzer()`, for a single-photon input `pcvl.BasicState([1, 0])`, returning `(ca, dist)` where `dist` is `{BasicState: probability}`.

    - Add `check_mzi(dist, theta)`: closed-form check using the Hadamard-BS-derived prediction `P(|1,0>) = cos(theta/2)**2`, `P(|0,1>) = sin(theta/2)**2` (derivation: two `BS.H()` matrices `1/sqrt(2)*[[1,1],[1,-1]]` sandwiching a phase `e^{i*theta}` on mode 0 applied to input `(1,0)` gives output amplitudes `((e^{i*theta}+1)/2, (e^{i*theta}-1)/2)`). Use `np.isclose(..., atol=TOLERANCE)` for both terms, same tolerance as the existing checks.

    - In `main()`, after the existing two examples, run the MZI example across `theta` values `[0, np.pi/2, np.pi]` (0 = fully constructive on mode 0, pi/2 = 50/50, pi = fully flipped to mode 1 — a visible, predictable sweep driven purely by the PS angle). For each theta: print the distribution and an explicit "closed-form check: PASS/FAIL" line (matching the existing PASS/FAIL reporting convention), and assert all three pass.

    - Update the module-level docstring/comment block to document the third example's closed-form facts (mirroring the existing "Closed-form facts being verified" comment style), including the amplitude derivation above.

    In `tests/test_perceval_fluency_demo.py`:

    - Import `build_mzi_circuit` (or `run_mzi_analyzer`/`check_mzi`, whichever the implementation exposes) alongside the existing imports.
    - Add `test_mzi_interference_theta_zero`, `test_mzi_interference_theta_half_pi`, `test_mzi_interference_theta_pi` (or a single parametrized test via `@pytest.mark.parametrize` over `[0, np.pi/2, np.pi]` — either is fine, keep it simple and consistent with the existing plain-function test style) asserting `check_mzi(dist, theta)` for each value.
  </action>
  <verify>
    `python perceval_fluency_demo.py` runs to completion and prints PASS for all checks, including the three MZI theta values.
    `pytest tests/test_perceval_fluency_demo.py -v` passes, including the new MZI test(s).
    `grep -n "pcvl.PS(" perceval_fluency_demo.py` shows PS is now actually instantiated and used in a circuit (not just named in a comment).
    `grep -n "QuantumLayer.simple" perceval_fluency_demo.py tests/test_perceval_fluency_demo.py` returns no matches.
  </verify>
  <done>perceval_fluency_demo.py builds and runs a BS->PS->BS Mach-Zehnder circuit whose output distribution is asserted (not eyeballed) to match cos^2(theta/2)/sin^2(theta/2) for theta in {0, pi/2, pi}, demonstrating that PS's phase value predictably drives interference-based output changes. Both the script and its test suite pass. PS is now a genuinely exercised primitive in the fluency demo, not just a name in a comment.</done>
</task>

</tasks>

<verification>
- `python perceval_fluency_demo.py` exits 0 and prints explicit PASS confirmations for all closed-form checks (single-photon split, HOM dip, and all three MZI theta values).
- `pytest tests/test_perceval_fluency_demo.py` passes (existing 3 tests + new MZI test(s)).
- `pcvl.PS(` appears in `perceval_fluency_demo.py` as an actual instantiation wired into a circuit via `circuit.add(...)`, not just a comment.
- Neither file imports or calls `QuantumLayer.simple()`.
- The dead `phase_shift()` stub is gone (no leftover unused code referencing PS separately from the new construction).
</verification>

<success_criteria>
PREQ-01 and ROADMAP.md's Phase 8 success criterion 4 are fully satisfied: `perceval_fluency_demo.py` exercises all five named primitives (`Circuit`, `PS`, `BS`, `BasicState`, `Analyzer`) directly, without `QuantumLayer.simple()`, and every example's output — including the new PS-driven interference example — is verified against a closed-form prediction programmatically. The single gap identified in `08-VERIFICATION.md` (score 9/10) is closed, bringing Phase 8 to full verification.
</success_criteria>

<output>
After completion, create `.planning/phases/08-literature-scoping-prerequisites/08-04-SUMMARY.md`
</output>
