---
phase: 12-exact-reference-extension-tvd-validation
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - results/phase12_weight2_tvd_validation_summary.md
autonomous: true

must_haves:
  truths:
    - "A results/phase12_*.md write-up documents the measured TVD, herald-success/failure probability, and residual numbers, matching the results/phase7_*_summary.md precedent (numbers only, no owner interpretation baked in)"
    - "The confirmed Processor.add_herald() + PBS-containing circuit crash is filed as an upstream Perceval/Quandela bug report, or -- if filing is genuinely blocked (no repo access/auth) -- fully drafted and saved to disk with the blocker stated plainly"
  artifacts:
    - path: "results/phase12_weight2_tvd_validation_summary.md"
      provides: "Written record of Phase 12's measured TVD/herald/residual numbers, matching phase7's summary format"
  key_links:
    - from: "results/phase12_weight2_tvd_validation_summary.md"
      to: "photonic_weight2_iqp_distribution(2, 0, 1, [0.0, 0.0]) and exact_qubit_iqp_distribution(2, [0.0, 0.0], {(0,1): pi/4})"
      via: "actual re-run of Plan 12-01's locked TVD gate, numbers copy-pasted from real execution, not invented"
      pattern: "TVD"
---

<objective>
Produce the phase's required written record: a `results/phase12_*.md` summary documenting the measured TVD/herald/residual numbers (matching the `results/phase7_*_summary.md` precedent), and file the confirmed `Processor.add_herald()` + `PBS` crash as an upstream Perceval/Quandela GitHub issue.

Purpose: CONTEXT.md locks both function-level 3-tuple reporting (done in Plan 12-01) AND a written summary artifact as separate deliverables -- the write-up is the durable, human-readable record of Phase 12's validation result, matching this project's existing `results/phase7_*_summary.md` reporting convention (Claude computes/plots, owner interprets). The upstream bug report is low-cost (the repro is already fully characterized in 12-RESEARCH.md and 11-02-SUMMARY.md) and directly relevant to the Vincent Espitalier / Quandela conversation this project is building toward.
Output: `results/phase12_weight2_tvd_validation_summary.md`; an upstream GitHub issue filed against Quandela's Perceval repo (or a fully-drafted issue saved to disk if filing is blocked).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-exact-reference-extension-tvd-validation/12-RESEARCH.md
@.planning/phases/12-exact-reference-extension-tvd-validation/12-CONTEXT.md
@results/phase7_neighbor_locality_summary.md
@iqp_photonic_encoding.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write results/phase12_weight2_tvd_validation_summary.md</name>
  <files>results/phase12_weight2_tvd_validation_summary.md</files>
  <action>
Actually re-run Plan 12-01's locked TVD gate and the n=3 opportunistic test (if it was included) in a scratch Python invocation against the real, committed `iqp_photonic_encoding.py` -- do not invent or transcribe numbers from memory or from 12-RESEARCH.md's earlier scratch-script runs. Capture:
- The locked gate: n=2, i=0, j=1, θ=π/4 -- `qubit_dist = exact_qubit_iqp_distribution(2, [0.0, 0.0], pair_thetas={(0,1): np.pi/4})`, `photonic_dist, residual, herald_failure_prob = photonic_weight2_iqp_distribution(2, 0, 1, [0.0, 0.0])`, `tvd = total_variation_distance(qubit_dist, photonic_dist)`.
- If Plan 12-01 included the opportunistic n=3 test, capture its numbers too as a separate, clearly-labeled supplementary result (matching phase7's own "primary result" + "supplementary" separation pattern).

Write `results/phase12_weight2_tvd_validation_summary.md` following `results/phase7_neighbor_locality_summary.md`'s structure exactly:
- Title + brief context (what's being validated, one line).
- A "Locked planning decisions" section is not needed here (that's phase7-specific) -- instead include a short "Method" section: 1-3 sentences stating the measurement strategy (bare herald-unregistered processor + manual post-selection + `{P:V}`-annotated ancilla, avoiding two confirmed Perceval bugs -- cite 12-RESEARCH.md).
- A results table: `n | (i,j) | theta | TVD | herald_success_prob | herald_failure_prob | residual | passed (TVD<1e-6)`.
- If n=3 was included, a separate "Supplementary" table for it, same columns.
- NO interpretation/conclusion section with Claude's own analysis -- per this repo's CLAUDE.md convention ("Claude computes/plots, the owner interprets") and CONTEXT.md's explicit lock ("numbers only, no owner interpretation baked in"). Leave a final `## Interpretation` heading with a single placeholder line: `(Owner's interpretation — not yet written.)` -- do not write Claude's own conclusion under it, unlike phase7's summary which does include one (phase7 predates this explicit CONTEXT.md-locked constraint for Phase 12; follow Phase 12's own locked rule, not phase7's literal precedent, on this one point).
  </action>
  <verify>test -f results/phase12_weight2_tvd_validation_summary.md && grep -q "TVD" results/phase12_weight2_tvd_validation_summary.md</verify>
  <done>`results/phase12_weight2_tvd_validation_summary.md` exists, contains the actual re-measured TVD/herald/residual numbers from a real execution against the committed code (not invented), and ends with an unwritten `## Interpretation` placeholder rather than a Claude-authored conclusion.</done>
</task>

<task type="auto">
  <name>Task 2: File the upstream Perceval/Quandela bug report</name>
  <files>results/phase12_weight2_tvd_validation_summary.md</files>
  <action>
Write up the confirmed `Processor.add_herald()` + `PBS`-containing circuit -> `Processor.probs()` crash as a GitHub issue, using the repro already fully characterized in 12-RESEARCH.md (Pitfall 3) and 11-02-SUMMARY.md (Issues Encountered). Include: perceval-quandela version (1.2.4, pinned in this repo's venv), a minimal repro description (a `Processor` with any `PBS`-containing circuit + at least one `add_herald()` call + a call to `.probs()`), the exact error (`ValueError: matmul: Input operand 1 has a mismatch in its core dimension 0 ...` -- shape varies by mode count, e.g. "size 8 is different from 12" / "size 12 is different from 16"), confirmation that it's independent of thetas/state_prep, and -- as a clearly-labeled "observed workaround, mechanism not fully understood" aside -- the separate, unrelated `{P:V}` ancilla-annotation finding from 12-RESEARCH.md (mention only as related context, not as a fix for THIS crash, since research confirmed the crash persists even with `{P:V}` applied).

Attempt to file this via `gh issue create --repo Quandela/Perceval --title "..." --body "..."` (check the correct upstream repo slug first via `gh repo view Quandela/Perceval` or a quick search if `Quandela/Perceval` doesn't resolve -- Perceval's actual GitHub org/repo name should be verified, not assumed). If `gh` succeeds, capture the issue URL.

If filing fails for any reason (auth, no write access to file issues on an external public repo, repo slug not found, etc.), do NOT fabricate a URL or silently skip this -- save the fully-drafted issue text to `results/phase12_perceval_bug_report_draft.md` instead, and note in the Plan 12-02 SUMMARY that it needs manual submission (this is a reporting gap to flag honestly, not a blocker requiring a checkpoint -- filing a courtesy upstream bug report is not on this phase's critical path for WT2-02/03/05/06).

Add a one-line pointer to the filed issue (URL) or the draft file path at the bottom of `results/phase12_weight2_tvd_validation_summary.md`'s Method section.
  </action>
  <verify>gh issue view --repo Quandela/Perceval &lt;issue-number&gt; 2>/dev/null || test -f results/phase12_perceval_bug_report_draft.md</verify>
  <done>Either a live GitHub issue URL against the correct upstream Perceval repo, or a complete draft saved to `results/phase12_perceval_bug_report_draft.md` with the gap honestly noted in the plan's SUMMARY.</done>
</task>

</tasks>

<verification>
Confirm `results/phase12_weight2_tvd_validation_summary.md` exists with real re-measured numbers (not transcribed from 12-RESEARCH.md's earlier scratch runs) and an unwritten `## Interpretation` placeholder. Confirm the upstream bug report was either filed (URL captured) or fully drafted to disk with the gap honestly flagged.
</verification>

<success_criteria>
- `results/phase12_weight2_tvd_validation_summary.md` exists, numbers-only, matches phase7's format, interpretation left to the owner.
- Upstream Perceval crash filed as a GitHub issue or fully drafted with the gap honestly noted.
</success_criteria>

<output>
After completion, create `.planning/phases/12-exact-reference-extension-tvd-validation/12-02-SUMMARY.md`
</output>
