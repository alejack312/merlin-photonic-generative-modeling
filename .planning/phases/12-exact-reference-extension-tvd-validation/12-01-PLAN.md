---
phase: 12-exact-reference-extension-tvd-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - iqp_photonic_encoding.py
  - tests/test_iqp_photonic_encoding.py
autonomous: true

must_haves:
  truths:
    - "exact_qubit_iqp_distribution accepts an optional pair_thetas dict for Z_i*Z_j terms, and pair_thetas=None reproduces the pre-Phase-12 weight-1-only output exactly (no existing test breaks)"
    - "A herald-conditioned weight-2 photonic distribution can be computed from build_weight2_processor's actual wiring without crashing Processor.probs()"
    - "The herald-conditioned distribution, out-of-subspace residual, and herald-failure probability are three separate, never-merged, never-silently-renormalized numbers"
    - "At n=2, i=0, j=1, theta=pi/4, the photonic distribution matches the extended exact reference to TVD < 1e-6"
    - "Full test suite (existing 107 + new Phase 12 tests) passes with zero regressions"
  artifacts:
    - path: "iqp_photonic_encoding.py"
      provides: "exact_qubit_iqp_distribution(n, thetas, pair_thetas=None), _build_weight2_processor_no_herald(n, i, j, thetas), _weight2_input_state(n, herald_spec), photonic_weight2_iqp_distribution(n, i, j, thetas)"
    - path: "tests/test_iqp_photonic_encoding.py"
      provides: "WT2-02/03/05/06 test coverage: pair_thetas sanity + backward-compat, herald-failure/residual separation, the locked TVD gate at n=2 theta=pi/4"
  key_links:
    - from: "photonic_weight2_iqp_distribution"
      to: "_build_weight2_processor_no_herald"
      via: "direct call, reusing build_cz_insertion's own herald_spec rather than re-deriving the mode-mapping dict"
      pattern: "_build_weight2_processor_no_herald\\("
    - from: "_weight2_input_state"
      to: "herald ancilla photons"
      via: "explicit {P:V} annotation (not bare integer / {P:H}) -- the confirmed fix for PolarizationSimulator's distinguishability bug"
      pattern: "\\{P:V\\}"
    - from: "test_wt2_tvd_gate (n=2, theta=pi/4)"
      to: "exact_qubit_iqp_distribution(n, thetas, pair_thetas) vs photonic_weight2_iqp_distribution(...)"
      via: "total_variation_distance"
      pattern: "total_variation_distance\\("
---

<objective>
Implement and test the weight-2 exact reference extension and herald-conditioned TVD validation: extend `exact_qubit_iqp_distribution` with `Z_i*Z_j` pair terms, build a crash-free measurement path for `build_weight2_processor`'s actual output using the research-verified `{P:V}` ancilla annotation fix, and add full test coverage including the locked n=2/theta=pi/4 TVD gate.

Purpose: this is the phase's core technical deliverable (WT2-02, WT2-03, WT2-05, WT2-06) -- proving weight-2 works to the same rigor bar weight-1 already cleared, using a measurement strategy that avoids two confirmed Perceval bugs (a crash and a silent-wrong-numbers bug) rather than working around them with a lesser validation (shot sampling / computational-basis-only), since research already found and verified a clean fix inside the time-box.
Output: `iqp_photonic_encoding.py` gains 4 new functions; `tests/test_iqp_photonic_encoding.py` gains the WT2-02 through WT2-06 test coverage; full suite green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-exact-reference-extension-tvd-validation/12-RESEARCH.md
@.planning/phases/12-exact-reference-extension-tvd-validation/12-CONTEXT.md
@iqp_photonic_encoding.py
@tests/test_iqp_photonic_encoding.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend exact_qubit_iqp_distribution with Z_i*Z_j pair terms (WT2-02)</name>
  <files>iqp_photonic_encoding.py, tests/test_iqp_photonic_encoding.py</files>
  <action>
In `iqp_photonic_encoding.py`, extend `exact_qubit_iqp_distribution(n, thetas)` (currently at line 420) to `exact_qubit_iqp_distribution(n, thetas, pair_thetas=None)`. `pair_thetas` is an optional dict `{(i, j): theta_ij}` (i &lt; j) for `Z_i*Z_j` pair-generator terms, added on top of the existing weight-1 diagonal phase accumulation loop. Use the SAME bit-ordering (qubit 0 = MSB) and Z-eigenvalue sign convention `(-1)^bit_k` already established for weight-1 -- do not introduce a second convention. Exact addition per 12-RESEARCH.md's verified formula (research already confirmed this 2-line addition agrees with the photonic ground truth to 1e-16):

```python
def exact_qubit_iqp_distribution(n, thetas, pair_thetas=None):
    # ... existing docstring, extended per 12-RESEARCH.md Section "Recommended
    # Function/Test Signatures" #1: document pair_thetas semantics and
    # backward-compatibility (pair_thetas=None behaves identically to
    # pre-Phase-12 behavior).
    pair_thetas = pair_thetas or {}
    ...
    for i in range(dim):
        total_phase = 0.0
        for k in range(n):
            bit_k = (i >> (n - 1 - k)) & 1
            total_phase += thetas[k] * (1 if bit_k == 0 else -1)
        for (a, b), th in pair_thetas.items():
            bit_a = (i >> (n - 1 - a)) & 1
            bit_b = (i >> (n - 1 - b)) & 1
            za = 1 if bit_a == 0 else -1
            zb = 1 if bit_b == 0 else -1
            total_phase += th * za * zb
        phases[i] = np.exp(1j * total_phase)
    ...
```

Keep every other line of the existing function body unchanged (the `plus`/`had_n` construction, the final `probs` dict). Do not touch `photonic_iqp_distribution`, `run_full_circuit`, or any other existing function -- this task is additive only.

Add two tests to `tests/test_iqp_photonic_encoding.py`, placed near the existing ENC-04 tests:
- `test_exact_qubit_distribution_weight2_backward_compatible`: for the existing `test_enc04_toy_validation_runs_end_to_end` parametrize cases `(2, [0.3, 1.1])` and `(3, [0.3, 1.1, 0.75])`, assert `exact_qubit_iqp_distribution(n, thetas)` (no `pair_thetas` arg) is unchanged, and `exact_qubit_iqp_distribution(n, thetas, pair_thetas=None)` is identical to it (compare dicts key by key with `pytest.approx` or `np.isclose`).
- `test_exact_qubit_distribution_weight2_extension_sums_to_one`: for a couple of `(n, thetas, pair_thetas)` combos including at least `(2, [0.0, 0.0], {(0, 1): np.pi/4})`, assert the returned distribution's values sum to 1.0 within `atol=1e-9`.
  </action>
  <verify>python -m pytest tests/test_iqp_photonic_encoding.py -k "exact_qubit_distribution_weight2" -v</verify>
  <done>Both new tests pass; existing `test_enc04_toy_validation_runs_end_to_end` and all other pre-existing tests still pass unmodified (confirmed by the full-suite run at the end of Task 3).</done>
</task>

<task type="auto">
  <name>Task 2: Herald-conditioned weight-2 measurement path (WT2-03)</name>
  <files>iqp_photonic_encoding.py</files>
  <action>
In `iqp_photonic_encoding.py`, add three new functions immediately after `build_weight2_processor` (currently ends at line 293), implementing 12-RESEARCH.md's verified fix (n=2/theta=pi/4 gate confirmed at TVD~2.2e-16 in research; robust across 5+ configs). Do not re-derive `build_weight2_processor`'s mode-mapping dict independently -- copy its exact wiring so drift between the measurement path and the production function cannot invalidate the validation.

1. `_build_weight2_processor_no_herald(n, i, j, thetas)`: identical wiring to `build_weight2_processor` (state prep -&gt; theta-folded diagonal layer -&gt; `build_cz_insertion` via the SAME mode-mapping dict -&gt; conjugation -&gt; readout) but WITHOUT calling `add_herald` (confirmed crash: `add_herald` + `PBS` -&gt; `Processor.probs()` raises a `matmul` shape-mismatch `ValueError`, unconditionally, independent of annotation -- see 12-RESEARCH.md Pitfall 3). Returns `(proc, herald_spec)`; caller post-selects on `herald_spec` by hand. Easiest implementation: literally copy `build_weight2_processor`'s body and delete the two `proc.add_herald(...)` lines, still returning `herald_spec` from `build_cz_insertion` for the caller to use.

2. `_weight2_input_state(n, herald_spec)`: builds the `pcvl.BasicState` input for `_build_weight2_processor_no_herald`'s processor -- `all_h_input(n)`'s `{P:H},0` pattern for the n qubit ports, PLUS the two herald ancilla ports EXPLICITLY annotated `{P:V}` (not bare integers, not `{P:H}` -- Perceval's silent default is `{P:H}` and is confirmed wrong; `{P:V}` is confirmed correct, matching a trusted PBS-free ground truth to TVD~1e-16; see 12-RESEARCH.md Steps 3-4). Follow 12-RESEARCH.md's signature:

```python
def _weight2_input_state(n, herald_spec):
    parts = ["{P:H},0"] * n
    parts.append("{P:V}" if herald_spec[4] else "0")
    parts.append("{P:V}" if herald_spec[5] else "0")
    return pcvl.BasicState("|" + ",".join(parts) + ">")
```

(herald_spec's photon-count values are always 1 in this project per `heralded_cz`'s own `in_heralds` -- write the annotation string generically off the count rather than hardcoding "1", in case it's ever not 1.)

3. `photonic_weight2_iqp_distribution(n, i, j, thetas)`: the top-level weight-2 analogue of `photonic_iqp_distribution`. Builds `_build_weight2_processor_no_herald(n, i, j, thetas)`, runs `.probs()` with `_weight2_input_state`'s input (via `Analyzer`, matching `run_full_circuit`'s existing pattern), then for each output state:
   - Checks whether the two ancilla output modes match `herald_spec`'s expected photon pattern. If NOT matched, add that probability to `herald_failure_prob`.
   - If matched, strip the ancilla modes and decode the remaining `2n` qubit modes via `fock_to_bitstring`. If `None` (out-of-subspace), add to `residual`. Otherwise add to `dist[bitstring]`.
   - Herald-condition `dist` by renormalizing: divide every value by `(1 - herald_failure_prob)` -- i.e. `dist` is reported CONDITIONAL on herald success, matching `photonic_iqp_distribution`'s existing convention of only reporting valid in-subspace outcomes, but now also conditioned on herald success. `residual` stays as the WITHIN-herald-success-branch out-of-subspace mass (also renormalize residual by the same `(1 - herald_failure_prob)` factor so `sum(dist.values()) + residual == 1.0`).
   - Returns `(dist, residual, herald_failure_prob)` -- a 3-tuple per CONTEXT.md's locked reporting rule: herald_failure_prob is NEVER merged into residual, NEVER silently renormalized away, always reported as its own explicit number.
   - The `pi/4` fold is NOT a caller-supplied parameter -- `thetas` passes straight through to `_build_weight2_processor_no_herald`, which folds `pi/4` internally exactly as `build_weight2_processor` does (per 12-RESEARCH.md's explicit recommendation: don't parameterize `pair_theta`, since the CZ/ZZ identity is only exact at pi/4 and any other value would produce numerically-valid-but-physically-meaningless output).

Write a docstring on `photonic_weight2_iqp_distribution` stating the expected herald_failure_prob (~1 - 2/27 ~ 0.9259 at pi/4, matching Phase 10's established `heralded_cz` success rate) and citing 12-RESEARCH.md as the source of the `{P:V}` fix.
  </action>
  <verify>python -c "from iqp_photonic_encoding import photonic_weight2_iqp_distribution; d,r,hf = photonic_weight2_iqp_distribution(2,0,1,[0.0,0.0]); print(sum(d.values()), r, hf); assert abs(sum(d.values())+r-1.0)<1e-9; assert abs(hf-(1-2/27))<1e-6"</verify>
  <done>`photonic_weight2_iqp_distribution` runs without crashing, returns a 3-tuple, `sum(dist.values()) + residual ~ 1.0`, and `herald_failure_prob ~ 1 - 2/27 ~ 0.9259`.</done>
</task>

<task type="auto">
  <name>Task 3: TVD gate test, herald-accounting test, and full suite verification (WT2-05, WT2-06)</name>
  <files>tests/test_iqp_photonic_encoding.py</files>
  <action>
Add the following tests to `tests/test_iqp_photonic_encoding.py`, near the existing ENC-04 / Phase 11 tests, following `test_enc04_toy_validation_runs_end_to_end`'s style (docstring stating the claim being tested, explicit assertions, no bare asserts without messages on the TVD threshold):

1. `test_wt2_tvd_gate_n2_theta_pi_4` -- the actual WT2-05 locked pass/fail gate. n=2, i=0, j=1, `thetas=[0.0, 0.0]` (pure weight-2 pair term, matching 12-RESEARCH.md's Step 4 exact configuration). Compute:
   - `qubit_dist = exact_qubit_iqp_distribution(2, [0.0, 0.0], pair_thetas={(0, 1): np.pi / 4})`
   - `photonic_dist, residual, herald_failure_prob = photonic_weight2_iqp_distribution(2, 0, 1, [0.0, 0.0])`
   Assert `residual` is close to 0.0 (`atol=1e-9`), assert `herald_failure_prob` is close to `1 - 2/27` (`atol=1e-6`), assert both distributions sum to ~1.0, compute `tvd = total_variation_distance(qubit_dist, photonic_dist)`, and assert `tvd < 1e-6` (this is the literal ROADMAP Success Criterion 3 gate -- do not weaken the tolerance).

2. `test_wt2_herald_failure_and_residual_are_separate_numbers` -- the CONTEXT.md-locked accounting check. Using the same n=2 configuration, assert all three of: `dist` sums to ~1.0 minus nothing extra (i.e. `sum(dist.values()) + residual ~ 1.0` exactly, not `1 - herald_failure_prob` again -- herald_failure_prob was already divided out during renormalization in Task 2), `herald_failure_prob` is NOT folded into `residual` (assert `residual` is near 0.0 while `herald_failure_prob` is near 0.9259 -- they must be visibly different numbers, not the same value under two names), and none of the three returned values silently sum to something other than what's documented.

3. (Opportunistic, per CONTEXT.md -- include only if it doesn't meaningfully slow the suite, drop if it does) `test_wt2_tvd_gate_n3_bystander_qubit` -- n=3, pair (1,2), bystander qubit 0 at a nonzero weight-1 theta, mirroring 12-RESEARCH.md's Step 5 robustness check. Same assertions as test 1, with the exact reference computed via `exact_qubit_iqp_distribution(3, thetas, pair_thetas={(1,2): np.pi/4})` where `thetas[0]` is the bystander's weight-1 angle and `thetas[1], thetas[2]` carry any additional weight-1 correction the caller wants (0.0 is fine).

After adding all tests, run the FULL repository test suite (not just the new tests) to confirm zero regressions against the pre-existing 107 tests.
  </action>
  <verify>python -m pytest tests/ -v 2>&1 | tail -40</verify>
  <done>All new WT2 tests pass, including the locked `test_wt2_tvd_gate_n2_theta_pi_4` at TVD &lt; 1e-6. Full suite (107 pre-existing + new Phase 12 tests) passes with zero failures.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/ -v` and confirm 100% pass, with the new WT2-02/03/05/06 tests visible in the output and the pre-existing 107 tests unaffected. Manually inspect `photonic_weight2_iqp_distribution`'s docstring and the `_weight2_input_state` implementation to confirm the `{P:V}` annotation (not `{P:H}` or bare integer) is present on the ancilla ports -- this is the single most load-bearing line in the whole plan; a silent regression here would reintroduce the confirmed bug with no test failure if the TVD gate test happens to still pass by coincidence at other configs (it will not at n=2/theta=pi/4, since research measured `{P:H}` giving TVD~0.4639 at that exact point).
</verification>

<success_criteria>
- `exact_qubit_iqp_distribution` extended with `pair_thetas`, backward-compatible, tested.
- `photonic_weight2_iqp_distribution` returns `(dist, residual, herald_failure_prob)` as three separate numbers, never merged.
- `test_wt2_tvd_gate_n2_theta_pi_4` passes at TVD < 1e-6 (ROADMAP Success Criterion 3).
- Full suite green, zero regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/12-exact-reference-extension-tvd-validation/12-01-SUMMARY.md`
</output>
