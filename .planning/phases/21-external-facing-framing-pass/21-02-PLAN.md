---
phase: 21-external-facing-framing-pass
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - C:\Users\cuqui\merlin-quantum-case-study\README.md
autonomous: true

must_haves:
  truths:
    - "A reader of this repo's README sees a short v3.0 pitch (trainability + hardness-under-loss as co-lead findings) with a link out to the full case-study page for the rest of the story — not a full restatement of docs/technical-findings.md."
    - "The negative/inconclusive TRAIN-09/TRAIN-10/HARD-04 verdicts are named honestly in the pitch, not smoothed into pure-positive marketing language."
    - "The existing v1.0 README content (Headline result, Results, How to run, Links out, Process & AI Use) is unchanged."
  artifacts:
    - path: "C:\\Users\\cuqui\\merlin-quantum-case-study\\README.md"
      provides: "New '## v3.0: IQP Circuit Study' section between 'Process & AI Use' and 'License'"
      min_lines: 83
  key_links:
    - from: "README.md new v3.0 section"
      to: "docs/technical-findings.md"
      via: "relative markdown link, for readers who want the full internal write-up"
      pattern: "docs/technical-findings.md"
    - from: "README.md new v3.0 section"
      to: "the live alejandro-jackson case-study page (Plan 21-01's deliverable)"
      via: "external markdown link, or an explicit flagged placeholder if no live URL is confirmed"
      pattern: "case-studies/merlin-quantum"
---

<objective>
Add a short v3.0 pitch section to this repo's README, distinct from and linking out to (not restating) both `docs/technical-findings.md` (the internal write-up) and the portfolio case-study page (Plan 21-01's deliverable) — completing WRITE-07's secondary deliverable.

Purpose: the README is this repo's own front door (e.g. for anyone landing directly on GitHub) — it currently stops at v1.0 and has never been updated for v2.0/v2.1/v3.0. This plan closes that gap for v3.0 specifically, per this phase's locked scope (v2.0/v2.1 README gaps are not this phase's concern).
Output: `README.md` updated in place with one new top-level section.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Users\cuqui\merlin-quantum-case-study\.planning\phases\21-external-facing-framing-pass\21-CONTEXT.md
@C:\Users\cuqui\merlin-quantum-case-study\.planning\phases\21-external-facing-framing-pass\21-RESEARCH.md
@C:\Users\cuqui\merlin-quantum-case-study\README.md
@C:\Users\cuqui\merlin-quantum-case-study\docs\technical-findings.md
</context>

<important_notes>
- **Independent of Plan 21-01.** This plan does not depend on 21-01 executing first — the case-study page already exists at a known relative path (`/case-studies/merlin-quantum`) regardless of whether Plan 21-01's new sections have shipped yet, and this README section is written from `docs/technical-findings.md`'s already-locked numbers directly (per 21-RESEARCH.md's "Sourceable Numbers" section), not from Plan 21-01's drafted prose. Both plans can run in the same wave.
- **Live URL not confirmed.** `gh api repos/alejack312/alejandro-jackson --jq .homepage` returns `https://alejandro-jackson.vercel.app`, but that URL currently returns `DEPLOYMENT_NOT_FOUND` (verified via `curl -I` during planning) — it is a stale/unassigned domain, not a working deployment. Do not hardcode it as a working link. Instead: link using the relative path pattern `https://alejandro-jackson.vercel.app/case-studies/merlin-quantum` (matching the domain GitHub itself lists for this repo) AND add a one-line SUMMARY note flagging that this URL should be confirmed/fixed by the owner once a real deployment exists — this is a reversible, easily-fixed string, not worth blocking the plan on.
- **This repo's own git commit is separate from Plan 21-01's alejandro-jackson commit.** This plan's commit happens in `C:\Users\cuqui\merlin-quantum-case-study` (the repo this PLAN.md lives in) — normal, same-repo git flow, no cross-repo concern here.
</important_notes>

<tasks>

<task type="auto">
  <name>Task 1: Add "v3.0: IQP Circuit Study" section to README.md</name>
  <files>C:\Users\cuqui\merlin-quantum-case-study\README.md</files>
  <action>
Insert a new top-level section after the existing `## Process & AI Use` section (ends line 78, before `## License` at line 80) — do not touch any existing heading or content above it.

Heading: `## v3.0: IQP Circuit Study` (or equivalent — exact wording is this task's discretion, keep consistent with the README's existing terse heading style: `## Headline result`, `## Problem & approach`, etc.)

Content (short — this is a pitch, not a restatement; target 4-8 sentences plus a links list, matching the README's existing section lengths):
1. One-sentence framing: after v1.0's photonic generator, this project extended into a v3.0 milestone measuring two things about the same circuit family — trainability (does it show barren-plateau gradient behavior?) and hardness-under-loss (does the sampling-hardness argument survive realistic photon loss?) — plus a continuously-tunable weight-2 gate and an independent Julia-based numeric verification path.
2. State the two co-lead findings honestly, including the negative/inconclusive framing (source numbers: `docs/technical-findings.md:33-52,128-150` — same numbers Plan 21-01 uses, do not re-derive, do not soften):
   - Trainability: an exponential gradient-variance-decay signature (weight1/uniform R²=0.999, mixed/uniform R²=0.910) looked real at one bandwidth, but a follow-up sigma-grid sweep showed it is not robust to bandwidth choice — a genuine, reported negative result, not glossed over.
   - Hardness-under-loss: TVD-vs-eta and a counter-intuitive anticoncentration result (photon loss makes these circuits MORE anticoncentrated, not less — the reverse of an earlier internal guess, corrected against real data) — no eta-to-depolarizing-rate translation was attempted, by deliberate scope decision.
3. One sentence naming the supporting infrastructure work (ARB-01 tunable gate validation; 4 independent Julia cross-checks, all GO) without over-explaining it — this is supporting material, not a headline, matching CONTEXT.md's lock.
4. A short "Links out" list (mirroring the existing `## Links out` section's format) with:
   - Link to the full case-study page: `https://alejandro-jackson.vercel.app/case-studies/merlin-quantum` — for the complete external-facing story (Plan 21-01's deliverable).
   - Link to [`docs/technical-findings.md`](docs/technical-findings.md) — for the full internal technical write-up with every number's methodology and source traceability.
   - Optionally, links to the three source docs (`docs/trainability-study.md`, `docs/hardness-under-loss-study.md`, `docs/iqp-photonic-encoding.md`, `docs/julia-cross-check-study.md`) if it reads naturally — not required.
5. One sentence extending (not restating) the existing `## Process & AI Use` framing to v3.0 — e.g. "The same self-explanation rule applied throughout v3.0; see the case study for a concrete example (the trainability write-up's literature cross-reference is a transcript of my own reasoning, not Claude's summary of it)." Keep this to one sentence — the full disclosure paragraph already exists above in `## Process & AI Use` and should not be duplicated.
  </action>
  <verify>Read the modified README back; confirm the new section sits between `## Process & AI Use` and `## License`, every existing heading/section above it is byte-identical to before, the section is markdown-valid (renders correctly — check heading level, list syntax, link syntax), and no number in it contradicts what Plan 21-01 states or what `docs/technical-findings.md` says.</verify>
  <done>README.md has a new `## v3.0: IQP Circuit Study` section (or equivalently named) between Process & AI Use and License, stating both co-lead findings honestly (including the negative/inconclusive results), linking out to the case-study page and docs/technical-findings.md rather than restating them, and extending (not duplicating) the existing AI-disclosure paragraph.</done>
</task>

</tasks>

<verification>
- New section renders correctly as markdown (heading hierarchy, links, lists all valid).
- Existing README content (all sections above and below the insertion point) unchanged.
- Every number/verdict traces to `docs/technical-findings.md` or a cited source doc — no invented or rounded-differently numbers.
- Negative/inconclusive results (sigma-grid non-robustness, no eta-depolarizing translation) present, not softened.
</verification>

<success_criteria>
1. A short, distinct v3.0 pitch section exists in README.md, linking out rather than restating — satisfies ROADMAP success criterion 1 (README half).
2. Framing matches the mechanism-not-magic convention and does not soften Phase 20's honest verdicts — satisfies ROADMAP success criterion 2.
</success_criteria>

<output>
After completion, create `.planning/phases/21-external-facing-framing-pass/21-02-SUMMARY.md`.
</output>
