---
phase: 21-external-facing-framing-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:\Users\cuqui\projects\alejandro-jackson\src\pages\case-studies\merlin-quantum.tsx
autonomous: false   # ends with a checkpoint:human-verify — push to origin is explicitly gated per 21-RESEARCH.md

must_haves:
  truths:
    - "A visitor to the merlin-quantum case-study page sees new v3.0 content (trainability + hardness-under-loss as co-lead findings, ARB-01 gate validation + Julia cross-checks as supporting evidence) that reads as one continuous story with the existing v1.0 content, not a bolted-on second page."
    - "The TRAIN-09 sigma-grid non-robustness finding, the TRAIN-10 inconclusive data-dependent-init result, and HARD-04's deliberate no-translation-attempted decision are all stated as plainly as they are in docs/technical-findings.md — none softened, hedged away, or omitted."
    - "The counter-intuitive HARD finding (anticoncentration alpha DECREASES, not increases, as photon loss increases) is presented as a genuine, verified result, explicitly correcting the project's own earlier internal guess."
    - "The Role section explains that the v3.0 work was also done under this project's self-explanation-required workflow, naming the concrete transcribed-reasoning mechanism (TRAIN-07) as evidence, not just asserting the rule in the abstract."
    - "npm run lint and npm run build both pass clean in the alejandro-jackson repo after the edit — no regression to the existing v1.0 page content or any other page."
  artifacts:
    - path: "C:\\Users\\cuqui\\projects\\alejandro-jackson\\src\\pages\\case-studies\\merlin-quantum.tsx"
      provides: "Two new Section blocks (v3.0 headline findings; supporting gate-validation/Julia evidence) inserted between Technical Depth and Role, plus an extended Role QuoteBlock"
      min_lines: 524
  key_links:
    - from: "merlin-quantum.tsx new v3.0 Section(s)"
      to: "~/components/case-studies/shared (DataTable, CalloutBox pattern, Section)"
      via: "existing named imports at top of file"
      pattern: "DataTable|CalloutBox|Section"
    - from: "merlin-quantum.tsx Role QuoteBlock"
      to: "TRAIN-07 self-explanation checkpoint (docs/trainability-study.md in the sibling merlin-quantum-case-study repo)"
      via: "prose reference, not a hyperlink (cross-repo, no live link target)"
      pattern: "self-explanation|transcribed"
---

<objective>
Extend the existing MerLin case-study page (a separate Next.js portfolio repo) with new content covering the v3.0 IQP Circuit Study milestone — trainability/barren-plateau (TRAIN, Phase 17/17.1) and hardness-under-loss (HARD, Phase 18) as co-lead findings, ARB-01 gate validation (Phase 15/16) and the Julia cross-checks (Phase 19) as supporting evidence — following this project's "mechanism-not-magic, ownership-forward" convention, without softening any of Phase 20's honest negative/inconclusive verdicts.

Purpose: this is WRITE-07, the project's last v1 requirement — the recruiter/interview-facing artifact that makes the v3.0 milestone's real, honestly-reported findings legible to someone who will never read `docs/technical-findings.md`.
Output: `merlin-quantum.tsx` updated in place (two new page sections + an extended Role section); no new files, no new components.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Users\cuqui\merlin-quantum-case-study\.planning\phases\21-external-facing-framing-pass\21-CONTEXT.md
@C:\Users\cuqui\merlin-quantum-case-study\.planning\phases\21-external-facing-framing-pass\21-RESEARCH.md

# Ground-truth numbers (all pre-extracted with file:line sourcing in 21-RESEARCH.md — do not re-derive, do not paraphrase loosely)
@C:\Users\cuqui\merlin-quantum-case-study\docs\technical-findings.md

# Target file (this plan edits ONLY this file — a separate git repo from the one this plan lives in)
@C:\Users\cuqui\projects\alejandro-jackson\src\pages\case-studies\merlin-quantum.tsx
@C:\Users\cuqui\projects\alejandro-jackson\src\components\case-studies\shared.tsx
</context>

<important_notes>
- **Cross-repo.** This plan's ONLY file target lives in `C:\Users\cuqui\projects\alejandro-jackson`, a separate git repository from the one this PLAN.md lives in. All git operations across every task in this plan — Task 1's and Task 2's automatic per-task `git status`/`git add`/`git commit` (per GSD's standard `task_commit` protocol), and the checkpoint's own verification steps — MUST run with `alejandro-jackson` as the working directory. Do NOT run any git commands against `C:\Users\cuqui\merlin-quantum-case-study` as part of this plan's tasks — that repo's own commit happens separately, orchestrated by the planner/executor workflow, not by this plan's tasks.
- **Single combined commit for Tasks 1 and 2 (overrides GSD's default one-commit-per-task convention).** Task 1 and Task 2 both edit the same single file (`merlin-quantum.tsx`) as one continuous content addition — Task 1 inserts the two new v3.0 sections, Task 2 extends the Role QuoteBlock immediately after. Do NOT run the standard `task_commit` step after Task 1 completes. Hold the changes uncommitted through both tasks and create exactly ONE commit after Task 2 finishes, covering both tasks' work with a single commit message (state the one-line rationale in that message: "single commit — both tasks are one continuous content addition to the same file"). This yields exactly 2 commits ahead of `origin/main` in `alejandro-jackson` once this plan completes: the pre-existing `40889a1` plus this plan's one combined commit. Do not create a third commit for the checkpoint task — the checkpoint only verifies, it does not modify files.
- **Do not push.** `alejandro-jackson`'s `main` is already 1 commit ahead of `origin/main` from a prior, unrelated session (`40889a1`, "docs: add hiring-agent resume evaluation results"). This plan's own combined commit will sit on top of that. Do NOT run `git push` in that repo at any point in this plan — that requires the owner's explicit go-ahead (the checkpoint task covers this).
- **Production URL unresolved.** `gh api repos/alejack312/alejandro-jackson --jq .homepage` currently returns `https://alejandro-jackson.vercel.app`, but that URL returns `DEPLOYMENT_NOT_FOUND` (verified via `curl -I`) — it is stale, not a live deployment. This plan's content does not need a live URL (it only edits the TSX page itself; README's own link-out is Plan 21-02's concern, and that plan is instructed to flag the same gap rather than hardcode a dead link). If, during execution, a real production URL becomes discoverable (e.g. the owner mentions one, or a fresh `gh api` call returns something that resolves), note it in the SUMMARY — do not block on it.
- **No new chart components.** Per 21-RESEARCH.md's explicit recommendation and this phase's tight WRITE-07-only scope, reuse `DataTable` + the existing hand-rolled "Key insight" callout markup (see Task 1) rather than writing a new `RingMassProgressionChart`-style component. Decision + one-line rationale: reuse over new charting code — this phase is a content/framing pass, not a features phase, and `DataTable`/the callout pattern already carry the two headline numbers (sigma-grid verdict flips; alpha(eta) direction) clearly without new animation code to maintain.
- **Accent stays `blue`.** The page's existing `ACCENT: AccentColor = "blue"` constant (line 18) is reused for all new content — no new accent color. Decision + rationale: CONTEXT.md's lock is "one continuous story," and a new accent would visually partition v1.0 from v3.0 content, which contradicts that.
- **Section count and dark-alternation.** Two new `Section` blocks are inserted (not four, not one) — see Task 1 for the reasoning. `Section` `dark` values must continue the existing alternation: Technical Depth is `dark` → new Section 1 is *not* `dark` → new Section 2 is `dark` → Role (already not `dark`, unchanged) — this preserves the page's existing visual rhythm exactly, since inserting an even number of sections keeps every downstream section's adjacency-to-Role parity intact.
</important_notes>

<tasks>

<task type="auto">
  <name>Task 1: Insert two new v3.0 Section blocks (TRAIN+HARD co-lead findings; ARB-01+Julia supporting evidence)</name>
  <files>C:\Users\cuqui\projects\alejandro-jackson\src\pages\case-studies\merlin-quantum.tsx</files>
  <action>
Insert two new `Section` blocks between the existing `{/* ── Technical Depth ── */}` block's closing `</Section>` (currently line 449) and the `{/* ── Role ── */}` marker (currently line 451). Do not touch any other existing section.

**Decision (state explicitly in the SUMMARY):** two sections, not one and not four — one for TRAIN+HARD as co-lead headline findings (matching CONTEXT.md's lock that these two are the milestone's actual measured research claims), one for ARB-01+Julia as supporting/infrastructure evidence (matching CONTEXT.md's lock that these are not headline findings). This avoids both the "four repeated TL;DR blocks" anti-pattern 21-RESEARCH.md flags and an overly-dense single section trying to carry two independent stories (trainability-via-gradients and hardness-via-loss-sweep) in one block.

**Section 1 — `{/* ── v3.0: Trainability & Hardness Under Loss ── */}`:**
```tsx
<Section accent={ACCENT} label="v3.0" title="Trainability & Hardness Under Loss">
```
(not `dark` — continues alternation after Technical Depth's `dark`)

Content, in order:
1. One prose paragraph (`max-w-3xl`, matching the existing Context section's raw-prose pattern) framing the milestone as one shared question asked two ways: does this same photonic circuit family show barren-plateau (untrainable-gradient) behavior, and does its sampling-hardness argument survive realistic photon loss? State the methodology one-liner for each: gradients computed via exact parameter-shift (not MerLin `QuantumLayer` autograd, which can't accept this circuit's polarization-annotated states — source: `docs/technical-findings.md:20-29`); loss modeled via Perceval's explicit `pcvl.LC` component + herald-aware post-selection (not `Analyzer`, which silently ignores loss, and not `NoiseModel`, which silently no-ops on this project's polarization-annotated circuits — source: `docs/technical-findings.md:114-124`).
2. A `DataTable` (or `MetricsGrid` if 4 short metric cards reads more cleanly than a table — pick one, state which and why in the SUMMARY) with the TRAIN headline numbers: `weight1/uniform` exponential-decay R²=0.999 (n=2-6), `mixed/uniform` R²=0.910 (n=2-5), both `small_angle`-init cells inconclusive (R²=0.543 and 0.000) — source: `docs/technical-findings.md:33-38`.
3. A "Key insight" callout box, reusing the EXACT existing markup pattern from lines 385-397 (`<div className="rounded-lg border border-blue-400/20 bg-blue-400/5 px-4 py-3">` wrapping a `<p>` starting with `<span className="font-semibold text-blue-400">Key insight:</span>`), stating the TRAIN-09 tension plainly: the exponential barren-plateau signature looked real at the sweep's original fixed bandwidth (sigma=0.1), but a follow-up six-point sigma grid (`{0.03, 0.1, 0.3, 1.0, 3.0, 9.0}`) showed it is NOT robust — survives only near the original value, flips to inconclusive at intermediate sigma, and non-monotonically re-emerges at high sigma for `weight1/uniform` only (source: `docs/technical-findings.md:40-48`, exact per-sigma table at `docs/trainability-study.md:283-313`). Do not soften this into "results were mixed" — state the specific verdict-flip.
4. A second `DataTable` or short prose block with the HARD headline numbers: `tvd_to_lossless` rises monotonically from ~0.01-0.03 at eta=0.99 to ~0.50 at eta=0.05 (both scopes) — source `docs/technical-findings.md:128-131`.
5. A second "Key insight" callout box (same markup pattern) for the counter-intuitive HARD result: anticoncentration `alpha(eta)` **decreases** monotonically as eta decreases for both scopes — i.e. photon loss makes these circuits MORE anticoncentrated, not less — explicitly stated as the reverse of an earlier internal speculative guess this project made and then corrected against real data (source: `docs/technical-findings.md:132-136,243-250`; concrete values at `docs/hardness-under-loss-study.md:163-201`, e.g. weight1 n=6: alpha=16.2048 at eta=0.99 down to alpha=4.46e-15 at eta=0.05). Also state herald_failure_prob rising from ~0.926 to 0.999 at eta=0.05 (source: `docs/technical-findings.md:139-142`).
6. A closing honest-scope sentence: no eta-to-depolarizing-rate translation was attempted, by deliberate scope decision, not a failed attempt (source: `docs/technical-findings.md:144-150`); no complexity-theoretic proof or asymptotic-scale claim is made — both studies run at small, compute-bound n (source: `docs/technical-findings.md:278-302`). This must read with the same directness as the existing GEN-07 "not met" framing elsewhere on the page — do not hedge it into vague "future work" language.

**Section 2 — `{/* ── v3.0: Supporting Evidence ── */}`:**
```tsx
<Section accent={ACCENT} label="Supporting Evidence" title="Gate Validation & Independent Verification" dark>
```
Content, in order:
1. One short prose paragraph: the milestone also validated a continuously-tunable weight-2 entangling gate (`PostProcessedControlledRotationsItem`, extending the fixed-pi/4 `heralded_cz` gate already used in v1.0/v2.1) across a 16-point alpha sweep matching its closed-form success probability to ~1e-9 at every point, with full-pipeline TVD at floating-point-noise level (~1e-15 to 1e-16) — source: `docs/technical-findings.md:202-211`.
2. One short prose paragraph: findings were independently cross-checked via a second, from-scratch implementation in Julia (Yao.jl for the qubit-level circuit, BosonSampling.jl for the photonic-level weight-1/weight-2/loss models) — not a port of the Python code, a genuinely separate implementation — all four legs reaching agreement within 1e-6 of the Python results (measured TVDs from 1.12e-16 to 3.5e-15) — source: `docs/technical-findings.md:262-276`, `docs/julia-cross-check-study.md:68-249`.
3. A `DataTable` with columns like `["Check", "Independent implementation", "Result"]` summarizing: ARB-01 alpha-sweep match, ARB-01 full-pipeline TVD, Julia qubit-side cross-check, Julia weight-1 cross-check, Julia weight-2/Knill-CZ cross-check, Julia loss-model cross-check — 6 rows, using the pre-formatted-string convention `DataTable` requires (`rows: string[][]`, numbers as strings e.g. `"3.5e-15"`).
4. One closing sentence naming the milestone-level limit that applies across both this section and Section 1: ARB-01/ARB-02's tunable gate and HARD-01..07's `heralded_cz` gate are two different weight-2 gate families, never cross-tested under loss together — source: `docs/technical-findings.md:278-302`.

**Commit note:** do NOT commit after this task. See "Single combined commit for Tasks 1 and 2" in `important_notes` — the commit happens once, after Task 2.
  </action>
  <verify>Read the modified file back and confirm: (1) exactly two new `Section` blocks exist between the Technical Depth and Role markers, (2) `dark` alternation is `false` then `true` as specified, (3) every number quoted matches its cited `docs/technical-findings.md` (or underlying source doc) line exactly — no paraphrased/rounded-differently numbers, (4) both new sections import only already-imported components (`Section`, `DataTable`, `MetricsGrid` if used) — no new imports added beyond what's already at the top of the file.</verify>
  <done>Two new Section blocks exist, correctly positioned and alternating `dark`, containing the TRAIN/HARD co-lead findings (including both "Key insight" callouts) and the ARB-01/Julia supporting evidence, all numbers traceable to `docs/technical-findings.md` or its source docs, no negative/inconclusive result softened.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the Role QuoteBlock with v3.0 authorship + AI-disclosure framing</name>
  <files>C:\Users\cuqui\projects\alejandro-jackson\src\pages\case-studies\merlin-quantum.tsx</files>
  <action>
Edit the existing `Role` section's `QuoteBlock` (currently lines 452-465) to ADD 2-3 sentences covering the v3.0 milestone's authorship and AI-disclosure framing — do not delete or rewrite any existing v1.0 sentence; append after the existing text (before the closing `</QuoteBlock>`).

**Decision (state explicitly in the SUMMARY):** name the specific self-explanation-checkpoint mechanism (TRAIN-07: the owner's own reasoning about a cross-reference disagreement was transcribed verbatim into `docs/trainability-study.md`, not authored by Claude) rather than a generic "I verified my understanding" claim. Rationale: [[phase6-ai-disclosure-framing]]'s mechanism-not-magic convention calls for concrete evidence over abstract assertion, and this project has an unusually concrete, citable example — naming it is stronger, ownership-forward framing than the vaguer alternative.

Content to add (draft, adapt wording to fit the existing paragraph's voice — terse, no hype, first person):
- State that the v3.0 IQP circuit study (trainability, hardness-under-loss, gate validation, Julia cross-checks) was built and interpreted under the same rule: every AI-assisted result gets checked against the owner's own unaided explanation before it ships.
- Name the concrete mechanism once, briefly: e.g. "the trainability write-up's literature cross-reference (TRAIN-07) is a transcript of my own reasoning through a real disagreement in the data, not Claude's summary of it."
- Keep the existing v1.0 closing sentence ("Built as a credential-building exercise ahead of a Quandela internship conversation...") as the paragraph's final sentence, or move it to remain the closing line if new sentences are inserted before it — the QuoteBlock should still end on that framing, not trail off after the v3.0 addition.

Do not add a new `QuoteBlock`, `CalloutBox`, or section for this — it must land inside the existing Role `QuoteBlock`, per CONTEXT.md's explicit lock ("update the existing Role QuoteBlock... rather than adding a separate disclosure section").

**Commit note:** this task completes the combined content addition started in Task 1. Create exactly ONE commit now, covering both Task 1's and Task 2's changes together (see "Single combined commit for Tasks 1 and 2" in `important_notes`). Do not create a separate commit for Task 1's changes.
  </action>
  <verify>Read the modified Role section back; confirm exactly one `QuoteBlock` still exists in the Role `Section`, the v1.0 text is unchanged (not deleted/reworded), the v3.0 addition names TRAIN-07's transcription mechanism specifically, and the paragraph still ends on the existing "credential-building exercise" framing (either unchanged in place or moved to remain the closing sentence). Confirm `git log -1` in `alejandro-jackson` shows exactly one new commit on top of `40889a1` covering both Task 1 and Task 2's changes.</verify>
  <done>Role QuoteBlock covers both v1.0 and v3.0 authorship in one unattributed block, naming the TRAIN-07 self-explanation-checkpoint mechanism specifically as evidence of the ownership-forward convention. Exactly one combined commit exists for Tasks 1 and 2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Two new page sections covering the v3.0 IQP Circuit Study milestone (trainability + hardness-under-loss as co-lead findings, gate validation + Julia cross-checks as supporting evidence) inserted into the live case-study page, plus an extended Role/authorship section — all committed locally in the `alejandro-jackson` repo as a single combined commit, NOT pushed.</what-built>
  <how-to-verify>
    1. In `C:\Users\cuqui\projects\alejandro-jackson`, confirm `npm run lint` and `npm run build` both pass clean (run these as part of this task, before the checkpoint, and report the output — do not just assert they pass).
    2. Run `npm run dev` and visit `http://localhost:3000/case-studies/merlin-quantum`. Confirm: the two new sections render between "Technical Depth" and "Role" with no layout breakage, the dark/non-dark alternation reads correctly on scroll, both "Key insight" callouts are visually consistent with the existing one in "Key Finding", and the updated Role paragraph reads as one coherent block (not two disjointed halves).
    3. Spot-check 2-3 of the quoted numbers against `docs/technical-findings.md` in the `merlin-quantum-case-study` repo to confirm nothing drifted during drafting.
    4. Confirm the local commit exists (`git log -1` in `alejandro-jackson`) but has NOT been pushed (`git status` shows "ahead of origin" by 2 commits — the pre-existing `40889a1` plus this plan's single combined commit for Tasks 1 and 2).
  </how-to-verify>
  <resume-signal>Type "approved" to leave the commit local and unpushed (push is a separate, explicit owner action outside this plan's scope), or describe any content/tone/accuracy issues to fix before re-checking.</resume-signal>
</task>

</tasks>

<verification>
- `npm run lint` clean in `alejandro-jackson`.
- `npm run build` clean in `alejandro-jackson` (this repo's closest equivalent to `tsc --noEmit`, per 21-RESEARCH.md — no dedicated typecheck script exists).
- Every number in the new sections traces to a specific `docs/technical-findings.md` (or source doc) line cited in 21-RESEARCH.md's "Sourceable Numbers" section.
- No existing v1.0 content deleted or reworded outside the Role QuoteBlock's explicit extension.
- Exactly one local git commit exists in `alejandro-jackson` for this plan's combined Tasks 1+2 changes; no push performed.
</verification>

<success_criteria>
1. New v3.0 case-study content exists on the live page (post-deploy), distinct from and not duplicating `docs/technical-findings.md`'s internal write-up — satisfies ROADMAP success criterion 1 (case-study half).
2. TRAIN-09/TRAIN-10/HARD-04's negative/inconclusive verdicts and HARD's counter-intuitive alpha(eta) direction are stated exactly as measured, no softening — satisfies ROADMAP success criterion 2.
3. Owner has reviewed the rendered page and explicitly approved before any future push.
</success_criteria>

<output>
After completion, create `.planning/phases/21-external-facing-framing-pass/21-01-SUMMARY.md` in the `merlin-quantum-case-study` repo (this repo) — even though the edited file lives in `alejandro-jackson`, the SUMMARY belongs with this project's own phase tracking, per this project's established cross-repo documentation convention.
</output>
