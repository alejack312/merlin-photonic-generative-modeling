---
phase: 16-arb-01-extended-validation-postselection-bookkeeping
verified: 2026-08-09T01:21:00Z
status: passed
score: 3/3 must-haves verified
---

# Phase 16: ARB-01 Extended Validation & Postselection Bookkeeping Verification Report

**Phase Goal:** Extend the core ARB-01 gate to a mixed-generator composability test and a denser α characterization, and formally verify its `set_postselection` local→global ancilla mode-index translation via Forge — a narrow, bounded discrete-correctness check, not a numeric verifier.
**Verified:** 2026-08-09T01:21:00Z (Central European local time per system clock; UTC-normalized above)
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | n=3 mixed weight-1 + arbitrary-θ weight-2 composability test passes (TVD < threshold) | ✓ VERIFIED | `test_cp_composability_mixed_generators_n3` in `tests/test_iqp_photonic_encoding.py:861-907`, ran live: 3/3 parametrized cases pass (`pytest -k test_cp_composability_mixed_generators_n3` → `3 passed, 61 deselected in 11.10s`) |
| 2 | Denser α sweep (8-16 points) run, success probability plotted as a continuous curve | ✓ VERIFIED | `cp_alpha_sweep.py` ran live: 16 points, all matched closed-form `1/σ_max(α)⁴` to well within 1e-6 (max diff ~1e-9); `results/phase16_alpha_sweep.csv` (16 rows) and `results/phase16_alpha_sweep.png` (31.8KB, exists) regenerated on this run |
| 3 | Forge model of `set_postselection`-equivalent ancilla mode-index mapping built, run, confirms valid/non-aliasing | ✓ VERIFIED | `forge/ancilla_mapping.frg` ran live via `racket forge/ancilla_mapping.frg`: both `nonVacuous` (sat) and `noCounterexample` (unsat) passed, exit 0. Model's `distinctPorts` mapping matches the actual `mapping` dict in `iqp_photonic_encoding.py:622-627` exactly (verified by direct comparison) |

**Score:** 3/3 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `tests/test_iqp_photonic_encoding.py::test_cp_composability_mixed_generators_n3` | ARB-07 test, parametrized over 3 configs | ✓ VERIFIED | Exists (lines 856-907), substantive (47 lines, real assertions, no stubs), wired (calls `photonic_cp_iqp_distribution` and `exact_qubit_iqp_distribution` directly), executed live and passes |
| `cp_alpha_sweep.py` | 16-point sweep script | ✓ VERIFIED | Exists (95 lines), substantive, executed live — runs cleanly, asserts against closed form, writes CSV+PNG |
| `results/phase16_alpha_sweep.csv` | 16-row raw data | ✓ VERIFIED | Exists, 16 data rows + header, values match closed-form to ~1e-9 |
| `results/phase16_alpha_sweep.png` | Plotted success-probability curve | ✓ VERIFIED | Exists (31.8KB), regenerated by live script run |
| `forge/ancilla_mapping.frg` | Forge model of the mapping dict's injectivity | ✓ VERIFIED | Exists (72 lines), substantive (two `pred` blocks + `test expect` with sat/unsat checks), executed live, both checks pass. Directly cross-checked against the real `mapping` dict in `iqp_photonic_encoding.py:622-627` — matches exactly (2i,2i+1,2j,2j+1,2n,2n+1,2n+2,2n+3) |
| `results/phase16_forge_summary.md` | Pass/fail record | ✓ VERIFIED | Exists, contains what-was-modeled, bound, raw racket output, one-line verdict — all 4 required elements present |
| `docs/iqp-photonic-encoding.md` | New subsections + updated Conclusion | ✓ VERIFIED | Contains "Denser α Sweep (Phase 16)" (line 467) and "Forge Verification of the Ancilla Mode-Mapping (Phase 16)" (line 482) subsections, and the Conclusion section (line 488+) explicitly states "Phase 16's full scope is now complete" referencing ARB-07/08/09 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `test_cp_composability_mixed_generators_n3` | `photonic_cp_iqp_distribution(n,i,j,thetas,alpha)` | direct call | WIRED | Test calls the function directly, asserts TVD against `exact_qubit_iqp_distribution`'s extended reference; live run confirms real (non-stub) values returned |
| `cp_alpha_sweep.py` | `photonic_cp_iqp_distribution(n=2,i=0,j=1,...)` | direct call per α | WIRED | Live run confirms 16 real measured values, each matching closed-form independently computed |
| `forge/ancilla_mapping.frg`'s `distinctPorts` predicate | `iqp_photonic_encoding.py`'s `mapping` dict (lines 622-627) | 1:1 correspondence of the 8 keys | WIRED | Direct read-and-compare of both files confirms identical key structure: `{2i, 2i+1, 2j, 2j+1, 2n, 2n+1, 2n+2, 2n+3}` |

### Requirements Coverage

| Requirement | Status | Blocking Issue |
|-------------|--------|-----------------|
| ARB-07 | ✓ SATISFIED (code) | None — REQUIREMENTS.md checkbox/table still shows "Pending", a bookkeeping-only lag, not a code gap |
| ARB-08 | ✓ SATISFIED (code) | Same bookkeeping note as above |
| ARB-09 | ✓ SATISFIED (code) | Same bookkeeping note as above |

### Anti-Patterns Found

None blocking. One minor pre-existing doc inconsistency, already self-flagged by the 16-03-SUMMARY.md: `docs/iqp-photonic-encoding.md` line 216 (inside an earlier "Open questions and limitations" section, distinct from the Conclusion section this phase updated) still says Phase 16 items are "not yet done." This predates Phase 16's plans and was explicitly out of scope for 16-01/16-02/16-03 (which only owned the Conclusion section's bullet). Cosmetic/doc-consistency only — does not affect any of the three success criteria, all of which are independently verified against working code/models above.

### Human Verification Required

None. All three success criteria are structurally and numerically verifiable, and were verified by direct execution (pytest, the sweep script, and racket) rather than by trusting SUMMARY claims.

### Gaps Summary

No gaps. All three phase success criteria hold against live-executed code:
1. ARB-07's composability test exists and passes (3/3 parametrized cases, TVD < 1e-6, non-vacuity sanity check > 0.005).
2. ARB-08's 16-point α sweep exists, runs cleanly, and every point matches the closed-form prediction to ~1e-9 (well under the 1e-6 bar); CSV and PNG outputs present.
3. ARB-09's Forge model exists, matches the real mapping dict in the source exactly, and both required checks (sat non-vacuity, unsat non-collision) pass at n≤8.

Full repo test suite re-run live: 145/145 passing, zero regressions.

Two non-blocking bookkeeping items for the orchestrator to close out post-verification: (a) `.planning/REQUIREMENTS.md`'s ARB-07/08/09 rows still marked "Pending" despite being satisfied in code; (b) the stray pre-existing "not yet done" sentence at `docs/iqp-photonic-encoding.md:216` noted above.

---

*Verified: 2026-08-09T01:21:00Z*
*Verifier: Claude (gsd-verifier)*
