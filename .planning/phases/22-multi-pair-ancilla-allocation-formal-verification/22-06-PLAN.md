---
phase: 22-multi-pair-ancilla-allocation-formal-verification
plan: 06
type: execute
wave: 5
depends_on: ["22-05"]
files_modified:
  - docs/iqp-photonic-encoding.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [MPAIR-06]

must_haves:
  truths:
    - "docs/iqp-photonic-encoding.md records the pooled allocation scheme as a specification for future implementation, stating explicitly that no Python implements it yet."
    - "The section states that the model is the source of truth an eventual implementation must be checked against — the inverse of Phase 16's drift direction."
    - "The section states plainly that the chromatic index of K_n is a known theorem, so this constructively confirms known combinatorics rather than settling an open problem."
    - "The section states the scope boundary that index-level non-collision does not establish physical reuse validity, and cites the MPAIR-07 ruling."
    - "The owner has explained the pairwise-reduction argument and the MPAIR-07 verdict back in their own words before the phase closes."
  artifacts:
    - path: "docs/iqp-photonic-encoding.md"
      provides: "MPAIR specification section recording the verified scheme"
      contains: "Pooled Multi-Pair Ancilla Allocation"
  key_links:
    - from: "docs/iqp-photonic-encoding.md"
      to: "forge/pooled_ancilla_allocation.frg"
      via: "section citing the model as source of truth for a future implementation"
      pattern: "pooled_ancilla_allocation.frg"
    - from: "docs/iqp-photonic-encoding.md"
      to: "results/phase22_forge_summary.md"
      via: "honest-verdict sentence reproduced without softening"
      pattern: "phase22_forge_summary"
---

<objective>
Record the verified pooled allocation scheme in `docs/iqp-photonic-encoding.md` as a specification for future implementation, and close the phase with an owner self-explanation checkpoint.

Purpose: MPAIR-06 requires the scheme land in this project's single source-of-truth encoding document, parallel in structure to the existing Phase 16 Forge section but with the source-of-truth direction INVERTED — `forge/ancilla_mapping.frg` re-states an already-shipped Python formula and carries a standing drift warning because of it, whereas this model is upstream of any implementation and is what a future implementation must be checked against. `CLAUDE.md` additionally requires a self-explanation checkpoint at each milestone: the owner must be able to explain what was built and why it works, unaided.

Output: a new section in `docs/iqp-photonic-encoding.md`, an updated `.planning/REQUIREMENTS.md`, and a recorded owner self-explanation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@results/phase22_allocation_invariant.md
@results/phase22_forge_summary.md
@results/phase22_reuse_gate.md
@.planning/phases/22-multi-pair-ancilla-allocation-formal-verification/22-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the MPAIR specification section to docs/iqp-photonic-encoding.md</name>
  <files>docs/iqp-photonic-encoding.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `docs/iqp-photonic-encoding.md` lines 467-521 — the existing `### Denser α Sweep (Phase 16)`, `### Forge Verification of the Ancilla Mode-Mapping (Phase 16)`, `### What ARB-01/ARB-02 does/doesn't establish`, `### Literature comparison table (WRITE-02)` and `## Conclusion and Open Questions` headings; the new section's structure must parallel the Phase 16 Forge section and be inserted before `## Conclusion and Open Questions`
    - `results/phase22_allocation_invariant.md` (the formula, the compatibility rule, the pairwise-reduction argument, the bitwidth arithmetic, the scope boundary, and the owner review)
    - `results/phase22_forge_summary.md` (the verdict sentence to reproduce verbatim)
    - `results/phase22_reuse_gate.md` (the MPAIR-07 owner ruling to cite)
    - `.planning/phases/22-multi-pair-ancilla-allocation-formal-verification/22-RESEARCH.md` § "Common Pitfalls" Pitfall 4 (why the drift-warning direction inverts here)
  </read_first>
  <action>
Insert a new top-level section into `docs/iqp-photonic-encoding.md`, titled `## MPAIR: Pooled Multi-Pair Ancilla Allocation (Phase 22)`, positioned immediately before the existing `## Conclusion and Open Questions` heading. This is an additive insertion — do not modify, reorder, or reword any existing section, matching Plan 20-03's and Plan 21-02's insertion-only precedent in this repo.

Subsections, in order:

`### What this specifies` — the pooled/recycled scheme: ancilla modes are shared across pairs that do not share a qubit index, rather than each pair receiving a disjoint 4-mode block. Give the compatibility rule (`{i,j} ∩ {i',j'} = ∅`), the round-robin edge-colouring formula per parity, and the mode-index formula (block `c` occupies modes `2n+4c` through `2n+4c+3`), stating that this generalizes `_build_weight2_cp_processor_no_postselect`'s existing single-pair tail-ancilla dict — the `c = 0` case — rather than replacing it. Give the concrete payoff at `n=8`: `2*8 + 4*7 = 44` modes pooled versus `2*8 + 4*28 = 128` contiguous, i.e. mode count growing with the number of blocks rather than with `k`.

`### No Python implements this — the direction of truth is inverted` — this is the subsection MPAIR-06 exists for and it must be unambiguous. State that no code in `iqp_photonic_encoding.py` implements a k-pair pooled circuit; that this scheme is a specification, not a description of shipped behaviour; and that `forge/pooled_ancilla_allocation.frg` together with `results/phase22_allocation_invariant.md` is therefore the SOURCE OF TRUTH any future implementation must be checked against. Contrast this explicitly with `forge/ancilla_mapping.frg`, which re-states an already-shipped Python formula with nothing linking the two and carries a standing drift warning for that reason. Do not copy that drift-warning language here — the risk runs the other way, and the section should say so.

`### What was checked, and how` — the search question as D-05 frames it (minimum `K` such that a proper assignment exists over `K_n`), the largest `n` reached and the minimum `K` established at each, the two-part `test expect` discipline inherited from Phase 16 with the non-vacuity guard strengthened to require two mutually-compatible pairs that actually share a block, and the `for 7 Int` bitwidth with the `43`-at-`n=8` justification in one line. State the pairwise-reduction argument in two or three sentences — collision is a binary predicate, so under a fixed per-pair allocation "no collision over every subset" is exactly equivalent to pairwise checking, which is what licenses not enumerating the `2^28` subsets the original framing worried about. Link to `results/phase22_forge_summary.md` and `results/phase22_forge_run_log.md` for the full detail rather than restating their tables.

`### This confirms known combinatorics — stated plainly` — per D-05's honesty caveat. The chromatic index of a complete graph is a known theorem: `n-1` for even `n`, `n` for odd `n` (Koenig/Vizing). What this phase produced is a machine-checked constructive confirmation at bounded `n` plus a concrete colouring an implementation can use — not a new result, and not a settled open problem. Write this as a plain statement, not a hedge buried in a clause.

`### What Forge alone added — stated honestly` — reproduce the verdict sentence from `results/phase22_forge_summary.md` VERBATIM, without softening. If the verdict is that Forge did not earn its place at this scale, say exactly that, and note that this is the second time this project has reached that conclusion (the first being `ancilla_mapping.frg`'s own 2026-08-20 audit) and that MPAIR-05 treats it as a passing outcome. Match the register of the existing `### What Forge alone added — stated honestly` subsection this document already carries for Phase 16.

`### What this does and does not establish` — matching the `### What ARB-01/ARB-02 does/doesn't establish` subsection's format. Does establish: the minimum ancilla-block count and a concrete collision-free colouring, at bounded `n`, for index bookkeeping. Does NOT establish: that reusing those physical modes across sequentially-composed CP(alpha) unitaries reproduces dedicated-ancilla physics — cite `results/phase22_reuse_gate.md` and its owner ruling as where that was settled, and state that it is categorically outside what a bounded model finder can check. Also state that no k-pair Python implementation exists and no multi-ZZ hardness re-run was performed, both explicitly out of this phase's scope. Do not use language implying the Forge check "proves pooling is safe."

Then update `.planning/REQUIREMENTS.md`: mark MPAIR-01 through MPAIR-07 Complete in both the checklist block (lines 62-72) and the traceability table (lines 149-155), and correct the summary line's MPAIR count and the `Last updated` footer, which currently say "added MPAIR-01..06" and "43 v1 requirements total" while the body already lists seven MPAIR requirements and 44 total. Fix that inconsistency as part of this update rather than leaving it.
  </action>
  <verify>
    <automated>grep -n "MPAIR: Pooled Multi-Pair Ancilla Allocation" docs/iqp-photonic-encoding.md; grep -c "MPAIR-0[1-7]" .planning/REQUIREMENTS.md; venv/Scripts/python.exe -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `docs/iqp-photonic-encoding.md` contains the literal heading `## MPAIR: Pooled Multi-Pair Ancilla Allocation (Phase 22)`, positioned before `## Conclusion and Open Questions`.
    - All six subsections are present with the literal headings listed above.
    - The section contains the tokens `pooled_ancilla_allocation.frg`, `phase22_allocation_invariant`, `phase22_forge_summary`, `phase22_forge_run_log`, and `phase22_reuse_gate`.
    - The section contains the literal string `no Python implements` (or an unambiguous equivalent in the same sentence) and the words `source of truth`.
    - The section contains `Koenig` or `Vizing` together with the `n-1` / `n` chromatic-index statement.
    - The section does NOT contain the string `Last verified matching source` — that is `ancilla_mapping.frg`'s drift convention and does not apply in this direction.
    - The verdict sentence from `results/phase22_forge_summary.md` appears verbatim; `git diff` shows no rewording of it.
    - `git diff docs/iqp-photonic-encoding.md` shows insertions only — zero deletions outside the new section.
    - `.planning/REQUIREMENTS.md` shows MPAIR-01 through MPAIR-07 Complete in both the checklist and the traceability table, and the summary/footer counts are self-consistent with the body.
    - `venv/Scripts/python.exe -m pytest -q` reports 296 passed.
  </acceptance_criteria>
  <done>The scheme is recorded in the canonical encoding document as a specification with the inverted source-of-truth direction stated, the known-theorem caveat stated plainly, and the honest Forge verdict reproduced unsoftened.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Self-explanation checkpoint — owner explains the phase back unaided</name>
  <files>docs/iqp-photonic-encoding.md</files>
  <action>Run this checkpoint for real and BLOCK — per `CLAUDE.md`'s self-explanation rule and this project's recorded phase-execution outsourcing risk, it is not a formality. Put the four questions below to the owner unaided, one round, and do not answer any of them yourself or supply hints. When they reply, transcribe their answers VERBATIM into a `### Self-Explanation Checkpoint (Phase 22)` subsection appended to the new `## MPAIR: Pooled Multi-Pair Ancilla Allocation (Phase 22)` section in `docs/iqp-photonic-encoding.md`, following the transcription precedent at `docs/trainability-study.md:174` — do not draft, paraphrase, tidy, or complete any answer. If the owner hedges on any question, record that honestly and do NOT close the phase; record the follow-up instead of marking the checkpoint approved. Apply any factual correction the owner makes to Task 1's section before approval and note it in the SUMMARY.</action>
  <read_first>
    - `docs/iqp-photonic-encoding.md` § "MPAIR: Pooled Multi-Pair Ancilla Allocation (Phase 22)" (Task 1's output)
    - `results/phase22_allocation_invariant.md`
    - `results/phase22_reuse_gate.md`
    - `CLAUDE.md` § "Self-explanation checkpoints"
  </read_first>
  <what-built>
Phase 22 in full:
- `mpair07_reuse_check.py` and `results/phase22_reuse_gate.md` — the go/no-go physics gate, numerically comparing pooled versus dedicated ancilla wiring against an exact qubit-side reference, with the owner's own GO/NO-GO ruling recorded.
- `results/phase22_allocation_invariant.md` — the compatibility rule, the round-robin edge-colouring formula, the mode-index formula, the recomputed `for 7 Int` bitwidth, the pairwise-reduction argument, and the scope boundary, all in prose written before any Forge code.
- `forge/pooled_ancilla_allocation.frg` — the search-formulated model establishing the minimum ancilla-block count over `K_n`, with a non-vacuity guard requiring genuine pooling.
- `pooled_allocation_baseline.py`, `results/phase22_forge_run_log.md`, `results/phase22_forge_summary.md` — the timed colouring-search baseline and the honest Forge-versus-brute-force verdict.
- The new specification section in `docs/iqp-photonic-encoding.md`.
  </what-built>
  <how-to-verify>
`CLAUDE.md`'s self-explanation rule applies here, and this project's memory notes that GSD-subagent-executed phases carry a real outsourcing risk — so this checkpoint is run for real, not treated as a formality.

Answer these four unaided, in your own words. Do not read the answers off the documents while answering; check afterwards.

1. Why does "no ancilla collision for every subset of simultaneously-active pairs" collapse to checking pairs of pairs, and what property of the allocation would have to be true for that collapse to be invalid?
2. What did the MPAIR-07 gate actually test, mechanically — what differed between the two circuits that were compared, and why would a difference in their outputs have killed the whole pooling idea?
3. Why is `for 6 Int` insufficient for this model when it was sufficient for `ancilla_mapping.frg`? What number drives that, and what would have happened if it had been copied forward?
4. What does this phase NOT establish? Name at least two things.

Then say plainly whether you could explain each of these to Vincent unaided. Hedging on any of them is the signal to stop and build the understanding before closing the phase — not a cue to keep going.

Reply `approved` if the section reads accurately and you can explain all four, or describe what needs correcting or what you want walked through first.
  </how-to-verify>
  <acceptance_criteria>
    - The owner's answers to all four questions are transcribed VERBATIM into a `### Self-Explanation Checkpoint (Phase 22)` subsection appended to the new MPAIR section in `docs/iqp-photonic-encoding.md`, following this repo's existing precedent of transcribing the owner's own reasoning (`docs/trainability-study.md:174`, and this document's own ENC-03/ENC-04 owner-attempt subsections) rather than Claude authoring an interpretation.
    - The transcription is the owner's words. Claude does not draft, paraphrase, tidy, or complete any of the four answers.
    - If the owner hedges on any question, that is recorded honestly and the phase does NOT close until it is resolved — record the follow-up rather than marking the checkpoint approved.
    - Any factual correction the owner makes to Task 1's section is applied before approval, and the correction is noted in the SUMMARY.
    - `grep -n "Self-Explanation Checkpoint (Phase 22)" docs/iqp-photonic-encoding.md` returns a match.
    - `venv/Scripts/python.exe -m pytest -q` reports 296 passed.
  </acceptance_criteria>
  <resume-signal>Answer the four questions in your own words, then reply `approved` or describe what needs correcting.</resume-signal>
</task>

</tasks>

<threat_model>
No attack surface: documentation and planning-metadata updates only. No untrusted input, no network, no authentication, no persistence of user data. `T-22-SC` (supply chain) not applicable — no packages installed.
</threat_model>

<verification>
- `docs/iqp-photonic-encoding.md` contains the MPAIR section with all six subsections plus the transcribed self-explanation, inserted before `## Conclusion and Open Questions`, with insertions only.
- The section states that no Python implements the scheme and that the model is the source of truth, states the known-theorem caveat, and reproduces the Forge verdict verbatim.
- `.planning/REQUIREMENTS.md` marks MPAIR-01 through MPAIR-07 Complete with self-consistent counts.
- `venv/Scripts/python.exe -m pytest -q` reports 296 passed.
</verification>

<success_criteria>
MPAIR-06 is satisfied: the scheme is recorded in the canonical encoding document as a specification for future implementation, with the inverted source-of-truth direction, the known-theorem honesty caveat, the unsoftened Forge verdict, and the scope boundary all stated — and the owner has explained the phase back in their own words before it closes.
</success_criteria>

<output>
Create `.planning/phases/22-multi-pair-ancilla-allocation-formal-verification/22-06-SUMMARY.md` when done. Note whether the self-explanation checkpoint surfaced any correction or hedge.
</output>
