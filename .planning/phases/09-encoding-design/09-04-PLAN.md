---
phase: 09-encoding-design
plan: 04
type: execute
wave: 4
depends_on: ["09-03"]
files_modified:
  - docs/iqp-photonic-encoding.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false

must_haves:
  truths:
    - "The mapping is explicitly positioned against the Douce et al. CV precedent, stating precisely what is and isn't established, without overclaiming novelty or rigor the mapping doesn't have"
    - "The full mapping lives in docs/iqp-photonic-encoding.md as one coherent document covering ENC-01 through ENC-04's content, in a sensible reading order"
    - "The owner can explain the whole document back unaided -- the same bar as v1.0's self-explanation checkpoints"
    - "REQUIREMENTS.md and ROADMAP.md reflect Phase 9's actual completion status"
  artifacts:
    - path: "docs/iqp-photonic-encoding.md"
      provides: "Complete document: intro/overview, ENC-01, ENC-02, ENC-03, ENC-04 sections in reading order, conclusion/open-questions section"
      contains: "## ENC-02"
      min_lines: 150
    - path: ".planning/REQUIREMENTS.md"
      provides: "ENC-01 through ENC-05 checkboxes marked [x] with completion notes"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 9 plan list and progress table row updated to reflect 4/4 plans complete"
  key_links:
    - from: "docs/iqp-photonic-encoding.md"
      to: "docs/iqp-lit-scoping.md"
      via: "ENC-02's positioning section cites the Douce et al. summary already recorded there, rather than re-deriving it"
      pattern: "Douce"
---

<objective>
Complete `docs/iqp-photonic-encoding.md`: write ENC-02 (positioning against Douce et al., under an explicit hedged-tone constraint), assemble the full document into a coherent read (intro, correct section order, conclusion), and close out the phase's paperwork.

Purpose: this is the milestone's core deliverable's final wave. ENC-02 is where overclaiming risk is highest — the owner is a master's student using an LLM collaborator, and `09-CONTEXT.md` locks a hard tone constraint that the document must not stake out claims that risk making the owner look like they don't know what they're doing. This wave is not attempt-first-gated the way ENC-01/ENC-03/ENC-04 are (per `09-CONTEXT.md`'s cadence, which names those three specifically) — but it still ends with the same self-explanation bar ENC-05 requires, for the document as a whole.

Output: finalized `docs/iqp-photonic-encoding.md`, updated `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-encoding-design/09-CONTEXT.md
@.planning/phases/09-encoding-design/09-RESEARCH.md
@.planning/phases/09-encoding-design/09-01-SUMMARY.md
@.planning/phases/09-encoding-design/09-02-SUMMARY.md
@.planning/phases/09-encoding-design/09-03-SUMMARY.md
@docs/iqp-lit-scoping.md
@docs/iqp-photonic-encoding.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ENC-02 -- positioning against Douce et al.</name>
  <files>docs/iqp-photonic-encoding.md</files>
  <action>
    Write a `## ENC-02: Positioning Against Douce et al. (2017)` section (to be inserted between ENC-01 and ENC-03 in Task 2's final assembly pass). This section is under an explicit hard tone constraint from `09-CONTEXT.md`, which OVERRIDES the general instinct to sound confident: **the owner is a master's student, not yet graduated, using an LLM as a collaborator — the document must not stake out claims or defend positions that risk making the owner look like they don't know what they're doing.**

    Concretely, this means:
    - Hedge claims of novelty/contribution appropriately. Do not use language like "this proves," "novel breakthrough," or "demonstrates that X is hard" for anything not literally established by this phase's work. Prefer "this mapping suggests," "an open question this design surfaces is," "unlike Douce et al.'s X, this construction does Y, though Z remains unverified."
    - Explicitly own open questions and limitations rather than asserting a fully resolved result.
    - Be precise about what IS and ISN'T established when comparing to Douce et al. — don't reach for a stronger claim than the mapping supports.

    Structure, citing `docs/iqp-lit-scoping.md`'s existing Douce et al. summary directly (don't re-derive it from scratch):

    1. **What Douce et al. established**: the CV/continuous-quadrature hardness construction (squeezed states, `q̂`-diagonal gate layer, homodyne detection of `p̂`), and specifically how their construction realizes Hadamard-basis conjugation — via a measurement-based Fourier-transform teleportation gadget, post-selected on a homodyne outcome, not a native unitary.
    2. **How this DV/Fock-space mapping differs**: different formalism entirely (discrete photon-number eigenstates vs. continuous quadratures), different Perceval-native primitives (phase shifters/beamsplitters/photon-counting vs. squeezed light/homodyne — cite `09-RESEARCH.md`'s confirmation that Perceval has zero CV primitives, so this is a genuinely different toolkit, not a restatement). Name the specific single-qubit conjugation gate used in ENC-01 (e.g. `BS.H()` for dual rail) as a concrete point of contrast with Douce et al.'s gadget-based realization.
    3. **Where the honest parallel exists, if relevant to the chosen scheme** (per `09-RESEARCH.md`'s Pitfall 3): if ENC-01's multi-qubit diagonal-layer realization is itself probabilistic/measurement-based (e.g. dual rail's heralded/post-selected CZ gates), state plainly that this inherits a similar "conjugation/entangling structure realized via measurement, not a native operation" character to Douce et al.'s own approach — this is a point of *similarity*, not just contrast, and stating it is more defensible than implying DV avoids the problem entirely just because the single-qubit Hadamard is cheap. If the chosen scheme (e.g. QUDIT one-hot) has no such parallel because its conjugation is fully deterministic, state that plainly too, alongside its own cost (exponential modes).
    4. **Explicit statement of what is and isn't a genuine contribution here**: this document is a design/mapping exercise checkable in principle at small scale, not a peer-review-grade complexity-theoretic reduction proof (matching `.planning/REQUIREMENTS.md`'s Out-of-Scope table, which explicitly excludes "a formal complexity-theoretic reduction proof" from this milestone). State this limitation directly rather than letting the document's rigor level imply more than it delivers.
    5. **Explicitly listed open questions/limitations** carried over from ENC-01/ENC-03/ENC-04: generator-weight scope, any exponential-mode caveat (if QUDIT), any probabilistic-gate/success-probability caveat (if dual rail with multi-qubit gates), and anything ENC-04's toy check flagged as a mismatch or residual-probability concern.
  </action>
  <verify>`docs/iqp-photonic-encoding.md` contains a `## ENC-02` section. Manually re-read it against the tone constraint checklist above: no unhedged novelty claims, explicit ownership of limitations, precise (not inflated) comparison to Douce et al.</verify>
  <done>ENC-02 is satisfied: the mapping is positioned against Douce et al. with precise, hedged claims about what differs and what doesn't, honestly stating open questions rather than overclaiming.</done>
</task>

<task type="auto">
  <name>Task 2: Final assembly of docs/iqp-photonic-encoding.md and phase paperwork</name>
  <files>docs/iqp-photonic-encoding.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
    Assemble `docs/iqp-photonic-encoding.md` into one coherent document:
    - Reorder sections into reading order: intro/overview, `## ENC-01`, `## ENC-02` (move it here, right after ENC-01, removing the `<!-- ENC-02 inserted here by 09-04 -->` marker), `## ENC-03`, `## ENC-04`.
    - Expand the intro/overview section at the top: document purpose and scope (restate the hedged framing from ENC-02's point 4 up front too, so a reader isn't surprised by the rigor level later), how to read the document, and pointers to `docs/iqp-baseline.md` (qubit-side baseline) and `docs/iqp-lit-scoping.md` (literature scoping / Douce et al. source) as prerequisite reading.
    - Fill in the table of contents with working section links.
    - Add a short `## Conclusion and Open Questions` section at the end, collecting in one place the limitations/open-questions already stated piecemeal across ENC-01/ENC-02/ENC-03/ENC-04 (generator-weight scope, mode-cost caveats, probabilistic-gate caveats, ENC-04's toy-check outcome) — a reader should be able to see the full honesty ledger in one spot without hunting through the document.
    - Check for notation drift across sections (e.g. consistent generator/theta symbol usage, consistent scheme name, consistent n=2-3 example) written by different plans in this phase — fix any inconsistencies found.
    - Verify every fenced ```python code block in the document either matches a function actually defined in `iqp_photonic_encoding.py` or is a minimal, genuinely self-contained snippet — spot-check by running `pytest tests/test_iqp_photonic_encoding.py -v` and the full `pytest tests/` suite to confirm no regressions from this phase's additions.

    Update `.planning/REQUIREMENTS.md`: mark ENC-01 through ENC-05 checkboxes `[x]` with a one-line completion note each (mirroring the style already used for LIT-01 through PREQ-02's entries), and update the Traceability table's Status column for ENC-01 through ENC-05 to "Complete" with today's date.

    Update `.planning/ROADMAP.md`: Phase 9's "Plans:" line (currently "Not yet planned...") to list the 4 plans with brief objectives (matching Phase 8's list format), and the Progress table's Phase 9 row (`Plans Complete` → `4/4`, `Status` → `Complete`, `Completed` → today's date).
  </action>
  <verify>`pytest tests/ -v` passes with no regressions. `docs/iqp-photonic-encoding.md` reads in ENC-01 → ENC-02 → ENC-03 → ENC-04 → Conclusion order with a working intro/TOC. `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` reflect Phase 9 as complete.</verify>
  <done>The document is a single coherent read satisfying ENC-05's documentation requirement, all code in it is verified runnable via the test suite, and the phase's tracking docs are current.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The finalized `docs/iqp-photonic-encoding.md` (ENC-01 through ENC-04 in reading order, plus intro and conclusion), and Phase 9's tracking docs updated to reflect completion.</what-built>
  <how-to-verify>
    1. Read `docs/iqp-photonic-encoding.md` start to finish as one document.
    2. Run `pytest tests/ -v` and confirm the full suite passes.
    3. Per this repo's CLAUDE.md self-explanation checkpoints and ENC-05's explicit bar ("the owner can explain it unaided, same bar as the v1.0 self-explanation checkpoints"): explain, unaided, end to end — as if explaining it to Vincent Espitalier — the chosen scheme, why the mapping preserves IQP's structure, the basis correspondence, the n=2-3 toy-check result, and how it's positioned against Douce et al. (including what's hedged and why).
       If you can't do this yet, say so plainly — per this repo's CLAUDE.md, this is not a cue to keep going, it's the signal to stop and build understanding before calling Phase 9 done.
  </how-to-verify>
  <resume-signal>Type "approved" once you can explain the full document yourself, or describe what's still unclear.</resume-signal>
</task>

</tasks>

<verification>
- `pytest tests/ -v` passes with no regressions across the whole suite.
- `docs/iqp-photonic-encoding.md` contains all five ENC- sections' content (ENC-01 through ENC-05, with ENC-05 satisfied by the document's existence and completeness itself) in reading order, plus an intro and conclusion.
- `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` reflect Phase 9 as 4/4 plans complete.
- Task 3's final self-explanation checkpoint was reached before this plan is marked complete.
</verification>

<success_criteria>
ENC-02 and ENC-05 are satisfied: the mapping is precisely and honestly positioned against Douce et al. without overclaiming, the full mapping is documented as one coherent read in `docs/iqp-photonic-encoding.md`, and the owner can explain the entire document unaided -- matching the v1.0 self-explanation bar. Phase 9 (ENC-01 through ENC-05) is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/09-encoding-design/09-04-SUMMARY.md`
</output>
