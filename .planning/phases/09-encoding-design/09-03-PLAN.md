---
phase: 09-encoding-design
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - docs/iqp-photonic-encoding.md
  - iqp_photonic_encoding.py
  - tests/test_iqp_photonic_encoding.py
autonomous: false

must_haves:
  truths:
    - "The owner attempted/sketched the ENC-04 validation approach (reference method, metric, threshold) before Claude implemented and ran it"
    - "The owner explained the ENC-04 validation results back in their own words, unaided, before the phase proceeds to final assembly"
    - "A small-scale (n=2-3) comparison actually ran and produced real numbers -- not just a described plan"
    - "Any mismatch or caveat the toy check reveals is reported honestly in the doc, with ENC-01/ENC-03 sections revised if the result requires it"
  artifacts:
    - path: "docs/iqp-photonic-encoding.md"
      provides: "ENC-04 section: stated validation plan plus the actual n=2-3 comparison results (numbers), honestly reported"
      contains: "## ENC-04"
      min_lines: 40
    - path: "iqp_photonic_encoding.py"
      provides: "Exact qubit-side IQP reference distribution (numpy, ~15 lines) and a comparison routine (e.g. total variation distance) against the photonic side"
    - path: "tests/test_iqp_photonic_encoding.py"
      provides: "A test asserting the toy-check comparison actually runs end-to-end and produces a numeric result"
  key_links:
    - from: "docs/iqp-photonic-encoding.md"
      to: "iqp_photonic_encoding.py"
      via: "the comparison numbers reported in ENC-04's section are produced by running this module's reference/comparison functions, not hand-typed"
      pattern: "total variation|TVD"
    - from: "iqp_photonic_encoding.py"
      to: "iqp_photonic_encoding.py"
      via: "the photonic-side distribution used in the comparison is built from ENC-01/ENC-03's prep/diagonal/conjugation/basis-correspondence functions, not a fresh ad hoc circuit"
      pattern: "build_state_prep_circuit|build_diagonal_layer_circuit|build_conjugation_circuit"
---

<objective>
Produce ENC-04: a stated, checkable-in-principle validation plan for the mapping, and actually run it at small scale (n=2-3) — comparing the Fock-outcome distribution from the ENC-01/ENC-03 circuit against the exact qubit-side IQP distribution for the same generator set.

Purpose: `09-CONTEXT.md` is explicit this must actually run, not just be described — the mapping's central claim (that the photonic circuit reproduces IQP's output distribution under the ENC-03 correspondence) gets its first real check here. Third and final attempt-first piece per `09-CONTEXT.md`'s cadence; if the check reveals a mismatch, that gets reported honestly and ENC-01/ENC-03 get revised, matching this repo's established pattern (v1.0's GEN-07, Phase 7's neighbor-locality result) of not smoothing over negative results.

Output: extends `docs/iqp-photonic-encoding.md` (adds ENC-04 section with real numbers) and `iqp_photonic_encoding.py`/`tests/test_iqp_photonic_encoding.py` (adds the numpy reference simulation and comparison routine).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-encoding-design/09-CONTEXT.md
@.planning/phases/09-encoding-design/09-RESEARCH.md
@.planning/phases/09-encoding-design/09-01-SUMMARY.md
@.planning/phases/09-encoding-design/09-02-SUMMARY.md
@docs/iqp-baseline.md
@docs/iqp-photonic-encoding.md
@iqp_photonic_encoding.py
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Nothing yet for ENC-04 — this checkpoint comes before Claude implements and runs the toy check, per the same attempt-first cadence used for ENC-01 and ENC-03.</what-built>
  <how-to-verify>
    Here are the validation options `09-RESEARCH.md` lays out for a real, runnable n=2-3 comparison:

    **Qubit-side (ground truth) options:**
    1. **Hand-derive directly (recommended, self-contained)** — for n=2-3, `\|+⟩^⊗n → diagonal phase layer → H^⊗n → measure` is a 4×4 or 8×8 state-vector computation: build the `2^n`-dim state vector directly in plain `numpy` (no new dependency), apply the diagonal phase per basis state from your ENC-01 generator set, apply the `H^⊗n` matrix, take `\|amplitude\|²`. About 15 lines.
    2. Van den Nest's cosine-formula trick (`docs/iqp-baseline.md`) — produces Z-word *expectation values*, not a full distribution; more machinery needed to turn it into something comparable to a full output distribution. Available, not required.
    3. The sibling repo's `IqpSimulator` (`iqp-mmd-barren-plateau`) — a working exact simulator, but pulling a cross-repo dependency chain into this repo for an n=2-3 check is disproportionate. Not recommended.

    **Photonic-side:** build the n=2-3 circuit from `iqp_photonic_encoding.py`'s `build_state_prep_circuit`/`build_diagonal_layer_circuit`/`build_conjugation_circuit` (from ENC-01), run it through `Processor("SLOS", ...)` + `Analyzer`, read `.distribution`, translate each output `BasicState` to a bitstring via ENC-03's `fock_to_bitstring` (discarding or reporting residual probability on out-of-subspace outcomes per what ENC-03 already decided).

    **Comparison metric:** total variation distance or per-outcome absolute difference between the two probability tables — simplest and most defensible at n=2-3 scale (4-8 outcomes). No need for MMD/KL at this scale.

    **If your chosen scheme has probabilistic/heralded components** (e.g. dual rail's multi-qubit gates, if your ENC-01 worked example used one): decide up front, explicitly, whether you're comparing (a) the distribution *conditioned on* successful heralding, or (b) something that accounts for the non-unit success probability. These are different claims — pick one and state why.

    Sketch or attempt: which qubit-side reference method, which comparison metric, and what threshold would count as "the mapping checks out" versus "there's a mismatch worth reporting," for your n=2-3 worked example. A rough written plan is enough.
  </how-to-verify>
  <resume-signal>Paste your attempt/sketch, or say "I've attempted it, go ahead and implement" to proceed to Task 2.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Implement and run the n=2-3 toy validation check</name>
  <files>docs/iqp-photonic-encoding.md, iqp_photonic_encoding.py, tests/test_iqp_photonic_encoding.py</files>
  <action>
    Referencing the owner's Task 1 attempt (incorporate their approach where reasonable; note explicitly where the final version follows or diverges and why):

    In `iqp_photonic_encoding.py`, add:
    - `exact_qubit_iqp_distribution(n, generators, thetas)` — plain numpy: build the `2^n`-dim state vector for `\|+⟩^⊗n`, apply the diagonal phase per computational basis state from the given generator set (same generator set as ENC-01's n=2-3 worked example), apply `H^⊗n`, return a dict `{bitstring: probability}`.
    - `photonic_iqp_distribution(n, generators, thetas)` — build the ENC-01 circuit (prep + diagonal + conjugation) for the same generator set, run via `Processor("SLOS", ...)` + `Analyzer`, translate outputs to bitstrings via ENC-03's `fock_to_bitstring`, discard/renormalize or track residual probability on out-of-subspace outcomes exactly as ENC-03 specified, return a dict `{bitstring: probability}`.
    - `total_variation_distance(dist_a, dist_b)` — standard TVD over the union of both distributions' keys.

    Actually run these for the n=2-3 worked example (the same generator set used throughout ENC-01/ENC-03) and get real numbers.

    Append a `## ENC-04: Validation Plan and Toy-Scale Check` section to `docs/iqp-photonic-encoding.md`:
    - State the validation plan (what it checks, and why it's checkable in principle even though a future implementation phase would need to run it at larger n).
    - Report the actual n=2-3 results: both distributions (as tables), the TVD (or chosen metric) value, and an explicit stated threshold/verdict.
    - **If the numbers reveal a mismatch or need a caveat** (e.g. residual probability on out-of-subspace outcomes, a probabilistic-gate success-probability effect, a scoping limitation from ENC-01's generator-weight choice): report it honestly, as its own subsection, and go back and add a short caveat/correction note to the ENC-01 and/or ENC-03 sections above if the mismatch changes what those sections claim. Do not smooth over a real discrepancy — this repo's established pattern (v1.0's GEN-07, Phase 7's neighbor-locality result) is to document negative/partial results plainly, not around them.
    - Include the runnable code snippet that actually produced the reported numbers (not a hypothetical/illustrative one).

    Add to `tests/test_iqp_photonic_encoding.py`: a test that runs `exact_qubit_iqp_distribution` and `photonic_iqp_distribution` for the n=2-3 worked example and asserts `total_variation_distance(...)` returns a finite, in-range ([0,1]) numeric value (this test asserts the *check runs end-to-end*, not that it necessarily passes a tight threshold — the actual pass/fail judgment on the numbers belongs in the doc's honest reporting, per CLAUDE.md's rule that Claude computes/plots but the owner interprets).
  </action>
  <verify>`pytest tests/test_iqp_photonic_encoding.py -v` passes (including the new end-to-end comparison test). `docs/iqp-photonic-encoding.md` contains `## ENC-04` with an actual numeric TVD (or chosen metric) value and both distribution tables, not a hypothetical description.</verify>
  <done>ENC-04 is satisfied: a validation plan is stated, an n=2-3 comparison actually ran producing real numbers, and any mismatch is reported honestly with ENC-01/ENC-03 revised if warranted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The ENC-04 section of `docs/iqp-photonic-encoding.md` (validation plan + actual n=2-3 comparison numbers, honestly reported) plus `exact_qubit_iqp_distribution`/`photonic_iqp_distribution`/`total_variation_distance` in `iqp_photonic_encoding.py`.</what-built>
  <how-to-verify>
    1. Read the ENC-04 section in `docs/iqp-photonic-encoding.md`, including the reported numbers.
    2. Run `pytest tests/test_iqp_photonic_encoding.py -v` and confirm all tests pass.
    3. Per this repo's CLAUDE.md ("Interpreting benchmark/metric results — Claude may compute and plot them, but the owner writes the interpretation first, Claude checks it"): write, in your own words, what the reported TVD/comparison number means for the mapping's central claim — does it support the mapping, and if there's a mismatch, what caused it and whether ENC-01/ENC-03 need further revision beyond what's already noted.
       If you can't do this yet, say so plainly.
  </how-to-verify>
  <resume-signal>Type "approved" once you've written your own interpretation of the results, or describe what needs revisiting before moving to final assembly.</resume-signal>
</task>

</tasks>

<verification>
- `pytest tests/test_iqp_photonic_encoding.py -v` passes with no failures, including the end-to-end comparison test.
- `docs/iqp-photonic-encoding.md` contains `## ENC-04` with actual numeric results (not a hypothetical description) and an honest mismatch/caveat report if applicable.
- The owner's Task 1 attempt is referenced in the doc, and Task 3's self-explanation checkpoint (owner's own interpretation of the numbers) was reached before this plan is marked complete.
</verification>

<success_criteria>
ENC-04 is satisfied: a stated, checkable-in-principle validation plan exists and was actually run at n=2-3, producing real numbers that are honestly reported (including any mismatch), and the owner attempted the validation design and then wrote their own interpretation of the results.
</success_criteria>

<output>
After completion, create `.planning/phases/09-encoding-design/09-03-SUMMARY.md`
</output>
