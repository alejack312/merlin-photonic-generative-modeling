---
phase: 09-encoding-design
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - docs/iqp-photonic-encoding.md
  - iqp_photonic_encoding.py
  - tests/test_iqp_photonic_encoding.py
autonomous: false

must_haves:
  truths:
    - "The owner attempted/sketched the ENC-03 basis correspondence (both forward and reverse direction) before Claude wrote the full section"
    - "The owner explained the ENC-03 correspondence back in their own words, unaided, before the phase proceeds to ENC-04"
    - "A concrete forward map (bitstring -> Fock state/BasicState) is stated for the scheme chosen in 09-01, citing Perceval's own documented per-encoding convention"
    - "A concrete reverse map (measured Fock/photon-count outcome -> bitstring, or an explicit 'outside the computational subspace' verdict) is stated -- not just the forward direction"
    - "The correspondence is stated as a falsifiable claim (a specific observation that would contradict it), not a hand-wavy analogy"
  artifacts:
    - path: "docs/iqp-photonic-encoding.md"
      provides: "ENC-03 section: bidirectional, falsifiable basis correspondence for the chosen scheme, with a worked n=2-3 example"
      contains: "## ENC-03"
      min_lines: 40
    - path: "iqp_photonic_encoding.py"
      provides: "bitstring_to_fock (forward map) and fock_to_bitstring (reverse map, with an explicit invalid/out-of-subspace return value) helper functions"
    - path: "tests/test_iqp_photonic_encoding.py"
      provides: "Round-trip tests for the basis-correspondence helper functions, including at least one out-of-subspace case"
  key_links:
    - from: "docs/iqp-photonic-encoding.md"
      to: "docs/iqp-photonic-encoding.md"
      via: "ENC-03's correspondence reuses the exact scheme and BasicState conventions established in ENC-01"
      pattern: "## ENC-01"
    - from: "iqp_photonic_encoding.py"
      to: "iqp_photonic_encoding.py"
      via: "reverse map function is the actual inverse of the forward map function, verified by round-trip test"
      pattern: "def fock_to_bitstring|def bitstring_to_fock"
---

<objective>
Produce ENC-03: a concrete, falsifiable basis correspondence between qubit computational-basis bitstrings and photonic Fock-state/photon-count outcomes, for the scheme chosen in Plan 09-01 — both directions, including what happens to outcomes that fall outside the valid computational subspace.

Purpose: ENC-01 established how to build the circuit; ENC-03 establishes how to read its output. A mapping isn't falsifiable without a stated answer for both "what state do I send in" and "what does a given measurement result mean" — including the failure case. Second piece of the phase's attempt-first cadence per `09-CONTEXT.md`: owner attempts this correspondence before Claude writes the full section.

Output: extends `docs/iqp-photonic-encoding.md` (adds ENC-03 section) and `iqp_photonic_encoding.py`/`tests/test_iqp_photonic_encoding.py` (adds basis-correspondence functions and tests).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-encoding-design/09-CONTEXT.md
@.planning/phases/09-encoding-design/09-RESEARCH.md
@.planning/phases/09-encoding-design/09-01-SUMMARY.md
@docs/iqp-photonic-encoding.md
@iqp_photonic_encoding.py
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Nothing yet for ENC-03 — this checkpoint comes before Claude writes the correspondence section, per the same attempt-first cadence used for ENC-01 in Plan 09-01.</what-built>
  <how-to-verify>
    ENC-01 (just completed) fixed your encoding scheme and its forward convention is already implicit in how you built the state-prep circuit — Perceval's own documented per-encoding rule (`09-RESEARCH.md`'s Basis Correspondence table, from `port.py`'s `_to_fock`):

    | Scheme | `"011"` (n=3) maps to |
    |---|---|
    | Dual rail | `BasicState([1,0, 0,1, 0,1])` — rail pairs, `[1,0]`=0, `[0,1]`=1 |
    | Single-rail (RAW) | `BasicState([0,1,1])` — each mode's occupation number IS the bit |
    | QUDIT (one-hot) | `BasicState([0,0,0,1,0,0,0,0])` — single photon in mode `int("011",2)=3` of 8 |
    | Polarization | `\|H,V,V⟩` — one polarized photon per qubit-mode |

    What ENC-03 still needs, that this table alone doesn't give you:

    1. **The reverse direction.** Given a measured output `BasicState` (or Fock outcome) from the `Analyzer`'s `.distribution`, which classical bitstring does it correspond to?
    2. **The failure case — this is what makes the correspondence falsifiable, not a slogan.** What happens to an outcome that doesn't correspond to any valid logical bitstring? Concretely, per scheme: dual rail can produce a "bunched" outcome (2 photons in one rail-pair, 0 in the other) which isn't a valid `0`/`1`; QUDIT one-hot could in principle see a photon lost/no-detection outcome; single-rail/polarization have their own analogues. You need to state, explicitly, what your chosen scheme's invalid outcomes look like and what the mapping says about them (discard? report as an error rate? something else?).
    3. **A falsifiability statement.** What specific observation would contradict your stated correspondence? (e.g. "if a valid dual-rail outcome ever decoded to two different bitstrings, the correspondence would be wrong" — name your own version of this.)

    Sketch or attempt your own answer to all three, for your chosen scheme, before Claude writes the full section. A rough written description is enough.
  </how-to-verify>
  <resume-signal>Paste your attempt/sketch, or say "I've attempted it, go ahead and implement" to proceed to Task 2.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write the ENC-03 basis correspondence and companion code</name>
  <files>docs/iqp-photonic-encoding.md, iqp_photonic_encoding.py, tests/test_iqp_photonic_encoding.py</files>
  <action>
    Append a `## ENC-03: Basis Correspondence` section to `docs/iqp-photonic-encoding.md` (after the ENC-01 section, before the `<!-- ENC-02 inserted here by 09-04 -->` marker if present, otherwise at the end of the current content — Plan 09-04 will do final section reordering).

    Referencing the owner's Task 1 attempt (incorporate their approach where reasonable; note explicitly where the final version follows or diverges and why, matching the "Owner's attempt" callout pattern from the ENC-01 section):

    - State the forward map explicitly for the n=2-3 worked example from ENC-01, citing Perceval's own documented per-encoding convention (from `09-RESEARCH.md`'s table / `port.py`'s `_to_fock`) rather than re-deriving it from scratch.
    - State the reverse map: given an output `BasicState` from `Analyzer.distribution`, the rule that recovers a bitstring, worked through explicitly for at least one example outcome from the n=2-3 case.
    - State explicitly what counts as an "outside the computational subspace" outcome for the chosen scheme, and what the mapping does with it (discard from the reported distribution with a stated renormalization, or report as an explicit residual-probability figure — pick one and say why, don't leave it implicit).
    - State the falsifiability claim plainly: what observation would contradict this correspondence.
    - Include a small runnable Perceval code snippet demonstrating the round trip (bitstring → Fock state → run through the ENC-01 circuit's relevant piece → back to bitstring or explicit "invalid").

    Add to `iqp_photonic_encoding.py`:
    - `bitstring_to_fock(bitstring, n)` (or scheme-appropriate name/signature) — forward map, returns a `pcvl.BasicState`.
    - `fock_to_bitstring(basic_state, n)` — reverse map, returns the bitstring string, or `None` (with a documented meaning) for an out-of-subspace outcome.

    Add to `tests/test_iqp_photonic_encoding.py`:
    - Round-trip tests: `fock_to_bitstring(bitstring_to_fock(b, n), n) == b` for a handful of n=2-3 bitstrings.
    - At least one explicit out-of-subspace test case (e.g. a bunched dual-rail state) asserting `fock_to_bitstring` returns the documented invalid marker, not a wrong bitstring.
  </action>
  <verify>`pytest tests/test_iqp_photonic_encoding.py -v` passes, including the new round-trip and out-of-subspace tests. `docs/iqp-photonic-encoding.md` contains `## ENC-03` with both forward and reverse maps stated and at least one runnable code snippet.</verify>
  <done>ENC-03 is satisfied: a concrete, falsifiable, bidirectional basis correspondence is stated for the chosen scheme, including explicit handling of outcomes outside the computational subspace, backed by tested code.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The ENC-03 section of `docs/iqp-photonic-encoding.md` (forward map, reverse map, out-of-subspace handling, falsifiability statement) plus `bitstring_to_fock`/`fock_to_bitstring` in `iqp_photonic_encoding.py`, tested by round-trip and out-of-subspace tests.</what-built>
  <how-to-verify>
    1. Read the ENC-03 section in `docs/iqp-photonic-encoding.md`.
    2. Run `pytest tests/test_iqp_photonic_encoding.py -v` and confirm all tests pass, including the new ones.
    3. Explain, unaided, in your own words:
       - How the reverse map handles an outcome outside your chosen scheme's valid computational subspace, and why that choice (discard/renormalize vs. report a residual) is the right one for what ENC-04's validation will need next.
       - Why this correspondence is falsifiable rather than just an analogy — what specific observation would contradict it.
       If you can't do this yet, say so plainly.
  </how-to-verify>
  <resume-signal>Type "approved" once you can explain both points yourself, or describe what's unclear so this section can be revised before moving to ENC-04.</resume-signal>
</task>

</tasks>

<verification>
- `pytest tests/test_iqp_photonic_encoding.py -v` passes with no failures, including round-trip and out-of-subspace cases.
- `docs/iqp-photonic-encoding.md` contains `## ENC-03` with both forward and reverse maps and an explicit out-of-subspace/falsifiability statement.
- The owner's Task 1 attempt is referenced in the doc, and Task 3's self-explanation checkpoint was reached before this plan is marked complete.
</verification>

<success_criteria>
ENC-03 is satisfied: a concrete, falsifiable basis correspondence between qubit bitstrings and photonic Fock outcomes is stated in both directions, including out-of-subspace handling, and the owner attempted and then explained it back unaided.
</success_criteria>

<output>
After completion, create `.planning/phases/09-encoding-design/09-02-SUMMARY.md`
</output>
