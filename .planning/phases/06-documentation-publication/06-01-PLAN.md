---
phase: 06-documentation-publication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - LICENSE
  - docs/mmd-loss.md
  - docs/raster-order.md
  - mmd-loss.md (removed, moved to docs/)
  - raster-order.md (removed, moved to docs/)
  - .planning/phases/06-documentation-publication/06-technical-note.md
autonomous: true

must_haves:
  truths:
    - "README documents problem, approach, and results with real numbers/plots, not just prose"
    - "README states the GEN-07 'not met' result plainly near the top, not buried"
    - "AI/Claude Code involvement is disclosed honestly, framed ownership-forward (ownership language, not 'the AI caught my mistake')"
    - "docs/mmd-loss.md and docs/raster-order.md exist, are linked from the README, and are no longer untracked scratch files at repo root"
    - "Repo has a LICENSE file"
    - "The 3-5 sentence technical note to Vincent Espitalier exists as a reviewable draft, LinkedIn-message style, no subject line, no explicit CTA"
    - "All committed work is pushed to origin/master; repo remains private (owner flips visibility manually)"
    - "Runnable code is verified working (pytest suite passes) before claiming DOC-02's 'working, runnable code'"
  artifacts:
    - path: "README.md"
      provides: "Problem/approach/results writeup with numbers, visuals, links to deep-dive docs, AI disclosure"
      min_lines: 80
    - path: "LICENSE"
      provides: "MIT license text"
    - path: "docs/mmd-loss.md"
      provides: "Moved mechanism deep-dive (MMD implementation comparison)"
    - path: "docs/raster-order.md"
      provides: "Moved mechanism deep-dive (radius-sorted bin ordering fix)"
    - path: ".planning/phases/06-documentation-publication/06-technical-note.md"
      provides: "Draft technical note text ready to copy-paste and send to Vincent Espitalier"
  key_links:
    - from: "README.md"
      to: "docs/mmd-loss.md, docs/raster-order.md, DESIGN_DECISIONS.md, results/phase4_summary.md, results/phase5_summary.md"
      via: "markdown links"
      pattern: "\\[.*\\]\\(.*(docs/|DESIGN_DECISIONS|results/).*\\)"
    - from: "README.md"
      to: "results/phase4_natural_comparison.png, results/phase3_loss_curve.png"
      via: "embedded markdown images"
      pattern: "!\\[.*\\]\\(results/.*\\.png\\)"
---

<objective>
Package `merlin-quantum-case-study` into public-repo-ready documentation: a README with real numbers and plots (DOC-01), verified runnable code and a pushed, publication-ready repo (DOC-02), and a short technical note ready to send to Vincent Espitalier (DOC-03).

Purpose: This is the primary deliverable proving the project can be explained unaided to a technical reader (Vincent, Quandela) — honesty about GEN-07's "not met" result is the core credibility asset per CLAUDE.md and the project's own self-explanation discipline.
Output: `README.md`, `LICENSE`, `docs/mmd-loss.md`, `docs/raster-order.md` (moved from root), a technical-note draft, all committed and pushed to `origin/master`. Repo stays PRIVATE — visibility toggle is the owner's manual final step, not part of this plan.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-documentation-publication/06-CONTEXT.md
@.planning/phases/06-documentation-publication/06-RESEARCH.md
@results/phase4_summary.md
@results/phase5_summary.md
@DESIGN_DECISIONS.md
@mmd-loss.md
@raster-order.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move deep-dive docs, add LICENSE, verify repo cleanliness</name>
  <files>docs/mmd-loss.md, docs/raster-order.md, mmd-loss.md, raster-order.md, LICENSE</files>
  <action>
    1. Create `docs/` directory if it doesn't exist. Move `mmd-loss.md` -> `docs/mmd-loss.md` and `raster-order.md` -> `docs/raster-order.md` (use `git mv` if already tracked, otherwise plain move since these are currently untracked — then `git add docs/mmd-loss.md docs/raster-order.md`). Do not edit their content, just relocate.
    2. Create `LICENSE` at repo root with MIT license text, copyright holder "Alejandro Jackson", year 2026.
    3. Verify `.gitignore` still correctly excludes `venv/`, `__pycache__/`, `*.pyc`, `.pytest_cache/`, `*.egg-info/`, `.ipynb_checkpoints/` (already confirmed sufficient by research — no edit expected, just confirm by reading the file).
    4. Scan repo root and `results/` for any scratch/dead files not referenced by README (to be written in Task 2), DESIGN_DECISIONS.md, or NOTES.md. Research confirmed root is currently clean (only the two moved files were untracked) — if you find anything unexpected, report it before deleting; do not silently delete anything non-obvious.
    5. Run `python -m pytest -q` and confirm all tests pass — this is DOC-02's "working, runnable code" verification (not `quickstart.py`, which is MerLin's own example, not this project's deliverable).
    6. Commit: `git add docs/mmd-loss.md docs/raster-order.md LICENSE && git commit -m "docs(06-01): move deep-dive docs into docs/, add MIT LICENSE"`. Note: `mmd-loss.md`/`raster-order.md` at root were never tracked (git status confirmed untracked), so no `git rm` is needed for the old paths — just add the new ones.
  </action>
  <verify>`ls docs/mmd-loss.md docs/raster-order.md LICENSE` all exist; `git status --short` shows no untracked mmd-loss.md/raster-order.md at root; `python -m pytest -q` output shows all tests passing (48/48 per research baseline, confirm live count).</verify>
  <done>docs/ contains both deep-dive files, LICENSE exists, pytest passes, changes committed.</done>
</task>

<task type="auto">
  <name>Task 2: Write README.md</name>
  <files>README.md</files>
  <action>
    Write `README.md` at repo root for a technical reader (ML/quantum background — go straight into MMD², ring_mass, photonic circuit specifics without over-explaining basics). Structure:

    1. **Title + one-line description.**
    2. **"How this was built" one-liner** near the top mentioning Claude Code assistance (see AI disclosure framing below) — one sentence only here, full section later.
    3. **Headline results, stated plainly, near the top** — not buried in a caveats section. State GEN-07's honest outcome directly: the generator's held-out MMD² is close to the real-data floor (0.0125±0.0003 trained vs 0.0360±0.0048 untrained vs 0.0114 floor, from `results/phase5_summary.md`), but ring_mass≈0.68-0.69/gap_mass≈0.05 confirm the generated distribution is "an improvement, still not two distinct rings" — GEN-07 (recognizable two-ring shape) is not fully met. Include the measured improvement path: ring_mass 0.609 → 0.691 via the natural-order-correspondence fix (K=462, radius-sorted bins), cite `DESIGN_DECISIONS.md`'s three 2026-07-25 entries and `docs/raster-order.md` for the mechanism.
    4. **Problem & approach**: MMD-trained photonic generative model on the two-ring `circles` dataset via MerLin's `QuantumLayer`, full-distribution/histogram matching (not single-point averaging or discrete sampling) — brief, links to `DESIGN_DECISIONS.md` for full rationale.
    5. **Results with visuals** — embed as markdown images:
       - `results/phase3_loss_curve.png` (training loss curve, Phase 3)
       - `results/phase4_natural_comparison.png` (generated-vs-real scatter/heatmap, the final GEN-07 natural-order-correspondence checkpoint — this is the representative "final" result, not the earlier sigma/batch-sweep variants)
       - Copy the "Headline numbers" markdown table verbatim from `results/phase5_summary.md` (benchmark comparison: trained/untrained/floor MMD², ring_mass, gap_mass, wall-clock, param count) — do not generate a new chart image, this phase is explicitly scoped "no new generator/benchmark code."
    6. **How to run**: `pip install -r requirements.txt` (Python 3.12 venv required — `torch<2.13`, `perceval-quandela>=1.2.1`), then point to the actual generator entry points: `train.py` / `generator/train.py` / `natural_order_train.py` (the GEN-07 "best" variant) and `python -m pytest -q` as the runnable-code smoke test. Do NOT reference `quickstart.py` as this project's deliverable — it's MerLin's own unrelated classifier example.
    7. **Links out** section: `DESIGN_DECISIONS.md` (full architecture/tuning rationale), `docs/mmd-loss.md` and `docs/raster-order.md` (mechanism deep-dives), `results/phase4_summary.md` and `results/phase5_summary.md` (full phase evidence), phase SUMMARY.md files under `.planning/phases/`.
    8. **"Process & AI Use" section near the bottom** (fuller than the top one-liner): state that Claude Code assisted with implementation/verification under a self-imposed discipline. Apply mechanism-not-magic, ownership-forward framing — prefer "I verify every AI-assisted component against my own unaided explanation before it ships" over "the AI caught my mistake" or "the AI checked whether I understood the code." Same underlying facts (self-explanation checkpoints per this project's CLAUDE.md), ownership-forward phrasing. Internal artifacts (DESIGN_DECISIONS.md, SUMMARY.md files, git history) are candid as-is and are the paper trail proving genuine understanding — reference them here as evidence, don't scrub them.
    9. **License** line pointing to `LICENSE`.

    Exact prose wording is your discretion — draft it fully, the owner reviews before/at commit.
  </action>
  <verify>`README.md` exists at repo root, contains embedded image links to the three PNG/table sources above, contains the GEN-07 honest-result numbers verbatim matching `results/phase5_summary.md`, contains both AI-disclosure mentions (top one-liner + bottom section) using ownership-forward phrasing, links to `docs/mmd-loss.md` and `docs/raster-order.md`.</verify>
  <done>README.md committed (`git add README.md && git commit -m "docs(06-01): add README with results, visuals, AI disclosure"`), readable end-to-end by a technical reader without needing to open other files first.</done>
</task>

<task type="auto">
  <name>Task 3: Draft technical note to Vincent Espitalier, final push</name>
  <files>.planning/phases/06-documentation-publication/06-technical-note.md</files>
  <action>
    1. Write `.planning/phases/06-documentation-publication/06-technical-note.md` containing a 3-5 sentence draft, LinkedIn-message style (short, casual, no subject line — not a formal email). Structure: opens with the result (what was built: MMD-trained photonic generative model in MerLin, honest outcome), includes a brief pretext connecting it to the prior IQP-MMD project as the methodology source before the photonic-specific result, gives the GEN-07 shortfall exactly one honest clause (e.g. "...ring structure improved substantially but isn't fully resolved yet"), and has no explicit ask/CTA — share the work and let it stand.
    2. Below the draft, add a one-line "ready to send via: LinkedIn message" note for the owner's reference.
    3. Confirm current git state: `git log --oneline -5`, `git status --short` (should be clean after Tasks 1-2's commits plus this task's commit).
    4. Commit: `git add .planning/phases/06-documentation-publication/06-technical-note.md && git commit -m "docs(06-01): draft technical note for Vincent Espitalier"`.
    5. Push all of this plan's commits: `git push origin master`.
    6. Do NOT run `gh repo edit --visibility public` or any visibility-changing command — the repo stays PRIVATE. Report to the owner in your final output: "Repo is prepared and pushed to origin/master. Flip visibility to public yourself via `gh repo edit alejack312/merlin-photonic-generative-modeling --visibility public` or the GitHub UI when ready."
  </action>
  <verify>File exists with a draft in the 3-5 sentence range; `git log` shows the commit; `git push origin master` succeeds (or reports already up to date only if nothing new); `gh repo view alejack312/merlin-photonic-generative-modeling --json visibility` still shows `"PRIVATE"` (confirms visibility was not touched).</verify>
  <done>Technical note drafted and committed, all Plan 06-01 commits pushed to origin/master, repo visibility unchanged (still private).</done>
</task>

</tasks>

<verification>
- `python -m pytest -q` passes (DOC-02 runnable-code check).
- `README.md`, `LICENSE`, `docs/mmd-loss.md`, `docs/raster-order.md` all exist and are committed.
- README states GEN-07 "not met" honestly near the top, with real numbers (not placeholder text) matching `results/phase5_summary.md` and `results/phase4_summary.md`.
- Technical note draft exists, is 3-5 sentences, LinkedIn-style, no CTA.
- `git log` shows this plan's commits pushed to `origin/master`.
- `gh repo view alejack312/merlin-photonic-generative-modeling --json visibility` still returns `"PRIVATE"`.
</verification>

<success_criteria>
DOC-01 (README with real numbers/plots), the packaging half of DOC-02 (working runnable code, prepared+pushed public-ready repo, visibility left to owner), and DOC-03 (technical note ready to send) are all satisfied for this repo. Portfolio case study (DOC-04) is handled in the separate 06-02 plan against the `alejandro-jackson` repo.
</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation-publication/06-01-SUMMARY.md`
</output>
