---
phase: 06-documentation-publication
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - C:\Users\cuqui\projects\alejandro-jackson\src\pages\case-studies\merlin-quantum.tsx
  - C:\Users\cuqui\projects\alejandro-jackson\src\components\case-studies\shared.tsx
  - C:\Users\cuqui\projects\alejandro-jackson\src\pages\case-studies\index.tsx
autonomous: false

must_haves:
  truths:
    - "A portfolio case study for this project exists as an interactive Next.js/TSX page, not a markdown file"
    - "The new case study is discoverable from the case-studies index page (/case-studies), not just reachable by direct URL"
    - "The new case study appears in other case studies' CrossLinks, and its own CrossLinks correctly excludes itself"
    - "The page's accent color does not collide with an existing case study's accent in the same allStudies group"
    - "The 'Key Finding' section states the honest GEN-07 shortfall plainly (good MMD² != clean ring structure), mirroring iqp-mmd.tsx's honesty pattern, not glossed over"
    - "At least 2 bespoke animated/interactive components exist in the page (not static <img> drop-ins), matching iqp-mmd.tsx's ACGapChart/BandwidthSweepViz pattern"
    - "npm run lint and npm run build both pass in the alejandro-jackson repo with no new errors"
  artifacts:
    - path: "C:\\Users\\cuqui\\projects\\alejandro-jackson\\src\\pages\\case-studies\\merlin-quantum.tsx"
      provides: "Full case-study page: hero, key metrics, TL;DR, context, methodology, key finding, technical depth, role, what's next, source link, cross-links"
      min_lines: 300
    - path: "C:\\Users\\cuqui\\projects\\alejandro-jackson\\src\\components\\case-studies\\shared.tsx"
      provides: "allStudies array updated with the new case study's slug/title/accent/keywords entry"
      contains: "merlin-quantum"
    - path: "C:\\Users\\cuqui\\projects\\alejandro-jackson\\src\\pages\\case-studies\\index.tsx"
      provides: "caseStudies array updated with the new case study's card entry (title, slug, accent, keywords, description, metrics)"
      contains: "merlin-quantum"
  key_links:
    - from: "src/pages/case-studies/merlin-quantum.tsx"
      to: "src/components/case-studies/shared.tsx allStudies"
      via: "CrossLinks current prop must match the allStudies slug exactly"
      pattern: "CrossLinks current=\"merlin-quantum\""
    - from: "src/pages/case-studies/index.tsx caseStudies"
      to: "src/pages/case-studies/merlin-quantum.tsx"
      via: "slug field drives Link href=/case-studies/${slug}"
      pattern: "slug:\\s*\"merlin-quantum\""
---

<objective>
Build the DOC-04 portfolio case study for this project as a full interactive Next.js/TSX page in the separate `alejandro-jackson` portfolio repo, following `iqp-mmd.tsx`'s exact structural convention, and register it so it's discoverable from the case-studies index and cross-linked from every other case study.

Purpose: A portfolio case study is one of Phase 6's four required deliverables (DOC-04) and the piece Vincent/Quandela and other reviewers are most likely to actually browse — it must read as a second, independent demonstration of the "MMD ≠ Shape" lesson from `iqp-mmd.tsx`, now in a photonic/spatial domain.
Output: `src/pages/case-studies/merlin-quantum.tsx` (new page), `src/components/case-studies/shared.tsx` (allStudies entry), `src/pages/case-studies/index.tsx` (caseStudies card entry), committed and pushed to `alejandro-jackson`'s own `origin/main` — a repo entirely separate from `merlin-quantum-case-study`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-documentation-publication/06-CONTEXT.md
@.planning/phases/06-documentation-publication/06-RESEARCH.md
@results/phase4_summary.md
@results/phase5_summary.md
@DESIGN_DECISIONS.md

Reference implementation (read fully before writing, do not summarize from memory):
C:\Users\cuqui\projects\alejandro-jackson\src\pages\case-studies\iqp-mmd.tsx
C:\Users\cuqui\projects\alejandro-jackson\src\components\case-studies\shared.tsx
C:\Users\cuqui\projects\alejandro-jackson\src\pages\case-studies\index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the merlin-quantum case-study TSX page</name>
  <files>C:\Users\cuqui\projects\alejandro-jackson\src\pages\case-studies\merlin-quantum.tsx</files>
  <action>
    Create `C:\Users\cuqui\projects\alejandro-jackson\src\pages\case-studies\merlin-quantum.tsx`, importing from `~/components/case-studies/shared` (`CaseStudyLayout`, `CaseStudyHero`, `MetricsGrid`, `Section`, `CalloutBox`, `DataTable`, `ProcessStep`, `QuoteBlock`, `CrossLinks`, `type AccentColor`), using `"motion/react"` for `motion`/`useInView` (matching `shared.tsx`'s own import convention, not `iqp-mmd.tsx`'s `"framer-motion"` import).

    Set `const ACCENT: AccentColor = "blue"` — `"violet"` and `"emerald"` (CONTEXT.md's original suggestions) are both already claimed by `dalas` and `quantum-algorithms` respectively in the `allStudies` group; `"blue"` is the only unclaimed accent in that group (confirmed by reading `shared.tsx`'s `allStudies` array). This substitution is locked, not open for re-litigation during execution.

    Follow `iqp-mmd.tsx`'s exact section structure and order:
    1. **Hero** (`CaseStudyHero`): title "MerLin Photonic Generative Modeling" (or similar), subtitle summarizing MMD-trained photonic generator on the two-ring circles dataset, badges (`["MerLin", "PyTorch", "Perceval", "Photonic QML", "MMD Loss", "Quantum Generative Modeling"]`).
    2. **Key Metrics** (`MetricsGrid`, 4 entries): e.g. held-out MMD² (0.0125±0.0003), ring_mass (0.691), parameter count (220), wall-clock training time (425.93s / ~7.1min). Pull exact numbers from `results/phase5_summary.md` and `results/phase4_summary.md` — do not invent or round differently than the source.
    3. **TL;DR** (`CalloutBox`): 3-4 sentence summary — what was built, the honest outcome (training clearly helped, held-out MMD² close to real-data floor, but ring structure not fully resolved).
    4. **Context** (`Section`): the problem (photonic generative modeling via MerLin's `QuantumLayer`), why MMD full-distribution matching was chosen over point-estimate/discrete-sampling, and explicit continuity with `iqp-mmd.tsx`: this is the same "does a good scalar loss number mean the model actually learned the target's structure?" question, now asked in a spatial/photonic domain instead of gate-model/combinatorial.
    5. **Methodology** (`Section` + `ProcessStep` sequence, numbered 01-05 or so): latent noise -> `QuantumLayer` encoding, K=462 bin-centers, closed-form MMD² over a Gaussian kernel, the natural-order-correspondence fix (radius-sorted bin centers + center-of-mass-sorted Fock states, replacing arbitrary index pairing) as its own numbered step since it's the key technical contribution.
    6. **Key Finding** (`Section`, dark, label "Key Finding"): title something like "Good MMD² != Clean Ring Structure" (mirrors `iqp-mmd.tsx`'s "MMD ≠ Shape" pattern). State plainly: held-out MMD² sits close to the real-data floor (0.0125±0.0003 vs 0.0114 floor) — looks good in isolation — but ring_mass≈0.68-0.69/gap_mass≈0.05 confirm the generated distribution is still not two recognizably distinct rings. Do not soften this.
    7. **Custom animated visualizations** (2-3 bespoke components, written locally in this file following `ACGapChart`/`BandwidthSweepViz`'s pattern: `useRef` + `useInView({ once: true, margin: "-40px" })` + staggered `motion.div` width animations, `transition={{ delay: i * 0.1 }}`):
       - A ring_mass-improvement chart across the three tuning axes (sigma sweep: 0.616 -> batch sweep: 0.609-0.618 -> natural-order fix: 0.691), matching `ACGapChart`'s target/learned dual-bar style but adapted to show improvement across axes.
       - A benchmark bar comparison: trained (0.0125) vs untrained (0.0360) vs floor (0.0114) held-out MMD², matching `BandwidthSweepViz`'s grid-of-bars style.
       Embed both inside `Section` blocks with a `DataTable` alongside each, same as `iqp-mmd.tsx`'s Anti-Concentration section pairs a chart with a table.
    8. **Technical Depth** (`Section`, grid of cards): 4-6 cards covering: MerLin `QuantumLayer` circuit construction, closed-form MMD² kernel implementation, the `ModGrouping`/K=462 no-fold decision, the radius/center-of-mass rank-pairing mechanism, held-out benchmarking methodology (BMK-01), and the honest self-diagnosis process (ruling out MMD loss itself as the primary culprit via the quickstart classifier's own weak baseline accuracy).
    9. **Role** (`Section` + `QuoteBlock`): solo project, own the full pipeline (architecture decision, loss implementation, training, diagnosis, benchmarking), built as a credential-building exercise for a Quandela internship conversation, methodology inherited/extended from the prior IQP-MMD project (own work, cite it explicitly as the source of the MMD/diagnostic approach).
    10. **What I'd Do Next** (`Section`, dark, list of 4-5 items): e.g. strengthen `fock_state_sort_order`'s center-of-mass heuristic (rank-domain corr(p_real,q)=0.38 is the diagnosed weak point), try a learned/differentiable output-ordering instead of a fixed heuristic, BMK-03 apples-to-apples QGAN comparison on a matched dataset, exact MNIST-patch paper replication (stretch goal noted in PROJECT.md).
    11. **Source Code** (`Section`): link to `https://github.com/alejack312/merlin-photonic-generative-modeling` (note in a code comment that this returns 404/private until the owner flips visibility per the separate 06-01 plan — do not block on this, ship the link as-is).
    12. **CrossLinks**: `<CrossLinks current="merlin-quantum" />` as the final element.

    Exact copy/prose wording and exact visual layout choices are your discretion, following `iqp-mmd.tsx`'s tone and density.
  </action>
  <verify>File exists, imports resolve, `ACCENT` is `"blue"`, contains at least 2 custom animated chart components (not `AnimatedBar` reuse alone, not static `<img>`), contains a "Key Finding" section with the honest ring-structure shortfall stated plainly, ends with `<CrossLinks current="merlin-quantum" />`.</verify>
  <done>merlin-quantum.tsx exists as a complete, self-contained case-study page matching iqp-mmd.tsx's structural depth (hero through cross-links, no section skipped).</done>
</task>

<task type="auto">
  <name>Task 2: Register the case study, verify build, commit and push</name>
  <files>C:\Users\cuqui\projects\alejandro-jackson\src\components\case-studies\shared.tsx, C:\Users\cuqui\projects\alejandro-jackson\src\pages\case-studies\index.tsx</files>
  <action>
    1. In `shared.tsx`, add a new entry to the `allStudies` array (around lines 448-479): `{ slug: "merlin-quantum", title: "MerLin Photonic Generative Modeling", accent: "blue" as AccentColor, keywords: "MerLin / Photonic / MMD Loss" }` (adjust title/keywords to match Task 1's final hero title, but slug must be exactly `"merlin-quantum"` to match Task 1's `CrossLinks current` prop).
    2. In `index.tsx`, add a new entry to the `caseStudies` array (around lines 11-79) with `title`, `slug: "merlin-quantum"`, `accent: "blue"`, `keywords: string[]`, `description` (1-2 sentence card summary), and `metrics: { label, value }[]` (3 entries, e.g. Held-out MMD², ring_mass, Params — matching Task 1's hero metrics style but condensed for the card).
    3. Fix the hardcoded count in `index.tsx`'s header copy: `"Five engineering case studies"` -> `"Six engineering case studies"` (line ~197).
    4. Run `cd "C:\Users\cuqui\projects\alejandro-jackson" && npm run lint` — fix any errors before proceeding.
    5. Run `cd "C:\Users\cuqui\projects\alejandro-jackson" && npm run build` — this performs TypeScript type-checking as part of `next build`; fix any errors before proceeding. Pay particular attention to the `AccentColor` type usage in both edited files (a bad accent key breaks the type for every existing case study, not just the new one — this is the Gate 2 blast-radius risk research flagged).
    6. Commit in the `alejandro-jackson` repo (separate git history from `merlin-quantum-case-study` — do not mix commits): `cd "C:\Users\cuqui\projects\alejandro-jackson" && git add src/pages/case-studies/merlin-quantum.tsx src/components/case-studies/shared.tsx src/pages/case-studies/index.tsx && git commit -m "feat(case-studies): add MerLin photonic generative modeling case study"`.
    7. Push: `git push origin main`.
  </action>
  <verify>`npm run lint` exits 0; `npm run build` exits 0 with no new TypeScript errors; `git log -1` in `alejandro-jackson` shows the new commit; `git push` succeeds; `grep -c "merlin-quantum" src/components/case-studies/shared.tsx src/pages/case-studies/index.tsx` each return >=1.</verify>
  <done>Case study registered in both allStudies and caseStudies, build/lint clean, committed and pushed to alejandro-jackson's own origin/main.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>A new interactive case-study page (`/case-studies/merlin-quantum`) with custom animated charts, registered on the case-studies index and in cross-links across all 6 case studies.</what-built>
  <how-to-verify>
    1. In `C:\Users\cuqui\projects\alejandro-jackson`, run `npm run dev` and visit `http://localhost:3000/case-studies/merlin-quantum` — confirm the page renders, all sections appear in order, the two custom charts animate on scroll into view, and no console errors appear.
    2. Visit `http://localhost:3000/case-studies` — confirm the new "MerLin Photonic Generative Modeling" card appears (6th card, header now says "Six engineering case studies"), with a blue accent visually distinct from the other 5 cards.
    3. From the `merlin-quantum` page, scroll to the bottom "More case studies" section — confirm it lists the other 5 studies (not itself) and each link works. From `iqp-mmd`'s own page, confirm its cross-links now include the new `merlin-quantum` card too.
    4. Confirm the "Key Finding" section reads honestly (states the ring-structure shortfall plainly, doesn't oversell the result).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. layout bug, wrong numbers, chart not animating, accent color looks wrong).</resume-signal>
</task>

</tasks>

<verification>
- `npm run lint` and `npm run build` pass in `alejandro-jackson` with no new errors.
- `merlin-quantum.tsx` exists, matches `iqp-mmd.tsx`'s section depth, uses `ACCENT = "blue"`.
- `shared.tsx`'s `allStudies` and `index.tsx`'s `caseStudies` both contain a `merlin-quantum` entry.
- `index.tsx` header copy says "Six" not "Five".
- Owner has visually verified the live page and cross-links (checkpoint task).
- Commit exists in `alejandro-jackson`'s own git history, pushed to its own `origin/main` — never mixed with `merlin-quantum-case-study`'s commits.
</verification>

<success_criteria>
DOC-04 (portfolio case study drafted in the IQP-MMD case-study format) is satisfied: a full interactive TSX page exists, is discoverable from the case-studies index, is cross-linked bidirectionally with all other case studies, uses a non-colliding accent color, and states the GEN-07 honest result as its "Key Finding" — owner-verified live.
</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation-publication/06-02-SUMMARY.md`
</output>
