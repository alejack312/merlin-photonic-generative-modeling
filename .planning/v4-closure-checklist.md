# v4.0 closure checklist

Date: 2026-09-07
Branch: `codex/v4-implementation`  
Base: `de80e9313beed614528fd6332b2f78aab83c0b50`  
Current integration point: the final commit recorded by `git rev-parse HEAD` in the handoff readback

This is the execution checklist for the original binding requirements. It separates implementation evidence from scientific acceptance. `BLOCKED` means an owner or external input is required; `INCONCLUSIVE` means the project has run evidence, but the evidence does not establish the full requirement; `NOT IMPLEMENTED` means the required implementation or run is absent.

| Requirement / group | Status | Missing implementation or evidence | Next concrete action | Owner / external dependency | Budget / acceptance evidence |
|---|---|---|---|---|---|
| TRAIN-01..04, CHAN-01..04, DEPLOY-01..02, DEPLOY-05, REFRAME-03 | PASS (bounded) | None for the declared NumPy, analytic-map, and fixed-photon `g2=0` scope. | Preserve legacy paths and rerun the applicable regression gates at integration. | None. | Full suite; focused v4 tests; resource pilot; compiler/map artifacts and hashes. |
| DEPLOY-03 | INCONCLUSIVE for the original broad wording | Fixed-photon n=2/3 source-once controls pass, but no `g2>0` joint source/loss validation exists. D1 deliberately excludes `g2>0`. | Keep the multiphoton row explicitly outside the selected scope; do not relabel the fixed-photon controls as universal full-Fock evidence. | Owner only if D1 is reopened; otherwise this is a declared scope limit. | `physical_control_manifest.json`; direct Perceval command; preserved historical failure artifact. |
| DEPLOY-04 | INCONCLUSIVE | Final-only direct full-Fock agrees with the composed map on tested fixtures, while intermediate projection differs by TVD `0.585411845271861`. This delimits two instruments but is not a proof for every topology/order. | Preserve the final-only boundary; add or retain a chain-specific no-return argument and independent small-n fixtures before any larger-n extrapolation. | No owner choice for the current boundary; physical capability/evidence is required for expansion. | Independent n=3 probe; n=2/3 manifest; direct physical vs analytic-map self-consistency labels. |
| RING-04 | INCONCLUSIVE | n=4 spatial and Hamming final-only photonic evaluations pass; n=6/n=8 photonic deployment is unsupported by the validated physical boundary. | Do not deploy n=6/n=8 photonic rings until a larger-n physical validation boundary is declared and passed. | Physical validation capability; no sibling dependency. | Registered n=4 ring JSON artifacts; direct-vs-compiled TVD below `2e-16`. |
| REPRO-03 | INCONCLUSIVE | `training_smoke` is faithful retraining; `bandwidth_marginal_sweep` is stopped partial replay; `ghosh_kim_small_n` is adapted checkpoint replay; other rows are blocked or scale-limited. | Supply exact source arrays/checkpoints for missing rows and run the registered retraining cells, or retain the explicit stopped/adapted/blocked dispositions. | Sibling source inputs/capabilities; sibling checkout itself must remain unchanged. | `results/v4_tcdp/sibling_replays/`; exact-path blocked manifests; sibling HEAD `f6d6ebe...`. |
| SWEEP-04 | INCONCLUSIVE | Bounded n=6/n=8 derived comparison artifacts now exist, but there is no complete sibling matched arm, no larger-n physical arm, and spatial replication is partial. | Extend only when exact sibling inputs and a validated physical deployment boundary exist; then regenerate paired figures and uncertainty without relabeling map references as physical results. | External sibling inputs; physical validation boundary; no new owner design choice. | `results/v4_tcdp/metrics/registered-comparison-evidence.md`; committed comparison JSONs. |
| COMPARE-01, COMPARE-04 | INCONCLUSIVE | Same-model ring raw/compiled/deployed-map comparisons are present, but sibling/substrate arms and complete figure panels are absent. | Add the exact sibling arm and figure generation after REPRO-03 and physical prerequisites close. | External sibling data and physical validation. | n=4 registered ring artifacts; n=6/n=8 map-derived comparison artifacts; hashes in each JSON. |
| NAT-01..02 | PASS (ideal/model-derived) | No missing implementation for the selected discrete/continuous optimizer or matched warm-start contracts. These results do not establish noisy superiority. | Preserve target improvement, fixed-reference distance, and acceptance as separate quantities; do not interpret as hardware efficacy. | None for the current ideal/model scope. | Registered n=4 seeds 0–4 and n=6/n=8 seed-0 reports; focused NAT tests. |
| NAT-03 | PASS (registered stop-rule evidence) | None for the retained stop rule. Registered n=4 runs are green/completed, so the “two calendar days without green n=4” condition was not triggered. A fresh bounded n=8 matched run records 264.50 seconds against the 1,200-second limit and completed both arms. | Do not launch a two-day n=4 wait; preserve the timing artifact and stop-rule interpretation. | None. | `results/v4_tcdp/nat/closure_timing/20260907_n8_seed0_timing-report.json`; focused NAT tests. |
| NULL-03..08 | INCONCLUSIVE | Owner-authored predictions and falsification criteria are now recorded in [docs/v4-owner-predictions-2026-09-08.md](../docs/v4-owner-predictions-2026-09-08.md). Registered control evidence still must be evaluated; NULL-08 is bound to identical continued-ideal arms, while the continuous-versus-discrete statement is explicitly exploratory and non-binding. | Run the registered k=0, compiler/reference, fixed-photon loss, throughput, source-isolation, and matched NAT checks; retain metric-specific tolerances and separate absolute mass from conditional quality. | None for authorship; remaining dependency is registered evidence and, for NULL-07, source-model capability. | Dated owner note; exact deterministic checks use `1.0e-16` where supported, while binding plan probability/map and sampled/optimization tolerances remain in force. |
| NULL-09 | PASS (bounded control panel) | None for implemented mutation and equality controls. | Keep metric-specific mutations separate from map/success mutations. | None. | Comparison smoke artifacts and focused comparison tests. |
| WRITE-07 | BLOCKED | The required owner explanation of signs/winding, source vs gate noise, projection, loss, conditioning, and NAT scope is not yet owner-authored. | After the literature review, owner writes the explanation in their own words; Codex may check technical consistency but must not author it retroactively. | Owner learning and authorship. | Explanation transcript or dated project note before interpretive synthesis. |
| WRITE-08..09, REVIEW-02 | PASS (review gate; scientific rows remain incomplete) | The independent post-integration review findings were repaired and rechecked at the current integration head. Scientific requirements that remain incomplete are recorded in their own rows. | Preserve the final review disposition and keep the incomplete scientific rows explicit. | None for the review gate; owner/external dependencies remain on the affected scientific rows. | Full suite, explicit sibling integration, artifact validator, current-head reviewer disposition. |
| COMM-02 | BLOCKED | No owner journal/Vincent note or hold record exists. | Owner authors a draft or records an explicit hold; sending remains separately unauthorized. | Owner. | Dated draft/hold record; no external send. |

## Work that can proceed without owner predictions

- Add and verify missing timing provenance for the already-approved bounded NAT n=8 run; do not run the two-day n=4 arm while registered n=4 is green.
- Validate the committed sibling blocked/stopped/adapted manifests and source identity without changing the sibling checkout.
- Run the full suite, explicit sibling integration, artifact validation, and focused physical/comparison checks with a writable Perceval path.
- Perform the independent post-integration review and repair code/evidence defects.
- Reconcile the ledger, completion report, and reviewer handoff with the final commit and exact artifact hashes.

## Work that must remain open

- Do not claim `g2>0` source behavior, general final-only/intermediate equivalence, or n=6/n=8 photonic deployment.
- Do not call checkpoint replay faithful retraining or model-derived deployed-map arms physical photonic results.
- Do not invent NULL predictions, WRITE-07 prose, scientific explanations, or a COMM-02 communication artifact.
