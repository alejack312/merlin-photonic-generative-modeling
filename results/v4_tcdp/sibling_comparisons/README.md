# Sibling comparison namespaces

`closure_20260908_complete_v4/` is the canonical eight-cell sibling-to-substrate comparison used by the v4 ledger and reports. It contains one training-smoke cell, four bandwidth cells, and three Ghosh–Kim cells.

The other dated namespaces are preserved execution history from bounded closure attempts. They may contain partial or superseded outputs and are not acceptance evidence. They are retained to preserve provenance and failed-run diagnostics; consumers should use only the canonical summary unless a namespace is explicitly named.
