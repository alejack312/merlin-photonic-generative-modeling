Complexity phase transitions in instantaneous quantum polynomial-time circuits
Chae-Yeun Park1, 2 and Michael J. Kastoryano1, 3, 4 1Institute for Theoretical Physics, University of Cologne, 50937 Ko¨ln, Germany
2Xanadu, Toronto, ON, M5G 2C8, Canada 3Amazon Quantum Solutions Lab, Seattle, Washington 98170, USA 4AWS Center for Quantum Computing, Pasadena, California 91125, USA
(Dated: June 27, 2023)
We study a subclass of the Instantaneous Quantum Polynomial-time (IQP) circuit with a varying density of two-qubit gates. In addition to a known anticoncentration regime, we identify novel parameter conditions where the model is classically simulable or the output distribution follows the Porter-Thomas distribution. By showing that those parameter regimes do not coincide, we argue the presence of more than two phases in the model. The learnability of the output distribution of this model is further studied, which indicates that an energy-based model fails to learn the output distribution even when it is not anticoncentrated. Our study reveals that a quantum circuit model can have multiple fine-grained complexity phases, suggesting the potential for quantum advantage even when the output distribution is far from the Porter-Thomas distribution.

arXiv:2204.08898v2 [quant-ph] 24 Jun 2023

It is widely believed that classical computers require an exponential amount of resources to simulate a general quantum system. Computational complexity theory provides a rigorous mathematical foundation of this belief by proving that the output distribution of a certain subclass of quantum system is classically intractable. Such examples include BosonSampling [1], instantaneous quantum polynomial-time (IQP) circuits [2, 3] as well as random circuits with one and two qubit gates [4, 5] on which Google's recent "quantum advantage" claim [6] is based. On the other hand, when the unitary operators allowed in the system are insufficient for universal quantum computation, one can often devise an efficient classical algorithm for this task. Such examples include free fermions [7], matchgates [8­10], and Clifford [11, 12] circuits. Then a natural question arises: "Where is the boundary between classically simulable and intractable quantum systems?"
For Clifford circuits, it is known that adding any nonzero density of T -gates induces a transition to a classically intractable circuit [13­16]. However, only a partial answer is available for other models. Several studies [17­ 19] showed that classically easy and difficult phases exist in dynamics of interacting bosons, but those results are often limited to the worst-case instances. On the other hand, Ref. [20, 21] claimed that a transition between thermalizing and many-body localized phases in interacting spins with a periodic driving is related to sampling complexity. Albeit their numerical results demonstrate that the output distribution follows the Porter-Thomas distribution in the thermalizing phase, their complexity arguments rely on a strong assumption that circular unitary ensemble is classically intractable.
In this work, we study complexity phase transitions in IQP circuits using the density of two-qubit gates as a parameter. Compared to other models, rigorous arguments on the classical hardness of average instances have been established for the IQP circuits [2, 3, 22]. In addition to a known parameter condition for anticoncentration [22], we find parameter regimes where the model is classically

simulable or the output distribution follows the PorterThomas distribution. By showing that those parameters regimes do not coincide, we suggest the model has more than two distinct phases.
We further explore the classical learnability of the output distribution. We first show that the output distribution of the worst-case instance is not PAC-learnable for circuits with N (1) two-qubit gates. However, this theoretical tool is too limited when studying typical instances. We address this problem by introducing a tractability measure based on the classical parent Hamiltonian whose Gibbs state represents the distribution. Our measure is closely related to the energy-based model [23] that has been widely used for modeling the distribution. By computing the tractability measure as well as training a neural network, we argue that classical learnability also undergoes a transition but earlier than the transition to the anticoncentration or to the Porter-Thomas distribution. This further supports that the model has several complexity phases.
Instantaneous quantum polynomial-time circuit in the
Erdos-R´enyi graph.­ Throughout the paper, we consider the output distribution p(x) = | x| |2 of a quantum state generated by an IQP-type circuit | = HN Udiag |+N where Udiag is a diagonal unitary gate in the computational basis, H is the Hadamard gate, N is the number of qubits, and |x is a vector in the computational basis. We especially use

Udiag = Fi,ij = exp[i iZi] exp[i ij ZiZj ] (1)

i

i<j

where Zi is the Pauli-Z matrix acting on the i-th qubit.
We always sample {i} from the uniform distribution
U[0,2] but two body interactions {i,j} are only turned on with a probability q, i.e., for each pair (i, j), we set ij = (q - q~)~ij where (x) is the Heavyside step function, q~ is a random sample from U[0,1], and ~ij is randomly sampled from U[0,2]. In other words, the twobody interaction terms are chosen using the Erdos-R´enyi

2

random graph [24]. Formally, the Erdos-R´enyi G(N, q) is defined for N vertices, and each unique pair of vertices (i, j) are connected by an edge with probability q. Thus the diagonal gate contains only single-qubit gates when q = 0, whereas it also has 2-qubit gates when q > 0. As the Erdos-R´enyi random graph shows phase transitions in its connectedness [24] and treewidth [25], it is natural to expect that IQP circuits over the random graph also show several phases in their properties.
The hardness proof of the sampling task typically relies on three conjectures: (C1) The polynomial hierarchy is infinite, (C2) It is #P-Hard to approximate (up to a constant relative error) the output probability p(x) for a constant fraction of instances, and (C3) the output distribution is anticoncentrated. For the IQP circuit over a complete graph (i.e., q = 1), Ref. [3] showed that the output distribution satisfies C3, while C2 is true if approximating the partition function of the classical Ising model with complex weights defined over the same graph is #P-Hard for a constant fraction of instances (which is widely believed to be true). The result is relaxed to a sparse graph with q = O(log N/N ) in Ref. [22], but it requires a stronger condition that computing the partition function of the Ising model in a sparse graph is also difficult, which is less obvious. While approximating the output probability of IQP circuits over some 2D lattices has been proven to be #P-Hard [26­28], the average-case hardness for circuits over random graphs remains as a conjecture. Thus, the primary aim of the first part of this paper is to unveil the relationship between graph sparsity and the complexity of the sampling task with several theoretical and numerical techniques.
Sampling difficulty of the IQP circuit.­ Our first result is that there is a classical algorithm for sampling from IQP circuits over the random graph when it is too sparse.
Proposition 1 (Classical algorithm for sampling). Let C be the distribution of IQP circuits over the Erdos-R´enyi random graph with the connection probability q < 1/N . Then 1 - O(1/N ) fraction of C allows an efficient classical algorithm for sampling from the output distribution.
We briefly sketch the idea here. Full details of the algorithm can be found in Appendix A. For a given graph G, our circuit has a two-qubit gate between i-th and j-th qubits iff the graph has an edge (i, j). We can interpret the circuit as a tensor network, which becomes a subgraph of the line graph of G. Then the tensor network contraction algorithm [29] can simulate the circuit in O(N 3 exp((G)tw(G))) time where (G) is the maximum degree of G and tw(G) is the treewidth of G. When q < 1/N , the maximum degree of G is less than O(log N ) for 1 - O(1/N ) fraction of instances and the treewidth of the random graph is constant [25], so we have a polynomial time algorithm for computing the output distribution. One can also extend this algorithm for the sampling task using the tensor network description of the marginal distribution instead of the output state.
We next show that the output distribution of IQP cir-

KL(P ||PPT)
N -/ KL(P ||PPT)

1.5

1.0 0.5 qc0  0.106

0.0

0.0

0.1

  0.106

1.5

  1.007

1.0

  0.000

0.5

0.0

0

5

N 1/ (q - qc0 )

N = 24
0.2 q

N =8

0.3

0.4

FIG. 1. Phase transition of the KL divergence between the observed distribution of the output probability p(x) from the IQP circuit and the Porter-Thomas distribution. Data from the system of sizes N = 8 to N = 24 collapses to a single curve. The transition point pc  0.106 is observed. The results are obtained from 214 random circuit instances for 8  N  22 and 210 instances for N = 24. For each instance of a random circuit, we randomly sampled all parameters and graph edges. As datapoints for small N are subject to discretization error (as we binned the distributions), they are excluded from the data collapse analysis.

cuits over the random graph is anticoncentrated (C3).
Formally, we say that the output distribution is anticon-
centrated if there exists constants ,  > 0 such that for any x  {0, 1}N ,

PrU

| x|U |0 |2



 2N

 .

(2)

The following proposition shows that IQP circuits over the random graph shows this property.

Proposition 2 (Anticoncentration). Consider IQP circuits defined over the Erdos-R´enyi random graph. There exists a constant  > 0 such that the output distribution of the circuit is anticoncentrated when q   log(N )/N .

The anticoncentration of IQP circuits over the random graph but with diagonal gates of discrete parameters is proved in Ref. [22]. We show in Appendix B that the same result also holds for continuous random parameters as in our case.
Still, anticoncentration itself is not sufficient for the difficulty of the distribution. Thus lots of existing literature in quantum sampling advantage [4­6, 27, 30] also have considered the exact distribution of the output bitstrings. When the output distribution of a circuit shows quantum advantage, one expects that the distribution of the output probability p(x) is close to that of a random vector in the Hilbert space, which is known as the PorterThomas [31] distribution. Thus distance between the output distribution of the circuit and the Porter-Thomas distribution is widely used to verify quantum advantage in the sampling task [4­6, 27, 30].

3

In our model, we numerically locate the critical point
qc where the output distribution becomes the PorterThomas distribution for q > qc.

Observation 1. The output distribution follows the Porter-Thomas distribution in the thermodynamic limit (N  1) if and only if q > qc with qc  0.106.

Our observation is based on the strong finite-size scaling behavior of the Kullback­Leibler (KL) divergence between the statistics of output probabilities p(x) and the Porter-Thomas distribution, which is shown in Fig. 1. Precisely, all data from 8 to 24 qubits collapse to a single curve f~ after rescaling, which is given by

N -/ yN = f~(N 1/ (q - qc)),

(3)

where yN are obtained data KL(P ||PP T ) for N -qubit system, ,  are critical exponents, and qc is the phase transition point. In the thermodynamic limit (N  1), our collapsing result implies that there is an abrupt change of the distribution at q = qc.
We note that, even though our observation is based on numerical results, finite-size scaling analysis [32, 33] is one of the most rigorous tools for identifying phase transitions and successfully used for analyzing phases in complex models [34­37], whose theoretical investigations are limited. In Appendix C, we show that there is a constant  > 0 such that, for q = (log N/N ), the KL divergence converges to a constant as N increases albeit the distribution is anticoncentrated. We also provide additional evidence of the phase transitions using entanglement spectra of output quantum states in Appendix D.
Our results so far indicate that several complexity measures do not coincide, and the model may exhibit several phase transitions. Nonetheless, we still do not know exactly when the distribution becomes classical intractable, as the complexity results rely on the assumption that approximating the partition function of the classical Ising model over corresponding graphs is also difficult [3, 22]. Therefore, proving computational hardness of the Ising model yields different consequences of our results. For example, if computing the partition function of the Ising model over a sparse graph with q = O(log N/N ) is proven to be classically hard, there is a circuit whose output distribution does not follow the Porter-Thomas distribution but still classically difficult to be sampled from. So there can be a better measure detecting sampling complexity of the distribution. On the other hand, if computing the partition function with q = O(log N/N ) turns out to be easy (i.e., there is a classical algorithm for this task), there is a family of the IQP circuit whose output distribution is anticoncentrated but can be classically estimated. This can be an example of non-trivial classically tractable circuit with large entanglement (such as a Clifford circuit).
Learning the output distribution.­ We now shift our focus to a learning task, i.e., whether a classical algorithm can reproduce the output distribution of a circuit when

samples are provided. Albeit the relation between sampling difficulty and learnability is subtle, it is often believed that learning the output distribution of a circuit is also classically hard when the sampling task is difficult, as it implies that one can mimic quantum randomness [38] using a classical computer with a polynomial number of samples from a real quantum device. Hence, the ability of a classical machine learning algorithm to learn the distribution can be another diagnostic tool to measure the intrinsic complexity of the distribution.
As in the sampling task, we use a tool from the complexity theory, the PAC learning framework, to study the basic property of the circuit. The PAC learning theory of discrete distributions often considers two different classes of classical models named evaluator and generator [39]. Given a polynomial number of samples from p(x), an evaluator model learns how to evaluate the probability distribution, i.e., for any given x, it can evaluate p(x) up to some error. On the other hand, a generator model can generate a sample using random numbers. Then the efficient learnability w.r.t. those models is defined as follows.
Definition 1. Let DN be a class of distributions over {0, 1}N . We say that DN is efficiently learnable w.r.t. evaluator (or generator) if there is an algorithm that, for any target distribution P  DN and any given ,   [0, 1], outputs a circuit for evaluating Q(x) (or generating samples from Q) with probability at least 1 -  in time polynomial in 1/, 1/ and N , where the distribution Q satisfies TV(P, Q)  . Here, TV(P, Q) =
x{0,1}N |P (x) - Q(x)|/2 is the total variational distance.
We now consider the output distribution from IQP circuits. When q = 0, the circuit produces a product state, and we expect that it is easy to learn the distribution, which we prove as follows.
Proposition 3. The output distribution from a product state is efficiently learnable both w.r.t. evaluator and generator.
On the other hand, under a plausible assumption, it is also possible to argue that learning the output distribution of IQP circuits over a sparse graph with any non-zero q is classically hard for the worst-case instance.
Proposition 4. Consider the noisy parity distribution for k + 1 bits Ps,(x, y) defined for s, x  {0, 1}k,   (0, 1/2), and y  {0, 1}, which is given by Ps,(x, x · s) = (1 - )2-k and Ps,(x, ¬x · s) = 2-k. Under the assumption that a product distribution Ps,  1k is not efficiently learnable w.r.t. an evaluator, where 1k is the uniform distribution over k bits, the output distribution IQP circuits with N (1) two-qubit gates is not efficiently learnable w.r.t. an evaluator, either.
We provide proofs of Propositions 3 and 4 in Appendix E.

4

J

2.5

N = 24

2.0

1.5

1.0

0.0

0.1

10-2 10-3

q = 0.00 q = 0.02 q = 0.04 q = 0.06

0

1

2

Nq

N

=

8
(a)

0.2

0.3

0.4

q

q = 0.08

(b)

q = 0.10

q = 0.12

KL[pW (x)||p(x)]

10-4 8

12

16

20

N

FIG. 2. (a) Averaged sums of the Hamiltonian coefficients J = k= 0 |J(k)| after normalization. (Inset) The same plot
as a function of N q. When N q  1.0, J = log J / log N diverges with N , which implies a super-polynomial scaling of J . (b) The Kullback-Leibler divergence between the output distribution of the IQP circuits p(x) and that from the energy-based model after convergence p~W (x). The parameter q ranges in value from q = 0.0 (the lightest color) to q = 0.12 (the darkest color) with spacing 0.02. Each data point shows the KL divergence between the output distribution from each circuit realization (randomly chosen {i} and {ij}) and the trained distribution using that distribution. For each q and N , we ran 24 independent circuit realizations and show averaged results by lines.

However, those results tell little about learning the average-cases of the IQP circuit. We thus instead consider a concrete model for the probability distribution known as the energy-based model [23] and ask whether such a model can learn the distribution (this type of question is often called representation dependent hardness) [40].
When p(x)  0 for all x, one can consider a classical parent Hamiltonian which gives p(x)  e-H(x). An energy-based model learns H(x) instead of p(x). As

x  {0, 1}N , one can always expand H(x) as

-H(x) = J0 + JiZi(x) + JijZi(x)Zj(x)

i

ij

+ JijkZi(x)Zj(x)Zk(x) + · · · , (4)
ijk

where the sum is over the distinct indices, and Zi(x) = (-1)xi . This is just a change of variable x to 1 - 2x =: z  {-1, 1}N . Each energy-based model can learn dif-

ferent form of coefficients {J0, Ji, Jij, · · · }. For example, the Boltzmann machine [41] (based on the classical

Ising model) exactly uses the second order Hamiltonian

(with Ji,js). Thus it can exactly capture any second or-
der correlations of xi, but not the higher order terms.
On the other hand, a single layer fully connected network with weights wi and x3 activation function gives -H(x) = ( i wixi)3, which can approximate some third order correlations. However, any of those models cannot

exactly express all high-order terms unless exponentially

many parameters are provided, as the number of coeffi-

cients (Js) is exponentially large.

Based on this observation, we use two different diag-

nostics for the learning complexity of the output distri-

bution. First, we compute J = S=  |JS| where JS are coefficients from the Hamiltonian expansion H(x) =

- of

S JS indices

i
S

Zi(x), and the summation is over all subsets  {1, · · · , N }. For a given p(x)  0, this

measure can be computed using the Hadamard-Walsh

transformation of log p(x) (see Appendix F). We also note

that this measure can be seen as a classical analogy of the

quantum circuit complexity [42­44]. Second, we indeed

train a simple energy-based model for the output distri-

bution of the IQP circuits. For each output distribution

of an IQP circuit p(x) from a given graph and parame-

ters {i, ij}, we optimize the log-likelihood cost function given as x p(x) log p~W (x) where p~W (x) is the distribution of the energy-based model (i.e., pW (x)  efW (x) where fW (x) is the output of the network with weights W for an input x). Then we compute KL[p~W (x)||p(x)], the distance between the converged distribution p~W (x) and p(x) (see Appendix G for detailed machine learning

set-ups).

We can already expect some behaviors of those diag-

nostics for extreme cases. When q = 0, one can find

a Hamiltonian with single body terms that exactly represents the output distribution, i.e., p(x)  e- i JiZi(x)

(see Appendix F). In this case, J = O(N ) and we expect

that a simple network sufficiently learns the distribution.

On the other hand, when the distribution is classically

hard, we expect that our measure J is exponentially

large and the model does not learn the distribution. We

thus introduce a normalized measure J = log J / log N

that converges to a constant when J is polynomial in N ,

whereas it diverges if J is exponentially large.

We plot the scaling of the measure J computed for

the output distribution of the circuits in Fig. 2(a). Even

though the exact phase transition point is not located,

5

we see that the normalized measure J increases with N for N q  1.0, which suggests that learning the output distribution becomes difficult in this regime. We also plot Fig. 2(b) the distance between the distribution of the network after training p~W (x) and the circuit output distribution p(x) trained for up to N = 20.
We see that the converged distance increases exponentially with N even when 0.06  q, where the output distribution is still far from the scrambled Porter-Thomas distribution. Thus both figures suggest that learning the distribution becomes difficult earlier than the distribution becomes anticoncentrated or scrambled.
In Appendix F, we provide additional numerical results supporting the learnability transition from approximate parent Hamiltonians as well as machine learning results with a different optimization algorithm. We further note that the networks we used here (with  2N 2 parameters with  = 30) have  2 × 103 times more parameters than the IQP circuit itself (with  N 2/2 parameters), which agrees with observations that classical description of a distribution from a quantum system can be extremely inefficient [45­47]. Our results suggest that a classical learner cannot easily spoof the output distribution of a circuit even when it is far from the Porter-Thomas distribution.
Conclusion.­ By combining the complexity arguments with extensive numerical studies, we have shown that IQP circuits over the random graph undergo several

phase transitions related to complexity as the density of two-qubit gates increases. We additionally argued that the classical learnability the output distribution also becomes classically intractable as the density increase but much earlier than that of the sampling. Our study shows that the IQP circuit has several fine-grained complexity phases and suggests that the model may show a quantum advantage even when the output distribution is far from the Porter-Thomas distribution. An interesting open question is whether models with 2D architecture [6, 27, 48, 49] also have such an intermediate phase. As numerical study of those models require more careful analysis due to strong finite-size effects, we reserve the answer to this question for future research.
Acknowledgements.­ CYP thanks Jaeyoon Cho, Changhun Oh, Youngrong Lim, and Anuj Apte for helpful discussions and David Wierichs for useful comments. This project was funded by the Deutsche Forschungsgemeinschaft under Germany's Excellence Strategy - Cluster of Excellence Matter and Light for Quantum Computing (ML4Q) EXC 2004/1-390534769 and within the CRC network TR 183 (project grant 277101999) as part of project B01. The numerical simulations were performed on the Juwels and Juwels-Booster clusters at the Forschungszentrum Juelich. The work presented here was completed while both authors were at the University of Cologne.

[1] S. Aaronson and A. Arkhipov, The computational complexity of linear optics, in Proceedings of the forty-third annual ACM symposium on Theory of computing (2011) pp. 333­342.
[2] M. J. Bremner, R. Jozsa, and D. J. Shepherd, Classical simulation of commuting quantum computations implies collapse of the polynomial hierarchy, Proc. R. Soc. A 467, 459 (2011).
[3] M. J. Bremner, A. Montanaro, and D. J. Shepherd, Average-case complexity versus approximate simulation of commuting quantum computations, Phys. Rev. Lett. 117, 080501 (2016).
[4] S. Boixo, S. V. Isakov, V. N. Smelyanskiy, R. Babbush, N. Ding, Z. Jiang, M. J. Bremner, J. M. Martinis, and H. Neven, Characterizing quantum supremacy in nearterm devices, Nat. Phys. 14, 595 (2018).
[5] A. Bouland, B. Fefferman, C. Nirkhe, and U. Vazirani, On the complexity and verification of quantum random circuit sampling, Nat. Phys. 15, 159 (2019).
[6] F. Arute, K. Arya, R. Babbush, D. Bacon, J. C. Bardin, R. Barends, R. Biswas, S. Boixo, F. G. Brandao, D. A. Buell, et al., Quantum supremacy using a programmable superconducting processor, Nature 574, 505 (2019).
[7] E. Lieb, T. Schultz, and D. Mattis, Two soluble models of an antiferromagnetic chain, Annals of Physics 16, 407 (1961).
[8] E. Knill, Fermionic linear optics and matchgates, arXiv preprint quant-ph/0108033 (2001).
[9] L. G. Valiant, Quantum circuits that can be simulated

classically in polynomial time, SIAM Journal on Computing 31, 1229 (2002). [10] B. M. Terhal and D. P. DiVincenzo, Classical simulation of noninteracting-fermion quantum circuits, Phys. Rev. A 65, 032325 (2002). [11] D. Gottesman, The heisenberg representation of quantum computers, arXiv preprint quant-ph/9807006 (1998). [12] S. Aaronson and D. Gottesman, Improved simulation of stabilizer circuits, Phys. Rev. A 70, 052328 (2004). [13] S. Zhou, Z. Yang, A. Hamma, and C. Chamon, Single T gate in a clifford circuit drives transition to universal entanglement spectrum statistics, SciPost Physics 9, 087 (2020). [14] J. Haferkamp, F. Montealegre-Mora, M. Heinrich, J. Eisert, D. Gross, and I. Roth, Quantum homeopathy works: Efficient unitary designs with a system-size independent number of non-clifford gates, arXiv preprint arXiv:2002.09524 (2020). [15] S. Bravyi and D. Gosset, Improved classical simulation of quantum circuits dominated by clifford gates, Phys. Rev. Lett. 116, 250501 (2016). [16] L. Leone, S. F. Oliviero, Y. Zhou, and A. Hamma, Quantum chaos is quantum, Quantum 5, 453 (2021). [17] A. Deshpande, B. Fefferman, M. C. Tran, M. Foss-Feig, and A. V. Gorshkov, Dynamical phase transitions in sampling complexity, Phys. Rev. Lett. 121, 030501 (2018). [18] C. Oh, Y. Lim, B. Fefferman, and L. Jiang, Classical simulation of Boson sampling based on graph structure,

6

Phys. Rev. Lett. 128, 190501 (2022). [19] N. Maskara, A. Deshpande, A. Ehrenberg, M. C. Tran,
B. Fefferman, and A. V. Gorshkov, Complexity phase diagram for interacting and long-range bosonic hamiltonians, Phys. Rev. Lett. 129, 150604 (2022). [20] J. Tangpanitanon, S. Thanasilp, M.-A. Lemonde, and D. G. Angelakis, Quantum supremacy with analog quantum processors for material science and machine learning, arXiv preprint arXiv:1906.03860 (2019). [21] J. Tangpanitanon, S. Thanasilp, M.-A. Lemonde, N. Dangniam, and D. G. Angelakis, Signatures of a sampling quantum advantage in driven quantum many-body systems, Quantum Science and Technology 8, 025019 (2023). [22] M. J. Bremner, A. Montanaro, and D. J. Shepherd, Achieving quantum supremacy with sparse and noisy commuting quantum computations, Quantum 1, 8 (2017). [23] Y. LeCun, S. Chopra, R. Hadsell, M. Ranzato, and F. Huang, A tutorial on energy-based learning, Predicting structured data 1 (2006). [24] P. Erdos, A. R´enyi, et al., On the evolution of random graphs, Publ. Math. Inst. Hung. Acad. Sci 5, 17 (1960). [25] C. Lee, J. Lee, and S.-i. Oum, Rank-width of random graphs, Journal of Graph Theory 70, 339 (2012). [26] X. Gao, S.-T. Wang, and L.-M. Duan, Quantum supremacy for simulating a translation-invariant Ising spin model, Phys. Rev. Lett. 118, 040502 (2017). [27] J. Bermejo-Vega, D. Hangleiter, M. Schwarz, R. Raussendorf, and J. Eisert, Architectures for quantum simulation showing a quantum speedup, Phys. Rev. X 8, 021010 (2018). [28] J. Haferkamp, D. Hangleiter, A. Bouland, B. Fefferman, J. Eisert, and J. Bermejo-Vega, Closing gaps of a quantum advantage with short-time hamiltonian dynamics, Physical Review Letters 125, 250501 (2020). [29] I. L. Markov and Y. Shi, Simulating quantum computation by contracting tensor networks, SIAM Journal on Computing 38, 963 (2008). [30] L. S. Madsen, F. Laudenbach, M. F. Askarani, F. Rortais, T. Vincent, J. F. Bulmer, F. M. Miatto, L. Neuhaus, L. G. Helt, M. J. Collins, et al., Quantum computational advantage with a programmable photonic processor, Nature 606, 75 (2022). [31] C. E. Porter and R. G. Thomas, Fluctuations of nuclear reaction widths, Phys. Rev. 104, 483 (1956). [32] M. E. Fisher and M. N. Barber, Scaling theory for finitesize effects in the critical region, Phys. Rev. Lett. 28, 1516 (1972). [33] J. Cardy, Finite-size scaling (Elsevier Science, 2012). [34] C. C. Price and N. B. Perkins, Critical properties of the kitaev-heisenberg model, Phys. Rev. Lett. 109, 187201 (2012). [35] D. J. Luitz, N. Laflorencie, and F. Alet, Many-body localization edge in the random-field heisenberg chain, Phys. Rev. B 91, 081103 (2015). [36] B. Skinner, J. Ruhman, and A. Nahum, Measurementinduced phase transitions in the dynamics of entanglement, Phys. Rev. X 9, 031009 (2019). [37] A. Lavasani, Y. Alavirad, and M. Barkeshli, Measurement-induced topological entanglement transitions in symmetric random quantum circuits, Nature Physics 17, 342 (2021). [38] S. Aaronson and S.-H. Hung, Certified randomness from

quantum supremacy, arXiv preprint arXiv:2303.01625 (2023). [39] M. Kearns, Y. Mansour, D. Ron, R. Rubinfeld, R. E. Schapire, and L. Sellie, On the learnability of discrete distributions, in Proceedings of the twenty-sixth annual ACM symposium on Theory of computing (1994) pp. 273­282. [40] In principle, the energy-based model itself is neither an evaluator nor a generator, as the RBM contains the spinglass problem evaluating probability or sampling from which is generally NP-Hard [50]. In practical use case, however, where the Markov chain Monte-Carlo generates an accurate sample in a polynomial time, the probability distribution can also be estimated [51, 52]. Thus it is both an evaluator and generator when this condition holds. [41] G. E. Hinton, Boltzmann machine, Scholarpedia 2, 1668 (2007). [42] M. A. Nielsen, A geometric approach to quantum circuit lower bounds, arXiv preprint quant-ph/0502070 (2005). [43] M. A. Nielsen, M. R. Dowling, M. Gu, and A. C. Doherty, Quantum computation as geometry, Science 311, 1133 (2006). [44] D. Stanford and L. Susskind, Complexity and shock wave geometries, Phys. Rev. D 90, 126007 (2014). [45] X. Gao and L.-M. Duan, Efficient representation of quantum many-body states with deep neural networks, Nat. Commun. 8, 1 (2017). [46] M. Y. Niu, A. M. Dai, L. Li, A. Odena, Z. Zhao, V. Smelyanskyi, H. Neven, and S. Boixo, Learnability and complexity of quantum samples, arXiv preprint arXiv:2010.11983 (2020). [47] C.-Y. Park and M. J. Kastoryano, Expressive power of complex-valued restricted boltzmann machines for solving nonstoquastic hamiltonians, Phys. Rev. B 106, 134437 (2022). [48] J. C. Napp, R. L. La Placa, A. M. Dalzell, F. G. Brandao, and A. W. Harrow, Efficient classical simulation of random shallow 2d quantum circuits, Physical Review X 12, 021021 (2022). [49] A. M. Dalzell, N. Hunter-Jones, and F. G. Branda~o, Random quantum circuits anticoncentrate in log depth, PRX Quantum 3, 010333 (2022). [50] F. Barahona, On the computational complexity of Ising spin glass models, Journal of Physics A: Mathematical and General 15, 3241 (1982). [51] M. Jerrum and A. Sinclair, Polynomial-time approximation algorithms for the Ising model, SIAM Journal on computing 22, 1087 (1993). [52] R. M. Neal, Annealed importance sampling, Statistics and computing 11, 125 (2001). [53] N. Robertson and P. D. Seymour, Graph minors. ii. algorithmic aspects of tree-width, Journal of algorithms 7, 309 (1986). [54] A. Pal and D. A. Huse, Many-body localization phase transition, Phys. Rev. B 82, 174411 (2010). [55] Y. Atas, E. Bogomolny, O. Giraud, and G. Roux, Distribution of the ratio of consecutive level spacings in random matrix ensembles, Phys. Rev. Lett. 110, 084101 (2013). [56] M. Hinsche, M. Ioannou, A. Nietner, J. Haferkamp, Y. Quek, D. Hangleiter, J.-P. Seifert, J. Eisert, and R. Sweke, A single t-gate makes distribution learning hard, arXiv preprint arXiv:2207.03140 (2022). [57] D. P. Kingma and J. Ba, Adam: A method for stochastic optimization, arXiv preprint arXiv:1412.6980 (2014).

7

[58] D. Pfau, J. S. Spencer, A. G. Matthews, and W. M. C. Foulkes, Ab initio solution of the many-electron schr¨odinger equation with deep neural networks, Phys. Rev. Research 2, 033429 (2020).

[59] S.-I. Amari, Natural gradient works efficiently in learning, Neural computation 10, 251 (1998).
[60] C.-Y. Park and M. J. Kastoryano, Geometry of learning neural quantum states, Phys. Rev. Research 2, 023232 (2020).

Appendix A: Classical algorithm for sampling from the IQP distribution
In this section, we (1) introduce a classical algorithm for sampling from the output distribution of IQP circuits and (2) show that more than 1 - O(1/N ) fraction of the IQP circuit instances over the Erdos-Reyni is efficiently classically simulable when the connection probability is smaller than 1/N . Our algorithm is based on tensor network contraction, whose complexity is given by in terms of the treewidth of the underlying graph [29].

1. Complexity of contracting tensor networks

In this subsection, we introduce a result by Markov and Shi [29] which studied the complexity of contracting tensor networks. Let us first define a tree decomposition of a graph.
Definition 2. For a graph G = (V, E), a tree decomposition of G is a pair (X , T ) where X = {X1, · · · , Xn} is a collection of vertices of G (sometimes called bags), and T is a tree whose nodes are Xi, which satisfies the following properties:
1. For all v  V , v appears at least one bag.

2. For every edge (v, w)  E, there is a node in T that contains both v and w.

3. If Xi and Xj both contain a vertex v  V , all nodes Xk of a tree in the path between Xi and Xj also contain v.
For each resulting tree from a tree decomposition, the width of the tree is the size of largest bag minus 1. The treewidth of a graph is defined as follows:
Definition 3. The treewidth of a graph tw(G) is defined by the the minimum width among all possible tree decompositions of G.
We next define the line graph as follows:
Definition 4. The line graph L(G) of a undirected graph G = (V, E) is a graph with vertex set E where two vertices are adjacent if they have a common vertex in G.
There are several well-known properties of the treewidth. First, the treewidth does not decrease under taking minors of a graph.

Lemma 1. Let H be a minor of G, i.e., it is a graph obtained by contracting edges of a subgraph of G. Then tw(H)  tw(G).
See Ref. [53] for a proof. Next, we have a bound for the treewidth of the line graph as follows [29]:
Lemma 2. For any graph G with the maximum degree (G), the treewidth of the line graph is bounded by

(tw(G) - 1)/2  tw(L(G))  (G)(tw(G) + 1) - 1

(A1)

Under this set-up, Ref. [29] proved the following result:

Theorem 1. Let us consider a tensor network with whose underlying graph is G = (V, E). Here, each tensor is
assigned to a vertex and an edge (v, w)  E describes that the corresponding indices of tensors v and w are contracted. Then one can contract the tensor network in time O(|V |O(1) exp[tw(L(G))]).

8

FIG. A1. Converting an IQP circuit whose two-qubit gates applies to edges of a graph to a tensor network. (a) A graph instance G and (b) the associated circuit. We use two-qubit gates with open ends to indicate the IsingZZ gate. (c) By contracting the single qubit gates, input states, and projected output states, we obtain a tensor network whose underlying graph is a subgraph of the line graph of G.

2. Tensor network algorithm for sampling from an IQP circuit over a graph

We now use above lemmas to provide a tensor network algorithm for sampling from an instance of the IQP circuit. In the main text, we consider an IQP circuit whose interaction graph is given by G = (V, E). The circuit associated with this graph has N = |V | qubits, RZ gate for each qubit e-iiZi , and IsingZZ gate between i and j-th qubits (e-iZiZjij ) if (i, j)  E. Thus, for a given output binary string x1 · · · xN , we have

i,ij (x1 · · · xN ) = x1 · · · xN |Fi,ij |+N ,

pi,ij (x1 · · · xN ) = |i,ij (x)|2.

(A2)

As we study a tensor network algorithm for given fixed parameters {i} and {ij}, let us omit those parameters in further description for clarity.
We now consider a tensor network description of the circuit. Following the standard description [29], we convert the input state for each qubit |+ and the state we project to |xi to rank-1 tensors. Next, each RZ gate becomes a rank-2 tensor with one input and one output. Finally, the IsingZZ gate is converted into rank-4 tensor with two inputs and two outputs.
From this tensor network representation, we can readily contract single qubit gates as well as input and output states (i.e., they are merged into two-qubit gates). The tensor network after this contraction becomes a graph whose nodes are the tensors for the two-qubit gates (contracted with input/output states) and edges are between nearby two-qubit gates. We show in Fig. A1 how a original graph to construct a circuit is converted to a tensor network. A caveat is that the exact tensor network description of the circuit depends on the ordering of two-qubit gates even though the result does not.
A tensor in the final expression has zero, two, three, or four legs depending on the position of the two-qubit gate in the circuit. If a two-qubit gate had one wire directly connected to the input or output wire, it has three legs (as one leg is already contracted). Likewise, if its two wires were connected to input or output wires, and other wires are connected to other two-qubit gates, the resulting tensor has two legs. For example, let us consider a two-qubit gate whose one output wire is projected to xi|. Then the tensor is given as Tj[ak]l,xi where a is the index of the tensor, xi is the given bit we project the state to, and j, k, l are internal indices represents wires connected to other tensors, which are summed over when we contract the tensor network. Using this notation, we can formally express a component of the output state as

(x1 · · · xN ) = tTr{T [a], T [a]xi , T [a]xjxk },

(A3)

where tTr indicate a tensor network contraction, {T [a]} is a set of tensors without output bit indices (the corresponding two-qubit gate is not adjacent to any projection xi|), {T [a]xi } are tensors with one output bit index (the two-qubut gate adjacent to one projection), and {T [a]xjxk } are tensors with two output bit indices (the gate adjacent to two
projections). In the expression, we omitted internal indices as they are summed over.

9

Let us denote G~ by a graph for this tensor network description. A simple observation is that G~ is a subgrapgh of
the line graph L(G) where the maximum degree of vertices is less than 4. This is because we assign a two-qubit gate for each edge of G, which becomes a node in G~, and an edge between two nodes in G~ exists only if they share the same wire (and if they are also adjacent). By applying lemmas 1 and 2, we know that the treewidth of G~ is upper
bounded by

tw(G~)  (G)(tw(G) + 1) - 1.

(A4)

In addition, the treewidth of the line graph of G~ is bounded by tw(L(G~))  (G~)(tw(G~) + 1) - 1  4tw(G~) + 3  4(G)(tw(G) + 1) - 1

(A5)

where we have used (G~)  4 (as each tensor has at most 4 legs) for the second inequality. Thus the overall complexity of obtaining (x1 · · · xN ) from an IQP circuit over a graph G = (V, E) is O(|G~|O(1) exp[tw(L(G~))]) = O(|E|O(1) exp[tw(4(G)tw(G))]).
We next show that the output probability distribution can also be computed with the same complexity. This is
because we can write the output probability as

p(x1 · · · xN ) = tTr{T [a]  T [a], T [a]xi  T [a]xi , T [a]xj xk  T [a]xj xk },

(A6)

where T is the complex conjugate of the tensor. Thus one can compute the output probability using a tensor network
over the same graph but with doubled internal dimension. Likewise, one can also compute the marginal distribution p(xS) for a subset S  {1, · · · , N } by using tensors summed over local indices xi if i  [n] \ S where [n] = {1, · · · , N }. Therefore, we can compute all marginal probabilities p(xS) for a given xS in O(|E|O(1) exp[tw(4(G)tw(G))]).
We then consider a sampling algorithm for this circuit. We first decompose the probability to nested conditionals

N
p(x1 · · · xN ) = p(x1) p(xi|x1 · · · xi-1).
i=2

(A7)

Then we can sample from the distribution using the following procedure. First, we compute p(x1 = 0) and choose a random number r from the uniform distribution U[0, 1]. Then choose x1 = 0 if r < p(x1 = 0) or 0 otherwise. We next compute the conditional probability p(x2|x1) = p(x1x2)/p(x1). As we already know p(x1 = 0) and p(x1 = 1) = 1 - p(x1 = 0), we only need to compute p(x2x1) for a given x1. We can sample x2 from the distribution using the same steps. By repeating these steps, we can sample one bitstring x1 · · · xN . The overall time complexity of this
algorithm comes from computing the marginal distributions. As we need N marginal distributions, the algorithm takes O(N |E|O(1) exp[tw(4(G)tw(G))]) to generate a single bitstring.
Finally, we consider the case when a graph G is an instance of the Erdos-R´enyi graph, where each edge is chosen
with probability q. When q < 1/N , it is proved that the treewidth of the graph is almost surely a constant [25]. In addition, it is shown in Ref. [22] that there is  > 0 such that Pr[(G)  2 log(N )]  N 1-/4 when q   log(N )/N . As this condition is satisfied for  = 8 when q < 1/N and N  e1/8  1.13, 1 - O(1/N ) fraction of the Erdos-R´enyi graph instances has (G)  16 log N . Thus our sampling algorithm takes O(N |E|O(1) exp[tw(4(G)tw(G))]) = O(N · N 2 exp[const · log(N )]) = O(poly(N )) time.

Appendix B: Anti-concentration of the output distribution of the IQP circuit over the Erdos-R´enyi graph

Ref. [22] considered IQP-type circuits over the Erdos-R´enyi graph where the single and two-qubit diagonal gates are given by RZ and CPhase gates with discrete random angles, respectively. In this case, the authors proved that there is  > 0 such that the output distribution of the circuit is anticoncentrated when the connection probability of the random graph is larger than O(log N/N )
In this section, we provide a proof that the output distribution is anticoncentrated under the same probability condition even when our gate set is given by {e-iiZi }  {e-iZiZji,j } (RZ and IsingZZ gates) with continuous random angles i and i,j, which are sampled from the uniform distribution over [0, 2]. We prove the anticoncentration property by showing that Z = EU [pU (x)2] is smaller than /22N for a constant  > 1, where the average is over all possible parameters of the circuit with the given connectivity, and pU (x) = | x|U |0 |2 is the probability to obtain x for the given circuit. Then Paley­Zygmund inequality implies that, for any 0    1

PrU pU (x)  2-N  -1(1 - )2,

(B1)

which implies the anticoncentration of the output distribution. Formally, we write our result as follows:

10

Proposition 5. Consider an IQP circuit defined over the random graph with the connection probability q . Then there is a constant  > 0 and N0  N such that the output probability of the circuit is anticoncentrated if q = (6/) log N/N for all N  N0.
We prove this by obtaining the upper bound of Z. First, Z = EU [p(x)2] = EU [p(0)2] from the symmetry of the circuit. We first have

| 0|U |0 |4 = 2-4N

exp i ij(xixj + yiyj - zizj - wiwj) exp -i i(xi + yi - zi - wi)

x,y,z,w{-1,1}N

i<j

i

= 2-4N

exp i ij(xixj + yiyj - zizj - wiwj) exp -i i(xi + yi + zi + wi) . (B2)

x,y,z,w{-1,1}N

i<j

i

Averaging this expression over {i} only remains terms with ci(x, y, z, w) := xi + yi + zi + wi = 0 in the summation. Given that ij = 0 with the probability 1 - q and have a random value with the probability q, we also have

E exp i ij (xixj + yiyj + zizj + wiwj ) = (1 - q)1 + qxixj+yiyj-zizj-wiwj
i<j

=1

if xixj + yiyj - zizj - wiwj = 0 .

1 - q otherwise

(B3)

Thus, for Fij(x, y, z, w) := xixj + yiyj - zizj - wiwj, we have

Z = 2-4N

(1 - q)|{(i<j):Fi,j =0}|,

ci (x,y ,z ,w)=0

(B4)

where the summation is over all x, y, z, w  {-1, 1}N which satisfies ci(x, y, z, w) = 0 for all i. We now introduce a variable k = {k1, · · · , kN } where each ki = {xi, yi, zi, wi} is a 4-dimensional vector  {-1, 1}4.
We note that, among 16 possible values of ki, only 6 values satisfy the constraints ci. The following table shows all possible values of ki with the constraints ci.

ab cdef
xi 1 1 1 -1 -1 -1 yi 1 -1 -1 1 1 -1 zi -1 1 -1 1 -1 1 wi -1 -1 1 -1 1 1

Here, we used a-f to indicate each possible value of {xi, yi, zi, wi}. Thus the summation in Eq. (B4) can be rewritten as the summation over {a, b, c, e, d, f }N . In addition, we also see that all possible pairs of (ki, kj) with Fi,j = 0 are {(b, c), (b, d), (c, b), (c, e), (d, b), (d, e), (e, c), (e, d)} =: N . As d = -c, e = -b, f = -a, we observe that (ki, kj)  N if and only if (ki, -kj)  N . Using this notation, we write Z as

Z = 2-4N

(1 - q)|{(ki,kj )N}|

k{a,b,c,-a,-b,-c}N

(B5)

As flipping the sign of ki for any i does not change the summand, we can simplify the expression to

= 2-3N

(1 - q)|{(ki,kj )N}|.

k{a,b,c}N

(B6)

Let A = |{i : ki = a}|, B = |{i : ki = b}|, C = |{i : ki = c}| be the number of occurrences of a, b, c in k. Then we obtain

|{(i < j) : (ki, kj)  N }| = BC

(B7)

and

Z =2-3N

M (A, B, C)(1 - q)BC ,

A,B,C0,A+B+C=N

(B8)

11

where

M (A, B, C) =

N A

N -A B

N -A-B C

=

N! A!B !C !

(B9)

is the number of ks with (A, B, C). We compute the upper bound of the summation by splitting the cases. First, we

consider the case A  (1 - )N for some 0 <  < 1/2 satisfying

N N

 2N/2/N . For example,  = 1/10 satisfies this

condition (which is from

n k

 2nH(k/n) where H(p) is the Shannon entropy in base 2). Then at least one of B or C

is larger than N/2 (w.l.o.g. assume C  N/2).

The summation for this case is upper bounded by

(1-)N N -A N A
A=0 B=0

N -A B

(1-)N N -A
(1 - q)BC  2

N A

A=0 B=0

N -A B

(1 - q)NB/2

(1-)N
2

N A

(1-)N -A
N B(1 - q)NB/2

A=0

B=0

N

 2 × 2N

N B e-qNB/2.

(B10)

B=0

where we used

N -A B

 (N - A)B/B!  N B for the second inequality and 1 - q  e-q for the last inequality. When

q  (6/) log N/N ,

N B e-qNB/2  N B e-3B log N  N -2B .

(B11)

Thus

N B=0

N

B

e-q

N

B/2



1 + 1/N



2,

and

this

part

contribute

at

most

4 × 2-2N

to

Z.

Next, we consider A  (1 - )N . Then we obtain

N

N-A N A

N -A B

(1 - q)BC 

N

N N N AB

A=(1-)N B=0

A=(1-)N B=0

N


N A

N

N B

A=0

B=0



2N/2N N

2  2N .

(B12)

To summarize, we showed that Z  5 × 2-2N if q  (6/) log N/N for some constant . This completes the proof that the distribution anti-concentrates.
In addition, we also can obtain a lower bound of Z as

N N -A
Z = 2-3N

N A

A=0 B=0

N -A B

(1 - q)BC  2-3N

N N -A

N A

A=0 B=0

N -A B

(1 - q)NB

= 2-3N (2 + (1 - q)N )N ,

(B13)

where we used C  N for the first inequality and the property of the multivariate coefficients for the last equality. If q  /N for some  > 0, we have

(1 - q)N  (1 - /N )N = [(1 - /N )N/].

(B14)

Note that (1 - /N )N/ approaches to e-1 as N increases, and we can find N0 := N0() such that (1 - /N )N/ - e-1  1/12,

(B15)

for all N  N0. Then we have the lower bound Z  2-3N 2 +

11e-1 12

N
.

(B16)

Following Ref. [49], we can conclude that the output distribution is not anticoncentrated when q < /N for any constant  > 0.

12

KL(P ||PPT)

1.5
1.0
0.5
0.0 0.0

KL(P ||PPT)

1.5 (b)
1.0
(a) 0.5

= 24 N

N =8

1.0

2.0

q/(log(N )/N )

0.0 103
(c) 102 3.0 101

 = 0.40  = 0.80  = 1.20  = 1.60  = 2.00  = 2.40  = 2.80

22N EU [pU (x)2]

8 10 12 14 16 18 20 22 24 N
FIG. A2. (a) KL divergence between the output distribution of the IQP circuits over the random graph with the connection probability q and the Porter-Thomas distribution as a function of q/(log(N )/N ). For different values of q =  log N/N , we plot (b) the KL divergence between the output distribution and the Porter-Thomas distribution as well as (c) the squared probability p(x)2 averaged over all circuit instances and the output strings x. These figures show that the output distribution at   2.0 is anticoncentrated but does not follow the Porter-Thomas distribution.

Appendix C: Anticoncentration versus Porter-Thomas distribution for the IQP circuit
One of our main results in the paper is that there exists a parameter regime that the output distribution is anticoncentrated albeit it does not follow the Porter-Thomas distribution. In this section, we provide additional numerical evidence supporting our argument. Precisely, we find a parameter regime such that the KL divergence between the output distribution of the IQP circuits and the Porter-Thomas distribution converges to a constant (as N increase), but Z = EU [pU (x)2]  /22N for some constant  > 0 which is a sufficient condition for the anticoncentration (see Sec. B).
We show a plot of the KL divergence as a function of q/(log N/N ) in Fig. A2(a). It suggests that the KL divergence does not decreases with N , but diverges or converges to a constant for any fixed q/(log N/N ), which implies that the distribution never becomes the Porter-Thomas distribution for a fixed q/(log N/N ). We further compare the scaling of the KL divergence and Z = EU [p(x)2] that we studied in Sec. B in Fig. A2(b) and Fig. A2(c). Plots are from different values of   [0.4, · · · 2.8] where q =  log N/N . We see that the KL divergence converges to a constant as N increases when   1.6. This implies that the output distribution maintains a distance to the Porter-Thomas distribution even for large N . In contrast, we see that Z scales with ()/22N , which is the condition for the anticoncentration (Sec. B). This is the parameter regime that the output distribution is anticoncentrated but does not follow the Porter-Thomas distribution.

Appendix D: Entanglement properties of the IQP circuit

In this section, we diagnose the phase transition of IQP circuits over the random graph using entanglement spectra. As the original circuit has no entanglement at q = 0, we consider a slightly modified circuit with N -1 body interactions instead of the conventional one body terms ({Zi}). In other words, we consider a diagonal unitary operator Di,ij such that

Di,ij = exp[i iZ¬i] exp[i ij ZiZj ].

i

i<j

(D1)

13

where Z¬i = j= i Zj is the product of Pauli-Z operators of all sites except i. Thus we consider the IQP circuit given by

(x) = x|HN Di,ij |+N

(D2)

and the output distribution from the circuit

r(x) = x|HN Di,ij |+N 2.

(D3)

This choice ensures that the output states are always entangled, which makes the classification of phases via the

entanglement spectrum [54] meaningful.

Even though this setup changes the entanglement properties of the output states, it does not change the complexity

as there is a simple relation between the distribution r(x) and p(x) that from the IQP circuit with RZ gate. Precisely,

p(x) = r(x) if x has even parity (i.e.

N k=1

xk

is

even)

and

p(x)

=

r(x)

otherwise,

with

x

=

(1 -

xi),

which

we

prove

in the following subsection.

1. Output distribution of the IQP circuit with Z¬i

In this subsection, we show that using Z¬i instead of Zi for the IQP circuits does not change the complexity of sampling, which follows from the theorem below.
Theorem 2. We consider two diagonal unitary operators Di,ij and Fi,ij , where D is composed of Z¬i = k=i Zk and two-qubit phase shift gates {eiZ¬i , eijZiZj } whereas F is constructed with one- and two-qubit gates
{eiZi , eijZiZj }. We write r(x) = | x|HN Di,i,j |+N |2 and p(x) = | x|HN Fi,ij |+N |2. When the total number of qubits is even, we have

p(x) = r(x), if k xk is even r(x), otherwise

(D4)

where xi = (1 - xi). Thus a sampler for p(x) can also sample from r(x) (and vice versa).

Proof. Direct calculation gives

x|HN Di,i,j |+N = x|HN exp i

ij ZiZj +

N
iZ¬i |+

i<j

i

= 1 2N

x|HN exp i ijZiZj + i

y

i<j

i

iZ¬i |y

= 1 2N

x|HN |y exp i ij(1 - 2yi)(1 - 2yj) + i

y

i<j

i

i(-1)

k yk (1 - 2yi)

(D5) (D6) (D7)

where we used Zi |y = (-1)yi = (1 - 2yi) and Z¬i |y = k=i Zk |y = ZN Zi |y = (-1) k yk (-1)yi . By decomposing the summation over y  {0, 2N - 1} into Seven = {y : k yk is even} and Sodd = {y : k yk is odd}, we have

= 1 2N

x|HN |y exp i ij(1 - 2yi)(1 - 2yj) + i i(1 - 2yi)

ySeven

i<j

i

= 1 2N

+

x|HN |y exp i ij(1 - 2yi)(1 - 2yj) - i i(1 - 2yi)

ySodd

i<j

i

x|HN |y exp i ij(1 - 2yi)(1 - 2yj) + i i(1 - 2yi)

ySeven

i<j

i

= 1 2N

+

x|HN |y exp i ij(1 - 2yi)(1 - 2yj ) + i i(1 - 2yi)

y  Sodd

i<j

i

x|HN |y y|Fi,i,j |y +

x|HN |y y|Fi,i,j |y

ySeven

y  Sodd

(D8)
(D9) (D10)

14

P (u)

0.8 0.6 0.4 0.2 0.0
0

GOE GUE5.0
2.5 1.35 1.30 1.25

S(A)

10

20

1

2

3

u = (i+1 - i)/(i+2 - i+1)

0.6 0.4 0.2 0.0
0 0.6 0.4 0.2 0.0
0 0.6 0.4 0.2 0.0
0

q = 0.00 0.6

0.4

0.2

0.0

2

0

q = 0.16 0.6

0.4

0.2

0.0

2

0

q = 0.28 0.6

0.4

0.2

0.0

2

0

q = 0.04 0.6

0.4

0.2

0.0

2

0

q = 0.20 0.6

0.4

0.2

0.0

2

0

q = 0.32 0.6

0.4

0.2

0.0

2

0

q = 0.08
2 q = 0.24
2 q = 0.36
2

FIG. A3. Entanglement properties of the IQP circuit over the random graph. Left: Distribution of the ratio between adjacent entanglement spectra for q = 0.0 (pink) and q = 1 (teal). We see that the distribution follows that from the Gaussian orthogonal ensemble (GOE) when q = 0.0 whereas it shows the Gaussian unitary ensemble (GUE) property when q = 1.0. Inset shows the entanglement entropy as a function of the size of the system. Right: Entanglement spectra gaps for different values of q. As q increases, the distribution changes from the GOE to GUE.

where we changed the variable yi = 1 - yi in the latter sum (y has thesame parity as y as we only consider even N ) and used the notation |y = i |1 - yi. As x|H|y = (-1) / k xkyk 2N and x|H|y = (-1) k xk(1-yk)/ 2N = (-1) k xk (-1) / k xkyk 2N , we see that p(x) = r(x) when k xk is even. On the other hand, when k xk is odd, we have

= 1 2N

x|HN |y y|Fi,i,j |y +

(-1) x|HN |y y|Fi,i,j |y

ySeven

y  Sodd

= 1 2N

x|HN |y y|Fi,i,j |y +

x|HN |y y|Fi,i,j |y

ySeven

y  Sodd

= x|H N Fi,i,j |+N

(D11) (D12) (D13)

where we have used x|HN |y = (-1) k yk x|HN |y in the second line. Thus, p(x) = r(x) if k xk is odd.

2. Entanglement spectra with varying density of two-qubit gates
We now study entanglement properties of the output state [Eq. (D2)] from IQP circuits with Z¬i. For each circuit realization, we compute the density matrix after tracing out the half of the qubits (B = [N/2 + 1, · · · , N ]) and obtain the density matrix A. By diagonalizing A, we obtain an ordered entanglement spectrum {i}. Then the distribution for the ratio of the adjacent eigenvalues u = (i+1 - i)/(i+2 - i+1) is computed. We use the ratios as they give better signature [54], and the exact distributions for the Gaussian orthogonal ensemble (GOE) and the Gaussian unitary ensemble (GUE) are known [55].
In Fig. A3 (Left), we plot the distribution of the entanglement spectra obtained for N = 20. The distribution clearly follows that from the GOE when q = 0.0 whereas it follows the GUE when q = 1.0. We also obtained that the bipartite entanglement entropy (inset) follows the volume law (S()  N ) and the area law (S() = O(1)) for q = 1.0 and q = 0.0, respectively.
We also plot how the distribution changes as q increases in Fig. A3 (Right). One sees that the distribution does not change smoothly, but an initial peak appears for small q. We believe that this is a signature of another phase between the GOE and GUE phases that is related to anticoncentration.

15
Appendix E: Learning the output distribution of the IQP circuit
In the main text, we argued that the output distribution of IQP circuits over the random graph is learnable when q = 0 (i.e., only with single qubit gates). On the other hand, we stated that, under a plausible assumption, there is an instance with N (1) edges whose output distribution is difficult to be learnt. In this section, we prove those statements.

1. Efficient learnability of a distribution with a single binary outcome

Let us first consider learning a distribution P over a single bit x  {0, 1} where P (x = 0) =: p0 and P (x = 1) = 1-p0. Our task is reconstructing a probability distribution Q(x) such that TV(P, Q)   where

TV(P, Q)

=

1 2

|P (x) - Q(x)|

x{0,1}

(E1)

is the total variational distance between two probability distributions.
For a given N samples, we estimate p0 as q0 = N0/N where N0 is the number of 0s in the samples. Then one can see the number of observed N0 follows the binomial distribution X  B(N , p0). Thus we have

Pr(|p0 - q0|  ) = Pr(|N p0 - N0|  N ) = Pr(|E(X) - X|  N )



N p0(1 - p0) (N )2



1 4N

2

,

(E2)

where we used E(X) = N p0 and the Chevshev's inequality with Var(X) = N p0(1 - p0)  N /4. As we can write the total variational distance as

TV(P, Q) =

1 2

|P (x = 0) - Q(x = 0)| + |P (x = 1) - Q(x = 1)|

=

1 2

|p0 - q0| + |(1 - p0) - (1 - q0)|

= |p0 - q0|,

(E3)

we have

Pr TV(P, Q)  

=

1

-

Pr(|p0

-

q0|

>

)



1

-

1 4N 2

.

(E4)

Therefore, for any given ,   [0, 1], we can find Q such that TV(P, Q)   with probability 1 -  when

N



1 42

.

(E5)

As our algorithm finds q0, it is an evaluator. In addition, using a given random number from the uniform distribution r  U[0,1], we can construct a circuit which outputs 0 if r < q0 or 1, otherwise. Thus the distribution is also learnable w.r.t. a generator.

2. Efficient learnability of product distributions

We now use the result from the previous section to prove the learnability of a product distribution. For a given

N , we consider a probability distribution given previous subsection, we assume that N samples

by p(x) from the

=

N i=1

pi(xi)

where

x

=

distribution are provided.

{x1, · · · , xn} In addition,

 {0, 1}N . As in the let N0i be the number

of samples where the i-th bit is 0. Because {pi(xi)} are mutually independent, one can see that each N0i follows the

binomial distribution B(N , pi(x = 0)). Then, using the above result, we can reconstruct a qi(xi) which is close to

pi(xi). Finally, we can construct the product of the reconstructed distributions Q(x) =

N i=1

qi(xi)

which

is

close

to

P (x).

More precisely, as

TV(P, Q)

=

1 2

|P

(x)

-

Q(x)|



N

max
i

TV(pi,

qi),

x{0,1}N

(E6)

16

this quantity can be bounded by  when TV(pi, qi)  /N . Substituting  with /N in Eq. (E5), we know that such a distribution Q is learnable with the probability 1 -  if

N



N2 42

.

(E7)

3. Difficulty of learning the output distribution of the IQP circuit with a sparse connectivity

We now prove that, under a slightly modified version of the learning parities with noise assumption, the output distribution of the IQP circuit with N (1) edges is not efficiently learnable. Formally, the learning parities with noise is defined as follows.
Conjecture 1 (Learning parities with noise). For any bitstring s  {0, 1}k, we define a boolean function fs(x) = s · x for all x  {0, 1}k. Then for any   (0, 1/2) the "noisy parity distribution on k + 1 bits" is defined as

Ps,(x, y) =

2-k(1 - ) 2-k 

, if y = fs(x) , , otherwise

(E8)

where we consider (x, y)  {0, 1}k+1 as a bitstring with k + 1 bits. Then a conjecture is that, there is   (0, 1/2) and s  {0, 1}k such that there is no classical efficient algorithm for learning Ps,(x, y) w.r.t. an evaluator (polynomial in
k).

However, for our problem, we need a slightly stronger assumption.
Conjecture 2. For some   (0, 1/2) and s  {0, 1}k, there is no classically efficient algorithm for learning Ps,(x, y)  1k, where 1k is the uniform distribution defined over k bits.
This condition is equivalent to that learning Ps,(x, y) is difficult for some s  {0, 1}k with w(s) < k/2, where w(s) is the Hamming weight of s (the number of 1 in the bitstring). We note that it is possible to prove Conjecture 2 using Conjecture 1 if we use the multiplicative error model.

Proposition 6 (Hardness of the product distribution). Assume that a probability distribution P (x) is classically hard w.r.t. an evaluator. Then the probability distribution P  1k (for k = |x|) is also difficulty w.r.t. an evaluator, but up to a multiplicative error. In other words, there is no classically efficient algorithm finding Q(x, y) such that, for any  > 0,

(1 - )2-kP (x) < Q(x, y) < (1 + )2-kP (x).

(E9)

Proof. Let us assume that there is such a classically efficient algorithm. Then we estimate P (x) with P~(x) = 2kQ(x, 0). Then, one can easily see that TV(P~, P )  , which implies that P is efficiently learnable.

We now consider Ds,(n, k), which is defined for n-bits whose first k + 1 follows Ps, and the last n - (k + 1) bits is a trivial distribution. In other words, for the trivial distribution Tl over l-bits defined as Tl(x) = 1 iff x = 0l, we have Ds,(n, k) = Ps,  Tl. The above conjecture says that learning the distribution Ds,(n, k) with k = n(1) is also classically hard (as (ploy(k)) = (poly(n)); see Ref. [56] for detailed discussion).
Then let us consider a circuit for N qubits initialized with |0N . We first apply the Hadamard gate to the first k qubits and the RX gate (e-iX/2) to the k + 1-th qubit. Then for a given s = {s1, · · · , sk}, we apply the CNOT gates between i-th and the k + 1-th qubits for all si = 1. This gives a quantum state

| = 1

2k
cos(/2) |x  |s · x  |0n-(k+1) - i sin(/2) |x  |s · x  1  |0n-(k+1) .

2k x=0

(E10)

Then it is simple to check that the output probability follows Ds,(N, k) with  = sin2(/2). Using the Hadamard transformation for the RX gate e-iX/2 = He-iZ/2H and the CNOT gate CNOTij = HjCZijHj (where i is the control qubit and j is the target qubit), the circuit has initial Hadamard gates followed by the diagonal gate. We
demonstrate this circuit in Fig. A4 (upper).
However, the transformed circuit still does not have the final Hadamard layer on the first k qubits to be the IQP
circuit. We thus use the circuit identity given in Fig. A4 (lower). We note that the measurement outcomes m follows
the uniform distribution p(m = 0) = p(m = 1) = 1/2.

|0

H

|0

H

|0

H

|0

H

|0

RX ()

17

|0 H

|0 H

= |0

H

|0 H

|0

H

RZ ()

H

|

Zm

= |

H

|0 H

H

FIG. A4. Circuit identities we used. (Upper) A circuit whose measurement outcomes follow the noise with parity distributions can be transformed into a circuit with Hadamard gates followed by diagonal gates. (Lower) The teleportation circuit we use to add the last Hadamard layer to match with the IQP circuit. Here, m is the measurement outcome from the right circuit.

Combining these results, we construct a circuit as follows. For some k and N  2k + 1, our circuit has the initial state |0N followed by the Hadamard layer HN . We next apply RZ () to the 2k + 1-th qubit, and then for a given s  {0, 1}k, apply the CZ gate between i-th and 2k + 1-th qubits if si = 1. After that, we apply the CZ gates between i-th and k + i-th qubits for all 1  i  k. Finally, the last Hadamard later HN is applied and all qubits are measured in the computational basis. For this circuit, one can check that the outcome distribution is 1k Ps,  Tn-(2k+1), a product between three distributions: The first k bits follows the uniform distribution, the next k + 1 bits follow Ps,, and the last n - (2k + 1) bits follow the trivial distribution Tn-(2k+1). After decomposing the CZ gate into the RZ and IsingZZ gates as CZ = ei/4ei/4ZZ e-i/4Z1e-i/4 1 Z , we see that the circuit is exactly the IQP circuit we considered in the main text with 2k two-qubit gates.
Thus under the assumption that Conjecture 2 is true, there is an IQP circuit with 2n(1) = n(1) two-qubit gates (i.e., with underlying graph of 2n(1) edges), whose output distribution is not efficiently learnable w.r.t. an evaluator.
We finally note that it is also possible to write down a single IQP circuit in a two-dimensional lattice whose output distribution is not classically efficiently evaluated by a polynomial-size circuit (unless the polynomial hierarchy collapses) up to a constant multiplicative error [45].

Appendix F: Classical parent Hamiltonian of the output distribution
In the main text, we introduced a measure given by the summation of weights of the classical parent distribution to argue the classical hardness of learning the distribution. In this section, we show the this measure can be computed when the probability distribution p(x) is given, and discuss how this measure scales when we allow an approximate distribution.

1. Algorithm for obtaining the classical parent Hamiltonian

As we only consider binary outcomes, i.e. x = (xi) where xi  {0, 1}, we can always write H(x) =

- S JS iS Zi(x) where S is the set of all subsets of indices {1, · · · , N } and Zi is a classical spin variable defined as Zi(x) = (-1)xi . For simplicity, we rewrite the sum using a binary representation of S given as y(S) = (yi)

where yi = 1 if i  S or 0 otherwise, which yields H(x) = -

2N -1 y=0

J (y)Zy (x)

where

Zy (x)

=

(-1)x·y .

As this is

nothing but the Walsh-Hadamard transformation (W {f (x)} = y f (x)(-1)x·y), we can obtain J(y) using the inverse

transformation, i.e. J(y) = W -1{-H(x)}, which is (1/2N ) x log p(x)(-1)x·y when y = 0 and J(y = 0) = - log Z.

The Gibbs state of this Hamiltonian exactly represents the given distribution p(x). Our method here works when

p(x)  0 for all x, which holds for the output distribution of the IQP circuits.

18

2. Analytic expression of the classical parent Hamiltonian when q = 0.0

When q = 0.0, we obtain

x|HN e i iZi |+N = x|e i iiXi |0N = xk| cos(k) 1 +i sin(k)X|0 .
k

(F1)

We thus have p(x) = | x|HN e i iZi |+N |2 = k fxk (k) where f0() = cos2() and f1() = sin2(). It follows that the classical Hamiltonian -H = i iZi reproduces p(x) for i = log tanh(i).

3. Truncating Hamiltonian and approximating the distribution for a given target error

In the main text, we used a Hamiltonian that exactly represents the given target distribution. However, one can also obtain a Hamiltonian that approximates the target distribution by truncating small terms. In fact, when there are lots of small terms in the Hamiltonian, we can ignore them when the target accuracy  is given.

Proposition 7 (Efficient approximate description of amplitudes). For 0 <  < 1, suppose that we have H~ (x)

which satisfies maxx |H(x) - H~ (x)| < . Let p(x) = e-H(x)/Z and p~(x) = e-H~ (x)/Z where Z = Z = x e-H~ (x). Then ||p(x) - p~(x)||1 := x |p(x) - p~(x)|  2(e - 1).

x e-H(x) and

Proof.

|p(x) - p~(x)| = |e-H(x)/Z - e-H~ (x)/Z|

x

x



|e-H(x)/Z - e-H~ (x)/Z| +

|e-H~ (x)/Z - eH~ (x)/Z|

x

x



1 Z

e-H(x)|1 - e-[H~ (x)-H(x)]| +
x

x

 max |1 - e-[H~ (x)-H(x)]| + |Z/Z - 1|
x

e-H~ (x) Z

Z Z

-1

(F2)

Furthermore, |Z/Z - 1| = |Z - Z|/Z = | e-[H~ (x)-H(x)]|. Thus we have the bound |y|  1, we have the bound 2(e - 1).

x e-H~ (x) - e-H(x)|/Z = | x e-H(x)(e-[H~ (x)-H(x)] - 1)|/Z  maxx |1 - x |p(x) - p~(x)|  2 maxx |1 - e-[H~ (x)-H(x)]|. As |1 - e-y|  (e - 1)|y| for

Corollary 1. For a probability distribution p(x) = e- y J(y)Zy(x) and 0 <  < 1, we define

Jt(y) =

J (y) 0

if |J(y)|  /2N otherwise.

(F3)

Then the probability distribution pt(x)  eft(x) for ft(x) = W -1{Jt(x)} satisfies ||pt(x) - p(x)||1  2(e - 1) Proof. maxx |ft(x)-f (x)|  maxx | y[Jt(y)-J (y)](-1)x·y|  maxx y |Jt(y)-J (y)||(-1)x·y|  /2n y |(-1)x·y| = .
One can also prove the inverse under a multiplicative approximation error.
Proposition 8. For given p(x) > 0, consider p~(x) that approximates p(x) as (1/c)p(x) < p~(x) < cp(x) for c > 1, then |J(y) - J~(y)| < log c for all y = 0. Proof. This follows from the inverse transformation J(y = 0) = (1/2N ) x log p(x)(-1)x·y.
However, multiplicative approximations are not practical in most cases. As reduction to the additive error is not straightforward, we instead introduce a simple numerical technique: For given H(x) and J(y) = W {-H(x)}, we consider p(x)  exp[-W -1{J(y)}] where J(y) is a term after truncation given as

J(y) =

0 J (y)

if |J(y)| <  otherwise.

(F4)

J

19

103 (a)
102

101

0.0

0.1

106 (c)
105

104

103

102

101

0.0

0.1

N = 20

log J / log N

1.6

q = 0.0

1.5

q = 0.02

q = 0.04

1.4

q = 0.06

1.3

q = 0.08

q = 0.1

1.2 (b)

1.1

N =8

1.0

0.2

0.3

0.4

q

8 10 12 14 16 18 20
N

N = 20

4.0

q = 0.0

3.5

q = 0.02

q = 0.04

3.0

q = 0.06

log |S|/ log N

2.5

q = 0.08

q = 0.1

N =8

2.0 (d)
1.5

1.0

0.2

0.3

0.4

q

8 10 12 14 16 18 20
N

|S|

FIG. A5. (a) The sum of the Hamiltonian coefficients J studied in the main text and (b) its scaling behavior. The remaining number of terms of the parent Hamiltonian |S| for fixed target error  = 10-3 (c) as a function of q and (d) as a function of N with a normalization to observe the scaling behavior. From (b) and (d), we see that both measures increase super-polynomially with N when q  0.04.

We then numerically find a maximal  which satisfies ||p(x) - p(x)||1 <  for a given target error . In practice, we use the following algorithm: First, we divide the range [mink J(y), maxk J(y)] evenly on a log scale {1, 2, · · · , n}. For each k, we compute ||p(x) - p(x)||1 and find a range [i, i+1] such that ||pi (x) - p(x)||1 <  < ||pi+1 (x) - p(x)||1. We then binary search between [i, i+1] to find the target .

4. Other signatures of phase transitions in classical parent Hamiltonians

In this subsection, we give additional data supporting phase transitions in the learnability of the distribution

based on our Hamiltonian measure. First, we revisit J studied in the main text in Fig. A5(a) and (b), which show

this measure as a function of q and its scaling behavior with N for different q, respectively. Especially, we see that

J = log J / log N diverges as N increases in Fig. A5(b), which implies J increases super-polynomially.

We next study the number of remaining terms of the parent Hamiltonian obtained from the algorithm described

in the previous section. After finding  for a given target error , we compute the number of remaining terms of the

Hamiltonian, which is given by |S| where S = {y = 0|J(y) = 0}.

For target error  = 10-3, we plot |S| in Fig. A5(c) and (d). Similar to the sum of the Hamiltonian y= 0 |J(y)|,

Fig. A5(d) shows that the |S| scales super-polynomially even when q  1.0 < qc0 . To be specific, log |S|/ log(N ) increases faster than linear when q = 0.08 and q = 0.10, which is not expected if |S| = O(N ) for some  > 0. In

addition, Fig. A5(c) shows a plateus for q > qc0 , which we consider a property of the scrambled phase.

We additionally study each term of the parent Hamiltonian J(y) in Fig. A6. Instead of listing all J(y) (whose

domain is y  0, · · · , 2N - 1), we use an aggregated version of it given by w(y)=k |J(y)| where w(y) is the Hamming

weight of y (

N -1 i=0

yi

).

We

plot

these

quantities

obtained

for

N

=

20

in

Fig.

A6,

which

show

three

different

behaviors

depending on q. First, when q is small, we see that it has only a single peak. Next, for 0.02  q  0.06, we observe

that a slope is developed for small k. The shape becomes a usual binomial distribution as q further increases. As

constant |J(y)| yields

w(y)=k |J (y)| 

w(y)=k 1 =

N k

, we regard this as a signature that |J(y)| becomes uniform.

Moreover, the plot also shows that the magnitude of the peak increases as q increases in this phase.

20

q = 0.00 20
10

q = 0.02 15 10 5

15 q = 0.04 10 5

q = 0.06 10
5

10 q = 0.08 5

0 15 q = 0.10 10
5

0 q = 0.12
20
10

0 40 q = 0.14
20

0 60 q = 0.16 40 20

0 q = 0.18
60 40 20

0 q = 0.20
75 50 25

0 100 q = 0.22
50

0 100 q = 0.24
50

0 q = 0.26
100
50

0 q = 0.28
100
50

0

0

0

0

0

0

10

20 0

10

20 0

10

20 0

10

20 0

10

20

k

k

k

k

k

FIG. A6. Averaged weights of the classical parent Hamiltonian for different q  [0.0, 0.02, · · · , 0.28]. For each value k of the x-axis, we plot w(y)=k |J(y)| where w(y) is the Hamming weight of y. As we discussed in Sec. F, we see a single peak at N - 1 when q = 0.0. We also see that the plot resembles the binomial distribution when q is large enough (q  0.14), which is a signature that the Hamiltonian coefficients become uniform |J(y)|  C (see text for details). In between, we see that another
structured behavior where both sides of the distribution grow.

Appendix G: Details of machine learning setup

We describe details of our machine learning setup in this Section.

1. Modelling the distribution with the energy based model

The energy based model uses a neural network of a single real-number output as the energy function, i.e. pW (x) = efW (x)/Z where pW (x) is the probability the model describes, fW (x) is the output of the network, and Z = x fW (x) is the partition function. Here W is a one-dimensional vector of all parameters of the network. One can train this model using a gradient-descent type of algorithm by minimizing the cross entropy L[pW (x)] = - x p(x) log pW (x) = C + H(p) where p(x) is the target distribution and H(p) = - x p(x) log p(x) is the Shannon entropy of p(x). Note also that we use p(x) as the reference distribution for the KL divergence here, which is opposite to the KL divergence
we have shown in Fig. 4 in the main text (where we have used pW (x) as the reference distribution). Evaluating L[pW (x)] is not straightforward for the energy-based model in practice, as the partition function Z is hard to be evaluated. Instead, it is more feasible to estimate the gradient of the loss function, which is given by

-g = -W L[pW (x)] = -W - p(x) log pW (x) = p(x)W [log pW (x) - log Z]

x

x

=

x

p(x)W fW (x) -
x

[W fW (x)]efW (x) Z

 W fW (x) xp(x) - W fW (x) xpW (x),

(G1) (G2) (G3)

where ·xp(x) is an average over samples from p(x). Then we can train the network using usual first order optimization methods such as Adam [57].
In general, one samples from the distribution pW (x) with Monte-Carlo methods such as Metropolis-Hastings algorithm. In our simulation, however, we compute pW (x) exactly by computing fW (x) over all possible x and exactly

21

10-1

10-1

||r(x) - p~W (x)||1 ||r(x) - p~W (x)||1

10-2

10-2

2 = 0.999

2 = 0.9999

1

×

10

2
-4

×

10

5
-4

×

10

1
-4

×

10

2
-3

×

10

5
-3

×

10

1×
-3

10

2×
-2

10

-2



1

×

10

2
-4

×

10

5
-4

×

10

1
-4

×

10

2
-3

×

10

5
-3

×

10

1
-3

×

10

2×
-2

10

-2



q = 0.0 q = 0.04 q = 0.08 q = 0.12 q = 0.16

FIG. A7. Converged L1 distance between the converged distribution from an energy based model pW (x) and the distribution from the IQP circuit p(x). For each q, we randomly pick a single IQP instance and trained the energy-based model for different hyperparameters  (learning rate) and 2 (average rate for the Fisher information matrix).

sample from the computed distribution. This is feasible up to N = 20 qubits (the maximum number of qubits we simulated) by maintaining all 2N values. Thus we always generate unbiased samples regardless of the shape of the
distribution (e.g. even for a spin-glass type distribution that Monte-Carlo methods may fail).
For each epoch, we sample x = x1, · · · , xS from p(x) as well as pW (x) where M is the min-batch size and estimate F and g from them. As we generate samples from p(x) for each epoch, usually bigger sample size S performs better
(see also e.g. Ref. [47, 58]). In our simulation, we choose S = 1024 which is the maximum size (among the perfect
power of 2) we can simulate with the given computational resource.

2. Neural network architecture
As our target distribution (the output distribution of a circuit) does not have a particular symmetry, we use a network with fully connected layers for fW (x). We use a fully-connected network with two hidden layers each has M = N hidden units. The visible and the first hidden layers have bias terms whereas the last layer does not. Thus the number of parameters from the first, second, and the last layers are N ×M +M , M ×M +M , and M , respectively. We used  = 30 for the results in the main text, and the network has total 60, 240 parameters for N = 8 and 373, 800 for N = 20.

3. Hyperparameters
The hyperparameters for Adam optimizer are the learning rate (), the momentum for the gradient (1), and the momentum for component-wise gradient norm (2). We use the default value for 1 = 0.9 and grid search  for 2 = [0.999, 0.9999]. For a single circuit realization for given q, the converged L1 distance between the target distribution p(x) and pW (x) is shown in Fig. A7. Based on the results, we choose  = 10-3 and 2 = 0.999, which is well-performing for all tested values of q, to obtain results in the main text. We still expect similar quantitative results from other choices of hyperparamters.

Appendix H: Results from the natural gradient descent
1. Natural gradient descent optimizer
The natural gradient descent (NGD) [59] is a second order method for optimizing a neural network. Albeit it requires a significantly larger amount of computational resources, it may give better convergence properties for some

22

q = 0.00

10-1

q = 0.02 q = 0.04

q = 0.06

10-2

q = 0.08 q = 0.10 q = 0.12

KL[pW (x)||r(x)]

10-3

10-4

10-5 8

12

16

20

N

FIG. A8. The KL divergence between the output distribution of the IQP circuits p(x) and that from the energy based model after training p~W (x) with the NGD. Other settings are the same as Fig. 4 in the main text.

dataset [58, 60]. For the natural gradient descent, we additionally compute the Fisher information matrix given as F = (Fij) where

Fij = 

pW (x)[Wi log pW (x)][Wj log pW (x)] - p(x)[Wi log pW (x)] p(x)[Wj log pW (x)]

x

x

x

Wi fW (x)Wj fW (x) xpW (x) - Wi fW (x) xpW (x) Wj fW (x) xpW (x).

(H1) (H2)

Then the parameter of the network is updated as Wt+1 = Wt - Ft-1gt where Wt is the weight at epoch t, Ft and gt are moving averages of the Fisher matrix F and the gradient g. We use Ft = Ft-1 + 2F and gt = gt-1 + 1g where F and g are Fisher matrix and the gradient estimated at each epoch, respectively. We note that we solve the linear equation Ftv = gt at each step, which requires O(|W |2-3) number of operations (depending on the condition number of F ) where |W | is the number of parameters in the network. As the first order optimization algorithms (e.g. Adam) only require O(|W |) operations for each step, we see that the NGD requires a significantly larger amount of
computational resources.

2. Neural network architecture
Because of the computational overhead mentioned above, we use a relatively smaller network (compared to the case of Adam). Our network has one hidden layers which has 10N hidden units where N is the number of qubits. Only the visible layer has a bias term and the total number of the parameters of the network is 10N 2 + 11N . We note that the number of parameters in this case is still an order of magnitude larger than that of the IQP circuit itself.

3. Results
As for the Adam optimizer, we plot the converged distance between p(x) and p~W (x) for 24 different circuit realizations after hyperparameter optimization in Fig. A8. We see that the errors increase exponentially with N for 0.06  q < qc0 as in the results from Adam. We also observe that the converged KL divergence decreases with N for q = 0.00 and q = 0.02, which is different from what we observed with Adam optimizer. As the converged values are indeed smaller (the averaged converged KL divergence is < 2 × 10-5 when N = 20 and q = 0.0) than when we used Adam (which was > 10-4) albeit its smaller network size, we conclude that the distributions when q < qc1 are better handled by the NGD than Adam.

