"""MMD-trained photonic generative model (v1.0 milestone): a MerLin
``QuantumLayer`` trained to reproduce the two-ring ``circles`` dataset via a
closed-form MMD^2 loss, with no discriminator and no sampling step."""
