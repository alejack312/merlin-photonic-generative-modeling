[pytest]
testpaths = tests
pythonpath = src# Repo-local tmp_path root: the shared %TEMP%\pytest-of-<user> can be left
# unreadable by sandboxed runs, which errors every tmp_path fixture.
addopts = --basetemp=.pytest-tmp
